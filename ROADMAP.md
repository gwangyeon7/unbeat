# Unbeat 풀스택 포트폴리오 로드맵

## 목표
- 1순위 타겟: 페이타랩 (데이터 엔지니어링)
- 2순위: 바디코디, 드림어스, 포스타입, 러쉬에잇, 디자이노블 등 백엔드/풀스택 공고
- 투자 기간: 3개월 이상
- 언어 우선순위: Python 심화 → Kotlin/Spring Boot → TypeScript/NestJS → 인프라 마무리
- AI Agent/LLM 자체 개발(RAG, MCP 등)은 이번 로드맵에서 제외, 추후 확장 옵션으로만 남겨둠 (단, "빠른 선곡" 추천에 Claude Haiku 4.5 API를 호출하는 것은 예외로 승인함 — 자체 에이전트/RAG 구축이 아니라 단발성 API 호출이라 별개로 판단, [[학습노트 38번]])

## 프로젝트 개요
Unbeat: Spotify API 기반으로 새로운 음악을 찾아주는 서비스.
현재 상태: FastAPI 백엔드(main.py, 검색/추천/로그인) 존재, 프론트엔드는 미착수.

---

## Phase 1. Python 심화 (4~5주)
**타겟 공고:** 페이타랩, 디자이노블

- [x] 프론트엔드 신규 구축 (Next.js + TypeScript) — 검색, 추천 UI (Last.fm은 OAuth 불필요해서 로그인은 스킵)
- [x] 이벤트 로깅 추가 (검색/추천클릭 이벤트, MariaDB `unbeat.events` 테이블에 적재, 익명 session_id로 사용자 구분)
- [x] MySQL 또는 PostgreSQL에 원본 데이터 적재 (MariaDB `unbeat.events`에 search/recommend_click 이벤트 적재 확인)
- [x] DuckDB로 분석 웨어하우스 구축 (Snowflake 대체) — `data/etl_load.py`로 MariaDB -> DuckDB 적재 확인
- [x] dbt-duckdb로 변환 레이어 구성 (staging → mart), 데이터 품질 테스트 — `dbt run`/`dbt test` 실행 확인, 테스트 13개 전부 통과
- [x] AARRR / 퍼널 / 코호트 분석 (SQL 기반) — 검색→클릭 전환율, 주간 코호트 리텐션 확인
- [x] Redis로 추천 결과 캐싱 — 브라우저에서 cached:false -> cached:true 전환 실제 확인
- [x] 외부 데이터 소스 1개 추가 연동 (Open-Meteo 날씨 API, 키 불필요) — weather_daily 테이블 적재 + 검색량 상관관계 분석까지 확인
- [x] 홈 화면 무드 탐색 (검색 없이 태그 기반 곡 추천) — `/discover-by-mood` 신규 엔드포인트(Last.fm `tag.getTopTracks`, cache-aside + `mood_browse` 이벤트 로깅), 프론트 무드 칩 UI. "운동" 칩 클릭 시 검색 없이 곡 목록 뜨는 것 실제 확인 (본인의 실제 유튜브 뮤직 사용 패턴 피드백 반영). (버그 수정) 검색을 한 뒤에 무드 칩을 누르면 이전 검색 결과("검색된 곡"/"검색된 아티스트")가 그대로 남아있어서 "눌러도 메인이 안 바뀐다"는 피드백 — `handleSearch`는 검색 시 무드 상태를 지우는데 반대로 `handleSelectMood`는 검색 상태를 안 지우는 비대칭이 원인. 무드 선택 시에도 검색/추천 관련 상태를 같이 초기화하도록 수정해 메인이 무드 결과로 완전히 교체되게 함 — 라이브 확인 대기
- [ ] 유튜브 인기 급상승 API 연동 (홈 화면 "국내/해외 인기 급상승") — "Last.fm이 주는 게 별로 없는 것 같은데 유튜브 API도 가져왔으면서 왜 쓰는 느낌이 없냐"는 피드백. 그동안 YouTube Data API는 재생 버튼용 `/youtube-search`(호출당 100유닛, 하루 100회 한도)에만 쓰이고 있었는데, `videos.list(chart=mostPopular)`로 실제 지역별 트렌딩 데이터를 가져오는 `GET /youtube-chart?region=KR|US` 신규 추가 — 이 엔드포인트는 훨씬 저렴해서(part 2개 기준 약 5유닛) 캐싱만으로 쿼터 걱정 없이 씀. 기존 "국내 인기 차트"(Last.fm geo.getTopTracks)를 이걸로 교체(정확한 regionCode 기반이라 이름 추측 휴리스틱보다 정확), 짝을 이루는 "해외 인기 급상승"(US 리전) 섹션도 신규 추가. 영상 제목엔 Last.fm처럼 깔끔한 아티스트/곡명 필드가 없어서 "아티스트 - 곡명" 패턴 파싱 + 실패 시 채널명을 아티스트로 대신 쓰는 절충 로직(`_parse_youtube_music_title`) 추가. 이미 videoId를 알고 있는 트랙이라 재생 시 `/youtube-search`를 다시 안 태우도록 프론트도 같이 수정(쿼터 절약). (버그 수정) 처음 라이브 확인 시 무드 칩 클릭이 500 에러로 실패하고 유튜브 급상승 두 섹션이 통째로 비어있던 문제 발견 — 무드 쪽은 응답 스키마를 바꿨는데 옛 캐시(`{"tracks": [...]}` 형태)가 남아있어서 `KeyError`가 난 것이었고(캐시 키를 `v2`로 버전업해서 해결), 유튜브 쪽은 `videoCategoryId=10`+`mostPopular` 조합이 결과를 안 주는 경우가 있어서(카테고리 필터 없이 재시도하는 폴백 추가). (UI 수정) 섹션 제목에 붙어있던 "(유튜브)" 표기를 제거 — 데이터 출처(Last.fm/유튜브 중 뭘 썼는지)는 구현 디테일이라 사용자에게 노출할 필요가 없다는 피드백 — 라이브 재확인 대기
- [ ] 무드 탐색 결과 국내/해외 구분 — "출퇴근길/신나는 같은 무드 칩을 누르면 죄다 팝송만 나온다, 한국 노래랑 구분해서 나눠 보여줄 수 없냐"는 피드백(유튜브 뮤직이 무드별로 "맞춤 믹스"/"상쾌한 유산소 운동" 등 여러 행으로 나눠 보여주는 걸 레퍼런스로 보여줌). Last.fm은 국적을 알려주는 필드가 없어서, 아티스트명에 한글이 포함되면 국내로 보는 실용적 기준(`_is_domestic_artist`)에 로마자로 등록되는 K팝 그룹 목록을 보조로 더해 판별. `/discover-by-mood`가 후보를 50곡으로 넉넉히 받아 국내/해외로 나누고, 국내 곡이 너무 적으면(서구 편향 태그에서 실제로 자주 발생) "k-pop" 태그 풀에서 보충. 프론트는 "국내 인기곡"/"해외 인기곡" 두 섹션으로 분리해서 렌더링. (버그 수정) 응답 스키마를 바꾸면서 옛 캐시(`{"tracks": [...]}` 형태)와 충돌해 500 에러가 나던 문제 — 캐시 키를 `v2`로 버전업해서 해결. (성능 개선) "뜨긴 하는데 느리다"는 피드백 — 국내/해외 후보를 따로따로 순차 필터링하던 걸, 합쳐서 한 번의 병렬 조회로 처리하도록 바꾸고 동시성도 8→12로 올림. (UI 개선) "유튜브처럼 빠른 선곡/다시 듣기 스타일로, 너무 다 카드트랙일 필요는 없다"는 피드백으로 큰 정사각 카드 대신 작은 썸네일+2줄 텍스트의 줄형 컴포넌트(`CompactTrackRow`)와 3열 페이지네이션 그리드(`CompactTrackGrid`, 9개씩 좌우 화살표로 넘김) 신규 추가 — 국내/해외 인기곡 두 섹션에 적용, 글로벌 차트/유튜브 급상승/인기 아티스트 등 다른 섹션은 기존 카드 스타일 유지. 사이드바 "다시 듣기"도 같은 줄 스타일(`FavoriteTrackRow`가 내부적으로 `CompactTrackRow` 재사용)로 통일했는데, 즐겨찾기 트랙 도메인엔 이미지 컬럼이 없어서 썸네일은 항상 플레이스홀더로 뜨는 한계는 있음 — 라이브 재확인 대기
- [ ] 아티스트명 "- Topic" 접미사 제거 + 카드 이미지 품질 개선 — "노래들 이미지는 다 들어갔으면 좋겠다", "인기 아티스트 카드가 맘에 안 든다", "카드 트랙 이미지가 구려서 그냥 가수 프로필처럼 보인다"는 피드백 3건을 한 번에 정리. (1) "LiSA - Topic"처럼 유튜브 자동 생성 채널명 접미사가 아티스트명에 그대로 노출되던 문제 — 원인이 두 군데였음(우리 유튜브 제목 파싱의 채널명 폴백, 그리고 Last.fm 원본 데이터 자체에도 케이팝 트랙 다수가 이미 "- Topic" 붙은 채로 등록돼 있던 것). `_clean_artist_name()`으로 두 경로 모두에서 접미사 제거. (2) 트랙 카드 이미지가 곡 커버 대신 아티스트 범용 사진처럼 보이는 문제 — `resolve_track_image()`가 Last.fm 이미지를 먼저 쓰던 순서를 뒤집어 iTunes 검색 결과(실제 발매 커버일 확률이 높음)를 먼저 쓰도록 변경. (3) "인기 아티스트" 카드가 이미지 없이 회색 원으로만 뜨던 문제 — Last.fm에 이미지가 없으면 iTunes 앨범 커버로 보완(엄밀히는 인물 사진이 아니라 대표작 커버라는 한계는 있음), 아티스트 10명분을 스레드풀로 병렬 조회해서 속도 저하 최소화. (버그 수정) 실제로 라이브에서 확인해보니 "지금 인기 있는 곡 (글로벌)" 카드 4개가 전부 같은 아티스트 프로필 사진으로 뜨는 문제 발견 — 코드 자체는 고쳤지만 `/chart`·`/chart/artists`·`/search`·`/recommend`·`/similar-tracks`·`/discover-by-mood`의 캐시 키를 그대로 둬서, 이 fix 이전에 저장된 캐시(기본 TTL 1시간)가 만료되기 전까진 옛날 방식(Last.fm 이미지 우선)으로 계산된 결과가 계속 나가고 있었음 — [[학습노트 29번]]에서 이미 한 번 겪은 "응답이 바뀌면 캐시 키도 버전을 올려야 한다"는 교훈을 이번엔 스키마가 아니라 이미지 로직 변경에 놓친 것. 위 6개 엔드포인트 캐시 키에 전부 버전 접미사(v2, `discover-by-mood`는 기존 v2가 스키마용이라 v3)를 붙여서 해결 — 라이브 확인 대기 ([[학습노트 36번]])
- [x] 홈 화면 사이드바 (즐겨찾기 태그 요약) — 기존 즐겨찾기 데이터를 재활용해 새 위젯 추가. 카드 스타일 통일 실제 확인. (참고: '최근 검색'은 이후 검색창 드롭다운 방식으로 UX 변경됨 — 아래 항목 참고)
- [x] 통합 검색(아티스트+곡) + 한글 오타 교정 — `GET /search`가 `artist.search`+`track.search` 동시 호출, `rapidfuzz`로 한글 아티스트명 후보 목록 기반 오타 교정(영문은 Last.fm `artist.getCorrection`). "아이위" 검색 시 "아이유"로 교정되어 정상 결과 뜨는 것 실제 확인 (중간에 Redis 캐시 때문에 반영 안 되는 것처럼 보였던 이슈 해결 후). (버그 수정) 띄어쓰기 없는 한글 검색어("꽃이피고지듯이")에서 실제 곡 대신 유튜브 스크로블 오염 데이터로 보이는 청취자 2명짜리 엉뚱한 "아티스트"가 뜨는 문제 발견 — Last.fm API 직접 호출로 원인 확인(`track.search`는 진짜 0건, `artist.search`가 오염 데이터를 매칭) 후 청취자 수+이름 키워드 기반 필터(`_is_junk_artist_match`) 추가. (기능 추가) "엉뚱한 결과만 막는" 걸로는 부족하다는 요청으로, PyKoSpacing(사전 학습된 한국어 띄어쓰기 교정 모델) 연동 — 아티스트/곡 둘 다 못 찾았고 띄어쓰기 없는 한글 검색어일 때만 교정된 문자열로 재시도. 설치 용량이 큰(TensorFlow 포함 약 300MB) 패키지라 `requirements.txt`에 git URL로 추가, 캐시 키 v4 버전업 — 라이브 확인 대기 ([[학습노트 45번]]). (버그 수정) "청취자 5명 이하 AND 특정 키워드 포함"이던 쓰레기 아티스트 필터가, 키워드 없는 방송/영상 설명문 형태 항목("국보급 허스키 보이스로... 8호 가수의 〈꽃이 피고 지듯이〉♪", 청취자 2명)은 못 걸러내는 걸 발견 — 이름 키워드 조건을 없애고 청취자 수만으로 판단하도록 단순화(`JUNK_ARTIST_LISTENER_THRESHOLD=5`), 캐시 키 v5 버전업 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 50번]]). (버그 수정) 같은 문제가 영문에도 있다는 걸 발견 — "thatthat"(That That, 싸이&슈가) 검색 시 실제 곡 대신 THOTTWAT 등 무관한 결과만 뜸. 한글은 PyKoSpacing이 담당했으니 영문은 `wordninja`(가벼운 순수 파이썬 단어 분리 라이브러리)로 대칭 추가 — 캐시 키 v6 버전업 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 52번]]). (기능 추가) "댓댓"(That That의 한글 발음 전사)처럼 규칙 기반 교정 두 단계로도 못 잡는 검색어 대응 요청 — 위 두 폴백까지 다 실패했을 때만 Claude Haiku 4.5에게 의도한 곡을 추측시키고, 그 추측을 Last.fm에 재검증(anti-hallucination, `/quick-picks/ai`와 동일 원칙)한 뒤에만 결과로 채택하는 세 번째 폴백(`_ai_guess_query`) 추가. AI 사용 범위 확장이라 사용자 승인 받음("Ai 쓸수만 있으면 난 좋다니까"), 비용은 검색어 단위로 90일 전역 캐싱해서 같은 실패 검색어당 최대 1회만 발생하도록 설계(추정 호출당 $0.0004~0.0005). 프론트에 "(AI 추측: ... 로 검색됨)" 힌트 노출. 캐시 키 v7 버전업 — ast.parse/tsc만 확인, 라이브 확인 대기 ([[학습노트 53번]]). (버그 수정) 라이브 확인해보니 AI 폴백이 전혀 발동 안 함 — 원인은 아티스트 검색엔 있던 쓰레기 필터(§45/§50)가 트랙 검색엔 없어서, "댓댓" 검색 시 팬캠/방송클립 스크로블(청취자 7~43명)이 그대로 매치돼 "결과 있음"으로 카운트되고 AI 폴백까지 도달을 못 했던 것. 트랙용 쓰레기 필터(`_is_junk_track_match`, 청취자 50명 이하) 신규 추가, 캐시 키 v8 버전업 — ast.parse만 확인, 라이브 재확인 대기 ([[학습노트 54번]]). (버그 수정) "동암역 2번 출구" 진단 중 위 청취자 수 필터가 장범준(버스커버스커)의 정식 발매곡 "신풍역 2번 출구 블루스"(청취자 6~46명)까지 걸러내는 걸 실제 로그로 확인 — Last.fm의 한국 트랙 단위 스크로블이 워낙 얕아서 정식곡도 쓰레기와 청취자 수 범위가 겹침, 숫자 기준 자체가 부적합했던 것. 필터를 패턴 기반(대괄호 제목, 아티스트 필드 6자리 날짜, "OO CH/TV" 채널명 패턴, 팬캠/직캠 키워드)으로 재설계하고, §53 AI 폴백 발동 조건을 "트랙 없음"에서 "있어도 전부 청취자 100명 미만(저신뢰)"으로 확장 — AI가 더 확실한 트랙을 찾으면 맨 앞에 얹고 기존 저신뢰 트랙은 남겨둠. 캐시 키 v10 버전업 — ast.parse만 확인, 라이브 재확인 대기 ([[학습노트 55번]]). (버그 수정) 재확인 중 AI 폴백이 여전히 발동 안 함 — "신뢰도" 판단이 청취자 수만 보고 검색어 관련성을 안 봐서, 전혀 다른 역 이름의 곡("신풍역 2번 출구 블루스", 청취자 318명)이 인기 있다는 이유만으로 전체를 "신뢰함"으로 오판. rapidfuzz `partial_ratio`(검색어-트랙명 유사도 90점 이상)를 청취자 수 조건에 AND로 추가 — 로컬 비교 검증 중 기존에 쓰던 `token_sort_ratio`는 이 케이스에서 진짜/가짜 점수가 역전되는 걸 확인해서 `partial_ratio`로 교체. 캐시 키 v11 버전업 — ast.parse만 확인, 라이브 재확인 대기 ([[학습노트 56번]]). (버그 수정) 재확인 중 AI가 "동암역 2번 출구"를 "싸이"로 완전히 잘못 추측했는데, Last.fm의 `artist` 파라미터가 엄격한 필터가 아니라서 "아티스트만 맞고 곡은 무관한" 결과(싸이의 다른 곡들)가 그대로 검증을 통과해 화면에 뜨는 걸 발견 — 추측 곡명과 결과 곡명 사이 유사도(`partial_ratio >= 80`) 검증 추가. 동시에 사용자가 "검색할 때마다 비용 나가는 거 아니냐"고 확인 요청 — 캐싱 자체는 검색어 단위로 잘 되고 있었지만, 검증 성공/실패와 무관하게 항상 90일로 캐싱돼서 한 번 잘못된 AI 추측이 90일간 고정되는 문제가 있었음(§51의 네거티브 캐시 원칙 미적용). 캐싱 책임을 함수 내부에서 호출부로 옮기고 검증 성공 시 90일/실패 시 1일로 TTL 분리. 캐시 키 v12 버전업 — ast.parse만 확인, 라이브 재확인 대기 ([[학습노트 57번]]). (버그 수정) 사용자가 로그 모순("트랙 검증 실패" 바로 다음 줄에 "검증됨=True" 캐싱)을 직접 짚어줘서 발견 — 트랙 추측이 실패해도 "아티스트만이라도" 검증하던 폴백이, 유명 아티스트("싸이")는 항상 실존해서 사실상 무조건 통과되는 구멍이었음. 트랙을 추측했는데 실패한 경우엔 아티스트 폴백도 같이 실패 처리하도록(AI가 애초에 트랙 추측을 안 했을 때만 아티스트 단독 검증) 좁힘. 캐시 키 v13 버전업 — ast.parse만 확인, 라이브 재확인 대기 ([[학습노트 58번]]). (버그 수정) 위 §58 수정 후 처음 라이브 테스트했는데도 여전히 안 고쳐짐 — AI 폴백 결과가 `/search` 캐시와 별개의 중첩된 캐시(`search-ai-fallback:v1`)로 저장되고 있었는데 이 안쪽 캐시 버전을 안 올려서, 예전에 잘못 캐싱된 값을 그대로 재사용하고 있었음. 안쪽 캐시 v2로, 그 사이 이미 오염된 바깥쪽 `/search` 캐시(v14)도 v15로 같이 버전업 — 중첩 캐시는 안쪽만 올리면 안 되고 바깥쪽도 같이 올려야 한다는 걸 실제로 겪음. (라이브 확인) 재시작(v15) 후 "동암역 2번 출구" 재검색 — 무관한 "싸이" 결과 없이, 첫 결과로 유튜브 폴백 트랙("동암역 2번출구", Dom)과 "Last.fm에 없어서 유튜브에서 찾았어요" 안내 문구가 정상 노출되는 것 실제 확인 ([[학습노트 59번]], [[학습노트 58번]], [[학습노트 57번]]). (기능 추가) "Last.fm에 진짜 없으면 방법이 없냐"는 질문에 옵션 제시 후 승인받아 유튜브 자체 검색을 진짜 마지막 폴백으로 추가 — Last.fm(크라우드소싱 카탈로그, 니치 곡 커버리지 낮음)과 달리 유튜브는 업로드된 모든 영상이 대상이라 훨씬 넓은 인덱스. `_youtube_raw_query_search()`로 검색어 원문 그대로 유튜브에서 찾아 Last.fm 메타데이터 없는 "비검증" 재생 결과 제공, `youtubeFallbackUsed` 응답 필드로 프론트에서 구분 표시. 기존 `/youtube-search`·유튜브 급상승(§28)과 동일한 쿼터/캐싱 관례(성공 14일/실패 1일) 재사용. 캐시 키 v14 버전업 — ast.parse/tsc만 확인, 라이브 확인 대기 ([[학습노트 59번]]). (버그 수정) "위대한 쇼맨 ost"(영화 OST 검색)가 "위너"(K팝 그룹)로 엉뚱하게 교정되는 걸 발견 — §25/§45의 한글 아티스트 후보 매칭 `score_cutoff`가 60으로 너무 낮아서 글자 하나만 겹쳐도 매치되던 게 원인. 로컬에서 rapidfuzz로 정상/오탐 케이스들의 실제 점수를 비교해 65로 상향(정상 케이스는 전부 66.67점 이상, 오탐은 60.0점에 몰려있었음) — 테스트 중 실존 아티스트 "지드래곤"이 "있지"로 잘못 교정되는 것도 같은 원인으로 추가 발견해 같이 수정됨. 이 오교정은 §53 AI 폴백 발동 자체를 막는 부작용도 있었어서, 수정 후엔 "위대한 쇼맨 ost" 같은 검색어가 AI 폴백까지 정상적으로 넘어감. 캐시 키 v16 버전업 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 62번]]). (버그 수정) "위대한 쇼맨 ost" 검색 결과 중 "호야팀 K-WAVE 단체 무대 ♬ 위대한 쇼맨 OST"(댄싱하이) 같은 댄스 서바이벌 방송 커버 무대 영상이 §54/§55 쓰레기 필터를 통과해서 나오는 걸 발견 — 같은 결과 안의 진짜 원곡("Hugh Jackman ... From Now On")과 겉보기엔 둘 다 지저분해 보이지만 후자만 진짜라는 걸 구분해서 "단체 무대" 키워드를 `JUNK_TRACK_KEYWORD_MARKERS`에 추가. 캐시 키 v17 버전업 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 55번]] 추가 수정). (버그 수정) "2010년대 kpop"으로 검색하면 여러 곡 모음을 기대했는데 §53 AI 폴백이 먼저 실행되며 "특정 곡 하나"(싸이 강남스타일)만 추측해버려서, 정작 [[학습노트 32번]]에서 만든 태그 기반 "관련 곡(장르/연대)" 모음이 그 밑에 묻혀 눈에 안 띄던 문제 발견 — 장르/연대 키워드 판별을 함수 앞부분으로 옮기고, 그런 검색어면 §53 AI 단일곡 추측과 §59 유튜브 단일곡 폴백을 아예 건너뛰도록 수정(부수 효과로 이런 검색어의 불필요한 Claude Haiku 호출도 없어짐). 캐시 키 v18 버전업 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 32번]] 추가 수정). (버그 수정) 위 수정 후 확인해보니 "2010년대 kpop"의 "관련 곡" 6곡이 전부 서구 팝(Harry Styles, Billie Eilish 등)이고 케이팝이 하나도 없다는 지적 — `decade_tag`("2010s") 후보를 먼저 다 채우고 `genre_tag`("k-pop")를 뒤에 잇던 순서 때문에, 이용자층이 훨씬 큰 "2010s" 태그 혼자 최종 15곡을 다 채워버려서 "OR 매칭"이라던 설계 의도와 달리 사실상 먼저 채운 태그가 결과를 독식하고 있었음. 두 태그 후보를 라운드로빈으로 번갈아 섞도록 변경 — "장르 AND 연대"를 엄밀히 보장하진 않지만(§32의 원래 한계는 그대로) 최소한 한쪽이 완전히 안 보이는 극단적 편향은 완화됨. 캐시 키 v19 버전업 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 32번]] 추가 수정)
- [ ] 검색어에 장르·연대 키워드 지원 — "2010년대 kpop"처럼 검색해도 그에 맞는 곡이 나왔으면 좋겠다는 피드백. 아티스트/곡 이름 검색으로는 못 찾는 이런 검색어를 위해, 검색어에서 장르 키워드(`_extract_genre_tag`, 무드 칩과 같은 한글→Last.fm 태그 매핑)와 연대 키워드(`_extract_decade_tag`, "2010년대"/"90년대" 등에서 10년 단위 태그 추출)를 뽑아 `tag.getTopTracks`로 관련 곡을 찾아 `tagTracks`로 별도 반환. "장르+연대 둘 다 시도하되 덜 정확해도 된다"는 방향으로, 태그 하나만 받는 Last.fm API 제약상 완전한 AND 매칭 대신 두 태그 결과를 합치는 느슨한 매칭으로 구현. 정확한 검색 결과("검색된 곡")와는 성격이 달라 "관련 곡 (장르/연대)"로 섹션을 분리하고, 무드 탐색과 같은 이유로 가사 있는 곡만 필터링 + `CompactTrackGrid` 줄 스타일로 표시 — 라이브 확인 대기
- [x] 검색창 '최근 검색' 드롭다운화 + 상단 고정 헤더(로고+검색창) 레이아웃 — 사이드바 방식 대신 유튜브 뮤직 스타일 드롭다운으로 교체(항목별 삭제 가능), 검색창을 헤더로 승격해 항상 접근 가능하게 함. 실제 확인
- [x] 홈 화면 중앙 기본 콘텐츠(인기 차트) + 즐겨찾기 사이드바 재배치 — `GET /chart`(Last.fm `chart.getTopTracks`) 신규 추가, 검색/무드 선택 없이도 중앙에 "지금 인기 있는 곡"이 기본으로 뜨게 함. 즐겨찾기(아티스트/다시 듣기)는 좌우 사이드바로 이동해 중앙을 비워둠. (멜론 "인기있어요"/유튜브 뮤직 "맞춤 믹스"처럼 홈 진입 즉시 콘텐츠가 채워져야 한다는 본인 피드백 반영) — 좌/중앙/우 3열 레이아웃 실제 확인
- [x] 홈 화면 중앙 콘텐츠를 여러 행으로 분리 — "우리는 차트 하나뿐인데 다른 서비스들은 여러 개로 나뉘어 있다"는 피드백(스포티파이 인기 상승 곡/인기 아티스트, 유튜브 뮤직 다시 듣기/빠른 선곡/맞춤 믹스 참고). `GET /chart`에 `country` 파라미터 추가(Last.fm `geo.getTopTracks`)해서 "국내 인기 차트" 행 추가, `GET /chart/artists`(Last.fm `chart.getTopArtists`) 신규 추가해서 "인기 아티스트" 행 추가 — 기존 "지금 인기 있는 곡"(글로벌)까지 총 3행 + 무드 칩 — 라이브 확인 대기. (기능 추가) "검색해도 검색된 곡 빼고는 다 똑같다, 유튜브 뮤직처럼 검색과 관련된 곡이 홈에도 떴으면 좋겠다"는 피드백으로, 검색 중엔 검색과 무관한 "지금 인기 있는 곡 (글로벌)"/"인기 아티스트" 두 섹션을 숨기고 검색된 아티스트(또는 검색된 곡의 아티스트) 본인의 다른 곡으로 대체 — 세 가지 대안 중 "검색한 아티스트의 다른 곡들로 채우기" 선택받음. 백엔드에 `GET /artist-top-tracks`(`artist.getTopTracks` 기반, 가사 필터 §44 적용) 신규 추가 — `/recommend`(비슷한 아티스트들의 곡)와 의미가 달라서 재사용하지 않고 분리. "다시 듣기"/"빠른 선곡"/"국내·해외 인기 급상승"은 요청 범위 밖이라 그대로 유지. (버그 수정) `lastfm_get()`이 "Artist not found" 같은 정상 실패도 예외로 던지는 걸 못 잡아서 섹션이 통째로 안 뜨던 문제 발견 — §60과 같은 패턴으로 `try/except` 처리, 캐시 키 v2 버전업. "댓댓 싸이" 검색으로 핵심 동작(검색 아티스트의 다른 곡 섹션 노출) 라이브 확인 완료(실제로는 위 버그가 아니라 브라우저 하드 리프레시 안 한 게 원인이었음). (버그 수정) 이어서 "아이유"로 재검색했더니 헤더는 바로 바뀌는데 목록엔 잠깐 직전 검색(싸이)의 곡이 그대로 남는 걸 발견 — `searchContextArtist`는 즉시, `searchArtistTracks`는 fetch 완료 후에 바뀌는 타이밍 차이가 원인. 새 fetch 시작 전에 목록을 먼저 비우도록 수정 — tsc만 확인, 라이브 재확인 대기. 재생/즐겨찾기/원상복구 등 나머지 세부 확인도 대기 ([[학습노트 61번]])

---

## Phase 2. Kotlin / Spring Boot (3~4주)
**타겟 공고:** 바디코디, 드림어스, 포스타입

- [x] "플레이리스트/즐겨찾기" 도메인을 별도 서비스로 분리 (Spring Boot + JPA + MySQL) — 별표 클릭→즐겨찾기 저장→새로고침해도 유지 실제 확인
- [x] 즐겨찾기를 트랙(곡) 단위로 확장 + Last.fm 태그(`track.getTopTags`) 자동 수집·저장 — 서구 유명곡(BRITPOP 등)은 태그 정상 수집 확인, 대신 케이팝 트랙은 Last.fm 태그 데이터 자체가 거의 없다는 실제 한계 발견
- [x] 즐겨찾기 곡에 다중 태그 그룹핑 적용 (대표 태그 1개 → 가진 태그 전부) + 원곡 링크(`url`) 저장 — "다시 듣기" 목록에서 여러 태그 그룹에 동시에 뜨는 것, 링크 클릭되는 것 실제 확인
- [x] 즐겨찾기 태그 잡음 필터링 (`LastFmTagClient.cleanTags`) — 아티스트 자기 자신 이름 태그("아이유" 곡에 "iu")와 국적/언어 등 장르·무드 무관 메타 태그("korean" 등)를 제외하도록 수정. "곡 하나 즐겨찾기했는데 태그 3개(iu/kpop/korean)가 붙어서 지저분하다"는 피드백 반영 — 재등록 후 "korean"/"iu" 안 붙는 것 실제 확인 (단, "K-POP"과 "KPOP"처럼 표기만 다른 중복 태그가 별개로 남는 한계는 발견 — 태그 UI 자체를 걷어내는 다음 항목으로 우회 해결)
- [x] 즐겨찾기 사이드바 "다시 듣기" 태그 그룹핑 UI 제거, 플랫 목록으로 전환 — 필터링해도 K-POP 같은 장르 태그는 남는데, 태그를 눌러도 아무 동작이 없고 곡 하나가 여러 그룹에 중복으로 뜨는 게 오히려 헷갈린다는 피드백. 태그 데이터 수집 자체는 그대로 두고(Spring Batch 자동 그룹핑용), 화면엔 "곡명 · 가수" 플랫 목록만 노출하도록 변경, `FavoriteTagSummary` 컴포넌트 제거 — 실제 확인
- [x] 트랙 재생을 사이트 안 유튜브 미니 플레이어로 전환 — 트랙 카드를 누르면 Last.fm 자체 페이지로 나가던 걸, `GET /youtube-search`(YouTube Data API v3 검색, 14일 캐싱)로 videoId를 찾아 화면 하단 고정 바에서 재생. "우리 사이트가 아니라 last.fm 따까리 같다"는 피드백 반영. 부수 효과로 `url` 컬럼 없는 예전 즐겨찾기 곡도 재생 가능해짐(아티스트+곡 이름만 있으면 검색되므로) — YOUTUBE_API_KEY 발급 후 실제 재생 확인
- [x] 유튜브 iframe을 그대로 노출하는 대신 자체 미니 플레이어 UI로 교체 (`MiniPlayer.tsx`) — 1차로 유튜브 embed iframe(로고/자막 버튼/재생바 노출)을 그대로 보여줬더니 "그래도 남의 걸 가져와서 뜨는 느낌"이라는 재피드백. YouTube IFrame Player API로 전환해 iframe은 1x1로 숨기고, 재생/일시정지 버튼·진행바(탐색 가능)·앨범아트는 전부 Unbeat 자체 UI로 그림 — 오디오 소스만 유튜브고 화면엔 우리 UI만 보임. 부수 효과로 `lastOpenedAt` 기록 시점도 "재생 버튼 클릭"에서 "유튜브 플레이어의 실제 PLAYING 이벤트"로 한 단계 더 정확하게 옮김 — 실제 확인
- [x] 미니 플레이어 정보량 확장 (스포티파이/유튜브 뮤직/애플 뮤직과 비교) — "다른 음악 서비스는 재생바가 크고 정보도 많은데 우리만 작고 부실하다"는 피드백. 좌(큰 앨범아트+곡정보+즐겨찾기 별표)/가운데(재생+진행바)/우(볼륨 슬라이더+음소거) 3분할 레이아웃으로 확장, 앨범아트 크기 키움(56px→64px), 재생바에서 즐겨찾기 토글까지 가능하게 함 — 실제 확인 (볼륨 슬라이더/음소거 아이콘 디자인이 "짜친다"는 후속 피드백은 아래 항목에서 다듬음)
- [x] 미니 플레이어 진행바/볼륨바 커스텀 스타일링 — 네이티브 `<input type="range">`의 기본 큼직한 원형 손잡이가 "짜치다"는 피드백. `::-webkit-slider-thumb`/`::-moz-range-thumb` 커스텀 스타일로 얇은 트랙+작은 손잡이로 교체, 볼륨 아이콘도 이모지(🔊/🔇)에서 앱 다른 곳과 톤을 맞춘 스트로크 SVG 아이콘으로 교체 — 라이브 확인 대기
- [x] (시도했다가 되돌림) 즐겨찾기 사이드바 "다시 듣기"를 무드별 스마트 그룹으로 재도입 — 화면에 뜨기도 전에 "그거 결국 아까 내가 별로라던 태그 즐겨찾기랑 똑같은 거 아니냐"는 피드백을 받고 되돌림. 자세한 경위와 대체한 기능은 [[학습노트 21번]] 참고.
- [ ] 재생목록(Playlist) 기능 — 즐겨찾기(별표)와 별개로 사용자가 직접 이름을 짓고 곡을 골라 담는 재생목록. 유튜브 뮤직 사이드바("새 재생목록" 버튼 + 만든 재생목록 목록)를 직접 레퍼런스로 보여주며 요청함. playlist-service에 `Playlist`/`PlaylistTrack` 엔티티 + `/playlists` REST API 신규 추가(생성/삭제/곡 추가·삭제, 조회), 프론트 오른쪽 사이드바에 "내 재생목록" 섹션(생성, 접기/펼치기, 곡 삭제) + 트랙 카드마다 "＋" 버튼으로 여는 `AddToPlaylistModal` 추가 — Kotlin 로컬 빌드 성공 확인함(`./gradlew` 실행 시 무관한 파일에 남아있던 오타 한 글자가 원인이었던 컴파일 에러 수정 후), 실제 재생목록 생성/곡 담기 동작은 라이브 확인 대기. (UI 개선) 멜론 앱 재생목록 화면(체크박스 + 하단 선택반복/믹스업/다운/담기/삭제 액션바)을 직접 스크린샷으로 보여주며 "이런 식으로 재생목록을 따로 만들고 여러 곡을 한 번에 선택해서 처리하고 싶다"는 요청. 사이드바 안에서 곡을 펼쳐보던 좁은 아코디언 방식을 걷어내고, 재생목록 이름을 누르면 `MiniPlayer` 확장 재생 화면과 같은 패턴(`createPortal` 전체화면 오버레이)의 `PlaylistDetailOverlay`가 새로 뜨도록 변경. "다운"(우린 오디오 파일을 갖고 있지 않고 유튜브로 스트리밍만 함)과 "믹스업"(AI 기반 자동 믹스는 별도 서비스가 필요해 없음)은 Unbeat 특성상 그대로 구현할 수 없어 제외하기로 사용자가 직접 선택했고, 실제로 의미 있는 나머지 4개 중에서도 체크박스로 곡을 고른 뒤: (1) 이어재생 — 고른 곡을 순서대로 재생, (2) 반복재생 — 고른 곡만 계속 반복(loop), (3) 담기 — 고른 곡을 한 번에 다른 재생목록으로 복사, (4) 삭제 — 고른 곡을 한 번에 이 재생목록에서 제거, 를 사용자가 골라 구현. 이어재생/반복재생은 새 백엔드 엔드포인트 없이 프론트에서만 처리 — 평소엔 곡이 끝나면 큐(`queue` state)가 항상 "지금 곡과 비슷한 곡"으로 자동 재채워지는 라디오 방식인데, `isManualQueueRef`/`repeatListRef`로 "지금은 사용자가 고른 고정 목록을 그대로 소비 중"이라는 걸 표시해서 `handleTrackEnded`가 그동안은 라디오 재채움 대신 그 목록을 순서대로 틀고, 반복재생이면 다 소진 시 처음으로 되돌아가며, 이어재생(비반복)이면 다 소진 후 평소 라디오 방식으로 자연스럽게 복귀하도록 구현. 담기는 기존 트랙 카드 "＋" 버튼이 쓰던 `AddToPlaylistModal`을 그대로 재사용하되, 단일 곡만 받던 props를 항상 배열로 받도록 일반화(호출부 대부분은 그대로 1개짜리 배열로 감싸서 큰 변경 없이 재사용). 삭제도 기존 재생목록 내 곡 개별 삭제 함수를 여러 id 배열을 받도록 합쳐서 재사용 — 새 Kotlin/백엔드 변경 없이 프론트에서 기존 REST 엔드포인트를 여러 번(`Promise.all`) 호출하는 방식으로 구현, IntelliJ 재시작 불필요. (UI 개선) 라이브 확인 중 "팝 2"처럼 재생목록 이름을 눌러도 되는 건지 못 알아보고 두 번이나 헷갈리는 걸 발견 — 즐겨찾기 아티스트 목록(★ 태그, 테두리 있는 알약 버튼)과 달리 재생목록 이름은 테두리도 배경도 없는 그냥 텍스트라 클릭 가능한 요소로 안 보였던 게 원인. 호버 시 배경 강조 + 오른쪽에 "›" 화살표를 붙여서 클릭 힌트를 명확히 함. (UI 개선) 하단 액션바 "삭제"를 누르면 확인 없이 바로 지워지는 게 불안하다는 피드백 — 특히 "전체 선택" 상태에서 실수로 누르면 재생목록 전체가 한 번에 날아갈 수 있어서, "N곡을 삭제할까요?" 확인 모달을 추가하고 "삭제" 버튼을 눌러야 실제로 지워지게 함(개별 곡 ✕는 되돌리기 쉬운 작은 동작이라 그대로 즉시 삭제 유지). (버그 수정) 재생목록 상세 화면에서 곡 이름을 눌러도 재생이 안 되는 것처럼 보인다는 피드백 — 실제로는 재생이 시작됐지만 이 화면 자체가 전체화면(z-40)으로 하단 미니 플레이어(z-30)를 통째로 가리고 있어서 아무 변화도 안 보였던 것. 곡을 누르면 "이어재생"/"반복재생"과 동일하게 화면을 닫아서 미니 플레이어가 바로 보이도록 수정 — 라이브 확인 대기 ([[학습노트 34번]])
- [ ] 재생목록 자동 생성(취향 분석) — "새 재생목록 누르고 이름 타이핑해서 곡을 일일이 넣는 건 뻔하다, 내가 들은 곡을 분석해서 힙합이면 힙합/발라드면 발라드로 알아서 재생목록이 만들어졌으면 좋겠다"는 피드백. 즐겨찾기한 곡의 Last.fm 태그 기준으로 장르별 그룹핑 후, 2곡 이상 모인 장르만 재생목록으로 자동 생성. `Playlist`에 `isAutoGenerated` 플래그 추가해서 수동 재생목록과 구분, `POST /playlists/auto-generate` 신규 엔드포인트(다시 눌러도 중복 안 쌓이게 이전 자동 생성분은 지우고 재생성), 프론트 "내 재생목록" 헤더에 ✨ 버튼 추가. 실사용 중 대표 태그가 장르가 아닌 곡(창모 "빌었어"가 힙합인데 대표 태그가 "ost"였던 사례)이 안 묶이는 문제를 발견해서, 태그 3개 중 아는 장르가 있으면 우선하도록 매칭 로직 개선 — 라이브 확인 대기
- [ ] 트랙 카드 별표/＋ 버튼 클릭 버그 수정 — 카드 호버 재생 아이콘 오버레이가 `pointer-events-none` 없이 별표/＋ 버튼 위를 덮고 있어서, 버튼을 눌러도 클릭이 카드 재생으로 새는 버그를 발견하고 수정 — 라이브 확인 대기
- [ ] "다음 트랙" 자동 이어재생 큐 — "유튜브 뮤직처럼 곡을 하나 고르면 계속 비슷한 곡이 이어서 추천/재생됐으면 좋겠다"는 피드백(유튜브 뮤직 홈 화면 "다시 듣기"/"오늘의 발견"과 재생 중 화면의 "다음 트랙" 패널을 레퍼런스로 보여줌). 백엔드 `GET /similar-tracks`(Last.fm `track.getSimilar` 우선, 없으면/적으면 `artist.getSimilar`+`getTopTracks`로 보충, `/recommend`와 같은 패턴) 신규 추가. `MiniPlayer`가 유튜브 플레이어의 `ENDED` 이벤트를 감지해서 큐의 다음 곡으로 자동 전환, 재생바에 "다음 트랙" 목록 패널(펼쳐보기 + 클릭해서 바로 재생) 추가. 곡이 바뀔 때마다 큐도 "그 곡과 비슷한 곡"으로 다시 채워지는 라디오 방식. 실사용 중 트랙 단위 유사곡이 적은 곡(국힙 등)에서 큐가 9곡으로 항상 딱 끊기는 걸 발견해서, 완전히 비었을 때만이 아니라 결과가 적을 때도(20곡 채울 때까지) 아티스트 기반 보충을 같이 섞고 중복 제거하도록 개선 — 라이브 확인 대기 (가사 표시는 [[학습노트 26번]]에서 LRCLIB으로 별도 해결함). (버그 수정) 재생 중인 아티스트명만으로 `artist.getSimilar`를 부르면 동명이인의 완전히 다른 아티스트로 오매칭되는 경우 발견(무명 커버 버전 "Hanz" → 노르웨이 프로듀서 "Hanz"로 매칭되어 전혀 무관한 장르 추천됨) — 같은 제목의 다른 버전 중 가장 많이 들은 아티스트를 찾아 그 아티스트 기준으로 유사 아티스트를 조회하도록 수정, 캐시 키 v3 버전업 — 라이브 확인 대기 ([[학습노트 46번]]). (버그 수정) 큐 안의 곡을 클릭해서 재생하면 아직 안 들은 나머지 대기곡들까지 통째로 사라지고 방금 재생한 곡 기준의 새 목록으로 전부 바뀌는 문제 발견 — "노래를 아예 바꾸는것보단 노래수를 늘리고 안바뀌는게 나을거 같다"는 피드백 반영해, `refreshQueue()`를 전량 교체(`setQueue(data.tracks)`)에서 기존 대기곡 유지 + 새 추천 이어붙이기(dedup 키로 방금 재생한 곡만 제외하고 중복 없이 병합, `MAX_QUEUE_SIZE=40`으로 상한 트리밍)로 변경. API 실패 시에도 큐를 비우지 않고 그대로 유지하도록 변경. (추가 수정) 이어붙이기를 무조건 적용했더니, 검색 등으로 큐 바깥에서 완전히 다른 곡을 틀어도 예전 곡 기준 후보가 계속 남아 장르가 섞이는 문제를 발견("이어붙이기가 그 전에 들었던 노래랑 다른 노래의 다음 트랙을 붙이는 거면 장르 상관없이 붙는 거 아니냐"는 지적) — `handlePlay`에 `fromQueue` 플래그를 추가해, 큐 안에서 다음 곡으로 자연스럽게 이어지는 경우(자동 이어재생/큐 클릭)에만 이어붙이고, 그 외(검색/차트/즐겨찾기/재생목록 등 큐 바깥에서 고른 곡)는 다시 전량 교체하도록 조건을 좁힘 — tsc 통과만 확인, 라이브 확인 대기 ([[학습노트 47번]]). (버그 수정) 큐 스크린샷에서 앞쪽 곡은 이미지가 뜨고 뒤쪽으로 갈수록 이미지가 안 뜨는 패턴 발견 — 원인은 최종적으로 25곡(구 15곡)만 쓰면서도 필터링 전 원시 후보 최대 40개 전부에 대해 미리 iTunes 이미지를 조회하고 있었던 것(실제 안 쓰이는 후보에도 비용을 쓴 낭비) + 캐싱이 없어 같은 곡이 여러 엔드포인트에서 반복 조회될 때마다 매번 새로 iTunes를 부른 것. `resolve_track_image()`에 Redis 캐싱(30일 TTL) 추가, `/similar-tracks`는 가사 필터를 통과해 실제 큐에 남을 곡만 이미지를 나중에 조회하도록 변경, `SIMILAR_TRACKS_FINAL_COUNT` 15→25로 증량("처음부터 큐가 많이 나왔으면 좋겠다"는 피드백) — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 48번]]). (버그 수정) 백엔드 재시작 후에도 여전히 옛날처럼 15개로 시작하는 걸 발견 — 캐시 키(`similar-tracks:v3`)를 이번 변경에서 안 올려서 옛 캐시가 계속 나가고 있었음, `v4`로 올려서 수정 — 라이브 확인 대기. (버그 수정) 싸이 "That That" 큐에서 BLACKPINK/Lisa/JISOO처럼 명백히 메이저한 아티스트 곡들도 이미지가 안 뜨는 걸 발견 — `resolve_track_image()` 캐싱(바로 위 §48)이 iTunes 조회 성공/실패를 구분 안 하고 실패(`None`)까지 30일 캐싱해버려서, 일시적 요청 폭주로 실패한 결과가 한 달간 "이미지 없음"으로 굳어있었던 게 원인. 실패는 30분만 캐싱하도록 분리하고 캐시 키를 `track-image:v2`로 올려 기존 오염 데이터도 자동 초기화 — ast.parse만 확인, 라이브 확인 대기 ([[학습노트 51번]]). (기능 추가) §59 유튜브 검색 폴백 라이브 확인 중 "동암역 2번출구"(Dom)를 재생했더니 "다음 트랙" 패널이 텅 비는 걸 발견 — Last.fm에 아예 없는 아티스트라 유사곡 데이터 자체가 없었던 것. 유튜브의 "관련 영상" API는 2016년에 폐기돼서 진짜 유사곡 추천은 불가능하다고 설명한 뒤, 차선책으로 "같은 아티스트의 다른 곡"을 유튜브에서 검색해 채우는 폴백 추가 — Last.fm 기반 후보가 완전히 없을 때만 발동, 가사 필터는 의도적으로 건너뜀(데이터 자체가 없는 아티스트라 필터링하면 무조건 다 걸러짐). 캐시 키 v5 버전업 — ast.parse/tsc만 확인, 라이브 확인 대기 ([[학습노트 60번]]). (버그 수정) 정작 이 폴백을 위해 만든 케이스("Dom" - "동암역 2번출구")에서 라이브 확인해보니 `/similar-tracks`가 200 대신 400을 냄 — 공통 헬퍼 `lastfm_get()`이 Last.fm의 "Track/Artist not found" 같은 정상적인 실패도 무조건 예외를 던지는데, `track.getSimilar`/`artist.getSimilar` 호출부가 그 예외를 안 잡고 있어서 위 유튜브 폴백 코드까지 도달을 못 했던 것. 두 호출 각각 `try/except`로 감싸서 "Last.fm에 없음"을 빈 결과로 취급하고 계속 진행하도록 수정(기존 v5 캐시는 원래도 실패하던 케이스만 새로 정상화되는 거라 버전은 그대로 둠) — ast.parse만 확인, 라이브 재확인 대기 ([[학습노트 60번]])
- [ ] 안 듣는 곡 정리 기능 — "즐겨찾기해놓고 시간 지나면 안 듣는 곡도 생길 텐데, 그런 곡 지워도 될지 물어봐주는 기능을 만들고 싶다, 근데 아무거나 삭제하면 그건 좀 그렇다"는 피드백. 기준은 `lastOpenedAt`(없으면 즐겨찾기한 시점 `createdAt`)이 6개월 이전인 즐겨찾기 트랙(별표만 대상, 재생목록 곡은 제외). 항상 사용자가 오른쪽 사이드바 "정리하기" 버튼을 눌렀을 때만 후보를 조회하고(자동 실행 없음), 체크박스 목록(`StaleTracksModal`, 기본 전체 선택)에서 직접 고른 곡만 삭제 — 절대 조용히/자동으로 지우지 않음. 백엔드 `GET /favorite-tracks/stale?months=6` 신규 추가, 프론트 `getStaleFavoriteTracks` + 모달 연동 — Kotlin 변경분은 인앱 새로 작성한 단위 테스트 2건만 추가된 상태고 백엔드 재시작 전이라 로컬 빌드 미확인, 실제 정리 동작은 라이브 확인 대기
- [ ] "다시 듣기" 사이드바를 재생목록처럼 접이식으로 변경 — 즐겨찾기 곡이 많아지면 항상 펼쳐진 목록이 사이드바 칸을 너무 많이 차지한다는 피드백. 헤더를 누르면 펼쳐지고 평소엔 곡 개수만 보이는 한 줄로 접히도록 수정(재생목록 항목과 동일한 패턴) — 라이브 확인 대기
- [x] 확장 재생 화면(전체화면 오버레이) — 유튜브 뮤직 확장 플레이어(큰 앨범아트 + 오른쪽 "다음 트랙/가사/댓글/관련 항목" 탭)를 레퍼런스로 보여주며 "지금은 한 화면에 다 욱여넣으려니 글씨도 작고 보기 힘들다"는 피드백. `MiniPlayer` 하단 바의 앨범아트나 곡 정보를 누르면 페이지 이동 없이 같은 화면 위에 전체화면 오버레이가 뜨도록 구현 — 큰 앨범아트+재생/즐겨찾기 컨트롤(왼쪽), "다음 트랙"(기존 큐 재사용)/"관련 항목"(`recommendByArtist`로 유사 아티스트 인기곡 조회, 탭을 열 때만 지연 로딩)/"가사"(아래 항목 참고) 3개 탭(오른쪽). 댓글 탭은 YouTube 댓글 API 연동이 추가로 필요해 이번 범위에서 제외. 처음엔 재생바의 `backdrop-blur`가 `fixed` 오버레이의 containing block을 뷰포트가 아니라 재생바 박스로 좁혀버려서 화면이 안 뜨는 버그가 있었는데, `createPortal`로 `document.body`에 직접 렌더링해서 해결 — 실제 확인. (버그 수정) "관련 항목" 탭이 일부 곡(예: 김하온 "Skrr")에서 완전히 빈 채로 뜨는 문제 발견 — `/recommend`가 Last.fm `artist.getSimilar`에만 의존하는데, 국내 힙합 아티스트는 이 데이터 자체가 없는 경우가 실제로 있었음(다른 즐겨찾기 태그 기능에서도 같은 한계를 겪은 적 있음). 유사 아티스트가 하나도 없으면 그 아티스트 자신의 인기곡(`artist.getTopTracks`)으로 폴백하도록 수정 — 실제 확인. (UI 개선) "관련 항목"(유사 아티스트 인기곡)과 "다음 트랙"(유사 곡 큐)이 실사용해보니 결국 둘 다 "비슷한 곡 목록"이라 너무 비슷하다는 피드백으로 "관련 항목" 탭을 완전히 제거(탭이 "다음 트랙"/"가사" 2개로 줄어듦, `recommendByArtist` 호출도 `MiniPlayer`에서 제거). 대신 유튜브 뮤직의 "다음 트랙 전체를 재생목록에 저장" 버튼을 참고해서, "다음 트랙" 탭 목록 위에 "＋ 재생목록에 저장" 버튼을 추가 — 큐에 있는 곡을 하나씩 담을 필요 없이 지금 뜬 목록 전체를 한 번에 재생목록에 옮길 수 있음. 새 백엔드 API 없이, 이미 여러 곡을 받도록 일반화해둔 `AddToPlaylistModal`(재생목록 상세 화면의 "담기" 버튼과 같은 컴포넌트)을 그대로 재사용 — 라이브 확인 대기 ([[학습노트 35번]])
- [ ] 가사 탭 실제 연동(LRCLIB) — 계속 보류하던 가사 기능. Genius API는 라이선스 때문에 가사 원문을 안 주고 YouTube Data API엔 가사 필드가 아예 없어서 막혀있었는데, API 키 없이 무료로 크라우드소싱 가사(동기화 LRC 타임스탬프 포함)를 제공하는 LRCLIB(lrclib.net)을 찾아서 연동. 백엔드 `GET /lyrics`(LRCLIB `/api/search` 프록시, 30일 캐싱) 신규 추가 — "최대한 많은 곡에서 가사가 나왔으면 좋겠다"는 요청으로 폴백을 4단계(정확 매칭 → 자유 검색어 → "(Prod. ...)"/"(Feat. ...)" 같은 괄호 표기 제거 후 정확 매칭 → 제거 후 자유 검색)까지 확장. Musixmatch 같은 추가 소스도 검토했으나 무료 티어가 가사 전체가 아닌 일부만 미리보기로 주는 한계 때문에 지금은 LRCLIB만 쓰기로 결정(라이브 검증 후 필요하면 재검토). 확장 재생 화면 "가사" 탭에서 동기화 가사가 있으면 재생 위치(`currentTime`)에 맞춰 지금 부르는 줄을 굵게 하이라이트하고 자동 스크롤, 없으면 일반 가사 텍스트만 표시, 아예 없으면 "가사를 찾지 못했어요" 안내. 가사가 긴 곡에서 왼쪽 앨범아트가 화면 밖으로 밀려나는 레이아웃 버그(flex/grid `min-height: auto` 기본 동작 때문에 오른쪽 탭 콘텐츠가 부모 높이를 무시하고 계속 커지던 문제)를 `min-h-0` 체인으로 수정. 가사 자동 스크롤이 사용자가 직접 스크롤하는 것과 계속 충돌하던 문제는 휠/터치 조작 시 4초간 자동 스크롤을 멈추는 방식으로 완화했고, 가사 줄을 클릭하면 그 시점으로 바로 이동(seek)하는 기능도 추가 — 라이브 확인 대기. (버그 수정) 일부 곡(아이유 "라일락")에서 하이라이트/클릭 이동이 전혀 동작 안 하는 문제 발견 — `_first_lyrics_match()`가 LRCLIB 검색 결과 중 동기화 가사 유무를 안 따지고 "가사가 있으면(동기화든 일반이든) 그냥 첫 번째 후보"를 쓰고 있어서, 목록 앞쪽에 일반 가사만 있는 후보가 있으면 뒤에 동기화 가사가 있는 후보가 있어도 놓치는 구조였음. 후보 중 동기화 가사가 있는 걸 최우선으로 고르도록 수정 — 라이브 확인 대기 ([[학습노트 33번]]). (커버리지 확대) "가사/추천이 이 사이트 핵심이라 빈 곳이 있으면 안 된다"는 요청으로, LRCLIB 4단계를 전부 실패했을 때만 시도하는 5번째 자동 폴백으로 `lyrics.ovh`(무료, 키 불필요, 일반 텍스트만 제공, 동기화 없음) 추가 — 이 서비스는 LRCLIB과 달리 데이터 출처가 공식적으로 안 밝혀져 있어 저작권 측면에서 회색지대라는 점을 사용자에게 명시적으로 전달하고, "개인 포트폴리오 수준에서 감수 가능"이라는 승인을 받은 뒤 진행. 캐시 키 버전업(v2) 포함 — 라이브 확인 대기 ([[학습노트 43번]]). (안전장치 추가) 회색지대 소스를 쓰는 것에 대한 불안으로, 가사 탭에 "커뮤니티 데이터베이스 출처 · 저작권자 요청 시 즉시 삭제" 고지 문구를 추가하고, 실제 삭제 요청 대응용 `POST /admin/lyrics/takedown`(override/캐시 삭제 + 영구 차단 목록 등록) + 관리자 페이지 삭제 폼을 신규 추가 — 라이브 확인 대기 ([[학습노트 44번]])
- [ ] 차트/추천/무드 탐색/"다음 트랙" 큐에 가사 있는 곡만 필터링 — "가사 없는 곡을 계속 마주치니, 둘러보는 목록엔 가사 있는 곡만 나왔으면 좋겠다"는 요청. `/lyrics`의 조회 로직을 `_lookup_lyrics()`로 뽑아 공유하고, 여러 후보의 가사 유무를 스레드풀(`ThreadPoolExecutor`, 동시 8개)로 병렬 확인하는 `_filter_tracks_with_lyrics()`를 추가 — 순차 호출하면 후보 수만큼 LRCLIB 왕복이 쌓여 느려지는 걸 피함. `/chart`·`/discover-by-mood`·`/recommend`·`/similar-tracks`(다음 트랙 큐)에 적용 — "노래 개수를 줄이라는 게 아니다"라는 요청에 맞춰 각 엔드포인트가 Last.fm에서 필요한 것보다 훨씬 더 많이(예: 최종 10곡이 필요하면 30곡) 미리 받아온 뒤, 그중 가사가 실제로 있는 곡만 걸러서 원래와 비슷한 개수로 맞춤. 검색(`/search`)은 의도적으로 제외 — 사용자가 정확히 그 곡 제목을 검색했는데 가사가 없다는 이유로 결과에서 빠지면 "분명 검색했는데 왜 안 나오지" 싶을 수 있어서, "둘러보기" 목적의 목록에만 한정. 한 번 확인한 곡은 `/lyrics`와 같은 캐시(30일)를 공유해서, 필터링 중 확인된 곡을 나중에 가사 탭에서 다시 열어도 LRCLIB을 또 안 부름 — 라이브 확인 대기
- [ ] 가사 요청 + 수동 등록 기능 — "검색해서 다시 들으려는 곡인데 가사가 없으면, 좋아요처럼 '가사 올려주세요' 버튼을 눌러서 요청하고, 운영자가 확인 후 직접 등록해주면 안 되냐"는 피드백. 가사 탭에서 가사를 못 찾았을 때 뜨는 안내에 "가사 올려주세요" 버튼 추가 — 누르면 `POST /lyrics/request`가 기존 이벤트 로깅 파이프라인(`log_event`)에 `lyrics_request` 이벤트로 남김(실시간으로 가사가 생기진 않음). 운영자용 `POST /lyrics/override`(관리자 전용, `ADMIN_SECRET` 환경변수 + `X-Admin-Secret` 헤더로 접근 제한)를 신규 추가해서, 요청 로그를 보고 직접 구한 가사를 특정 곡에 등록하면 이후 해당 곡은 LRCLIB 조회 없이 등록된 가사가 항상 우선 적용됨(캐시에 만료 없이 저장). 로그인/권한 시스템이 아예 없는 프로젝트라 실제 인증 대신 최소한의 접근 제한으로 범위를 좁힌 선택 — 라이브 확인 대기. (기능 완성) 요청은 쌓이는데 처리할 화면이 없어서 "계속 안 올라가면 안 되지 않냐"는 지적으로, `GET /admin/lyrics-requests`(미해결 요청만 집계) + `/admin/lyrics` 관리자 페이지(비밀번호 게이트 + 목록 + 가사 등록 폼) 신규 추가. `POST /lyrics/override`는 가사 본문이 길어서(URL 길이 제한 우려) 쿼리 파라미터 대신 JSON 바디 방식으로 변경 — 라이브 확인 대기 ([[학습노트 42번]])
- [ ] Spring Batch로 정기 배치 구현: (1) 즐겨찾기 태그를 취향 프로필로 집계해서 "이런 취향이면 좋아할 만한 신곡" 추천에 쓰기, (2) N개월간 재생 이벤트 없는 곡을 "거의 안 듣는 곡" 그룹으로 자동 분류 (2번은 위 "안 듣는 곡 정리" 온디맨드 기능으로 상당 부분 대체됨 — 배치로 남길지는 (1) 취향 기반 추천 쪽만 남기고 재검토 필요)
  - **왜**: 본인이 겪는 실제 불편(플레이리스트가 넣은 순서대로만 재생됨, 안 듣는 곡이 안 걸러짐)에서 나온 아이디어. (1)은 원래 "즐겨찾기 목록을 태그별로 접어서 정리"하는 UI 그룹핑으로 시작했는데, 실제 써보니 곡이 몇 곡 안 될 때는 그룹만 늘고 눌러도 아무 동작이 없어 오히려 복잡하다는 피드백을 받고 UI 노출은 걷어냄([[학습노트 17번]] 참고). 대신 본인이 정리한 태그의 진짜 목적("이 곡 좋다"는 취향 신호를 데이터로 쌓아서, 나중에 비슷한 취향의 신곡을 추천해주는 것)에 맞춰 방향을 재정의 — 즐겨찾기 목록을 정리하는 기능이 아니라, 취향 기반 추천의 입력 데이터를 쌓는 기능으로 스코프를 좁힘. Spotify/YouTube Music도 1단계는 태그·메타데이터 기반 취향 프로필 + 2단계로 오디오 분석/협업 필터링을 얹는데, Last.fm은 오디오 분석 API가 없고(Spotify도 2024년 말부터 신규 앱엔 막음) 태그만 제공하므로 여기선 태그+행동 데이터(재생 이력) 기반 규칙/배치 처리로 한정 — LLM/대화형 추천(보류 항목)과는 별개.
  - **진행 상황**: 태그 데이터(`FavoriteTrack.tags`)와 "재생 의도 신호"(`FavoriteTrack.lastOpenedAt`, 곡 카드 클릭 시 갱신)는 먼저 만들어서 UI 노출 없이 계속 쌓고 있음 — 실제 배치/추천 로직은 데이터가 며칠 쌓인 뒤 착수 예정.
  - **홈 화면 "빠른 선곡" 섹션에 우선 라이트 버전 구현**: "지금 인기 있는 곡"이 실시간 차트라 새로고침해도 안 바뀌는 게 원래 의도인데, "유튜브 뮤직처럼 새로고침하면 좀 달라졌으면 좋겠다"는 피드백 → "그럼 추천 AI를 넣을 순 없냐"는 질문으로 이어짐. `AI Agent/LLM 자체 개발은 이번 로드맵에서 제외`(맨 위 목표 항목)라는 처음 결정을 그대로 지키되, 위에서 이미 쌓고 있던 태그 데이터를 활용해 "차트만큼 정교한 배치 기반 취향 프로필은 아니지만 최소한 실제 취향 신호에 뿌리를 둔" 절충안으로 구현. 즐겨찾기 태그 중 가장 많이 나온 태그 하나를 프론트에서 즉석으로 골라 `discoverByMood()`(무드 탐색과 동일 엔드포인트 재사용)로 후보를 받고, 그 후보를 새로고침마다 무작위로 섞어서(Fisher-Yates) "빠른 선곡" 섹션에 9곡 노출. 즐겨찾기가 아직 없는 신규 사용자는 취향 신호가 없으니 글로벌 차트를 대신 섞어서 보여줌(이 경우는 "취향 기반"이 아니라 "그냥 무작위 노출"이라는 한계를 인지하고 있음). 새 백엔드 엔드포인트 없이 프론트에서만 구현. 같이 요청받은 "다시 듣기" 메인 화면 섹션(기존엔 사이드바에만 있던 즐겨찾기 목록을 홈 화면에도 노출)도 함께 추가. (버그 수정) 라이브 확인 중 "빠른 선곡"도 [[학습노트 36번]]과 똑같이 한 아티스트로 도배되는 문제 발견 — 즐겨찾기 태그 중 제일 많은 태그가 "pop"처럼 너무 넓으면 그 태그 차트도 결국 그 순간 글로벌 차트를 뒤덮은 아티스트를 그대로 반영해서였음. 태그 대신 즐겨찾기한 아티스트 하나를 무작위로 골라 그 아티스트의 유사 아티스트 추천(`/recommend`)을 쓰도록 변경해서 해결 — 라이브 확인 대기 ([[학습노트 37번]]). (버그 수정) "다시 듣기는 최근 재생이 맞는데 즐겨찾기는 오른쪽 사이드바에 따로 있는데 왜 둘이 같은 걸 보여주냐, 중복 아니냐"는 지적 — 실제로 "다시 듣기"가 즐겨찾기 데이터를 그대로 재사용하고 있었음(추가 당시엔 "사이드바 전용이던 즐겨찾기를 메인에도 노출"이 목적이라 합리적이었으나, 이후 사이드바에 "즐겨찾기"라는 이름의 전용 섹션이 자리 잡으며 중복이 됨). 백엔드에 `POST/GET /recent-plays`(Redis 세션별 목록, `/recent-searches`와 같은 패턴, 최대 20개·TTL 90일) 신규 추가, 프론트는 유튜브 플레이어가 실제로 재생을 시작한 시점(`handleActuallyPlaying`)에 기록을 남기도록 하고, "다시 듣기" 섹션 데이터 소스를 즐겨찾기에서 이 최근 재생 기록으로 교체 — ast.parse/tsc만 확인, 라이브 확인 대기 ([[학습노트 49번]])
  - **"빠른 선곡"에 실제 LLM(Claude Haiku 4.5) 추천 도입 — 이 프로젝트 첫 유료 API**: 위 라이트 버전으로 답한 뒤에도 "요즘 유튜브가 쓰는 방식을 실제로 해보고 싶다"는 재요청 — 이번엔 `AI Agent/LLM 자체 개발 제외` 원칙 자체를 사용자가 직접 재검토하고 승인함(진짜 유튜브 뮤직 수준 자체 학습 모델은 혼자 만드는 범위를 벗어난다는 점을 먼저 설명하고, 현실적 등가물로 LLM API 활용을 제안 → 호출당 비용(~$0.002~0.003, Haiku 4.5 기준)을 먼저 계산해서 공유하고 동의를 받은 뒤 진행). 백엔드에 `POST /quick-picks/ai` 신규 추가 — 즐겨찾기 목록 전체를 Claude Haiku 4.5에게 보여주고 새 곡 추천을 받은 뒤, 모델이 실존하지 않는 곡을 지어낼 위험(hallucination)에 대응하기 위해 추천받은 각 곡을 Last.fm `track.search`로 재검증해서 실존이 확인된 곡의 진짜 메타데이터만 응답에 담음(LLM은 "이름표"만 제안, 실제 데이터는 항상 기존 검증된 소스에서 재조회). 세션+날짜 단위로 하루 한 번만 호출하도록 Redis 캐싱해서 비용 통제. 프론트는 이 엔드포인트가 실패(키 없음/호출 실패/빈 결과)하면 조용히 기존 아티스트 기반 방식(바로 위 항목)으로 폴백 — 키 없는 환경에서도 앱이 죽지 않게 함. `anthropic` 패키지를 `requirements.txt`에 추가하고 `ANTHROPIC_API_KEY`를 `.env`/`.env.example`에 새로 추가(값은 비어있음, 커밋 금지 — console.anthropic.com에서 직접 발급 필요) — 라이브 확인 완료: 네트워크 탭에서 `POST /quick-picks/ai` 200 OK 확인, 즐겨찾기에 없던 새 아티스트들이 추천에 섞여 나오는 것도 확인 ([[학습노트 38번]]). (이미지 개선) 라이브 확인 중 일부 트랙이 이미지 없이 플레이스홀더로 뜨는 걸 보고 "유튜브 뮤직은 쇼미더머니 참가곡처럼 비공식 발매곡도 이미지가 뜨는데 우리도 다양하게 못 쓰냐"는 요청 — iTunes/Last.fm 둘 다 실패한 곡에 한해 그 곡의 유튜브 영상 썸네일(`i.ytimg.com` 공개 정적 URL, API 쿼터 안 씀)로 마지막 보강하는 `_youtube_thumbnail_fallback()` 추가. 유튜브 검색 자체는 쿼터 제한(하루 약 100회)이 있는 리소스라, 곡 수가 많은 "다시 듣기"까지 확대할지 사용자에게 직접 물어봤고 "빠른 선곡만 적용"으로 범위를 좁힘 — `/youtube-search`(재생 버튼)와 정확히 같은 캐시 키를 공유해서 같은 곡에 대한 쿼터 소비가 두 기능 간에 중복 안 되게 설계 — 라이브 확인 대기 ([[학습노트 39번]]). (UI 개선) "다시 듣기"/"빠른 선곡" 둘 다 "양을 늘리고 가로로 스크롤할 수 있게 해달라"는 요청 — `CompactTrackGrid`가 무드 탐색/검색 결과 등 5곳에서 재사용되는 공용 컴포넌트라 임의로 전체를 바꾸지 않고 적용 범위를 사용자에게 확인, "다시 듣기/빠른 선곡 두 곳만" 적용하기로 함. `scrollable` prop을 추가해서 켜면 페이지네이션 버튼 대신 3행 고정+가로 드래그 스크롤로 표시(무드 탐색/검색 결과는 기존 방식 그대로 유지, 회귀 없음). "빠른 선곡" 곡 수도 9→15로 증량(백엔드 `AI_QUICK_PICKS_COUNT`, 프론트 슬라이스 상한, 캐시 키 v2→v3 동반 조정) — 라이브 확인 대기 ([[학습노트 40번]])
- [ ] 구독/프리미엄 결제 모의 플로우 추가 (바디코디의 결제/정산 도메인 커버)
- [ ] JUnit 기반 테스트 코드 작성
- [ ] 코드 리뷰 컨벤션 정리 (ktlint/checkstyle), 리팩터링 1회 이상 기록

---

## Phase 3. TypeScript / NestJS (2~3주)
**타겟 공고:** 러쉬에잇, 일본 이커머스 포지션

- [ ] 알림 서비스 분리 (NestJS, 신곡 알림 등)
- [ ] SQS/SNS를 LocalStack으로 흉내내어 이벤트 기반 아키텍처 구현
- [ ] PostgreSQL 또는 MongoDB 중 하나로 NoSQL/RDBMS 다양성 확보

---

## Phase 4. 인프라 / 운영 마무리 (2~3주)
**타겟 공고:** 러쉬에잇, 포스타입, 바디코디 (공통)

- [ ] Docker Compose로 전체 서비스 오케스트레이션
- [ ] GitHub Actions CI/CD 파이프라인 구축
- [ ] Terraform으로 AWS 리소스 일부 IaC화 (선택)
- [ ] Prometheus + Grafana (또는 CloudWatch)로 모니터링 환경 구축
- [ ] 장애 대응/재발 방지 문서 1건 작성 (가상 장애 시나리오 기반)
- [ ] Metabase 등으로 AARRR 대시보드 시각화 (BI 툴 경험)

---

## 보류 (추후 확장 옵션)
- MCP 기반 자연어 음악 추천 에이전트 (Tool calling, 멀티스텝 추론)
  - 제조업 AI Agent 공고, 디자이노블 LLM 우대사항 대응용으로 나중에 고려

---

## 채용공고 ↔ 스킬 매핑

| 회사 | 핵심 스택 | Unbeat에서 커버하는 부분 |
|---|---|---|
| 페이타랩 | Python, SQL, Snowflake/DW, dbt, AARRR | Phase 1 전체 |
| 디자이노블 | Python/TS, REST API, Git, AI 도구 활용 | Phase 1 + 전체 개발 과정(Claude 활용) |
| 바디코디 | Java/Kotlin, Spring Boot, Spring Batch, MySQL, Redis, SQS/SNS | Phase 2 |
| 드림어스 | Java/Kotlin/Go, REST API, RDBMS/SQL | Phase 2 |
| 포스타입 | Spring(Boot/Batch), JPA, AWS 전반 | Phase 2 + Phase 4 |
| 러쉬에잇 | TypeScript, NestJS, PostgreSQL/MongoDB, Docker/K8s, Terraform, CI/CD, BI툴 | Phase 3 + Phase 4 |
| 일본 이커머스 포지션 | TS/Node/Python, PostgreSQL/Redis/DynamoDB, 결제(KOMOJU/PayVerse) | Phase 2(결제) + Phase 3 |

---

## 낮은 우선순위 (스킵)
- PHP / Classic ASP / MSSQL — 공고 1건, 레거시 스택으로 학습 우선순위 낮음
