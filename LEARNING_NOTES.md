# Unbeat 프로젝트 노트 — 뭘/왜/어떻게

면접에서 "이 프로젝트에서 뭘 했냐"고 물어봤을 때 그대로 설명할 수 있게 정리한 문서.
새로운 걸 만들 때마다 여기 이어서 추가함.

---

## 0. 우선순위를 어떤 기준으로 정했나 (면접에서 "왜 이 순서로 했냐" 대비)

이 프로젝트는 특정 회사 하나가 아니라 지원하려는 여러 채용공고(페이타랩, 바디코디, 드림어스, 포스타입, 러쉬에잇, 디자이노블 등)의 요구 기술을 겹치지 않게 커버하는 게 목적. 그래서 매번 "뭘 먼저 할지"를 이 세 가지 기준으로 판단함.

1. **여러 공고에 걸쳐 겹치는 기술인가, 한 곳에만 필요한 기술인가**: 겹치는 기술을 먼저 함. 예를 들어 Redis는 바디코디(필수), 포스타입(AWS ElastiCache), 러쉬에잇(우대사항)까지 세 곳에 걸쳐 있는데, dbt나 외부 API 연동은 페이타랩 우대사항 한 곳에만 해당함. 그래서 Redis를 dbt보다 먼저 함.
2. **필수 요건인가, 우대사항인가**: 필수 요건에 더 가까운 걸 먼저 함.
3. **투자 시간 대비 포트폴리오에 보여줄 수 있는 효과가 큰가**: Redis 캐싱은 반나절이면 구현 가능한데(캐시 확인 -> 없으면 원본 조회 -> 저장, 이 패턴 하나만 넣으면 됨) 여러 회사 요구사항을 동시에 채우는 반면, dbt 프로젝트는 세팅(모델 구조, staging/mart 레이어, 테스트)에 시간이 더 들면서 커버하는 회사는 하나뿐이라 "투입 대비 효과"가 상대적으로 낮음.

큰 틀에서 Phase 순서(Python -> Kotlin/Spring Boot -> TypeScript/NestJS -> 인프라)를 정한 것도 같은 논리: Python을 제일 먼저 깊게 판 건 페이타랩(1순위 타겟)이 Python/데이터 중심이라서고, 그 다음이 Kotlin(바디코디/드림어스/포스타입 3곳 커버)인 것도 "여러 곳에 겹치는 기술 먼저" 원칙을 그대로 적용한 것.

**레포 구조(모노레포 vs 폴리레포)를 정한 이유**: GitHub 레포를 `unbeat` 하나로 유지하고 그 안에 `frontend/`, `backend/`, `data/` 폴더로만 나눔 (레포 자체를 서비스별로 쪼개지 않음). Kotlin/NestJS 서비스가 추가되는 Phase 2~3에서도 이 구조를 유지할 계획. 이유: (1) 이미 이 구조로 커밋 히스토리/PR 흐름을 쌓아둬서 지금 쪼개면 재작업 비용만 큼, (2) Google/Vercel 등 실무에서도 모노레포가 흔한 선택지라 "레포 분리 = 정답"은 아님, (3) 레포 분리(폴리레포)는 이미 경험 있어서, 모노레포를 직접 겪어보고 두 방식의 장단점을 비교해서 말할 수 있는 게 더 가치있다고 판단.

---

## 1. Spotify -> Last.fm 전환

**뭘 했나**: 원래 Spotify API로 아티스트 검색/추천을 만들려다, Last.fm API로 바꿈.

**왜 이렇게 했나**: Spotify는 최근 신규 앱에 Extended Quota Mode(정식 서비스 심사)를 요구해서 개인 프로젝트로 승인받기 어려움. Last.fm은 회원가입만 하면 API 키가 즉시 발급되고 심사가 없음. 포트폴리오 목적에선 "어떤 API를 썼냐"보다 "그 위에 어떤 백엔드/데이터 구조를 짰냐"가 중요해서 API 자체를 바꾸는 게 리스크가 적은 선택이었음.

**어떻게 동작하나**:
- Last.fm은 OAuth 로그인이 필요 없음 (읽기 전용 API는 `api_key` 파라미터만 있으면 호출 가능). 그래서 원래 있던 Spotify OAuth 로직(`/login`, `/callback`)을 다 걷어냄.
- `backend/main.py`의 `lastfm_get()` 함수가 모든 Last.fm 호출의 공통 창구. `artist.search`(아티스트 검색), `artist.getSimilar`(유사 아티스트), `artist.getTopTracks`(인기곡) 세 개 메서드를 씀.

**트러블슈팅 포인트 (면접에서 말할 거리)**:
- Last.fm 응답에 charset이 명시가 안 돼서 `requests` 라이브러리가 인코딩을 잘못 추측 -> 한글이 깨짐. `res.encoding = "utf-8"`로 명시적 고정해서 해결.
- FastAPI 기본 JSONResponse는 `Content-Type: application/json`에 `charset=utf-8`을 안 붙여서, 브라우저가 직접 API 주소로 들어갔을 때 한글을 잘못 렌더링. `default_response_class`를 커스텀 `UTF8JSONResponse`로 바꿔서 해결.
- 로컬 Python이 3.9라서 3.10+ 문법인 `str | None`을 못 씀 -> `typing.Optional[str]`로 바꿔서 해결 (Python 버전별 문법 차이를 실제로 겪은 사례).

---

## 2. 프론트엔드 (Next.js + TypeScript)

**뭘 했나**: `frontend/`에 Next.js 14 (App Router) + TypeScript + Tailwind로 검색/추천 화면을 만듦.

**왜 이렇게 했나**: 채용공고들(러쉬에잇, 디자이노블, 일본 이커머스 포지션)이 공통으로 TypeScript 프론트 경험을 요구해서 이 스택으로 선택. App Router는 최신 Next.js 표준 방식.

**어떻게 동작하나**:
- `lib/api.ts`: 백엔드를 호출하는 fetch 래퍼. 백엔드 주소는 `.env.local`의 `NEXT_PUBLIC_API_URL`로 분리 (하드코딩 안 함 -> 나중에 배포 환경 바뀌어도 코드 수정 없이 환경변수만 바꾸면 됨).
- `components/SearchBar.tsx`: 검색어 입력받는 폼. `"use client"` 지시어가 붙은 이유는 `useState`(브라우저에서 상태를 가져야 하는 컴포넌트) 때문 — Next.js App Router는 기본이 서버 컴포넌트라, 상태/이벤트 핸들러가 필요한 컴포넌트만 명시적으로 클라이언트 컴포넌트로 선언해야 함.
- `components/ArtistCard.tsx`, `TrackList.tsx`: 각각 검색 결과 카드, 추천 트랙 리스트를 렌더링만 하는 단순 컴포넌트 (상태 없음 -> 서버 컴포넌트로 둬도 되지만 지금은 부모가 클라이언트라 같이 클라이언트로 동작).
- `app/page.tsx`: 위 컴포넌트들을 조합하는 메인 화면. 검색 -> 아티스트 선택 -> 추천 흐름의 상태(`artists`, `tracks`, `selectedArtist` 등)를 여기서 관리.

---

## 3. 이벤트 로깅 (MariaDB)

**뭘 했나**: 사용자가 검색하거나 아티스트를 클릭할 때마다 그 행동을 MariaDB `unbeat.events` 테이블에 기록.

**왜 이렇게 했나**: AARRR/퍼널/코호트 분석을 하려면 "누가 언제 무엇을 했는지" 원본 데이터가 있어야 함. 로그인 기능이 없는 서비스라 "누가"에 해당하는 걸 익명 세션ID로 대체함 (GA4의 client_id와 같은 개념).

**어떻게 동작하나**:
- `frontend/lib/session.ts`: 브라우저 `localStorage`에 UUID를 한 번 만들어두고 계속 재사용. 이게 익명 사용자 식별자.
- 모든 API 요청에 `X-Session-Id` 헤더로 이 값을 실어 보냄 (`lib/api.ts`의 `fetchJson`).
- `backend/db.py`: MariaDB 연결과 `log_event()` 함수. 이벤트 적재가 실패해도 예외를 삼키고 조용히 넘어가게 만듦 — **이유**: 로깅은 부가 기능이라, 로깅이 실패했다고 실제 검색 API 응답까지 500 에러로 죽으면 안 되기 때문 (핵심 기능과 부가 기능의 장애 격리).
- `backend/schema.sql`: `events` 테이블 스키마. `session_id`, `event_type`(search/recommend_click), `artist_name`, `result_count`, `created_at` 컬럼. `session_id`, `event_type`, `created_at`에 인덱스를 걸어둠 — **이유**: 나중에 이 컬럼들로 GROUP BY/WHERE를 자주 할 거라서.

**트러블슈팅 포인트**:
- MariaDB `unbeat` 전용 계정을 만들 때, root 계정을 건드리지 않고 별도 계정(`unbeat`)을 만들어서 씀 — **이유**: 최소 권한 원칙(least privilege). 앱이 root로 DB에 붙으면 보안 사고 시 피해 범위가 커짐. `GRANT ALL PRIVILEGES ON unbeat.*`로 딱 이 프로젝트 DB에만 권한을 줌.
- `.env` 값을 바꿨는데도 예전 root 계정으로 계속 접속 시도하는 버그가 있었음. 원인은 `main.py`에서 `db.py`를 import하는 코드가 `load_dotenv()` 호출보다 먼저 실행되고 있었던 것 — `db.py`가 모듈 로드 시점에 환경변수를 읽는데, 그 시점에 `.env`가 아직 로드되기 전이라 기본값(root)으로 떨어짐. **import 순서가 실행 순서에 영향을 준다는 걸 보여주는 실제 버그 사례** (면접에서 "겪어본 버그" 얘기할 때 좋은 소재).

---

## 4. DuckDB 분석 웨어하우스 + AARRR 분석

**뭘 했나**: `data/` 폴더에 MariaDB의 이벤트를 DuckDB로 옮기는 파이프라인과, 그 위에서 퍼널/코호트 분석 SQL을 만듦.

**왜 이렇게 했나**:
- **OLTP/OLAP 분리**: MariaDB는 서비스 운영용(트랜잭션 처리에 최적화), DuckDB는 분석 전용(대량 집계 쿼리에 최적화). 실무에서 분석 쿼리를 운영 DB에 직접 날리면 서비스 자체에 부하를 줄 수 있어서, 별도 분석 저장소로 복사해서 쓰는 패턴을 씀. Snowflake 같은 클라우드 DW를 로컬에서 무료로 흉내낸 것.
- **DuckDB를 고른 이유**: Snowflake는 유료/클라우드라 개인 프로젝트에 부담. DuckDB는 로컬 파일 하나로 동작하면서 SQL 문법과 컬럼 지향 저장 방식(분석 쿼리에 유리)이 Snowflake랑 비슷해서, "DW 다뤄본 경험"을 무료로 연습하기 좋음.

**어떻게 동작하나** (`data/` 폴더):
- `seed_events.py`: 실제 트래픽이 아직 적어서, 분석해볼 만한 양의 "가짜지만 현실적인" 이벤트를 만들어 MariaDB에 넣는 스크립트. 8주치 기간에 걸쳐 150개 세션을 만들고, 각 세션마다 "가입 주는 무조건 활동, 이후 주차는 확률적으로 재방문" 하는 리텐션 곡선을 흉내냄. **왜 손으로 검색 안 하고 스크립트로 만드나**: 코호트 분석은 몇 주치 데이터가 필요한데, 수십 번 손으로 검색해서는 현실적인 날짜 분포를 만들 수 없기 때문. 실무에서도 테스트/분석용 더미 데이터는 스크립트로 시딩하는 게 일반적.
- `etl_load.py`: MariaDB `events` 테이블 전체를 읽어서(`pymysql` + `pandas`), DuckDB 파일(`unbeat.duckdb`)의 `raw_events` 테이블로 통째로 복사. 이게 "ETL(Extract-Transform-Load)"의 가장 단순한 형태 (지금은 Transform이 없는 E->L에 가까움, 나중에 dbt로 Transform 레이어를 추가할 예정).
- `sql/funnel.sql`: 세션 단위로 "검색은 했는데 추천을 클릭까지 했는지" 전환율 계산. 퍼널 분석의 기본형.
- `sql/funnel_weekly.sql`: 위 전환율을 주차별로 쪼개서, 시간에 따른 추이를 봄.
- `sql/cohort_retention.sql`: 각 세션의 첫 방문 주(cohort_week)를 기준으로, 그 이후 몇 주차까지 다시 활동했는지 집계. `date_trunc('week', ...)`로 주 단위로 묶고, `date_diff('week', ...)`로 가입 후 경과 주차를 계산.
- `run_analysis.py`: 위 SQL들을 실행해서 결과를 보여주고, 코호트 결과는 pandas `pivot()`으로 "가입주 x 경과주차" 표로 만든 다음, 0주차(가입 시점) 대비 %로 변환해서 리텐션 표를 만듦 — 이게 AARRR의 **Retention** 지표를 시각적으로 보여주는 형태.

**면접에서 말할 포인트**: "왜 굳이 OLTP/OLAP을 분리했나"라고 물으면 위 이유로 답하면 됨. "코호트 분석 결과를 어떻게 해석하나"는 — 가입 주가 최근일수록 아직 관찰 가능한 경과 주차가 적어서 표 오른쪽 위가 비어있는 게 정상(데이터가 없는 게 아니라 아직 그 시점이 안 지난 것)이라는 점도 같이 설명하면 좋음.

---

## 5. Redis 캐싱 (cache-aside 패턴)

**뭘 했나**: `/search-artist`, `/recommend`에 Redis 캐싱을 추가함. 같은 아티스트를 다시 검색하면 Last.fm API를 또 안 부르고 Redis에 저장된 결과를 바로 돌려줌.

**왜 이걸 먼저 했나** (dbt/외부 API보다 우선): 여러 채용공고에 걸쳐 겹치는 기술을 먼저 채우는 게 효율적이라 판단함. Redis는 바디코디(필수), 포스타입(AWS ElastiCache), 러쉬에잇(우대사항) 세 곳에 걸쳐 나오는데, dbt/외부 API 연동은 페이타랩 우대사항 한 곳에만 해당함. 게다가 캐싱은 반나절이면 구현 가능한 반면 dbt는 프로젝트 구조 세팅에 시간이 더 드는데 커버하는 회사는 하나뿐이라, 투입 시간 대비 효과를 봐도 Redis가 우선순위가 높음.

**어떻게 동작하나**:
- `backend/cache.py`: Redis 클라이언트와 `get_cached()`/`set_cached()` 헬퍼. **cache-aside 패턴**이란: (1) 요청이 오면 먼저 캐시를 확인하고 (2) 있으면 그대로 반환, (3) 없으면 원본(Last.fm API)을 조회한 뒤 (4) 그 결과를 캐시에 저장하고 반환 — 이렇게 "캐시가 원본 데이터 소스 옆에 나란히 붙어서 조회를 가로채는" 구조라 cache-aside(캐시를 옆에 낀다)라고 부름.
- 캐시 키는 `search-artist:아이유`, `recommend:아이유`처럼 `엔드포인트:아티스트명` 형태로 만듦. 대소문자/공백 차이로 캐시가 갈라지지 않게 `lower().strip()` 처리.
- TTL(캐시 유지시간)은 1시간으로 설정 — 아티스트 정보가 실시간으로 바뀔 이유가 없어서 넉넉하게 잡음. 너무 길게 잡으면 데이터가 오래돼도 안 갱신되고, 너무 짧으면 캐싱 효과가 없어서 이 트레이드오프를 고려한 값.
- 응답에 `"cached": true/false`를 넣어서, 이번 요청이 캐시에서 왔는지 Last.fm에서 새로 가져왔는지 눈으로 바로 확인할 수 있게 함 (원래 실제 서비스 응답엔 안 넣는 필드지만, 포트폴리오 데모용으로 캐시 동작을 보여주기 위해 추가).
- Redis 연결이 실패해도(`redis.RedisError`) 예외를 삼키고 그냥 캐시 없이 진행하게 만듦 — **이벤트 로깅과 같은 이유**: 캐싱은 성능 최적화용 부가 기능이라, 캐시 서버가 죽었다고 핵심 기능(검색/추천)까지 같이 죽으면 안 됨 (장애 격리).

**면접에서 말할 포인트**: "왜 TTL을 1시간으로 잡았나" -> 데이터 특성(아티스트 정보는 자주 안 바뀜)에 맞춘 트레이드오프 판단이라고 설명하면 됨. "캐시 무효화(invalidation)는 어떻게 하나" -> 지금은 TTL 만료로만 처리하는 단순한 구조고, 데이터가 실시간성이 중요했다면 쓰기 시점에 캐시를 지우는 write-through/invalidate 전략이 필요했을 거라는 것도 같이 설명하면 좋음.

---

## 6. dbt-duckdb 변환 레이어 (staging -> mart)

**뭘 했나**: `data/dbt_unbeat/`에 dbt 프로젝트를 만들어서, `etl_load.py`가 그대로 복사해온 지저분한 원본(`raw_events`)을 정제된 staging 테이블로 만들고, 그 위에 분석용 mart 테이블(퍼널, 코호트)을 쌓음. 이전에 `data/sql/*.sql` + `run_analysis.py`로 만든 건 "손으로 짠 SQL 스크립트"였다면, 이번 건 그걸 dbt라는 정식 변환 프레임워크로 다시 구조화한 것.

**왜 dbt를 썼나**: 페이타랩 우대사항에 "dbt를 활용한 데이터 품질 관리 및 카탈로그 구축"이 명시돼 있음. 그리고 실무적으로도, `run_analysis.py`처럼 SQL을 스크립트에 흩어놓으면 (1) 어떤 모델이 어떤 모델을 참조하는지 추적이 안 되고 (2) 데이터 품질을 검증할 방법이 없음. dbt는 이 두 문제를 `ref()`(모델 간 의존관계를 코드로 명시)와 `tests`(데이터 품질을 선언적으로 검증)로 해결함.

**어떻게 동작하나**:
- **staging 레이어** (`models/staging/stg_events.sql`): `raw_events` 원본을 정제(공백 제거, 소문자 통일)하는 단 하나의 지점. **왜 staging을 따로 두나**: mart 모델이 여러 개인데 각자 원본의 지저분함(트리밍, 캐스팅)을 처리하게 하면 로직이 중복되고, 나중에 원본 스키마가 바뀌면 모든 mart를 다 고쳐야 함. staging 하나만 원본과 맞닿게 하고, 나머지는 전부 staging만 보게 하면 변경 지점이 하나로 줄어듦.
- **`sources.yml`**: dbt한테 "raw_events는 내가 만드는 테이블이 아니라 외부(ETL)에서 이미 만들어진 테이블"이라고 선언하는 파일. 이렇게 선언해두면 dbt가 "이 소스 테이블이 언제 마지막으로 갱신됐는지" 같은 걸 추적할 수 있음(freshness 체크, 지금은 안 썼지만 실무에서 많이 씀).
- **mart 레이어**: `mart_session_first_seen`(세션별 첫 방문 주 = 코호트 배정 기준), `mart_funnel_weekly`(주차별 퍼널), `mart_cohort_retention`(코호트별 리텐션 원본 카운트). 전부 `{{ ref('stg_events') }}`처럼 다른 모델을 이름으로 참조 — dbt가 이 참조 관계를 보고 실행 순서(의존성 그래프, DAG)를 알아서 정함. 실제로 `dbt run` 로그를 보면 `stg_events`가 제일 먼저 만들어지고, 그다음 나머지 mart들이 만들어지는 걸 볼 수 있음.
- **`schema.yml` 테스트**: `not_null`(빈 값 없어야 함), `unique`(중복 없어야 함), `accepted_values`(event_type이 'search'/'recommend_click' 둘 중 하나여야 함) 같은 데이터 품질 규칙을 SQL 안 짜고 YAML로 선언. `dbt test` 한 번으로 13개 규칙을 한꺼번에 검증함 — 이게 "데이터 품질 관리"의 실제 형태.

**면접에서 말할 포인트**: "raw SQL 스크립트랑 dbt의 차이가 뭐냐"고 물으면 — 의존성 관리(ref)와 선언적 데이터 품질 테스트(schema.yml)가 핵심 차이라고 답하면 됨. 실제로 이 프로젝트에서 두 버전(`data/sql/` 순수 SQL 버전과 `dbt_unbeat/` dbt 버전)을 다 만들어봤기 때문에, 그 차이를 직접 겪은 경험으로 설명할 수 있음.

---

## 7. 외부 데이터 소스 연동 (Open-Meteo 날씨 API)

**뭘 했나**: `data/fetch_weather.py`로 서울 지역 일별 날씨(최고/최저기온, 강수량)를 가져와서 DuckDB `weather_daily` 테이블에 적재하고, `run_analysis.py`에 "날씨 x 검색 활동" 상관관계 분석을 추가함.

**왜 이렇게 했나 (제품 관점)**: Spotify의 "daylist"처럼, 음악 서비스들이 날씨/시간대에 따라 무드가 다른 곡을 추천하는 기능을 갖고 있는 경우가 많음. Unbeat에도 "비 오는 날엔 잔잔한 곡, 맑은 날엔 신나는 곡" 같은 날씨 기반 무드 추천을 나중에 넣을 수 있는데, 기능을 만들기 전에 "정말 날씨가 사용자의 검색 행동에 영향을 주는가?"를 데이터로 먼저 검증해보는 게 이 분석의 목적. 실제로 데이터 분석가/그로스 담당자들이 기능을 만들기 전에 가설을 데이터로 먼저 확인하는 것과 같은 흐름.

**왜 이렇게 했나 (JD 관점)**: 페이타랩 JD에 "외부 데이터 소스(GA, AppsFlyer, 날씨 API 등) 연동 및 적재 경험"이 정확히 명시돼 있어서, 위 제품 가설 검증에 쓸 외부 데이터로 날씨를 선택함. API 중에서도 Last.fm 키 발급 때 겪었던 것처럼 승인 절차가 있는 곳(공공데이터포털 등)은 시간이 걸릴 수 있어서, 키/가입이 아예 필요 없는 Open-Meteo를 선택함.

**어떻게 동작하나**:
- `raw_events`에서 이벤트 기간(최소~최대 날짜)을 먼저 조회하고, 그 기간에 맞춰서만 날씨를 가져옴 — 이벤트랑 무관한 날짜까지 무의미하게 가져오지 않기 위함.
- Open-Meteo의 `archive` 엔드포인트(과거 날씨 기록 조회)를 호출. 위도/경도로 지역을 지정(서울 좌표 하드코딩, 나중에 다른 도시로 바꾸고 싶으면 그 값만 바꾸면 됨).
- `data/sql/weather_vs_search.sql`: 날씨 테이블과 "일별 검색 이벤트 수"를 `LEFT JOIN`으로 결합. **왜 LEFT JOIN인가**: 날씨는 매일 있지만 검색이 하루도 없었던 날도 있을 수 있어서, 그런 날도 0건으로 살아있어야 정확한 분석이 됨 (INNER JOIN 쓰면 검색이 아예 없었던 날이 통째로 사라져서 "비 오는 날 검색이 준다"는 착시가 생길 수 있음).
- `run_analysis.py`에 pandas `.corr()`로 강수량과 검색량의 상관계수를 계산해서 출력. 이게 "외부 데이터를 서비스 데이터랑 결합해서 인사이트를 뽑아내는" 전형적인 그로스/데이터 분석 워크플로우.

**면접에서 말할 포인트**: "왜 이 API를 골랐냐"는 질문엔 "시간 대비 목적 달성"이라는 판단 기준으로 답하면 됨 (Last.fm 사례랑 같은 논리). "상관관계 분석에서 주의할 점"을 물으면 — 상관관계가 있다고 인과관계는 아니라는 것, 그리고 지금 데이터는 가짜 시드 데이터라 실제 유의미한 해석보다는 "이런 분석 파이프라인을 만들 줄 안다"는 걸 보여주는 목적이라는 걸 솔직하게 설명하면 됨.

---

## Phase 1 완료 요약

여기까지 페이타랩/디자이노블 타겟의 Phase 1이 전부 끝남: Last.fm 연동, Next.js 프론트, MariaDB 이벤트 로깅, Redis 캐싱, DuckDB+dbt 분석 웨어하우스, AARRR/퍼널/코호트 분석, 외부 날씨 API 연동까지.

---

## 8. Phase 2 시작 — 즐겨찾기 서비스 (Spring Boot + Kotlin + JPA)

**뭘 했나**: `playlist-service/`라는 폴더에 완전히 새로운 Spring Boot(Kotlin) 서비스를 만듦. "아티스트 즐겨찾기(찜)" 기능 하나만 담당하고, 기존 FastAPI 백엔드(포트 8000)와는 별개 프로세스로 포트 8081에서 돈다. 프론트엔드(`ArtistCard.tsx`의 별표 버튼, `page.tsx`의 "내 즐겨찾기" 목록)도 이 서비스를 호출하도록 같이 연결함.

**왜 즐겨찾기부터 했나 (제품 관점)**: Unbeat은 "검색 -> 추천"만 있고 한 번 본 아티스트를 다시 찾으려면 매번 검색해야 하는 구조였음. 즐겨찾기는 사용자가 실제로 쓸 법한 자연스러운 다음 기능이고(음악 서비스에 없으면 이상한 기능), 억지로 JD 맞추려고 낀 기능이 아님 — 날씨 API 때와 같은 기준으로 먼저 "제품에 필요한가"를 통과시킨 것.

**왜 이걸 Kotlin/Spring Boot로 했나 (JD 관점)**: 바디코디/드림어스/포스타입 세 공고가 공통으로 Java/Kotlin + Spring Boot + JPA + MySQL을 요구함. 기존 FastAPI에 기능만 하나 더 얹는 게 아니라, 아예 별도 서비스로 분리해서 만든 이유는 로드맵 Phase 2 목표 자체가 "폴리글랏 마이크로서비스 경험"이라서 — 한 회사 안에서도 팀/도메인마다 다른 언어 스택을 쓰는 경우가 실무에 흔하고, 그걸 미니 버전으로 직접 겪어보는 게 목적.

**어떻게 동작하나**:
- **DB는 새로 안 만들고 기존 MariaDB `unbeat` 데이터베이스를 그대로 재사용**함. 대신 테이블(`favorites`)은 이 서비스만 씀 — "서비스마다 자기 데이터는 자기가 소유한다(서비스별 데이터 소유권 분리)"는 마이크로서비스 원칙을 아주 작은 스케일로 흉내낸 것. 같은 물리 DB를 쓰긴 하지만 FastAPI 쪽 코드는 `favorites` 테이블을 절대 직접 안 건드림.
- **엔티티/JPA**: `Favorite.kt`가 `@Entity`. `session_id + artist_name` 조합에 `@UniqueConstraint`를 걸어서 같은 아티스트를 중복 찜 못 하게 DB 레벨에서 막음. `spring.jpa.hibernate.ddl-auto: update`로 앱을 처음 띄울 때 테이블을 자동 생성 — 포트폴리오/개발 단계라 빠르게 가는 선택이고, 운영 환경이었다면 Flyway/Liquibase 같은 마이그레이션 도구로 스키마 변경 이력을 관리했을 거라는 것도 인지하고 있음.
- **세션 식별 방식 통일**: 로그인 기능이 없는 건 FastAPI 서비스와 동일해서, 여기도 브라우저가 만든 `X-Session-Id` 헤더로 사용자를 구분함. 언어/프레임워크는 다르지만(Python/FastAPI vs Kotlin/Spring Boot) "익명 사용자를 어떻게 식별하는가"라는 설계는 서비스 전체에서 하나로 맞춤 — 이게 폴리글랏 아키텍처에서 중요한 지점(개별 서비스는 자유롭게 기술을 고르되, 서비스 간 계약(contract)은 일관되게 유지).
- **API**: `GET /favorites`(내 즐겨찾기 목록), `POST /favorites`(추가, 이미 있으면 중복 저장 안 하고 기존 걸 그대로 반환 — 멱등하게 설계해서 프론트에서 실수로 두 번 눌러도 안전), `DELETE /favorites/{id}`(삭제, 근데 요청자의 session_id랑 실제 소유자가 다르면 거부 — 로그인은 없지만 최소한의 "내 것만 지울 수 있다"는 소유권 체크는 넣음).
- **레이어 구조**: Controller(HTTP 요청/응답만 담당) -> Service(비즈니스 로직: 중복 체크, 소유권 체크) -> Repository(Spring Data JPA가 자동 구현하는 DB 접근 인터페이스). Spring Boot의 표준 3계층 구조를 그대로 따름.
- **JUnit 테스트**: `FavoriteServiceTest.kt`에 Service 계층 단위 테스트 3개 작성 — "중복 추가 시 기존 것 반환", "새 아티스트는 저장", "다른 세션은 삭제 불가". Repository는 Mockito로 가짜(mock) 객체를 만들어서, 실제 DB 없이도 비즈니스 로직만 빠르게 검증할 수 있게 함 (단위 테스트의 핵심: 외부 의존성을 격리).
- **프론트 연결**: `frontend/lib/playlistApi.ts`를 `lib/api.ts`와 별도로 만듦 — 이유는 위와 같은 논리로, 이 프론트가 결국 서로 다른 두 백엔드(FastAPI 8000, Spring Boot 8081)를 호출한다는 걸 코드 구조로도 드러내기 위함. `ArtistCard.tsx`의 별표 버튼을 누르면 `page.tsx`의 `handleToggleFavorite`가 이미 찜한 상태인지 보고 추가/삭제를 알아서 판단.
- **장애 격리**: 즐겨찾기 목록을 못 불러와도(`playlist-service`가 꺼져있어도) 검색/추천 같은 핵심 기능은 그대로 동작하게 만듦 (`useEffect`에서 `getFavorites()` 실패를 조용히 무시) — Redis/이벤트로깅 때와 같은 원칙: 부가 기능의 장애가 핵심 기능을 끌고 내려가면 안 됨.

**라이브 검증 완료**: IntelliJ에서 실행 중 두 가지 실제 이슈를 겪고 고쳤음 —
1. IntelliJ 2025.3.1이 최신 Gradle 9를 쓰는데, `io.spring.dependency-management`(1.1.4, 2024년 말 릴리즈)가 Gradle 9의 내부 API 변경(`LenientConfiguration`)이랑 안 맞아서 동기화가 깨짐. 그 플러그인을 빼고 Gradle 기본 `platform()` 기능으로 Spring Boot BOM을 직접 끌어오는 방식으로 교체, Spring Boot도 Gradle 9를 정식 지원하는 4.1.0으로 올려서 해결.
2. Hibernate가 "Unable to determine Dialect without JDBC metadata" 에러를 냄 — MariaDB가 JDBC 메타데이터에 자기 버전을 MySQL 호환 형식(`5.5.5-MariaDB-...`)으로 보고하는데, 최신 Hibernate의 자동 dialect 감지가 이걸 못 알아채는 문제. `spring.jpa.database-platform`을 `org.hibernate.dialect.MariaDBDialect`로 명시해서 해결.

그 다음 프론트(3000)+백엔드(FastAPI, 8000)+playlist-service(8081)를 다 띄우고, 브라우저에서 "아이유" 검색 → 별표 클릭 → ★로 바뀜 → 새로고침해도 "내 즐겨찾기"에 유지되는 것까지 실제로 확인함.

**면접에서 말할 포인트 추가**: "최신 버전 스택을 쓸 때 라이브러리 간 호환성 문제를 어떻게 진단했나"라는 질문에 실제 사례로 답할 수 있음 — 에러 메시지(NoSuchMethodError, 특정 API 이름)를 보고 "어떤 두 컴포넌트의 버전이 서로 안 맞는지"를 추론한 다음, 최신 버전으로 맞춰 올리거나(Spring Boot) 아예 그 레이어를 걷어내고 더 기본적인 방식(Gradle 네이티브 platform())으로 우회하는 두 가지 해결 전략을 각각 언제 쓰는지 설명하면 좋음.

**면접에서 말할 포인트**:
- "왜 기존 FastAPI에 안 붙이고 새 서비스로 분리했나" -> 폴리글랏 마이크로서비스 경험을 의도적으로 쌓으려는 선택이었다고 설명.
- "두 서비스가 같은 DB를 쓰는데 문제 없나" -> 지금은 포트폴리오 규모라 같은 물리 DB를 공유하지만, 테이블 단위로 소유권을 나눠서 서로 남의 테이블을 안 건드리게 설계했고, 서비스가 커지면 DB 자체도 분리하는 게 정석이라는 것까지 같이 설명하면 좋음.
- "멱등성을 왜 신경썼나" -> 프론트에서 버튼 연타/네트워크 재시도 같은 상황에서도 데이터가 깨지지 않게 하려고.

---

## 9. 트랙(곡) 즐겨찾기 + Last.fm 태그 자동 수집

**뭘 했나**: 기존 아티스트 즐겨찾기(`Favorite`)와 별개로, 추천받은 "곡" 하나하나를 즐겨찾기할 수 있는 `FavoriteTrack` 기능을 playlist-service에 추가함. 즐겨찾기하는 순간 백그라운드에서 Last.fm의 `track.getTopTags`를 호출해서 그 곡의 크라우드소싱 태그(예: britpop, kpop, ballad)를 같이 저장하고, 프론트에서는 그 태그를 기준으로 즐겨찾기한 곡들을 자동으로 그룹핑해서 보여줌.

**왜 이렇게 했나 (제품 관점, 본인 아이디어)**: 정연님 본인이 겪는 문제에서 출발함 — 장르는 다양하게 듣는데 곡 제목은 잘 기억을 못 하고, 멜론 같은 플레이리스트는 넣은 순서대로만 재생돼서 무드별로 골라 듣기가 불편함. "태그를 직접 입력해서 찾는" 방식(대화형 검색)은 원하는 게 아니었고 — 오히려 "일단 듣고 판단(즐겨찾기)하면, 정리는 시스템이 알아서" 해주길 원함. 그래서 사용자가 아무것도 입력하지 않아도, 즐겨찾기하는 행동만으로 자동 분류되게 설계함.

**왜 이렇게 했나 (기술 선택 이유 — AI/ML이 아니라 태그 기반으로 한 이유)**: Spotify/YouTube Music 같은 대형 서비스는 태그·메타데이터(1층) 위에 실제 오디오 파형 분석(danceability, energy, valence 같은 audio features)이나 YouTube의 MuLan 같은 신경망(2층)까지 쌓아서 무드를 분류함. 근데 그 2층(오디오 분석)은 Last.fm API 자체에 없고, Spotify도 2024년 말부터 신규 앱엔 그 기능(`audio-features`)을 막아버려서 우리가 쓸 수 있는 API로는 애초에 접근이 불가능함. 그래서 대형 서비스들도 쓰는 1층(태그/메타데이터 레이어)까지만 구현 — 이건 원칙적으로 AI/LLM 영역(로드맵의 "보류" 항목)과는 분리된 결정이고, 동시에 API 제약이라는 현실적인 이유도 있음.

**어떻게 동작하나**:
- `FavoriteTrack` 엔티티: `session_id + artist_name + track_name`에 유니크 제약. `tags` 컬럼에 Last.fm에서 받은 상위 3개 태그를 콤마로 이어붙여 저장 (예: `"britpop,indie"`).
- `LastFmTagClient` (playlist-service, Kotlin): Spring의 `RestClient`로 Last.fm `track.getTopTags`를 직접 호출. FastAPI 백엔드도 이미 Last.fm을 호출하고 있지만, "즐겨찾기 도메인은 playlist-service가 소유한다"는 서비스 경계를 지키기 위해 여기서 별도로 호출함 (마이크로서비스에서 서비스 간 데이터/책임을 어떻게 나누는지 보여주는 지점).
- **장애 격리**: 태그 조회가 실패해도(`try/catch`) 즐겨찾기 저장 자체는 그대로 진행되고 `tags`만 비어있게 저장됨 — Redis/이벤트로깅과 같은 원칙.
- **멱등성**: 이미 즐겨찾기한 곡을 다시 누르면 Last.fm을 또 호출하지 않고 기존 데이터를 그대로 반환 (`FavoriteTrackServiceTest`에서 "태그를 다시 조회하지 않는다"를 명시적으로 테스트함).
- 프론트(`page.tsx`): `favoriteTracks`를 `tags[0]`(대표 태그) 기준으로 `reduce`해서 그룹으로 묶어 렌더링. 사용자는 검색/입력 없이 그냥 별표만 누르면 됨.

**실제로 발견한 것 (라이브 검증 중 나온 진짜 발견)**: 케이팝 곡(예: "LOVE DAY", "쏘쏘")은 즐겨찾기해도 태그가 하나도 안 붙어서 전부 "태그 없음"으로 뜨는데, 서구 유명곡(Keane "Somewhere Only We Know")은 "BRITPOP" 태그가 정상적으로 붙음. 코드 버그가 아니라 **Last.fm의 크라우드소싱 태그 데이터가 케이팝 쪽은 실제로 훨씬 빈약하다**는 것을 확인함 (Last.fm 이용자 기반이 서구권 중심이라 생기는 실제 데이터 커버리지 한계). 날씨 API 때와 마찬가지로 "가져온 API의 한계를 직접 확인하고 그 사실을 인지한 채로 설계했다"는 사례.

**면접에서 말할 포인트**:
- "왜 AI/ML로 안 하고 태그 기반으로 했나" -> 위 기술 선택 이유 그대로 설명 (API 제약 + 원칙적 스코프 결정을 같이 언급).
- "실제 서비스 써보면서 발견한 문제나 한계가 있나" -> 케이팝 태그 커버리지 문제를 실제 데이터로 확인한 사례로 답하면 좋음. 이 문제를 인지했으니, 다음 개선 방향(트랙 태그가 없으면 아티스트 단위 태그로 폴백하는 것)까지 같이 얘기하면 "문제를 발견하고 대응 방향까지 설계할 수 있다"는 인상을 줄 수 있음.
- "멱등성/장애격리를 이 기능에서도 지켰나" -> 네, 이전 기능들과 동일한 원칙을 일관되게 적용했다고 설명.

---

**케이팝 태그 커버리지 보완 (폴백 로직) — 완료**: `LastFmTagClient.fetchArtistTags()`를 추가해서, 곡 단위 태그(`fetchTopTags`)가 비어있으면 아티스트 단위 태그(`artist.getTopTags`)로 한 번 더 시도하도록 `FavoriteTrackService.add()`를 수정함 (`trackTags.ifEmpty { fetchArtistTags(...) }`). 라이브 확인: 아이브 "LOVE DIVE"는 트랙 태그가 없었지만 아티스트 태그 폴백으로 "DANCE" 태그가 붙는 것 확인 — 무드까지는 못 잡아도 장르 단위 분류는 케이팝에서도 대부분 동작함.

**면접 포인트**: "1차 시도가 실패하면 더 넓은 범위로 재시도한다"는 폴백 패턴은 캐시 미스 시 원본 조회(Redis cache-aside)나 여러 데이터 소스를 계층적으로 시도하는 일반적인 설계 패턴과 같은 맥락 — "정확한 데이터가 없으면 조금 덜 정확해도 있는 데이터로 대체한다"는 트레이드오프 판단.

**다중 태그 그룹핑으로 확장 (완료)**: 원래는 태그 3개를 저장은 해두고도 화면에서는 `tags[0]`(대표 태그 1개)로만 묶었는데, 본인 피드백으로 "케이팝 안에서도 여름노래/잔잔한노래처럼 더 세분화하고 싶다"는 니즈가 나옴. 이미 저장해둔 나머지 태그를 그냥 안 쓰고 있었던 것뿐이라, 프론트(`app/page.tsx`의 `tracksByTag` reduce)에서 한 곡을 가진 태그 전부에 넣도록만 바꿈 — 새 데이터/API 호출 없이 이미 있는 데이터를 다 활용한 사례. 예: "DANCE"이자 "SUMMER"인 곡은 두 그룹 모두에 나타남.

**면접 포인트**: "요구사항이 바뀌면 항상 새로 만들어야 하나"라는 질문에 — 저장 스키마를 처음부터 넉넉하게(태그 최대 3개) 설계해뒀던 덕분에, 화면 요구사항이 바뀌었을 때 백엔드/DB 변경 없이 프론트 로직만 바꿔서 대응할 수 있었던 사례로 답할 수 있음.

---

---

## 10. 홈 화면 무드 탐색 (검색 없이 태그 기반 곡 추천)

**뭘 했나**: 홈 화면 맨 위에 "운동/휴식/집중/신나는/잔잔한/슬픔/로맨스/출퇴근길" 무드 칩을 추가함. 아무 칩이나 누르면 아티스트 검색 없이 그 무드에 맞는 인기곡이 바로 뜨고, 그 곡도 똑같이 별표로 즐겨찾기 가능함. 기존 "검색 → 아티스트 선택 → 추천" 흐름은 "아티스트로 직접 찾기"로 아래로 내려가고, 이제 홈 화면의 첫 흐름은 무드 탐색이 됨.

**왜 이렇게 했나 (본인 피드백에서 시작)**: 본인의 실제 유튜브 뮤직 사용 화면을 보여주면서 준 피드백 — 검색창을 직접 쓰는 경우가 거의 없고, 홈 화면의 무드/상황 칩이나 "다시 듣기"/"추천 재생목록"을 눌러서 탐색한다는 것. 그런데 Unbeat은 검색창에 아티스트명을 쳐야만 모든 흐름이 시작되는 구조였음 — 실제 사용 패턴과 안 맞는 제품 설계였던 것. "일단 뭘 입력해야 하는" 진입장벽을 없애는 게 목적.

**어떻게 동작하나**:
- `backend/main.py`의 `/discover-by-mood?tag=...`: Last.fm의 `tag.getTopTracks`를 호출 — 아티스트가 아니라 "태그"를 기준으로 그 태그가 붙은 인기곡 상위 10개를 바로 가져옴. 기존 `/search-artist`, `/recommend`와 완전히 동일한 패턴(Redis cache-aside + `X-Session-Id` 기반 이벤트 로깅)을 그대로 재사용 — 새 엔드포인트라고 새 구조를 만들지 않고 이미 검증된 패턴을 일관되게 적용함.
- `mood_browse`라는 새 이벤트 타입으로 로깅함 — 나중에 AARRR 분석에 "검색으로 들어온 사용자 vs 무드 탐색으로 들어온 사용자"의 리텐션/전환율을 비교하는 분석을 추가할 수 있는 기반이 됨 (아직 안 함, 다음 확장 포인트).
- `frontend/components/MoodChips.tsx`: 무드 라벨(한국어) ↔ Last.fm 태그(영문, 예: "chill", "workout")를 매핑하는 작은 상수 배열. Last.fm 자체가 한국어 태그는 거의 없어서, UI 라벨만 한국어로 보여주고 실제 조회는 영문 태그로 하는 방식.
- `TrackList.tsx`를 살짝 고침: 무드 탐색 결과는 Last.fm이 청취자 수(listeners)를 안 줘서(태그 인기 랭킹만 줌), `listeners`가 없으면 그 부분을 아예 렌더링 안 하게 방어 코드 추가.

**면접에서 말할 포인트**:
- "왜 새 엔드포인트를 아예 다른 구조로 안 짰나" -> 이미 검증된 cache-aside + 이벤트 로깅 패턴이 있는데 새로 만들 이유가 없었고, 오히려 일관성을 지키는 게 유지보수에 좋다고 설명.
- "실사용자 피드백을 반영한 사례가 있나" -> 이 기능 자체가 그 예시. 본인이 실제 쓰는 앱(유튜브 뮤직)의 사용 패턴을 관찰하고, 지금 만든 제품과의 괴리를 발견해서 구조를 바꾼 과정으로 설명.
- "무드 태그는 어떻게 정했나" -> Last.fm에 실제로 존재하는 크라우드소싱 태그 중, 유튜브 뮤직 홈 화면에 있는 것과 비슷한 8개(운동/휴식/집중 등)를 골라 매핑했다고 설명하면 됨.

**버그 수정: 검색 후 무드 칩을 눌러도 메인이 안 바뀌던 문제**

검색(예: "창모")으로 "검색된 곡"/"검색된 아티스트" 결과가 떠 있는 상태에서 무드 칩("출퇴근길")을 눌렀더니, 화면엔 무드 로딩 표시만 잠깐 뜨고 그 아래 이전 검색 결과가 그대로 남아있어서 "눌러도 안 바뀐다"는 피드백을 받음. 원인은 `handleSearch`와 `handleSelectMood` 두 핸들러가 서로의 상태를 대칭적으로 안 지우고 있었던 것 — `handleSearch`는 검색할 때 `selectedMood`를 지우는데(검색이 우선), 반대로 `handleSelectMood`는 `searchTracks`/`artists`/`tracks` 같은 검색·추천 상태를 안 지웠음. 그래서 무드 결과 섹션이 검색 결과 섹션 위에 "추가"만 되고, 화면에서 가장 크게 보이던 검색 결과는 그대로 남아 "안 바뀐 것처럼" 보였음. `handleSelectMood`에도 검색/추천 관련 상태 초기화를 추가해서, 무드를 선택하면 메인이 그 무드 결과로 완전히 교체되도록 수정.

**면접에서 말할 포인트(추가)**: "여러 진입점(검색/무드/아티스트 선택)이 같은 화면 상태를 공유할 때 생기는 버그" -> 한쪽 핸들러(`handleSearch`)만 상대방 상태를 정리하고 반대쪽(`handleSelectMood`)은 안 하는 비대칭 때문에, 두 흐름을 번갈아 쓰면 이전 결과가 잔상처럼 남는 문제였음 — 여러 "탐색 모드"가 하나의 화면을 공유하는 구조에서는 모드 전환 시 상태 초기화를 항상 대칭적으로 맞춰야 한다는 걸 실제 버그로 겪은 사례.

---

## 11. 카드 UI 이미지화 + Last.fm 플레이스홀더 이미지 트랩

**뭘 했나**: 홈 화면을 유튜브 뮤직/멜론처럼 실제 앨범 커버가 보이는 가로 스크롤 카드형 UI로 바꾸고, 트랙 카드에 진짜 이미지가 뜨게 함.

**왜 이렇게 했나**: 본인이 직접 유튜브 뮤직/멜론 홈 화면과 Unbeat을 나란히 비교하면서 "너무 밋밋하다"는 피드백을 줌. 카드형 레이아웃 자체는 만들었는데, 이미지 자리에 흰 별 모양 아이콘만 계속 떠서 실제로 안 고쳐진 상태로 두 단계 버그가 겹쳐 있었음.

**어떻게 진단하고 고쳤나 (실제 겪은 버그 2건)**:
1. **SVG fill/stroke 실수**: 이미지가 없을 때 보여줄 대체 아이콘(음표 모양)을 `fill="currentColor"`로 그렸는데, 이 path 자체는 원래 선(stroke)으로 그리도록 설계된 아이콘(Feather 스타일)이었음. 열린 경로를 강제로 채우면 SVG가 시작점과 끝점을 자동으로 이어서 의도치 않은 다각형을 만들어버려서, 결과적으로 별 모양처럼 보이는 도형이 렌더링됨. `fill="none" stroke="currentColor"`로 바꿔서 해결.
2. **Last.fm 플레이스홀더 이미지 함정**: 위 아이콘을 고쳤는데도 여전히 같은 흰 별이 떴음. 원인은 백엔드 쪽이었음 — Last.fm은 트랙에 진짜 이미지가 없을 때 빈 값을 주는 게 아니라, 자기네 기본 플레이스홀더 이미지(회색 별 아이콘) URL을 내려줌. `best_image()`가 이 URL도 "이미지가 있다"고 착각해서 그대로 반환했고, 그래서 진짜 이미지를 찾는 iTunes Search API 폴백 로직까지 아예 도달하지 못하고 있었음 (백엔드 로그에 iTunes 호출 흔적이 하나도 없는 걸 보고 "폴백이 아예 실행 안 되고 있다"는 걸 확인). 이 플레이스홀더 URL은 파일명 해시가 항상 고정(`2a96cbd8b46e442fc41c2b86b821562f`)이라, `best_image()`에서 그 해시가 포함된 URL을 걸러내도록 고쳐서 해결.

**디버깅 과정에서 쓴 방법**: 원인이 프론트(렌더링)인지 백엔드(데이터)인지 애매할 때, 백엔드에 임시로 `print()` 로그를 넣어서 "iTunes 폴백이 실제로 호출되는지"를 눈으로 확인함 — 로그가 한 줄도 안 찍힌다는 사실 자체가 "폴백 로직 문제가 아니라 그 앞 단계(best_image)에서 이미 잘못된 값을 반환하고 있다"는 걸 알려주는 단서가 됨.

**면접에서 말할 포인트**:
- "겉보기엔 똑같은 증상인데 원인이 여러 개였던 버그를 어떻게 진단했나" -> 프론트 재시작으로도 안 바뀌길래 "코드가 반영이 안 된 게 아니라 반영은 됐는데 로직 자체가 잘못됐다"는 쪽으로 가설을 바꾸고, 로그를 심어서 실제 실행 흐름을 확인하는 방식으로 좁혀갔다고 설명.
- "외부 API의 문서화 안 된 동작(플레이스홀더 URL)을 어떻게 알아챘나" -> "이미지 URL이 항상 있는데 항상 똑같은 모양으로 보인다"는 관찰에서, 그 URL 자체가 고정된 더미 값일 거라는 가설을 세우고 확인한 사례.

---

## 12. 재생 의도 신호 수집 (lastOpenedAt) + 통합 검색(아티스트+곡) + 오타 교정

**뭘 했나**: 세 가지를 이어서 작업함.
1. `FavoriteTrack`에 `url`, `lastOpenedAt` 컬럼 추가. 즐겨찾기한 곡 카드를 클릭해서 원곡 링크로 나갈 때 `PATCH /favorite-tracks/{id}/open`을 호출해 "재생 의도" 시점을 기록.
2. 검색을 아티스트 전용에서 "아티스트 또는 곡 제목"으로 확장 (`GET /search?q=...`, Last.fm `artist.search` + `track.search` 동시 호출).
3. 아티스트 검색 결과가 없을 때 Last.fm `artist.getCorrection`으로 오타 교정 재시도.

**왜 이렇게 했나**: 본인이 "플레이리스트 정리를 못 해서 알아서 정리되는 사이트를 만들고 싶다"는 니즈를 얘기하다가, 그걸 위한 "안 듣는 곡 자동 분류" 배치를 만들려면 먼저 "재생" 신호가 있어야 한다는 걸 발견함. Unbeat은 실제 오디오 스트리밍이 없어서(Last.fm/원곡 링크로 내보내는 구조) 진짜 재생은 못 재고, 대신 "즐겨찾기한 곡 카드를 클릭해서 나간 시점"을 재생 의도의 대리 신호로 씀. 검색 확장은 "노래 제목은 기억나는데 아티스트는 모른다"는 흔한 상황을 다루기 위함(본인이 직접 "노래 제목이나 아티스트 이름이나 쳐도 나와야지"라고 지적).

**어떻게 동작하나**:
- `FavoriteTrack.lastOpenedAt`: nullable. `markOpened()`가 소유권 체크(다른 세션 소유 곡은 갱신 불가) 후 갱신. 나중에 "즐겨찾기한 뒤로 한 번도 안 눌러본 곡"을 찾는 Spring Batch의 입력 데이터가 될 예정 (아직 배치 자체는 안 만듦 — 신호가 며칠 쌓인 뒤에 붙이는 게 맞다고 판단).
- `/search`: `track.search` 응답은 다른 Last.fm 응답들과 달리 `artist` 필드가 `{"name": ...}` 객체가 아니라 문자열이라, 기존 `track_image()`가 그대로 못 씀 — `resolve_track_image(artist_name, track_name, images)`로 로직을 분리해서 두 형태 모두 처리하게 리팩터링.
- 오타 교정은 아티스트 검색 결과가 0건일 때만 시도(불필요한 API 호출 방지), 검색어에 한글이 섞여 있는지로 두 경로로 나뉨: 영문은 `artist.getCorrection`, 한글은 `rapidfuzz` + 후보 목록(아래 참고). 교정된 이름을 찾으면 그 이름으로 재검색하고, `correctedArtistName`을 응답에 실어 프론트에서 "오타 교정: ~로 검색됨" 문구를 보여줌.

**실제로 발견한 버그와 수정 과정**: 한글 검색어("아이위" 등)에 `artist.getCorrection`을 그대로 적용했더니, 청취자 2명짜리 완전히 무관한 아티스트로 잘못 교정되는 걸 실제로 확인함. 원인은 `getCorrection`이 영문 철자 간 편집 거리(edit distance) 기반으로 동작하는데, 한글 오타와 영문 정식 이름("아이위" ↔ "IU")은 글자 자체가 안 겹쳐서 편집 거리 계산이 의미가 없어짐 — 알고리즘이 "가장 비슷한" 걸 억지로 찾다가 무관한 결과를 내놓은 것.

1차 대응은 한글 섞인 검색어는 이 기능 자체를 건너뛰는 것이었는데, "그럼 한글 오타는 어떻게 고치냐"는 피드백을 받고 다시 설계함. 관찰해보니 Last.fm은 **정확한 한글 철자**("아이유")는 이미 잘 찾아줌 — 문제는 "오타 있는 한글"→"정식 영문명"으로 바로 가려던 접근 자체였음. 그래서 `rapidfuzz`(파이썬 fuzzy-matching 라이브러리)로 자주 검색될 만한 한글 아티스트명 후보 목록(`KNOWN_KOREAN_ARTIST_NAMES`) 안에서 가장 비슷한 "정확한 한글 철자"를 찾고, 그 철자로 Last.fm을 재검색하는 2단계 방식으로 바꿈. `score_cutoff=65`로 관련 없는 입력은 걸러지게 함 (실제 테스트: "아이위"→"아이유"·"뉴짓스"→"뉴진스"는 66.7점으로 통과, 관련 없는 문자열은 매치 없음으로 확인).

이 방식은 후보 목록 밖의 아티스트는 못 잡는다는 명확한 한계가 있음 — Last.fm의 `getCorrection`처럼 전체 데이터베이스를 아는 게 아니라 직접 정한 목록 안에서만 찾기 때문. "완전히 엉뚱한 결과보다 아예 못 찾는 게 낫다"는 원칙으로 커버리지를 의도적으로 좁힌 트레이드오프.

**2차 버그(같은 증상, 다른 원인)**: 위 수정을 "검색 결과가 0건일 때만 교정 시도"하는 조건으로 넣었는데도 "아이우"(오타) 검색이 여전히 같은 저품질 결과("IU (아이우)")를 반환함. 원인을 다시 보니, Last.fm의 `artist.search`는 부분 문자열까지 매치해주는 검색이라 "아이우"라는 글자가 우연히 이름에 포함된 청취자 2명짜리 다른(아마 중복/장난성) 아티스트 페이지가 "결과 있음"으로 먼저 잡혀버려서, "결과 없을 때만 교정"이라는 조건 자체가 한 번도 안 걸린 것 — 오타 교정 로직까지 도달을 못 한 것. 그래서 순서를 바꿈: 한글 검색어는 결과 유무와 상관없이 **먼저** 후보 목록에서 가장 비슷한 철자를 찾고, 있으면 그 철자로 검색어 자체를 바꿔서 Last.fm을 호출하도록 함(정확히 입력한 경우는 자기 자신이 후보로 잡혀서 동작 그대로 유지).

**3차 원인(진짜 마지막): Redis 캐시**: 로직을 두 번이나 고쳤는데도 "아이위" 검색이 계속 똑같은 결과를 반환해서 한참 헤맸는데, 알고 보니 코드 문제가 아니라 **캐시** 문제였음. `/search`도 다른 엔드포인트들과 같은 cache-aside 패턴을 쓰는데, 맨 처음(버그 있던 시절) "아이위" 검색 결과가 이미 Redis에 캐시돼 있어서, 그 뒤로 백엔드 코드를 아무리 고쳐도 캐시 히트가 새 로직 실행 자체를 막고 있었음. `redis-cli flushdb`로 캐시를 지우고 나서야 정상 동작 확인됨. **교훈**: 캐시가 있는 시스템에서 "코드를 고쳤는데도 안 바뀐다"는 증상을 보면, 코드 자체를 계속 의심하기 전에 캐시부터 지워보는 게 훨씬 빠른 진단 순서다 — 이번 세션에서 앨범 아트 버그 때도 같은 패턴(캐시 의심을 늦게 함)으로 시간을 더 썼었음.

**최종 확인**: 캐시를 지운 뒤 "아이위" 검색 → "오타 교정: 아이유로 검색됨" 문구와 함께 아이유 관련 아티스트 카드 5개가 정상적으로 뜨는 것 확인. 카드가 여러 개인 건 버그가 아니라 Last.fm에 같은 아티스트의 중복 페이지가 실제로 여러 개 등록돼 있는 것(청취자 수가 제각각인 팬 생성 페이지들) — 실제 서비스에서도 흔히 겪는 외부 데이터 소스의 "정규화 안 된 중복 엔티티" 문제의 실사례.

**면접에서 말할 포인트**:
- "재생을 직접 못 재는데 어떻게 '안 듣는 곡'을 판단하나" -> 실제 재생 대신 클릭(재생 의도)을 대리 신호로 쓰는 트레이드오프를 의도적으로 선택했다고 설명. 완벽하진 않지만 "관측 가능한 것"으로 "관측 불가능한 것"을 근사하는 흔한 패턴.
- "외부 API 기능을 그대로 믿고 썼다가 문제된 적 있나" -> 이 한글 오타 교정 사례로 답하면 됨. 알고리즘이 어떤 가정(같은 문자 체계 내의 편집 거리) 위에서 동작하는지 이해 안 하고 그대로 갖다 쓰면 오히려 더 이상한 결과를 낼 수 있다는 것, 그리고 처음엔 "그냥 막기"로 대응했다가 사용자 피드백으로 "실제로 고쳐지게" 재설계한 과정까지 같이 설명하면 좋음.
- "커버리지가 좁은 목록 기반 방식을 왜 선택했나" -> 정확도(엉뚱한 결과 방지)와 커버리지 사이의 트레이드오프를 의식적으로 정확도 쪽으로 선택했고, 목록은 나중에 실제 검색 로그(성공적으로 결과가 나온 한글 검색어들)를 모아 자동으로 확장할 수 있다는 것까지 언급하면 데이터 기반 개선 방향을 보여줄 수 있음.
- "곡 제목 오타나 가사로 검색하는 건 왜 안 했나" -> Last.fm엔 그런 기능/데이터가 아예 없어서, 하려면 Genius 같은 별도 API(가사 검색은 되지만 실제 가사 텍스트는 안 주는 제약이 있음)가 필요하다는 것까지 파악해뒀고, 사용자 키 발급을 기다리는 중이라고 설명하면 됨.

---

## 13. 검색창 '최근 검색' 드롭다운 (유튜브 뮤직 스타일) + 분석 데이터와 UI 데이터 분리

**뭘 했나**: 왼쪽 사이드바에 고정으로 떠 있던 "최근 검색" 목록을 없애고, 검색창을 클릭(포커스)하면 그 아래 드롭다운으로 최근 검색어가 뜨게 바꿈. 각 항목엔 삭제(휴지통) 버튼이 있어서 개별적으로 지울 수 있음.

**왜 이렇게 했나**: 본인이 유튜브 뮤직 검색창을 눌렀을 때 나오는 드롭다운을 보여주면서, 사이드바 상시 노출 대신 이 패턴을 원한다고 함. 사이드바는 화면을 항상 차지하는데 실제로 자주 보는 정보는 아니라서, "검색하려고 할 때만" 보여주는 게 더 자연스러운 흐름.

**어떻게 동작하나**:
- **왜 MariaDB `events` 테이블을 그대로 안 썼나**: 기존 "최근 검색"은 분석용 이벤트 로그(`events` 테이블)에서 뽑아 쓰고 있었는데, 이번엔 사용자가 항목을 개별 삭제할 수 있어야 함. `events`는 AARRR/코호트 분석의 원본 데이터라 마음대로 지우면 안 되는 데이터인데, 삭제 기능을 그 위에 얹으면 "UI 편의를 위해 분석 원본 데이터를 훼손"하는 셈이 됨. 그래서 완전히 별도로, Redis에 세션별 리스트(`recent-searches:{session_id}`)를 새로 둠 — `events`는 계속 분석용으로만 순수하게 남고, 이 UI용 목록은 사용자가 마음대로 지워도 됨.
- `cache.py`: `push_search_history`(검색 시 추가, 중복 제거 후 맨 앞으로), `get_search_history`(조회), `remove_search_history`(개별 삭제) — Redis List 자료구조(`LPUSH`/`LTRIM`/`LREM`) 사용. TTL 30일.
- `SearchBar.tsx`: input `onFocus`에 드롭다운 열고, `onBlur`엔 150ms 지연 후 닫음 — 지연이 없으면 드롭다운 항목을 클릭하는 순간 input이 먼저 blur되면서 드롭다운이 닫혀버려 클릭 이벤트 자체가 씹히는 문제가 있어서 넣음.
- 삭제는 낙관적 업데이트(optimistic update) — 프론트에서 먼저 목록에서 지우고 백엔드 호출은 결과를 기다리지 않음. 실패해도 "다시 검색하면 또 나타나는" 정도라 되돌릴 필요가 없는 가벼운 데이터라 이렇게 처리함.

**면접에서 말할 포인트**:
- "왜 같은 '최근 검색'인데 저장소를 두 개로 나눴나" -> 데이터의 용도(분석 원본 vs UI 편의)에 따라 변경 가능성과 삭제 가능 여부가 다르므로, 하나의 데이터베이스/테이블에 다 넣기보다 목적에 맞는 저장소로 분리하는 게 맞다고 설명. 실무에서도 "이벤트 소싱 원본"과 "그걸 파생시킨 조회용 뷰/캐시"를 분리하는 것과 같은 사고방식.
- "왜 blur에 지연을 뒀나" -> 브라우저 이벤트 순서(blur가 click보다 먼저 발생)를 실제로 겪고 알게 된 문제라고 설명하면 실전 디버깅 경험으로 보임.

---

## 14. 상단 고정 헤더(로고+검색창) + 좌우 대칭 3열 레이아웃

**뭘 했나**: "Unbeat" 로고와 검색창을 화면 중간에서 상단 고정(`sticky top-0`) 헤더로 옮기고, 헤더와 본문(`<main>`)을 똑같은 `grid-cols-[200px_1fr_200px]` 3열 그리드로 맞춰서 중앙 콘텐츠가 화면 정중앙에 오게 함.

**왜 이렇게 했나**: 본인이 멜론/유튜브 뮤직 스크린샷을 보여주며 "실제 음악 서비스는 다 검색이 최상단에 상시 노출된다"고 지적함. 기존엔 로고가 화면 중앙에 크게 있고 검색창은 본문 중간에 있어서, 검색하려면 스크롤이 필요했음. 또한 오른쪽에만 사이드바(즐겨찾기 태그 요약)가 있고 왼쪽은 비어 있어서 레이아웃이 한쪽으로 쏠려 보인다는 피드백도 있었음.

**어떻게 동작하나**: 헤더와 본문 둘 다 `max-w-6xl` + `grid-cols-[200px_1fr_200px]`를 똑같이 써서, 로고(왼쪽 칸)/검색창(가운데 칸)이 본문의 왼쪽 사이드바/중앙 콘텐츠와 세로로 정확히 정렬됨. 이때 왼쪽 사이드바에 아직 채울 콘텐츠가 없었어서 빈 `<aside>`를 자리만 잡아두는 용도로 둬서, 오른쪽 사이드바와 폭이 대칭이 되어 중앙 콘텐츠가 진짜 화면 중앙에 오게 함 (그렇지 않으면 오른쪽 사이드바 폭만큼 중앙이 왼쪽으로 밀려 보임).

**면접에서 말할 포인트**:
- "레이아웃이 틀어져 보인다는 피드백을 어떻게 해결했나" -> 원인이 "오른쪽 사이드바만 있고 왼쪽이 없어서 그리드 중심이 안 맞는다"는 것을 파악하고, 당장 채울 콘텐츠가 없어도 자리(빈 칸)를 대칭으로 잡아두는 방식으로 먼저 구조를 잡은 뒤 콘텐츠를 채워나갔다고 설명 — UI 자리 배치(레이아웃 뼈대)와 콘텐츠 채우기를 분리한 접근.

---

## 15. 홈 화면 중앙 기본 콘텐츠(인기 차트) + 즐겨찾기 좌우 사이드바 재배치

**뭘 했나**: 두 가지를 같이 함.
1. `GET /chart` 신규 엔드포인트 추가 (Last.fm `chart.getTopTracks` — 특정 아티스트/태그와 무관한 서비스 전체 인기 차트). 검색이나 무드 칩 선택 같은 사용자 조작 없이도, 페이지 진입 즉시 "지금 인기 있는 곡"이 중앙에 뜨게 함.
2. 중앙에 있던 "내 즐겨찾기 (아티스트)"와 "다시 듣기 (태그별)" 블록을 왼쪽/오른쪽 사이드바로 이동.

**왜 이렇게 했나**: 본인이 유튜브 뮤직 홈 화면(맞춤 믹스, 운동 슈퍼믹스 등)과 멜론 홈 화면(최신 앨범, 인기있어요) 스크린샷을 보여주며 "이 서비스들은 다 가운데에 노래가 아무거나 일단 떠 있는데, 우리는 그런 게 없이 바로 즐겨찾기만 있다"고 지적함. 즉 우리 홈 화면 중앙의 기본 상태(검색도, 무드 선택도 안 한 상태)가 비어 보이는 게 문제였고, 그 자리를 차지하던 즐겨찾기 목록은 정작 "눈에 띄어야 할 자리"에 있을 필요가 없는(자주 안 보는) 정보였음.

**어떻게 동작하나**:
- `/chart`: 다른 엔드포인트들과 같은 cache-aside 패턴(`get_cached`/`set_cached`)을 쓰되, 세션이나 검색어와 무관하게 전 사용자가 똑같은 결과를 보는 차트라서 캐시 키도 세션 없이 전역 하나(`chart:top-tracks`)만 씀 — Last.fm 호출을 최소화.
- 프론트: 페이지 마운트 시(`useEffect`) 즐겨찾기·최근검색과 같이 `getTopChart()`도 같이 호출해서 `chartTracks` state에 저장, 중앙 콘텐츠 목록에서 검색 결과 다음, 무드 칩 앞에 배치 — 검색을 하면 검색 결과가 위에 먼저 보이고, 아무 것도 안 하면 차트가 기본으로 보임.
- 즐겨찾기 아티스트는 왼쪽 사이드바로, 즐겨찾기 태그 요약과 "다시 듣기"(태그별 트랙 목록)는 오른쪽 사이드바로 이동. CSS Grid에서 JSX 선언 순서(중앙→왼쪽→오른쪽)와 실제 화면상 배치 순서가 다르기 때문에 `lg:order-1`(왼쪽 사이드바)/`lg:order-2`(중앙)/`lg:order-3`(오른쪽 사이드바)로 순서를 명시함 — 안 그러면 그리드가 DOM 순서대로 칸을 채우면서 중앙 콘텐츠가 폭 200px짜리 첫 칸에 끼여버림.

**면접에서 말할 포인트**:
- "레퍼런스 앱을 참고해서 기능을 설계한 경험" -> 유튜브 뮤직/멜론의 홈 화면 패턴(항상 뭔가 채워진 기본 피드)을 관찰하고, 그 이유(사용자가 아무 조작 안 해도 즉시 콘텐츠 소비를 시작할 수 있게)를 유추해서 우리 서비스에 맞는 형태(전체 인기 차트)로 구현했다고 설명.
- "CSS Grid에서 시각적 순서와 DOM 순서가 다를 때 어떻게 처리했나" -> `order` 속성으로 시각적 배치와 마크업 순서를 분리한 사례로 답하면 됨.

---

## 16. 즐겨찾기 태그 잡음 필터링 (아티스트 자기 이름 태그 / 국적 태그 제외)

**뭘 했나**: 곡을 즐겨찾기할 때 자동 수집하는 Last.fm 태그에서, 그룹핑 기준으로 의미 없는 잡음 태그를 걸러내도록 `LastFmTagClient`에 `cleanTags()` 필터를 추가함. 구체적으로 (1) 아티스트 자기 자신의 이름과 같은 태그(예: "아이유" 곡에 붙는 "iu"), (2) 국적/언어나 "seen live"/"favorite" 같은 청취 메타 태그(예: "korean")를 제외.

**왜 이렇게 했나**: 본인이 "허각 & 정은지" 하나 즐겨찾기 했더니 오른쪽 사이드바에 K-POP/IU/KOREAN 세 그룹이 다 뜨는 걸 보고 "곡 하나 즐겨찾기했는데 불필요한 태그가 너무 많다"고 지적, 그중 "KOREAN"은 확실히 불필요하다고 확인해줌. 태그 그룹핑 기능의 목적은 "장르/무드로 곡을 다시 찾기 쉽게" 하는 것인데, "iu"(아티스트 자신)나 "korean"(국적)은 장르/무드 정보가 아니라서 그룹을 만들어봐야 각 그룹에 곡이 1개씩만 있는 의미 없는 잡음 그룹이 됨.

**어떻게 동작하나**:
- `NOISE_TAGS`: 국적/언어(`korean`, `japanese`, `chinese`, `american`, `british`, `asian`) + 청취 메타 태그(`seen live`, `favorite(s)`, `beautiful`, `love` 등) 블록리스트. 장르/무드가 아닌, Last.fm 사용자들이 습관적으로 붙이는 태그들.
- 아티스트 자기 이름 필터링은 두 단계: (1) 저장된 아티스트명과 태그 문자열을 정규화(공백/특수문자 제거, 소문자화)해서 직접 비교, (2) 그것만으론 "아이유"(한글로 저장) ≠ "iu"(Last.fm 태그는 영문 활동명)라 못 잡아내서, 오타 교정 때 만든 한글 후보 목록(`KNOWN_KOREAN_ARTIST_NAMES`)과 같은 방식으로 자주 즐겨찾기될 만한 아티스트 위주의 좁은 별칭 매핑(`ARTIST_SELF_TAG_ALIASES`, 예: "아이유"→"iu", "방탄소년단"→"bts")을 만들어 추가로 걸러냄.
- 이 매핑은 목록에 없는 아티스트는 못 잡는다는 명확한 한계가 있음 — 정확도 우선, 좁은 커버리지로 시작해서 점진적으로 늘리는 전략(오타 교정 기능과 동일한 트레이드오프, [[한글 오타 교정]] 참고).
- **한계**: 태그는 즐겨찾기 저장 시점에 한 번만 수집해서 DB에 고정 저장되는 구조라, 이미 저장된 기존 즐겨찾기 곡에는 새 필터가 소급 적용되지 않음. 확인하려면 별표를 해제했다가 다시 눌러서 재수집해야 함 (이전에 `url` 필드 추가했을 때와 같은 종류의 제약).

**면접에서 말할 포인트**:
- "외부 API가 주는 데이터를 그대로 신뢰하지 않고 후처리한 경험" -> Last.fm 태그가 "장르/무드"라는 프로덕트 목적에 안 맞는 잡음(아티스트 자기 이름, 국적)을 섞어서 주길래, 목적에 맞게 화이트리스트/블랙리스트로 정제하는 계층을 따로 뒀다고 설명.
- "기존 데이터에 새 로직이 소급 적용 안 되는 걸 어떻게 처리했나" -> 마이그레이션 스크립트로 일괄 재수집할 수도 있지만, MVP 단계에선 "재적용하려면 다시 즐겨찾기" 정도의 명시적 안내로 충분하다고 판단한 스코프 결정이라고 설명하면 됨.

---

## 17. 즐겨찾기 사이드바 "다시 듣기" — 태그 그룹핑 UI 제거, 플랫 목록으로 전환

**뭘 했나**: 오른쪽 사이드바의 "다시 듣기"를 태그별로 접혀 있던 그룹 UI(K-POP/IU/KOREAN 같은 소제목 아래 곡을 나눠서 표시)에서, 태그 구분 없이 즐겨찾기한 곡을 최신순으로 죽 나열하는 플랫 목록으로 바꿈. 태그 개수를 보여주던 "즐겨찾기 태그 요약" 위젯(`FavoriteTagSummary`)도 같이 제거.

**왜 이렇게 했나**: 바로 전 세션(16번)에서 태그 잡음(iu/korean)을 걸러내는 필터를 추가했는데도, 본인이 실제 화면을 보고 "태그를 눌러도 아무 데도 안 가는데 태그가 무슨 소용이냐", "K-POP 같은 것도 필요 없고 그냥 곡·가수만 있으면 될 것 같다"는 피드백을 줌. 근본 원인은 두 가지였음: (1) 태그가 클릭 가능한 UI처럼 보이는데 실제로는 아무 인터랙션이 없는 죽은 요소였고, (2) 필터링을 해도 "K-POP"처럼 진짜 장르 태그는 남는데, 즐겨찾기한 곡이 몇 곡 안 되는 지금 시점엔 곡 1개짜리 그룹이 여러 개 생기는 것 자체가 정보량 없이 화면만 복잡하게 만듦. 그룹핑이 실제로 유용해지려면(장르별로 수십 곡이 쌓여서 찾기 편해지는 시점) 아직 데이터가 부족한 단계라는 걸 실제 사용 피드백으로 확인한 셈.

**어떻게 처리했나**: 태그 "표시"만 걷어내고 태그 "수집"은 그대로 둠 — `FavoriteTrack.tags` 컬럼과 `LastFmTagClient` 수집 로직은 그대로 유지. 이유는 ROADMAP의 Spring Batch 항목(태그 기반 자동 그룹핑, N개월 미청취 곡 분류)이 이 태그 데이터를 입력으로 쓸 예정이라, 지금 당장 UI에 안 보여준다고 데이터 자체를 안 모으면 나중에 다시 소급 수집해야 하는 비용이 생김. "지금 쓸모없어 보이는 데이터라도, 나중에 쓸 계획이 확정돼 있으면 수집은 미리 시작해두고 노출만 미룬다"는 판단.

**면접에서 말할 포인트**:
- "만든 기능을 실제 피드백으로 다시 걷어낸 경험" -> 태그 그룹핑을 두 세션에 걸쳐 만들고 필터링까지 개선했는데, 실사용 피드백에서 "지금 데이터 양(곡 1~2개)에서는 그룹핑이 오히려 복잡도만 늘린다"는 걸 확인하고 UI만 되돌린 사례. 기능을 안 쓰는 게 아니라 "표시 시점"과 "데이터 수집 시점"을 분리해서, 나중에 데이터가 쌓이면 다시 켤 수 있게 남겨둔 것까지 설명하면 좋음.
- "당장 안 보여줘도 데이터를 계속 모으기로 한 이유" -> Spring Batch 배치의 입력 데이터가 되는 걸 이미 계획해뒀어서, 지금 멈추면 나중에 소급 수집 비용이 든다는 트레이드오프 판단이었다고 설명.

---

## 18. 트랙 재생을 사이트 안 유튜브 미니 플레이어로 전환

**뭘 했나**: 트랙 카드를 누르면 Last.fm 자체 트랙 페이지로 나가던 것을, YouTube Data API v3로 그 곡의 대표 영상을 찾아 화면 하단 고정 바에서 바로 재생되게 바꿈. 백엔드에 `GET /youtube-search?artist=&track=` 신규 엔드포인트(cache-aside, 14일 캐싱)를 추가하고, 프론트는 트랙 카드 클릭 → videoId 조회 → 하단 미니 플레이어(`<iframe>` 유튜브 임베드)에 표시하는 흐름으로 바꿈.

**왜 이렇게 했나**: 본인이 Last.fm 트랙 페이지(태그만 있고 재생 버튼이 없는 메타데이터 페이지)로 나가는 걸 직접 보고 "이러면 우리 사이트가 아니라 그냥 Last.fm 따까리 같다", "다른 어떤 음악 사이트도 이렇게 밖으로 안 보낸다"고 지적함. 근본 원인은 Last.fm이 애초에 스트리밍/재생 기능이 없는 "메타데이터+스크로블링" 서비스라는 것과, Unbeat 자체는 오디오 스트리밍 라이선스가 없다는 두 가지 제약이 겹친 것. 완전한 해법(자체 라이선스 스트리밍)은 포트폴리오 프로젝트 규모에서 현실적으로 불가능하지만, 절충안으로 "재생만큼은 사이트를 안 나가고" 볼 수 있게 하는 방향을 선택함 — YouTube 검색 결과를 iframe으로 임베드하는 방식은 라이선스 없이도 합법적으로 쓸 수 있는 방법(YouTube의 표준 임베드 iframe 자체가 이런 용도로 공개돼 있음).

**어떻게 동작하나**:
- `/youtube-search`: `q=아티스트+곡이름`으로 YouTube Data API `search.list`를 호출해 가장 관련도 높은 영상 1개의 videoId만 반환. 검색 1회당 100유닛(무료 일일 할당량 10,000유닛 = 하루 약 100번)이라 결과를 Redis에 **14일**(다른 캐시보다 훨씬 길게) 캐싱해서 할당량을 최대한 아낌 — 같은 곡의 대표 영상은 거의 안 바뀐다는 전제.
- 트랙 카드(`TrackList.tsx`)는 더 이상 `<a href>`로 감싸지 않고, 클릭하면 `onPlay` 콜백을 호출하는 구조로 바뀜. 이때 즐겨찾기 별표 버튼이 카드 안에 있는데, **HTML 명세상 버튼 안에 버튼을 중첩할 수 없어서**(무효 DOM, hydration 불일치 위험) 카드 자체는 `<button>` 대신 `role="button" tabIndex={0}` 달린 `<div>`로 만들고 별표만 진짜 `<button>`으로 둠. 별표 클릭 시 `e.stopPropagation()`으로 카드 전체의 재생 클릭이 같이 발동하지 않게 막음.
- 하단 미니 플레이어는 `nowPlaying` 상태(`{videoId, name, artist}`) 하나로 관리하고, 값이 있을 때만 `fixed inset-x-0 bottom-0` 바를 렌더링. 콘텐츠가 이 바에 가려지지 않도록 `<main>`에 조건부 `pb-28` 패딩을 추가.
- 부수 효과: 재생이 이제 (Last.fm이 준) 저장된 `url` 대신 아티스트+곡 이름으로 유튜브를 검색하는 방식이라, `url` 컬럼이 없는 예전 즐겨찾기 곡(그 컬럼이 생기기 전에 저장된 것들)도 자동으로 재생 가능해짐 — 예전엔 "링크 정보가 없어서 못 누름" 상태였던 게, 재검색 로직 자체가 바뀌면서 자연스럽게 해소된 케이스.
- "재생 의도" 신호(`lastOpenedAt`)를 기록하던 시점도 "원곡 링크 클릭"에서 "재생 버튼 클릭"으로 옮김 — 사이트를 안 나가니 더 이상 "클릭해서 나감"이 아니라 "재생 버튼을 눌러 곡을 틀었음"이 신호가 됨. 여전히 완벽한 재생 확인은 아니지만(실제 몇 초 들었는지는 모름), Last.fm으로 나가던 시절보다 사용자 행동과 더 가까워진 신호.

**알아둘 한계**: YouTube 검색이 항상 정확한 원곡을 1등으로 주는 건 아니라서(커버곡/라이브 영상이 먼저 뜨는 경우 있음), 완벽한 매칭은 보장 못 함.

**면접에서 말할 포인트**:
- "라이선스가 없어서 구현 못하는 기능을 어떻게 절충했나" -> 완전한 스트리밍은 불가능하지만, "사이트를 나가지 않는다"는 핵심 UX 문제만큼은 YouTube의 공개 임베드로 해결한 사례. 제약을 있는 그대로 받아들이기보다, 제약 안에서 사용자가 실제로 원하는 것(=이탈감 없는 재생 경험)이 뭔지 좁혀서 그 부분만 해결했다고 설명.
- "외부 API 할당량을 어떻게 관리했나" -> YouTube Data API의 검색 비용(100유닛/회, 일일 10,000유닛)을 미리 계산해서, 같은 곡을 반복 검색하지 않도록 캐시 TTL을 다른 캐시(1시간)보다 훨씬 길게(14일) 잡은 판단 근거를 설명하면 좋음.
- "HTML 구조상 걸린 문제를 어떻게 풀었나" -> 버튼 안에 버튼을 못 넣는 HTML 제약을 발견하고, 접근성(키보드 포커스/Enter 키 대응)까지 챙기면서 `div[role=button]`으로 대체한 과정을 설명하면 꼼꼼함을 보여줄 수 있음.

**추가 개선(2차): 단순 iframe 임베드에서 자체 플레이어 UI로**

1차로 `<iframe src="youtube.com/embed/...">`를 그대로 하단 바에 넣었는데, 실제로 써보니 유튜브 로고·자막(CC) 버튼·유튜브 자체 재생바가 그대로 노출돼서 본인이 "우리 사이트만의 고유 API를 갖고 싶었던 거였다", "영상도 작고 디자인도 마음에 안 든다"고 재차 피드백을 줌. 핵심은 "재생이 되긴 하는데, 보이는 게 여전히 남의 서비스 화면"이라는 것.

그래서 **YouTube IFrame Player API**(단순 embed URL이 아니라 JS로 플레이어를 제어하는 정식 API)로 전환함. 실제 유튜브 iframe은 `1px×1px`로 화면 밖에 숨기고, 재생/일시정지 버튼·진행바(탐색 가능)·앨범아트·곡 정보는 전부 Unbeat이 그린 커스텀 UI(`MiniPlayer.tsx`)로 대체 — 오디오 소스만 유튜브고, 사용자 눈에 보이는 건 처음부터 끝까지 우리 UI. `YT.Player` 인스턴스를 `useRef`로 한 번만 만들고, 곡이 바뀌면 iframe을 새로 만드는 대신 `loadVideoById()`로 같은 인스턴스에 영상만 교체(끊김 없는 전환). 진행바는 유튜브 API에 재생 위치 변경 이벤트가 따로 없어서 0.5초 간격 `setInterval`로 `getCurrentTime()`을 폴링하는 방식으로 구현.

부수 효과로 `lastOpenedAt` 기록 시점도 한 단계 더 정확해짐 — 1차에서는 "재생 버튼 클릭" 시점에 기록했는데(검색이 실패해서 실제로 못 틀어도 기록되는 허점 있었음), 이제는 유튜브 플레이어의 `onStateChange`가 진짜 `PLAYING` 상태를 알려주는 순간에만 기록하도록 옮김 — "버튼을 눌렀다"가 아니라 "진짜로 재생이 시작됐다"는 신호로 한 번 더 좁혀짐.

**면접에서 말할 포인트(2차)**:
- "1차로 만든 기능을 왜, 어떻게 다시 고쳤나" -> "재생이 사이트 안에서 된다"는 기능적 요구는 충족했지만, 실제로 써보니 "그래도 남의 서비스 화면을 그대로 보여준다"는 더 근본적인 문제(UX/제품 정체성)가 남아있었다는 걸 사용자 피드백으로 확인하고, 단순 임베드에서 API 기반 커스텀 플레이어로 다시 설계한 과정. 기능 구현과 "우리 제품처럼 느껴지게 만드는 것"은 다른 문제라는 걸 보여주는 사례로 설명 가능.
- "같은 신호(lastOpenedAt)를 왜 두 번 옮겼나" -> 처음엔 관측 가능한 것 중 가장 가까운 지점(클릭)에서 신호를 잡았다가, 더 정확한 관측 지점(실제 재생 이벤트)이 기술적으로 가능해지자 옮긴 것 — 정확도를 점진적으로 개선해나가는 과정으로 설명하면 좋음.

**추가 개선(3차): 정보량이 부실해 보인다는 피드백 — 재생바 레이아웃 확장**

2차로 자체 UI 플레이어까지 만들었는데, 본인이 스포티파이/유튜브 뮤직/애플 뮤직 스크린샷을 나란히 보여주며 "다른 서비스들은 재생바가 크고 정보도 많은데 우리는 작고 부실하다"고 지적함. 단순히 크기만 키우는 게 아니라 "작게 가더라도 담긴 정보/기능은 실제 서비스만큼 있어야 한다"는 요구였음.

레퍼런스들(스포티파이 하단바, 유튜브 뮤직 하단바)을 보면 공통적으로 좌(앨범아트+곡정보+좋아요)/가운데(재생 컨트롤+진행바)/우(볼륨 등 부가 컨트롤) 3분할 구조를 쓰길래 같은 구조로 재설계함:
- 앨범아트를 56px에서 64px로 키우고, 왼쪽 영역에 곡 정보와 함께 즐겨찾기 별표를 추가 — 재생바에서 바로 즐겨찾기 토글까지 가능해짐(기존 `handleToggleFavoriteTrack`/`isTrackFavorite` 로직 재사용, 트랙 카드와 똑같은 방식으로 동작).
- 오른쪽에 볼륨 슬라이더 + 음소거 버튼 추가 (`player.setVolume()`/`mute()`/`unMute()` 호출) — 이전엔 볼륨 조절 자체가 없었음.
- 모바일 폭에서는 3열이 좁아서 세로로 쌓이게(`grid-cols-1 sm:grid-cols-[1fr_2fr_1fr]`) 하고, 재생바가 세로로 길어진 만큼 본문 하단 여백(`pb-40`)도 데스크톱(`pb-28`)보다 넉넉하게 늘려서 콘텐츠가 안 가리게 함.

**면접에서 말할 포인트(3차)**:
- "레퍼런스 서비스들을 비교 분석해서 부족한 점을 찾은 경험" -> 여러 실제 서비스의 재생바 스크린샷을 놓고 공통 패턴(좌/중/우 3분할)을 뽑아낸 뒤, 그 구조를 기준으로 우리한테 없는 요소(볼륨, 재생바 안 즐겨찾기)를 역으로 찾아낸 과정으로 설명 가능.
- "이미 있는 기능(즐겨찾기 토글)을 다른 위치에도 노출한 이유" -> 트랙 카드에서만 되던 즐겨찾기를 재생 중인 화면(재생바)에서도 할 수 있게 해서, "지금 듣고 있는 곡을 마음에 들면 바로 저장"하는 흐름을 자연스럽게 만든 것 — 같은 로직을 여러 진입점에서 재사용하도록 설계했다는 점을 강조하면 좋음.

---

## 19. 홈 화면 중앙 콘텐츠를 여러 행으로 분리 + 미니 플레이어 슬라이더 다듬기

**뭘 했나**: 두 가지.
1. 홈 화면 중앙에 "지금 인기 있는 곡" 한 덩어리로만 있던 걸, 글로벌 차트/국내(한국) 차트/인기 아티스트 세 개 행으로 나눔. 백엔드 `GET /chart`에 `country` 쿼리 파라미터를 추가해 Last.fm `geo.getTopTracks`(국가별 차트)를 같이 처리하게 하고, `GET /chart/artists`(Last.fm `chart.getTopArtists`)를 신규 추가.
2. 미니 플레이어의 진행바/볼륨바를 네이티브 `<input type="range">` 기본 스타일에서, 얇은 트랙 + 작은 원형 손잡이로 커스텀 스타일링. 볼륨 아이콘도 이모지(🔊/🔇)에서 스트로크 기반 SVG 아이콘으로 교체.

**왜 이렇게 했나**: 본인이 스포티파이(인기 상승 곡/인기 아티스트/인기 앨범 및 싱글/인기 라디오)와 유튜브 뮤직(다시 듣기/빠른 선곡/맞춤 믹스/무드별 플레이리스트) 홈 화면 스크린샷을 나란히 보여주며 "저 서비스들은 여러 섹션으로 나뉘어 있는데 우리는 차트 하나뿐이라 작고 내용이 부실해 보인다"고 지적함. "정확히 똑같이 따라 하라는 건 아니고, 우리도 여러 개로 나뉘었으면 좋겠다"는 요지였음. 볼륨바는 별개로, MiniPlayer에 볼륨 슬라이더를 추가한 직후 "저 음소거 바는 너무 짜치다"는 피드백을 받음 — 브라우저 기본 range 슬라이더가 `accent-color`만 줬을 때 손잡이가 크고 투박하게 렌더링되는 문제였음.

**어떻게 동작하나**:
- `/chart`는 이제 `country` 파라미터 유무로 두 가지 Last.fm 메서드(`chart.getTopTracks` vs `geo.getTopTracks`)를 분기 처리 — 완전히 새 엔드포인트를 만들지 않고 기존 걸 확장한 이유는, 둘 다 "인기 차트"라는 같은 개념이고 응답 형태(트랙 리스트)도 동일해서 굳이 엔드포인트를 쪼갤 필요가 없었기 때문. 캐시 키만 국가별로 분리(`chart:top-tracks:south korea`).
- `/chart/artists`는 트랙이 아니라 아티스트를 반환하는 별개 개념이라 새 엔드포인트로 분리. 프론트에서는 이미 있던 `ArtistCard` 컴포넌트를 그대로 재사용해서(가로 스크롤 리스트로 감싸기만 함) 새 컴포넌트를 안 만들고 처리.
- 슬라이더 커스텀 스타일: Tailwind의 임의 값 선택자(`[&::-webkit-slider-thumb]:...`, `[&::-moz-range-thumb]:...`)로 각 브라우저의 슬라이더 손잡이 pseudo-element를 직접 지정 — `appearance-none`으로 기본 렌더링을 끄고, `h-3 w-3 rounded-full bg-white`로 작은 원형 손잡이를 새로 그림. 진행바와 볼륨바가 똑같은 `SLIDER_CLASS`를 공유해서 스타일이 어긋나지 않게 함.

**면접에서 말할 포인트**:
- "레퍼런스 여러 개를 비교해서 부족한 부분을 구조적으로 찾은 경험" -> 여러 실제 서비스의 홈 화면을 "행 단위로 어떻게 나뉘어 있나" 기준으로 비교해서, 우리 서비스에 없는 축(국가별 차트, 아티스트 랭킹)을 찾아내고 우리가 이미 가진 데이터 소스(Last.fm) 안에서 구현 가능한 것만 골라 추가했다고 설명. "똑같이 따라 하기"가 아니라 "패턴을 읽고 우리 상황에 맞게 절충"한 사례.
- "기존 엔드포인트를 확장할지 새로 만들지 어떻게 판단했나" -> 응답 형태와 개념이 같으면(트랙 차트 vs 국가별 트랙 차트) 파라미터로 확장하고, 아예 다른 개념이면(트랙 vs 아티스트) 새 엔드포인트로 분리하는 기준을 적용했다고 설명.
- "브라우저 기본 UI가 마음에 안 들 때 어떻게 다듬나" -> `<input type="range">`의 pseudo-element(`::-webkit-slider-thumb` 등)를 알아야 커스터마이징이 가능하다는 것, 크로스 브라우저 대응(webkit/moz 둘 다)까지 챙긴 걸 설명하면 좋음.

---

## 20. 즐겨찾기 사이드바 "다시 듣기" 무드별 스마트 그룹핑 (→ 되돌림, [[학습노트 21번]]에서 대체)

> **번복 기록**: 이 기능을 화면에 띄우자마자 "결국 아까 내가 별로라고 한 태그 즐겨찾기를 무드 이름만 바꿔서 다시 만든 거 아니냐"는 피드백을 받고 곧바로 되돌림. AskUserQuestion으로 "무드 기반 스마트 플레이리스트"를 골라주셔서 그 방향으로 만들었지만, 실제로 화면에서 보니 자동 분류라는 점 자체가 "직접 만드는 재생목록을 원한다"는 원래 요구와 안 맞았던 것 — 선택지를 잘못 설계했던 사례로 남겨둠. 아래 내용은 무엇을 만들었고 왜 실패로 판단됐는지 기록용으로 남기고, 실제로 유지 중인 기능은 [[학습노트 21번]] "재생목록(Playlist) 기능"임.

**뭘 했나**: 오른쪽 사이드바 "다시 듣기" 목록을 저장 순서 그대로의 플랫 목록에서, 홈 화면 무드 칩과 같은 어휘(운동/휴식/집중/신나는/잔잔한/슬픔/로맨스/출퇴근길) 기준의 그룹으로 재구성. 신규 파일 `frontend/lib/moodGrouping.ts`(그룹핑 로직), `frontend/components/FavoriteTrackRow.tsx`(그룹/기타 양쪽에서 재사용하는 곡 한 줄 UI)를 추가하고 `app/page.tsx`를 수정. 매칭 안 되는 곡은 "기타" 그룹으로 모으고, 그룹 헤더 클릭으로 접기/펼치기 가능.

**왜 이렇게 했나**: 본인이 이 프로젝트를 시작한 근본 계기 자체를 다시 짚은 피드백 — "추천을 아무리 많이 해줘도 너무 많으면 내가 뭘 좋아하는지 오히려 모르겠고, 썸네일만 보고 고르려니 선택장애가 온다. 사람들이 플레이리스트를 만들어 좋아하는 곡을 넣어두긴 하는데, 그 플레이리스트가 저장한 순서대로만 쌓여서 정리가 안 된다"는 문제의식. 정리 방식으로 (1)태그 자동 분류 (2)수동 플레이리스트 폴더 (3)무드 기반 스마트 플레이리스트 (4)자동+수동 혼합 중 (3)을, 적용 범위로 신규 페이지 대신 기존 사이드바 개선을 선택함.

바로 이전에 시도했다가 걷어낸 "태그 그룹핑 UI"([[학습노트 17번]])와 헷갈릴 수 있는데 핵심 차이가 있음: 그때는 Last.fm 원본 태그(K-POP/IU/KOREAN 등 임의의 값)를 그대로 그룹 이름으로 썼다가 "그룹이 너무 잘게 쪼개지고, 눌러도 아무 데도 안 간다"는 이유로 실패했음. 이번엔 그룹 어휘를 홈 화면에 이미 있는 무드 칩 8개로 강제로 좁혀서 (a) 그룹 개수가 최대 9개(무드 8개 + 기타)로 예측 가능하고 (b) 사용자가 이미 알고 있는 이름이라 낯설지 않음.

**어떻게 동작하나**:
- `FavoriteTrack.tags`는 Last.fm 크라우드소싱 장르 태그("kpop", "ballad", "rock" 등)에 가깝고, 무드 칩 태그("workout", "study" 등)와 문자열이 그대로 일치하는 경우는 드묾. 그래서 `moodGrouping.ts`의 `MOOD_TAG_ALIASES`에 무드마다 자주 같이 나타나는 장르 태그를 수동으로 매핑해두고(예: "잔잔한" ↔ ballad/acoustic/indie/folk), 곡의 태그 중 하나라도 그 무드의 별칭 집합에 속하면 그 그룹에 포함시킴 — 완전 자동 분류가 아니라 규칙 기반 완화 매칭. `LastFmTagClient.cleanTags`가 이미 아티스트 자기 이름/국적 태그 같은 잡음은 걸러준 상태라 이 단계에선 장르 신호만 다루면 됨.
- 곡 하나가 여러 무드 별칭 집합에 동시에 걸리면 억지로 하나만 고르지 않고 여러 그룹에 다 나타나게 둠 — "발라드"가 잔잔한/슬픔/로맨스 그룹에 동시에 걸리는 게 자연스러운 경우라 판단.
- 어느 무드에도 안 걸리는 곡은 사라지지 않고 "기타" 그룹으로 모아서, 그룹핑 규칙이 못 잡아낸 곡도 여전히 눈에 보이고 재생/즐겨찾기 해제가 가능하게 함.
- 새 백엔드 API는 필요 없었음 — `GET /favorite-tracks` 응답에 이미 `tags` 필드가 있어서 프론트에서 `useMemo`로 그때그때 계산.

**알아둘 한계**: `MOOD_TAG_ALIASES`는 직접 손으로 채운 매핑이라 커버리지가 완벽하지 않음(케이팝/한국 인디처럼 Last.fm 태그 자체가 빈약한 장르는 "기타"로 많이 몰릴 가능성). 실제 사용자 태그 분포를 보면서 별칭 목록을 계속 넓혀야 함.

**면접에서 말할 포인트**:
- "이전에 실패한 기능을 다시 만들 때 뭘 다르게 했나" -> 실패 원인(임의의 태그 → 그룹 폭발, 눌러도 무반응)을 먼저 정확히 짚고, 그 원인만 제거한 좁은 버전(고정된 8개 어휘)으로 다시 설계한 과정. 실패를 반복하지 않으려면 "왜 실패했는지"를 재사용 가능한 교훈으로 남겨야 한다는 걸 보여줄 수 있는 사례.
- "완벽하지 않은 자동 분류를 어떻게 설계했나" -> 정확한 분류기를 만들 데이터/모델이 없는 상황에서, 수동 별칭 매핑이라는 실용적인 절충안을 택하고 한계를 명시적으로 인지한 채(기타 그룹으로 누락분 흡수) 점진적으로 개선 가능한 구조로 만든 점을 설명.
- "새 기능에 백엔드 변경이 항상 필요한 건 아니라는 판단" -> 이미 응답에 들어있는 필드(`tags`)를 활용해서 프론트에서만 계산했고, 그 덕에 배포/검증 사이클이 훨씬 짧았다는 걸 설명 가능.

---

## 21. 재생목록(Playlist) 기능 — 직접 만들고 곡을 담는 진짜 재생목록

**뭘 했나**: 사용자가 이름을 직접 정해서 곡을 골라 담는 재생목록 기능을 처음부터 새로 추가. 즐겨찾기(★ 별표)와는 완전히 별개 도메인.
- 백엔드(playlist-service): `Playlist`(id, sessionId, name, createdAt), `PlaylistTrack`(id, playlistId, artistName, trackName, url, imageUrl, addedAt — `(playlist_id, artist_name, track_name)` UNIQUE) 엔티티 신규 추가. `/playlists`(목록/생성/삭제), `/playlists/{id}/tracks`(목록/추가), `/playlists/{id}/tracks/{trackId}`(삭제) REST API 추가. `PlaylistServiceTest`로 소유권 체크(다른 세션 재생목록 삭제 불가 등), 중복 추가 멱등성, 재생목록 삭제 시 소속 곡도 같이 삭제되는지 검증.
- 프론트: `lib/playlistApi.ts`에 재생목록 CRUD 함수 추가. 오른쪽 사이드바에 "내 재생목록" 섹션(＋ 버튼으로 새 재생목록, 이름 눌러 접기/펼치기, 곡 개별 삭제, 재생목록 자체 삭제). 트랙 카드(`TrackList.tsx`)와 즐겨찾기 목록(`FavoriteTrackRow.tsx`) 양쪽에 "＋" 버튼을 추가해서, 어디서든 곡을 눌러 원하는 재생목록에 담을 수 있게 함 — 누르면 화면 중앙에 `AddToPlaylistModal`이 뜨고, 기존 재생목록을 고르거나 그 자리에서 새로 만들 수 있음.

**왜 이렇게 했나**: 바로 앞([[학습노트 20번]])에서 태그 기반 자동 그룹핑을 두 번(원본 태그 한 번, 무드 어휘로 좁혀서 한 번) 시도했는데, 두 번째 시도조차 화면에 뜨자마자 "그거 결국 아까 별로라던 태그 즐겨찾기를 이름만 바꿔 다시 만든 거 아니냐"는 피드백을 받음. 핵심 문제를 다시 짚어보면: 사용자가 원한 건 "자동으로 잘 분류해주는 것"이 아니라 "유튜브 뮤직처럼 내가 직접 재생목록을 만들고 이름 붙이고 곡을 골라 넣는 것" — 실제로 유튜브 뮤직 사이드바 스크린샷("＋ 새 재생목록" 버튼과 사용자가 만든 재생목록 목록)을 보여주며 요청함. 두 도메인(자동 취향 신호 vs 수동 큐레이션)을 혼동했던 게 반복된 실패의 원인이었음 — 자동 그룹핑은 여전히 유효한 문제(즐겨찾기 태그를 신곡 추천에 쓰는 것, Spring Batch 항목)지만, "정리"의 답은 자동 분류가 아니라 수동 재생목록이었다는 걸 두 번 만에 확인함.

**어떻게 동작하나**:
- `FavoriteTrack`을 재사용하지 않고 `PlaylistTrack`을 별도 테이블로 둔 이유: 같은 곡이 여러 재생목록에 동시에 들어갈 수 있어야 하는데, `FavoriteTrack`은 세션당 곡 하나에 행 하나만 허용(`session_id+artist+track` UNIQUE)이라 다대다 관계를 표현할 수 없음. `PlaylistTrack`은 대신 `(playlist_id, artist_name, track_name)` 단위로 유일성을 검사해서, 같은 곡이 재생목록 A와 B에는 각각 들어갈 수 있되 같은 재생목록에 중복으로는 안 들어가게 함.
- 재생목록에 곡을 추가하는 진입점을 트랙 카드/즐겨찾기 목록 두 군데 다에 두었는데, 카드들이 가로 스크롤 컨테이너(`overflow-x-auto`) 안에 있어서 카드 바로 옆에 드롭다운을 띄우면 잘려 보이는 문제가 있음(`overflow-x`를 설정하면 CSS 스펙상 `overflow-y`도 같이 `auto`로 바뀌어서 세로로 튀어나온 요소가 스크롤 영역에 잘림). 그래서 카드마다 개별 팝오버 대신, `page.tsx`가 "지금 어떤 곡을 담으려는 중인지"(`addToPlaylistTrack`) 상태 하나만 들고 있다가 화면 중앙에 모달 하나(`AddToPlaylistModal`)를 띄우는 방식으로 통일 — `MiniPlayer`를 하단에 하나만 띄우는 것과 같은 패턴(전역 상태 하나 + 조건부 렌더링 오버레이 하나).
- 재생목록 목록(`GET /playlists`)은 홈 진입 시 한 번에 불러오지만, 각 재생목록 안의 곡 목록(`GET /playlists/{id}/tracks`)은 사이드바에서 그 재생목록을 처음 펼칠 때만 불러와서 `playlistTracksById`에 캐싱함 — 재생목록이 여러 개 쌓여도 안 펼친 것까지 매번 불러오지 않게.
- 재생목록 트랙 개수(`trackCount`)는 클라이언트에서 낙관적으로 증감시키지 않고, 곡을 추가/삭제할 때마다 `GET /playlists`를 다시 불러 서버 값을 신뢰하는 방식을 택함 — `addTrack`이 멱등이라(이미 있는 곡을 다시 추가해도 새로 안 만듦) 클라이언트에서 카운트를 직접 더하면 실제 값과 어긋날 수 있어서, 재조회가 더 단순하고 정확함.

**버그 수정: 트랙 카드 별표/＋ 버튼이 안 눌리고 재생만 되던 문제** — ＋ 버튼을 추가하고 나서 실사용 중 "별표를 눌렀는데 즐겨찾기 대신 재생이 된다"는 신고를 받음. 원인은 `TrackList.tsx` 카드 안의 호버 재생 아이콘 오버레이(`absolute inset-0`, 카드 전체를 덮음)가 DOM에서 별표/＋ 버튼보다 뒤에 있어서, 같은 스택 컨텍스트에서는 나중에 그려진 요소가 클릭을 먼저 받는다는 점 때문에 그 오버레이가 버튼 클릭을 가로채고 있었음(오버레이엔 자체 클릭 핸들러가 없어서 그대로 카드의 `onPlay`로 버블링됨) — `opacity-0`라 안 보인다고 클릭도 안 받는 건 아니라는 걸 놓친 실수. `pointer-events-none`을 붙여서 순수 장식 용도로만 쓰이게 고침.

**알아둘 한계**: 재생목록 안에서 곡 순서를 사용자가 직접 바꾸는 기능(드래그 정렬)은 아직 없음 — 지금은 추가한 순서(`addedAt` 역순)로만 보여줌. Kotlin 코드는 샌드박스가 Gradle 배포 서버(`services.gradle.org`)에 네트워크 접근이 막혀 있어 직접 컴파일 검증을 못 했는데, 로컬(IntelliJ)에서 `PlaylistServiceApplication` 재시작해보니 실제로는 무관한 파일(`PlaylistServiceApplication.kt` 12번째 줄에 남아있던 글자 하나)이 원인인 컴파일 에러였고, 그것만 지우니 정상 빌드됨 — 새로 작성한 `Playlist`/`PlaylistTrack`/`PlaylistService`/`PlaylistController` 자체는 문제 없었음.

**면접에서 말할 포인트**:
- "요구사항을 잘못 짚었다가 다시 바로잡은 경험" -> 사용자 피드백을 "정리가 안 된다"는 한 문장만 보고 자동 분류로 두 번 해석했다가, 실제로는 수동 큐레이션 도구를 원했다는 걸 뒤늦게 확인한 과정. 요구사항을 구현하기 전에 구체적인 레퍼런스(유튜브 뮤직 스크린샷)를 먼저 확보했으면 더 빨리 맞출 수 있었다는 것도 같이 설명 가능 — 모호한 요구를 다룰 때 왜 구체적 예시를 먼저 확보해야 하는지 보여주는 사례.
- "다대다 관계를 왜 새 테이블로 풀었나" -> 기존 `FavoriteTrack`의 유니크 제약(세션+곡 단위 1행)이 "곡 하나가 여러 재생목록에 속할 수 있다"는 새 요구사항과 구조적으로 안 맞는다는 걸 파악하고, 억지로 재사용하지 않고 관계 형태에 맞는 새 테이블(`PlaylistTrack`, `playlist_id+artist+track` 유니크)을 설계했다는 점.
- "낙관적 업데이트 대신 재조회를 선택한 이유" -> 멱등한 쓰기 연산(이미 있는 걸 다시 추가해도 에러 없이 기존 걸 반환) 위에서 클라이언트가 카운트를 직접 계산하면 서버 실제 상태와 어긋날 수 있다는 걸 인지하고, 정합성을 우선해 약간의 추가 요청을 감수했다는 트레이드오프 설명.

---

## 22. 재생목록 자동 생성 — 즐겨찾기 태그를 분석해서 장르별로 알아서 만들기

**뭘 했나**: [[학습노트 21번]]에서 만든 수동 재생목록 기능에 "자동 생성" 모드를 추가. 사이드바 "내 재생목록" 헤더의 ✨ 버튼을 누르면, 지금까지 즐겨찾기한 곡들을 Last.fm 태그(장르) 기준으로 분석해서 케이팝/발라드/힙합 같은 재생목록을 한 번에 만들어줌. 백엔드에 `AutoPlaylistService`(`favoritetrack` 도메인을 읽어서 `userplaylist` 도메인에 씀)와 `POST /playlists/auto-generate` 엔드포인트를 신규 추가하고, `Playlist`에 `isAutoGenerated` 플래그를 추가해 수동으로 만든 재생목록과 구분함.

**왜 이렇게 했나**: 수동 재생목록 기능을 만들자마자 "다른 서비스도 다 이렇게(이름 타이핑 + 곡 하나하나 추가) 하는데 이건 너무 뻔하다, 내가 들었던 노래를 분석해서 알고리즘처럼 장르별로 알아서 재생목록이 만들어졌으면 좋겠다"는 요청을 받음. 앞서 [[학습노트 20번]]에서 자동 그룹핑을 시도했다가 "그건 원치 않는 태그 즐겨찾기랑 같다"고 되돌렸던 것과 모순돼 보일 수 있는데, 핵심 차이는 **사용자가 명시적으로 요청한 동작이냐 아니냐**임 — 20번은 사용자가 아무것도 안 눌러도 사이드바에 자동으로 나타나는 방식(수동 재생목록을 만들고 싶어 했는데 대신 자동 분류만 보여준 것), 이번은 "새 재생목록"과 별개로 사용자가 ✨ 버튼을 명시적으로 눌러야만 발동하고, 결과물도 진짜 `Playlist` 엔티티라 이름 바꾸거나 곡을 더 추가하는 등 수동 재생목록과 동일하게 다룰 수 있음. 자동/수동을 배타적 선택지가 아니라 재생목록을 만드는 두 가지 방법으로 나란히 두는 게 핵심.

**어떻게 동작하나**:
- 곡의 "대표 장르"는 `FavoriteTrack.tags`(콤마로 이어붙인 문자열)의 첫 번째 태그로 판단함. `LastFmTagClient.cleanTags()`가 저장 시점에 이미 Last.fm 태그를 개수(count) 내림차순으로 정렬해서 상위 3개만 저장해두기 때문에, 첫 번째 태그가 그 곡에서 가장 비중 높은 태그라는 걸 재사용할 수 있었음 — 별도 계산 없이 `tags.split(",").first()`만 하면 됨.
- Last.fm 태그는 크라우드소싱이라 같은 장르도 표기가 제각각("kpop"/"k-pop", "hip hop"/"hip-hop")이라, `GENRE_ALIASES` 맵으로 자주 나오는 표기만 좁게 정규화하고 `GENRE_LABELS` 맵으로 한글 이름을 붙임 — `LastFmTagClient.ARTIST_SELF_TAG_ALIASES`와 같은 "완전한 사전보다 자주 나올 것 위주로" 트레이드오프.
- 같은 대표 장르가 2곡 미만이면 재생목록을 만들지 않음 — 1곡짜리 "재생목록"은 의미가 없어서.
- 버튼을 다시 눌러도 재생목록이 계속 쌓이지 않도록, 실행할 때마다 `isAutoGenerated=true`인 기존 재생목록만 지우고(수동으로 만든 건 안 건드림) 최신 즐겨찾기 기준으로 다시 만듦 — 여러 단계 쓰기(삭제 여러 건 + 생성 여러 건)가 한 번에 일어나야 하는 연산이라 `@Transactional`로 묶어서 중간에 실패해도 절반만 반영되는 일이 없게 함(이 서비스에서 `@Transactional`을 명시적으로 쓴 첫 번째 케이스 — 다른 서비스들은 단일 저장/조회라 필요 없었음).
- `AutoPlaylistService`를 `PlaylistService`와 분리한 이유: `PlaylistService`는 즐겨찾기 도메인을 몰라도 되는 순수 재생목록 CRUD인데, 자동 생성 로직만 `FavoriteTrackRepository`를 끌어다 쓰는 교차 도메인 로직이라 섞으면 책임 경계가 흐려짐.

**면접에서 말할 포인트**:
- "겉보기엔 비슷한 기능을 왜 다시 만들었나" -> 이전에 실패한 자동 그룹핑과 이번 자동 생성의 차이(암묵적 vs 명시적 트리거, 뷰 계산 vs 실제 엔티티 영속화)를 명확히 구분해서 설명할 수 있어야 함 — 요구사항의 표면적 유사성과 실제 UX 의도 차이를 구분하는 능력을 보여주는 포인트.
- "이미 있는 데이터를 어떻게 재사용했나" -> 별도로 장르 분류 로직/API 호출을 추가하지 않고, 즐겨찾기 저장 시점에 이미 정렬해서 저장해둔 태그 데이터(`FavoriteTrack.tags`)를 그대로 활용해서 새 기능을 거의 공짜로(추가 외부 API 호출 없이) 구현한 것.
- "언제 트랜잭션 경계를 명시적으로 그었나" -> 대부분의 CRUD는 단일 쓰기라 별도 트랜잭션 선언이 필요 없었지만, "여러 개 지우고 여러 개 새로 만드는" 배치성 연산에서는 중간 실패 시 데이터 일관성이 깨질 수 있어 `@Transactional`을 명시적으로 붙였다는 판단 기준 설명.

**추가 개선: 대표 태그가 장르가 아닐 때의 오분류**

실제로 써보니, 창모의 "빌었어"(힙합 곡, 드라마 OST) 즐겨찾기가 예상과 다르게 힙합으로 안 묶이는 문제를 발견함. 원인은 이 곡의 Last.fm 대표 태그(가장 count가 높은 태그)가 장르가 아니라 "ost"였던 것 — "곡에서 가장 비중 높은 태그 = 장르"라고 가정한 게 항상 맞진 않았음(연도, OST 여부, 무드어가 1순위로 올 수 있음). `primaryGenreOf()`를 "그냥 첫 태그"에서 "태그 3개 중 `GENRE_ALIASES`에 있는 걸 우선 찾고, 없으면 첫 태그로 폴백"하도록 수정 — Last.fm이 매긴 우선순위보다 "우리가 장르로 인식할 수 있는가"를 우선하는 규칙으로 바꿈.

**면접에서 말할 포인트(추가)**: "가정이 실제 데이터로 깨진 경험" -> "정렬된 데이터의 첫 번째 값이 내가 원하는 속성일 것"이라는 가정이 실제 크라우드소싱 데이터(장르 외에도 연도/OST/무드 태그가 섞여 들어옴)에서는 깨질 수 있다는 걸 실사용 중 발견하고, 정렬 순서에 의존하는 대신 "내가 아는 값 집합에 속하는가"로 판단 기준을 바꾼 사례 — 데이터 품질/분포를 가정하지 않고 검증 가능한 규칙으로 바꾸는 방향의 리팩터링.

---

## 23. "다음 트랙" 자동 이어재생 큐

**뭘 했나**: 유튜브 뮤직 스크린샷(홈 화면 "다시 듣기"/"오늘의 발견" 추천 행, 재생 화면 오른쪽의 "다음 트랙" 목록)을 레퍼런스로 보여주며 "곡을 하나 고르면 계속 비슷한 곡이 이어졌으면 좋겠다"는 요청을 받고 추가. 백엔드에 `GET /similar-tracks`(Last.fm `track.getSimilar` 우선, 결과 없으면 `artist.getSimilar`+`artist.getTopTracks`로 폴백 — `/recommend`와 동일한 패턴)를 신규 추가. `MiniPlayer`가 유튜브 플레이어의 `ENDED` 상태를 감지해서 큐의 첫 곡으로 자동 전환하고, 재생바에 "다음 트랙" 패널(펼쳐서 목록 확인 + 클릭하면 바로 그 곡으로 전환)을 추가함.

**왜 이렇게 했나**: "재생목록을 정리하고 싶다"는 초반 요구에서 시작해 재생목록 CRUD/자동 생성까지 만들었는데, 사용자가 원했던 더 큰 그림은 "유튜브 뮤직 수준의 추천 경험"이었음 — 그 중에서도 "곡 하나 골랐을 때 알아서 계속 비슷한 곡을 틀어주는 것"을 최우선으로 꼽음. 같이 나온 "가사도 보고 싶다"는 요청은 조사해보니 YouTube Data API에 가사 필드가 없고(유튜브 뮤직이 보여주는 가사도 실은 Musixmatch 같은 별도 라이선스 업체 데이터), 이전에 검토했던 Genius API도 라이선스 문제로 가사 원문 자체를 API로 안 줘서(퍼블리셔와의 계약상 자기네 웹페이지 안에서만 보여줌) 지금 갖고 있는 데이터 소스로는 사실상 막혀있다고 판단, 우선순위에서 보류하고 "다음 트랙" 큐부터 구현함.

**어떻게 동작하나**:
- 큐는 "고정된 목록을 미리 짜두는" 게 아니라 "지금 재생 중인 곡과 비슷한 곡" 목록을 곡이 바뀔 때마다 다시 채우는 방식(라디오 방식) — `handlePlay()`가 재생 시작에 성공하면 곧바로 `refreshQueue(artist, name)`를 호출해서 큐를 새로 받아옴. 곡이 끝나면(`handleTrackEnded`) 큐의 첫 곡을 그대로 `handlePlay()`에 넘기는데, 이 호출이 다시 새 큐를 채워주므로 별도로 큐를 비우거나 앞칸을 지우는 로직이 필요 없었음.
- **콜백 stale closure 버그를 두 개 같이 고침**: `MiniPlayer`의 유튜브 플레이어 인스턴스는 최초 1회(`useEffect(..., [])`)만 생성되는데, 그 안의 `onStateChange` 클로저가 마운트 시점의 `onEnded`/`onActuallyPlaying` 콜백을 그대로 붙잡고 있어서, 이후 곡이 바뀌어도 항상 "첫 곡 때 만들어진" 콜백만 호출되는 문제가 있었음(`onActuallyPlaying`은 `page.tsx`에서 매 렌더 `nowPlaying`을 클로저로 캡처한 인라인 함수라 이 문제에 특히 취약 — 두 번째 곡부터는 첫 곡을 "열어봤다"고 잘못 기록할 뻔함). `onEndedRef`/`onActuallyPlayingRef`를 만들어 매 렌더마다 최신 콜백으로 갱신하고, `onStateChange` 안에서는 `ref.current?.()`로 간접 호출하도록 고쳐서 항상 최신 콜백이 불리게 함.
- "다음 트랙" 패널을 사이드바가 아니라 재생바 위에 뜨는 오버레이로 만든 이유: 사이드바는 이미 즐겨찾기/재생목록이 차지하고 있고, `MiniPlayer`는 이미 "지금 재생 중인 곡" 하나만 아는 전역 오버레이라 그 바로 위에 붙이는 게 위치상 자연스러움 — `AddToPlaylistModal`을 사이드바 옆 팝오버 대신 화면 중앙 모달로 뺐던 것과 같은 "전역 오버레이 하나로 통일" 원칙.

**알아둘 한계**: 가사 표시는 보류됨 — 나중에 하려면 Musixmatch 같은 정식 라이선스 가사 API를 새로 찾아야 함(대부분 유료/제한적). 큐는 항상 "현재 곡 기준 유사곡"이라 사용자가 미리 여러 곡을 순서대로 짜두는 전통적인 "재생목록 재생" 큐는 아님 — 필요하면 나중에 재생목록을 큐 소스로 쓰는 옵션을 추가할 수 있음.

**면접에서 말할 포인트**:
- "stale closure 버그를 어떻게 진단하고 고쳤나" -> `useEffect(..., [])`로 한 번만 등록된 이벤트 핸들러가 이후 렌더의 최신 props/state를 못 보는 React의 흔한 함정을 식별하고, ref로 항상 최신 값을 간접 참조하게 만드는 표준 해법을 적용한 경험. 이 버그가 있었다면 재생 의도 신호(`lastOpenedAt`)가 계속 첫 곡한테만 잘못 기록됐을 거라, 기능 추가 중 기존 데이터 정확성 버그를 같이 찾아 고친 사례로 설명 가능.
- "큐를 고정 목록이 아니라 매번 다시 계산하는 방식으로 설계한 이유" -> 미리 긴 재생목록을 만들어두는 것보다, 매 곡마다 "지금 곡 기준 유사곡"을 다시 물어보는 게 구현이 훨씬 단순하면서도(별도 큐 소진/보충 로직 불필요) 사용자 경험상으로는 유튜브 뮤직의 "라디오"와 동일한 효과를 낸다는 걸 파악하고 더 단순한 쪽을 택한 것.
- "API로 지원이 안 되는 기능(가사)을 어떻게 다뤘나" -> 바로 "안 된다"고 하지 않고 YouTube Data API/Genius API 양쪽을 실제로 조사해서 왜 안 되는지(라이선스/약관) 근거를 확보한 뒤, 억지로 스크래핑 같은 편법을 쓰지 않고 정식 대안(라이선스 가사 API)이 필요하다고 명확히 보류 처리한 판단.

**추가 개선: 큐 길이가 곡마다 들쭉날쭉하던 문제 (9곡에서 항상 끊김)**

실사용 중 국힙 곡("BAND" 등)에서 "다음 트랙"이 항상 정확히 9곡에서 끊기는 걸 발견함. 원인은 폴백 조건이 `if not tracks`(트랙 기반 결과가 완전히 0개일 때만 보충)로 너무 엄격했던 것 — 이 곡들은 `track.getSimilar`가 0개를 반환해서 아티스트 기반 폴백(유사 아티스트 3명 × 인기곡 3곡)으로 넘어갔는데, 그 폴백 자체의 상한이 딱 9곡(3×3)이라 항상 그 개수에서 멈췄던 것. **고침**: (1) 트랙 기반 결과가 완전히 없을 때뿐 아니라 20곡을 못 채웠을 때도 아티스트 기반으로 보충하도록 조건을 완화하고, (2) 유사 아티스트 수(3→6)와 아티스트당 인기곡 수(3→4)를 늘려 폴백 자체의 상한도 24곡까지 올림, (3) 트랙+아티스트 두 소스를 합칠 때 `(아티스트, 곡명)` 키로 중복 제거하고 지금 재생 중인 곡 자체도 제외하도록 `add_track` 헬퍼로 정리.

**면접에서 말할 포인트(추가)**: "폴백은 만들었는데 왜 여전히 부족했는지 진단한 경험" -> "폴백이 있다"는 것과 "폴백의 결과가 충분히 많다"는 것은 별개 문제라는 걸 실제 개수(9곡 고정)를 보고 알아챈 것 — 폴백 발동 조건(완전히 0개일 때만)과 폴백 자체의 용량(3×3 상한) 두 곳을 모두 점검해서 원인을 좁혀나간 디버깅 과정.

---

## 24. 안 듣는 곡 정리 — 즐겨찾기했지만 오래 안 들은 곡을 확인 후 삭제

**뭘 했나**: 즐겨찾기(별표) 사이드바 "다시 듣기" 헤더에 "정리하기" 버튼을 추가. 누르면 백엔드 `GET /favorite-tracks/stale?months=6`을 호출해 6개월간 안 들은 즐겨찾기 곡 목록을 받아오고, `StaleTracksModal`(체크박스 목록, 기본 전체 선택)을 띄워서 사용자가 직접 고른 곡만 `removeFavoriteTrack`으로 하나씩 삭제함.

**왜 이렇게 했나**: "플레이리스트를 저장해도 시간이 지나면 안 듣는 곡이 쌓이는데, 지워도 될지 물어보고 한 번에 정리하는 기능을 만들고 싶다"는 요청. 다만 "몇 개월을 기준으로 잡을지", "자동으로 지워도 되는지"에 대해 본인도 "아무거나 삭제하면 그건 좀 그렇지 않냐"며 확신이 없었음 — 그래서 세 가지를 명확히 확인 후 진행: (1) 기준 기간은 6개월(추천값을 그대로 채택, 이후 조정 여지는 쿼리 파라미터로 남김), (2) 트리거는 항상 사용자가 "정리하기"를 눌렀을 때만(백그라운드 자동 실행 없음), (3) 대상은 즐겨찾기(별표)만이고 재생목록 안 곡은 건드리지 않음. 셋 다 "판단이 애매한 걸 자동화하지 말고, 후보만 보여주고 최종 결정은 사람이 하게 하자"는 같은 원칙에서 나온 선택.

**어떻게 동작하나**:
- "안 들었다"의 기준 시점은 `lastOpenedAt`(즐겨찾기 곡 카드를 눌러서 실제로 재생한 마지막 시각) — 근데 즐겨찾기만 해두고 한 번도 안 눌러본 곡은 `lastOpenedAt`이 아예 없으므로, 이 경우엔 `createdAt`(즐겨찾기에 추가한 시점)을 대신 기준으로 씀. `FavoriteTrackService.findStale()`: `(lastOpenedAt ?: createdAt)`이 `LocalDateTime.now().minusMonths(6)`보다 이전인 것만 필터링.
- 절대 자동으로 지우지 않음 — 백엔드는 후보 목록만 반환하고, 실제 삭제는 프론트에서 사용자가 체크한 항목만 골라 기존 `removeFavoriteTrack`(개별 삭제 API)을 `Promise.all`로 여러 번 호출하는 방식. 새로운 "일괄 삭제" 엔드포인트를 따로 만들지 않고 기존 단건 삭제 API를 재사용함 — 실패 시 부분 삭제가 나더라도 어차피 사용자가 다시 "정리하기"를 누르면 남은 곡이 다시 후보로 뜨므로 별도 트랜잭션 처리 없이도 데이터 정합성이 자연히 회복됨.
- 모달 기본값을 "전체 해제"가 아니라 "전체 선택"으로 잡은 이유: "정리하기"를 눌렀다는 행위 자체가 이미 정리할 의도가 있다는 신호라, 지우고 싶은 것만 하나씩 체크하게 하는 것보다 원치 않는 것만 해제하는 쪽이 실제 사용 흐름에 덜 걸림.

**면접에서 말할 포인트**:
- "삭제처럼 되돌리기 어려운 동작을 어떻게 설계했나" -> 자동 판단(배치로 조용히 지우기)과 완전 수동(사용자가 일일이 찾아서 지우기) 사이에서, "후보를 자동으로 추려주되 최종 확인은 사람이 한다"는 절충안을 택한 이유와, 그 판단 기준(별표만/6개월/명시적 트리거)을 사용자와 하나씩 확정해간 과정을 설명 가능.
- "언제 없는 값을 어떤 값으로 대체했나" -> `lastOpenedAt`이 없는(한 번도 안 연 곡) 경우를 놓치지 않기 위해 `createdAt`으로 폴백한 것 — "데이터가 없다"를 "안 듣는다"에서 제외하지 않고 오히려 "즐겨찾기한 뒤로 한 번도 안 열어봄"이라는 더 강한 신호로 해석한 설계 판단.
- "왜 새 삭제 API를 안 만들고 기존 걸 재사용했나" -> 일괄 삭제 전용 엔드포인트(원자적 처리, 부분 실패 롤백 등)를 새로 설계하는 대신, 이미 검증된 단건 삭제 API를 여러 번 호출하는 더 단순한 방식을 택하고 그 트레이드오프(부분 실패 가능성이 있지만 재시도 시 자연 복구됨)를 명확히 인지하고 있었다는 점.

**알아둘 한계**: Kotlin 백엔드 변경분(`FavoriteTrackService.findStale`, `/favorite-tracks/stale` 엔드포인트)은 아직 로컬 재시작 전이라 컴파일 확인이 안 됨 — 프론트는 `tsc --noEmit` 통과만 확인. 6개월이라는 기준값은 사람마다 체감이 다를 수 있는 임의값이라, 나중에 사용자가 직접 조정할 수 있는 UI 옵션으로 노출하는 것도 고려 가능(현재는 쿼리 파라미터로만 유연성을 남겨둠).

---

## 25. 확장 재생 화면 — 앨범아트 크게 + 다음 트랙/관련 항목/가사 탭

**뭘 했나**: 유튜브 뮤직의 확장 플레이어 화면(재생 중인 곡을 누르면 큰 앨범아트와 오른쪽 "다음 트랙/가사/댓글/관련 항목" 탭이 있는 화면으로 전환되는 것)을 레퍼런스로 보여주며 "지금은 좁은 재생바 한 줄에 다 욱여넣으려니 글씨도 작고 보기 힘들다"는 피드백을 받고 추가. `MiniPlayer` 하단 바에서 앨범아트나 곡 정보를 누르면, 페이지 이동 없이 같은 화면 위에 전체화면 오버레이가 뜸. 왼쪽엔 큰 앨범아트+곡 정보+재생/즐겨찾기 컨트롤, 오른쪽엔 "다음 트랙"(기존 큐 재사용)/"관련 항목"(신규)/"가사"(자리만) 3개 탭.

**왜 이렇게 했나**: 레퍼런스가 요구한 탭은 "다음 트랙/가사/댓글/관련 항목" 4개였지만, 그대로 다 만들지 않고 범위를 좁혀서 골랐음 — 댓글은 유튜브 댓글(CommentThreads) API를 새로 붙여야 하는데, 지금 서비스에서 댓글이 핵심 가치를 더해주는 기능은 아니라고 판단해 제외. 가사는 이전([[학습노트 23번]])에 YouTube Data API/Genius API 둘 다 가사 원문을 못 준다는 걸 이미 확인해뒀던 사안이라, 이번에도 API를 억지로 붙이는 대신 탭 자리만 만들고 "준비 중" 안내만 넣음 — 나중에 Musixmatch 같은 정식 라이선스 API를 붙일 때 탭 구조는 그대로 재사용 가능. 진입 방식도 별도 페이지(`/now-playing`) 대신 같은 화면 위 오버레이로 선택했는데, `MiniPlayer`가 이미 갖고 있는 재생 상태(`currentTime`/`isPlaying`/`volume`/유튜브 플레이어 인스턴스)를 그대로 재사용할 수 있고, 페이지 라우팅을 새로 만들면 뒤로가기·새로고침 시 재생이 끊기는 문제까지 신경 써야 해서 오버레이가 훨씬 단순했음.

**어떻게 동작하나**:
- "다음 트랙" 탭은 이미 있던 큐 데이터(`queue` prop, `/similar-tracks` 기반)를 재사용 — 새 API 호출 없이 UI만 더 크게 다시 그림.
- "관련 항목" 탭은 새로 추가한 데이터 — 곡 단위 유사곡(`/similar-tracks`)과 다르게, 아티스트 단위로 유사 아티스트의 인기곡을 보여주는 기존 `/recommend` 엔드포인트(`recommendByArtist`)를 재사용함. 두 탭이 똑같은 "비슷한 곡"이라도 기준(곡 유사 vs 아티스트 유사)이 다르다는 걸 탭으로 분리해서 보여줌.
- "관련 항목" 데이터는 탭을 처음 열 때만 불러오고(`isExpanded && activeTab === "related"`), 같은 곡에서 다시 열면 재요청하지 않도록 `lastRelatedArtistRef`로 마지막으로 불러온 아티스트를 기억함 — 곡을 재생할 때마다 무조건 불러오면 탭을 안 열어도 API 호출이 낭비되기 때문.
- 확장 화면 안의 재생/일시정지/진행바/즐겨찾기 버튼은 하단 미니바에 있던 것과 완전히 같은 상태(`isPlaying`, `currentTime`, `togglePlay`, `handleSeek` 등)를 그대로 참조 — 같은 컴포넌트 안에 두 UI(미니바 + 확장 화면)가 공존하는 구조라 상태를 두 벌로 나눌 필요가 없었음.

**알아둘 한계**: 가사 탭은 안내 문구만 있고 실제 가사는 안 나옴 — 나중에 유료 가사 API를 붙일 때 이 탭에 데이터만 꽂으면 됨. 댓글 탭은 아예 없음(범위 제외). 확장 화면은 라우트가 바뀌지 않는 오버레이라 브라우저 주소창 URL로 공유하거나 뒤로가기로 닫을 수는 없음(✕/접기 버튼으로만 닫힘) — 필요해지면 나중에 URL 쿼리 파라미터 정도로 상태를 얹는 것도 고려 가능.

**면접에서 말할 포인트**:
- "레퍼런스 기능을 그대로 다 구현하지 않고 좁힌 기준" -> 레퍼런스의 탭 4개 중 서비스 핵심 가치와 무관하거나(댓글) 이미 API 한계가 확인된 것(가사)은 자리만 만들거나 빼고, 기존 데이터로 바로 채울 수 있는 것(다음 트랙/관련 항목)부터 완성도 있게 만든 우선순위 판단.
- "같은 것처럼 보이는 두 추천을 왜 탭으로 나눴나" -> "다음 트랙"(곡 대 곡 유사도)과 "관련 항목"(아티스트 대 아티스트 유사도)이 내부적으로 다른 API/다른 판단 기준이라는 걸 사용자에게도 구분해서 보여준 것 — 백엔드에서 이미 서로 다른 목적으로 만들어둔 두 엔드포인트(`/similar-tracks`, `/recommend`)를 각각의 성격에 맞는 자리에 그대로 재사용한 사례.
- "언제 새 페이지 대신 오버레이를 택했나" -> 재생 상태를 이미 들고 있는 컴포넌트 안에서 뷰만 전환하는 게, 그 상태를 새 라우트로 옮기고 재생 연속성까지 신경 쓰는 것보다 구현·유지보수 비용이 훨씬 낮다고 판단한 트레이드오프.

**버그 수정: `backdrop-blur` 조상 안에서 `fixed` 오버레이가 뷰포트 전체를 못 덮던 문제**

실제로 눌러보니 전체화면이 아니라 재생바 높이만큼만 까맣게 나오고 그 안에 접기/닫기 버튼만 보이는 문제가 있었음. 원인은 `MiniPlayer`의 최상위 컨테이너가 `backdrop-blur`(`backdrop-filter`)를 갖고 있었던 것 — CSS 스펙상 `filter`/`backdrop-filter`가 `none`이 아닌 요소는 자식의 `position: fixed`에 대해 뷰포트 대신 자기 자신을 containing block으로 만들어버림(`transform`이 있을 때와 같은 효과). 그래서 확장 화면 오버레이(`fixed inset-0`)가 `MiniPlayer`의 얇은 하단 바 박스 기준으로 "전체"를 계산해서, 실제로는 재생바 높이만큼만 덮였던 것. `createPortal`로 확장 화면을 `document.body`에 직접 렌더링해서 `backdrop-blur` 조상의 영향권 밖으로 빼는 방식으로 고침 — 상태(재생 상태 등)는 그대로 `MiniPlayer` 클로저 안에 있으니 포탈로 렌더링 위치만 옮겨도 문제없음.

**면접에서 말할 포인트(추가)**: "화면엔 문제없어 보이는 CSS가 실제로는 레이아웃을 깨는 경우" -> `filter`/`backdrop-filter`가 시각 효과 속성처럼 보이지만 실제로는 자식의 `fixed`/`absolute` 포지셔닝 기준(containing block)까지 바꿔버릴 수 있다는 스펙 디테일을 실사용 중 실제 버그로 마주치고, `createPortal`로 DOM 트리 상의 부모-자식 관계를 끊어서 해결한 경험 — CSS 카스케이드/스태킹 컨텍스트 문제를 React 쪽 도구(포탈)로 우회한 사례.

**버그 수정: "관련 항목" 탭이 일부 곡에서 완전히 빈 채로 뜨던 문제**

김하온 "Skrr (feat. GISELLE)" 같은 국내 힙합 곡에서 "관련 항목" 탭을 열면 "관련 아티스트 곡을 찾지 못했어요"만 뜨고 아무것도 안 나오는 문제를 발견함. 원인은 `/recommend`가 오직 Last.fm `artist.getSimilar` 결과 하나에만 의존했던 것 — 국내 힙합/인디 아티스트는 이 유사 아티스트 데이터 자체가 비어있는 경우가 실제로 있음(즐겨찾기 태그 수집에서도 같은 종류의 데이터 공백을 이미 겪은 적 있음, [[학습노트 17번]]). "다음 트랙" 큐(`/similar-tracks`)는 이미 트랙 유사도가 부족할 때 아티스트 기반으로 보충하는 폴백이 있었는데([[학습노트 21번]] 추가 개선), "관련 항목"(`/recommend`)엔 애초에 그 자체가 아티스트 기반이라 더 내려갈 폴백이 없었음. `related_artists`가 비어 있으면 그 아티스트 자신의 인기곡(`artist.getTopTracks`)을 대신 채우도록 수정 — 완전히 빈 화면 대신 최소한 "이 아티스트의 다른 곡"이라도 보여줌.

**면접에서 말할 포인트(추가)**: "외부 API의 데이터 공백을 어떻게 다뤘나" -> 유사 아티스트 추천이라는 한 가지 데이터 소스에만 기댄 설계는 그 소스가 비어있는 아티스트(주로 마이너/로컬 신에서 발생)에서 통째로 실패한다는 걸 실사용 중 발견 — 트랙 유사도 큐에 이미 적용했던 "부족하면 상위 단계로 보충한다"는 폴백 패턴을 아티스트 추천에도 똑같이 적용해서, 한 API 호출의 성공/실패에 UI 전체가 좌우되지 않게 만든 사례.

---

## 26. 가사 탭 실제 연동 — LRCLIB로 계속 보류하던 가사 기능 해결

**뭘 했나**: 확장 재생 화면 "가사" 탭이 그동안 "준비 중" 안내만 띄우던 걸, 실제 가사가 나오도록 연동함. 백엔드에 `GET /lyrics` 엔드포인트를 추가해 LRCLIB(lrclib.net)의 `/api/search`를 프록시하고, 결과를 30일 캐싱함. 프론트는 동기화 가사(LRC 타임스탬프 포함)가 있으면 파싱해서 지금 재생 위치(`currentTime`)에 맞는 줄을 굵게 하이라이트 + 자동 스크롤하고, 동기화 가사가 없고 일반 가사만 있으면 그냥 텍스트로 보여주고, 아예 없으면 "가사를 찾지 못했어요" 안내를 띄움.

**왜 이렇게 했나**: [[학습노트 23번]]에서 YouTube Data API(가사 필드 자체가 없음)와 Genius API(라이선스 문제로 가사 원문 대신 페이지 링크만 줌) 둘 다 조사해서 막혀있다고 결론 내리고 보류했던 사안. 이번에 "정말 방법이 없냐"는 재질문을 받고 다시 조사한 결과, LRCLIB이라는 커뮤니티 기반 가사 데이터베이스를 찾음 — API 키 발급/가입 절차 없이 무료로 호출 가능하고, 동기화(LRC) 가사까지 제공하며, FOSS 음악 플레이어들(예: LRCGET 등 오픈소스 프로젝트)이 실제로 쓰고 있는 서비스임. Musixmatch(가입은 되지만 무료 티어는 가사 일부만 미리보기로 주고 전체 가사는 유료 라이선스 필요) 같은 대안과 비교했을 때, 포트폴리오 프로젝트 성격에 맞고 무료/키리스라는 점에서 LRCLIB을 택함. 다만 LRCLIB은 사용자가 업로드한 크라우드소싱 데이터라 저작권 라이선스가 명시적으로 정리된 상거래용 API는 아니라는 점은 인지하고 있음 — 상용 서비스로 전환한다면 Musixmatch 같은 정식 라이선스 API로 교체가 필요.

**어떻게 동작하나**:
- `/lyrics?artist=&track=`이 LRCLIB `/api/search`를 호출 — 같은 곡이라도 라이브/리믹스/앨범별로 여러 후보가 검색되는데, 그중 인스트루멘탈이 아니고 가사(동기화든 일반이든)가 있는 첫 번째 결과를 씀. 완벽한 매칭 로직(듀레이션 비교 등)은 아니지만, "일단 있으면 보여준다"는 실용적 기준으로 단순화.
- 프론트에서 `syncedLyrics` 문자열("[mm:ss.xx] 가사" 형식이 줄바꿈으로 이어붙은 LRC 포맷)을 정규식으로 파싱해서 `{time, text}[]`로 변환. "지금 부르는 줄"은 `currentTime`보다 시작 시각이 앞선 줄 중 가장 마지막 줄로 계산(`reduce`로 마지막 매치 인덱스 추적) — 유튜브 플레이어가 이미 0.5초 간격으로 폴링해서 갱신하던 `currentTime` 상태를 그대로 재사용해서 별도 타이머 없이 하이라이트가 따라감.
- 하이라이트된 줄에 `ref`를 걸어서 `scrollIntoView({ block: "center" })`로 자동 스크롤 — 사용자가 손으로 스크롤할 필요 없이 노래를 따라 화면이 움직임.
- "관련 항목" 탭과 같은 패턴으로, 가사도 탭을 처음 열 때만 요청하고(`lastLyricsKeyRef`로 아티스트+곡명 조합을 기억해서 같은 곡 재조회 방지) 매 곡 재생마다 미리 불러오지 않음.

**알아둘 한계**: LRCLIB은 크라우드소싱이라 곡 커버리지가 일정하지 않음 — 마이너한 곡이나 최신곡은 없을 수 있고, 그 경우 "가사를 찾지 못했어요"로 자연스럽게 처리됨. 여러 검색 후보 중 첫 번째만 쓰는 매칭 로직이라, 드물게 다른 버전(리믹스 등)의 가사가 나올 가능성도 있음.

**면접에서 말할 포인트**:
- "한 번 막혔던 기능을 다시 조사해서 뚫은 경험" -> 이전 조사(YouTube/Genius)가 틀린 게 아니라 조사 범위가 좁았던 것 — "가사 API" 전체를 다시 훑어서 상용 라이선스 API 말고 크라우드소싱/오픈 데이터 기반 대안이 있다는 걸 찾아낸 과정. 기술적으로 안 된다는 결론도, 조사 범위를 넓히면 다른 답이 나올 수 있다는 걸 보여주는 사례.
- "재생 위치와 UI를 어떻게 동기화했나" -> 새 타이머나 별도 상태 없이, 이미 진행바용으로 폴링하던 `currentTime`을 그대로 재사용해서 가사 하이라이트까지 얹은 것 — 같은 상태를 여러 UI가 구독하는 패턴으로 복잡도를 늘리지 않은 설계.
- "무료 데이터 소스를 쓸 때 인지하고 있어야 하는 리스크" -> LRCLIB이 무료이자 편리하지만 정식 라이선스 계약이 있는 API는 아니라는 점, 포트폴리오/개인 프로젝트 범위에서는 적절하지만 상용 서비스라면 Musixmatch 같은 라이선스 API로 바꿔야 한다는 트레이드오프를 스스로 판단하고 명시할 수 있는 것.

**추가 개선 1: 정확 매칭 후보가 전부 인스트루멘탈로 등록돼 있던 문제, 폴백을 4단계로 확장**

실사용 중 "수퍼비와 (Prod. by BewhY)"(수퍼비, 쇼미더머니 참가곡)에서 가사가 안 나오는 걸 발견함. 원인을 파봤더니 LRCLIB에 이 곡이 정확히 같은 제목으로 여러 번 등록돼 있는데, `track_name`+`artist_name`으로 정확 매칭했을 때 걸리는 후보들이 전부 `instrumental: true`(가사 없음)로 등록돼 있었음 — 반면 같은 곡이 "(Feat. BewhY)"처럼 다른 부제로 등록된 버전에는 실제 가사가 있었음. 커뮤니티가 올린 데이터라 같은 곡도 등록자에 따라 인스트루멘탈 여부가 다르게 표시된 것으로 보임. 이후 "최대한 많은 곡에 가사가 나왔으면 좋겠다"는 요청을 받고, `/lyrics`의 폴백을 4단계로 확장함: ① `track_name`+`artist_name` 정확 매칭 → ② 자유 검색어(`q=아티스트 곡명`) → ③ 제목에서 "(Prod. ...)"/"(Feat. ...)" 같은 괄호 표기를 정규식으로 떼어내고 다시 정확 매칭 → ④ 떼어낸 제목으로 다시 자유 검색. `/similar-tracks`가 `track.getSimilar`에서 결과가 없으면 `artist.getSimilar`+`getTopTracks`로 폴백하는 것과 같은 "정확한 조건 먼저, 없으면 점점 느슨하게" 패턴을 그대로 재사용함. 다만 이 경우에도 LRCLIB 자체에 등록이 안 된 곡은 여전히 못 찾음 — Musixmatch 같은 다른 소스를 추가로 붙일지 사용자에게 확인했고, "무료 티어가 가사 일부만 미리보기로 준다"는 한계 때문에 지금은 LRCLIB만으로 충분하다는 판단을 받음(추가 신호가 쌓이면 재검토 가능).

**면접에서 말할 포인트(추가)**: "같은 데이터인데 왜 검색 방식에 따라 결과가 달라지는지 진단한 경험" -> 정확 매칭 API가 "결과 없음"을 반환할 때 그게 진짜 데이터가 없는 것인지, 아니면 검색 조건이 너무 좁아서 놓친 것인지를 구분하기 위해 더 느슨한 검색으로 재확인해본 디버깅 과정. 크라우드소싱 데이터는 같은 실체가 여러 번 다르게(제목 표기, 인스트루멘탈 여부 등) 등록될 수 있다는 특성을 감안해서, 첫 번째 시도가 실패해도 바로 포기하지 않고 점점 조건을 완화하는 대체 경로를 두는 설계를 일관되게 적용한 사례. "커버리지를 무한정 넓히지 않고 어디서 멈췄나" -> 유료/가입이 필요한 추가 소스(Musixmatch)를 붙이는 건 사용자에게 트레이드오프(가입 필요, 부분 가사만 제공)를 명확히 설명하고 판단을 맡긴 것 — 혼자 결정하지 않고 비용이 드는 선택은 확인받는 태도.

**추가 개선 2: 오른쪽 탭 내용이 길어지면 왼쪽 앨범아트가 화면 밖으로 사라지던 레이아웃 버그**

가사가 긴 곡(예: "BAND")에서 가사 탭을 열면 왼쪽 앨범아트/재생 컨트롤이 통째로 사라지는 버그를 발견함. 원인은 CSS 레이아웃 쪽 — 확장 화면 전체를 감싼 바깥 컨테이너에 `overflow-y-auto`(페이지 전체 스크롤)를 걸어뒀는데, 오른쪽 탭 콘텐츠(`flex-1 overflow-y-auto`)가 자기 안에서 스크롤되도록 의도했던 게 실제로는 부모에게 `min-height: 0`을 명시하지 않으면 flex/grid 아이템이 내용 크기만큼 계속 커지는 기본 동작 때문에 무력화되고 있었음 — 결국 오른쪽 칸이 가사 전체 길이만큼 늘어나 버렸고, 왼쪽 칸(`justify-center`)은 그 늘어난 높이 안에서 "가운데"로 계산되면서 화면 훨씬 아래로 밀려나 있었던 것. **고침**: 바깥 컨테이너는 `overflow-hidden`으로 바꿔 페이지 자체는 스크롤되지 않게 하고, 그리드(`min-h-0`)·오른쪽 탭 박스(`lg:min-h-0`)·탭 콘텐츠(`min-h-0`)까지 체인 전체에 `min-h-0`을 명시해서 오른쪽 칸만 정확히 그 칸의 높이 안에서 내부 스크롤하도록 고정 — 이제 왼쪽 앨범아트는 항상 제자리에 고정되고, 가사가 아무리 길어도 오른쪽만 스크롤됨.

**면접에서 말할 포인트(추가)**: "flexbox/grid에서 자식이 부모 크기를 무시하고 계속 커지는 문제를 어떻게 진단했나" -> `flex-1`/그리드 아이템은 기본적으로 `min-height: auto`라서, 내부 콘텐츠가 크면 부모가 정해준 크기보다 더 커질 수 있다는 CSS 스펙의 잘 알려진 함정(flexbox의 "min-height auto" 이슈)을 실제 버그로 마주치고, 스크롤이 필요한 자식까지 이어지는 모든 레이어에 `min-h-0`을 명시해서 해결한 경험 — [[학습노트 25번]]의 `backdrop-filter` containing block 버그와 마찬가지로, "보이는 대로 동작하지 않는 CSS 레이아웃 규칙"을 실사용 중 발견하고 스펙 이해로 근본 원인을 짚어낸 사례.

**추가 개선 3: 가사 자동 스크롤이 사용자 스크롤과 계속 충돌하던 문제 + 가사 클릭으로 구간 이동**

레이아웃을 고친 뒤에도 "가사가 내려가기만 하고 못 올라간다"는 피드백을 받음 — 원인은 재생 위치가 바뀔 때마다(몇 초 간격) `scrollIntoView`가 무조건 다시 호출돼서, 사용자가 지난 가사를 읽으려고 위로 스크롤해도 다음 줄로 넘어가는 순간 강제로 다시 끌려 내려갔던 것. **고침**: 가사 목록에 휠/터치 스크롤 이벤트가 감지되면 4초간 자동 스크롤을 멈추는 `isUserScrollingLyricsRef` 플래그를 추가 — 유튜브 뮤직 등 실제 가사 UI들이 쓰는 "사용자가 직접 조작하면 잠깐 자동 추적을 양보한다"는 패턴을 재현함. 추가로 "이 가사 나오는 부분으로 바로 이동하고 싶다"는 요청을 받아, 동기화 가사의 각 줄을 클릭 가능하게 만들어 눌렀을 때 그 줄의 타임스탬프로 `seekTo`(기존 진행바 탐색과 같은 API) — 클릭으로 이동한 직후엔 자동 스크롤 억제 타이머도 같이 리셋해서, 곡 뒷부분으로 점프해도 바로 그 줄을 따라가도록 함.

**면접에서 말할 포인트(추가)**: "자동 동작과 사용자 조작이 충돌할 때 어떻게 절충했나" -> 자동 스크롤을 아예 없애면(사용자가 매번 직접 스크롤해야 함) 편의성이 떨어지고, 계속 강제로 따라가면(원래 동작) 사용자가 지난 가사를 읽을 수 없는 딜레마에서, "사용자가 방금 직접 조작했으면 일정 시간 자동 동작을 양보하고, 조용해지면 다시 자동으로 돌아간다"는 절충안을 택한 것 — 실시간으로 업데이트되는 UI(가사, 채팅 스크롤 등)에서 흔히 쓰이는 패턴을 처음부터 알고 설계한 게 아니라, 실제 사용자 불편을 통해 필요성을 인지하고 적용한 경험.

---

## 27. 차트/추천/무드/큐에 "가사 있는 곡만" 필터링

**뭘 했나**: 가사 탭이 잘 동작하는 걸 확인한 뒤에도, 홈 화면 차트나 "다음 트랙" 큐에서 재생한 곡이 막상 가사가 없는 경우를 계속 마주치는 문제가 남아있었음. `/chart`, `/discover-by-mood`, `/recommend`, `/similar-tracks`(다음 트랙 큐) 네 엔드포인트에 "가사가 실제로 있는 곡만" 걸러서 내려주는 필터를 추가함. 다만 `/search`(검색 결과)는 의도적으로 제외.

**왜 이렇게 했나**: "가사가 없는 곡은 아예 안 보였으면 좋겠다"는 요청을 받았을 때, 범위를 어디까지 잡을지(다음 트랙 큐만? 앱 전체?)와 검색 결과도 포함할지를 먼저 사용자에게 확인함 — 검색은 특히 애매한데, 사용자가 정확히 그 곡 제목으로 검색했는데 가사가 없다는 이유로 결과에서 빠지면 "결과가 이상하다"는 인상을 줄 수 있어서("내가 분명 검색했는데 왜 안 나오지"). 반대로 차트/추천/무드 탐색/다음 트랙 큐는 애초에 "사용자가 뭘 찾는지 정해두지 않고 둘러보다가 발견하는" 성격이라, 그 발견 후보를 가사 있는 곡으로 한정해도 "내가 원하는 게 안 보인다"는 문제가 안 생김. 검색과 발견(discovery)을 같은 "곡 목록"처럼 보여도 사용자 기대가 다르다는 걸 구분해서 필터 범위를 정한 것.

**어떻게 동작하나**:
- `/lyrics`에서 쓰던 4단계 LRCLIB 조회 로직을 `_lookup_lyrics(artist, track)`으로 뽑아내서, `/lyrics` 엔드포인트와 새 필터 헬퍼가 같은 함수(같은 30일 캐시)를 공유하게 함 — 필터링 과정에서 이미 확인된 곡은 나중에 사용자가 그 곡의 가사 탭을 직접 열어도 LRCLIB을 다시 안 부르고 캐시에서 바로 나감.
- `_filter_tracks_with_lyrics(tracks, limit)`: 후보 목록을 받아 `ThreadPoolExecutor`(동시 8개)로 각 곡의 가사 유무를 병렬 확인한 뒤, 있는 곡만 `limit`개까지 자름. 순차로 하나씩 물어보면 후보가 10개만 돼도 최대 40번(곡당 최대 4단계)의 LRCLIB 왕복이 순서대로 쌓여서 응답이 몇 초씩 걸릴 수 있는데, 병렬로 동시에 물어보면 그 시간을 8분의 1 수준으로 줄일 수 있음.
- "노래 개수를 줄이면 안 된다"는 요청 때문에, 각 엔드포인트가 Last.fm에서 가져오는 후보 자체를 필요한 것보다 훨씬 넉넉히 늘림 — 예를 들어 `/chart`는 최종 10곡을 채우려고 Last.fm에 30곡을 요청한 뒤 그중 가사 있는 곡만 10곡으로 자름. `/similar-tracks`(다음 트랙 큐)는 최대 40개 후보를 모아 최종 15개로, `/recommend`는 최대 40개 후보(유사 아티스트 8명 × 곡 5개)를 모아 15개로 자름 — 후보를 넉넉히 늘려도 실제 LRCLIB 필터링을 거치면서 원하는 개수보다 적게 남을 수 있다는 한계는 있음(가사 커버리지 자체가 100%가 아니므로).

**알아둘 한계**: 캐시가 비어있는(한 번도 확인 안 된) 곡이 많은 상태에서 처음 요청이 오면, 병렬화했어도 여전히 여러 건의 외부 API 왕복이 필요해서 첫 로딩이 평소보다 느릴 수 있음 — 시간이 지나며 캐시가 쌓일수록(30일 유지) 점점 빨라지는 구조. 후보를 아무리 넉넉히 늘려도 그 태그/아티스트 자체에 가사 있는 곡이 원래 적으면 최종 개수가 목표보다 적게 나올 수 있음.

**면접에서 말할 포인트**:
- "어디까지 필터링하고 어디는 빼야 하는지 어떻게 판단했나" -> 겉보기엔 다 "곡 목록"이지만, 사용자가 그 목록에 기대하는 것(검색="내가 찾은 그 곡" vs 차트/추천="뭐라도 발견하고 싶다")이 다르다는 걸 근거로 필터 적용 범위를 나눈 판단. 같은 UI 컴포넌트(트랙 카드 목록)라도 맥락에 따라 다른 정책이 필요할 수 있다는 걸 보여줄 수 있는 사례.
- "여러 외부 API 호출을 병렬화한 이유와 방법" -> I/O 바운드(네트워크 대기가 대부분인) 작업이라 GIL의 영향을 크게 안 받는다는 걸 알고 `ThreadPoolExecutor`로 동시 호출한 것 — 순차 호출 대비 체감 응답 시간을 몇 배 줄인 트레이드오프와, 캐시(기존에 이미 있던 `/lyrics`용 캐시를 그대로 재사용)로 반복 비용을 낮춘 설계를 같이 설명 가능.
- "제약 조건(개수를 줄이면 안 된다)을 어떻게 만족시켰나" -> 필터링은 결과를 줄이는 동작이니, 그걸 상쇄하려고 입력 쪽(Last.fm에서 가져오는 후보 수)을 필요한 것보다 몇 배 늘리는 방식으로 균형을 맞춘 것 — 다만 이것도 완전한 보장은 아니라는 한계(가사 자체가 드문 태그/아티스트는 여전히 적게 나올 수 있음)까지 인지하고 있다는 점.

---

## 28. 가사 요청 + 수동 등록 기능

**뭘 했나**: 가사 필터링([[학습노트 27번]])을 붙였어도, "차트/추천엔 안 보이지만 검색으로 정확히 그 곡을 찾아 들으려는" 경우엔 여전히 가사 없는 곡을 만날 수 있음. 이때 가사 탭의 "가사를 찾지 못했어요" 안내에 "가사 올려주세요" 버튼을 추가해서, 사용자가 요청을 남기면 운영자(본인)가 나중에 직접 가사를 구해 그 곡에 한해 수동으로 등록할 수 있게 함.

**왜 이렇게 했나**: 모든 곡의 가사를 실시간으로 만들어낼 방법은 없음(LRCLIB이 그 곡을 아예 안 가지고 있으면 끝). 대신 "좋아요" 버튼처럼 가벼운 요청 신호만 남기고, 실제 등록은 사람이 나중에 처리하는 비동기 흐름으로 설계 — 이미 있는 이벤트 로깅 파이프라인(`log_event`)에 새 이벤트 타입만 하나 추가하면 되므로 새 테이블이나 큐 없이 최소 구현으로 충분하다고 판단.

**어떻게 동작하나**:
- `POST /lyrics/request?artist=&track=`: `X-Session-Id` 헤더가 있으면 `log_event(session_id, "lyrics_request", "{artist} - {track}")`로 기존 events 테이블에 기록만 하고 끝. 실시간으로 뭔가 생기진 않음.
- `POST /lyrics/override?artist=&track=&plain_lyrics=&synced_lyrics=`: 운영자 전용. `ADMIN_SECRET` 환경변수와 요청의 `X-Admin-Secret` 헤더를 비교해서 다르면 403 — 이 프로젝트엔 로그인/권한 시스템이 아예 없어서, 최소한의 접근 제한으로 대신함(실제 서비스라면 진짜 인증이 필요하다는 걸 인지하고 스코프를 좁힌 선택).
- 등록된 가사는 `lyrics-override:{artist}:{track}` 캐시 키에 만료(`ttl=None`) 없이 저장. `_lookup_lyrics()`가 LRCLIB을 조회하기 전에 이 override 키를 먼저 확인하도록 순서를 바꿔서, 한 번 수동 등록된 곡은 이후 계속 등록된 가사가 우선 적용됨(LRCLIB 쪽 데이터가 나중에 바뀌어도 영향 안 받음).

**알아둘 한계**: 등록 UI가 따로 없어서, 운영자가 직접 API를 호출해야 함(curl/Postman 등) — 요청 건수가 늘면 간단한 관리자 화면이 필요할 수 있음. `ADMIN_SECRET`이 비어있으면(`.env`에 값 안 넣으면) override 엔드포인트가 항상 403이라 아예 못 씀. dbt `stg_events` 스키마 테스트에 `lyrics_request` 이벤트 타입도 추가해야 함(아직 안 함).

**면접에서 말할 포인트**:
- "실시간으로 해결 못 하는 요청을 어떻게 처리했나" -> 모든 걸 자동화하려 하지 않고, 사용자 요청은 가볍게 로그로만 남기고(기존 파이프라인 재사용) 처리는 사람이 비동기로 하도록 설계 — 새 인프라 추가 없이 최소 비용으로 흐름을 만든 사례.
- "인증 시스템이 없는 프로젝트에서 관리자 기능을 어떻게 보호했나" -> 실제 로그인/권한 체계를 새로 만드는 대신, 이 프로젝트 규모에 맞는 임시방편(공유 비밀값 헤더 검증)을 쓰고 그 한계(진짜 인증이 아니다)를 스스로 인지하고 있다는 걸 설명할 수 있는 지점 — 실무에서도 우선순위에 따라 "지금 필요한 만큼만" 구현하는 판단이 필요하다는 걸 보여줌.
- "캐시 조회 순서를 왜 그렇게 정했나" -> override를 LRCLIB 조회보다 먼저 확인하게 해서, 운영자가 수동으로 정정/등록한 데이터가 외부 소스보다 항상 우선하도록 함 — "더 신뢰할 수 있는 데이터를 먼저 본다"는 원칙을 코드 순서로 표현한 것.

---

## 29. 무드 탐색 결과 국내/해외 구분

**뭘 했나**: 무드 칩("신나는", "출퇴근길" 등)을 눌렀을 때 나오는 곡이 전부 해외 팝송 위주로만 뜨는 문제를 발견 — Last.fm의 무드/장르 태그 자체가 크라우드소싱 데이터라 서구 팝 중심으로 편향돼 있음. `/discover-by-mood`를 국내(domesticTracks)/해외(internationalTracks) 두 목록으로 나눠서 반환하도록 바꾸고, 프론트도 "국내 인기곡"/"해외 인기곡" 두 섹션으로 나눠서 보여줌.

**왜 이렇게 했나**: 유튜브 뮤직 실사용 화면(운동 무드 카테고리)을 보여주며 "국내랑 해외를 구분해서 여러 줄로 보여줄 수 없냐"는 피드백을 받음 — 앞서 홈 화면 차트([[학습노트 15번]])에서도 `/chart`에 `country` 파라미터를 추가해 "국내 인기 차트"를 글로벌 차트와 별도 행으로 분리한 적이 있는데, 무드 탐색에도 같은 사용자 니즈(국내/해외를 구분해서 보고 싶다)가 반복해서 나타난 것.

**어떻게 동작하나**:
- Last.fm에는 트랙/아티스트의 "국적"을 알려주는 필드가 없어서, 직접 판별 기준을 만듦(`_is_domestic_artist`) — (1) 아티스트명에 한글이 포함되면 국내로 간주(한글 오타 교정에 쓰던 `HANGUL_PATTERN` 재사용). 실사용 중 확인한 바로 국내 힙합/인디 아티스트(식케이, 노윤하, 김하온 등)는 Last.fm에도 한글 이름 그대로 등록된 경우가 많아서 이 기준만으로도 상당수 걸러짐. (2) 로마자로 등록되는 경우가 많은 K팝 그룹(BTS, BLACKPINK, NewJeans 등)은 알려진 이름 목록(`KNOWN_KOREAN_ARTIST_NAMES_LATIN`)으로 보완.
- `tag.getTopTracks`로 후보를 50곡(기존 30곡보다 늘림)까지 넉넉히 받아온 뒤, 위 기준으로 국내/해외 두 그룹으로 나눔.
- 무드 태그 자체가 서구 편향이라, 후보를 아무리 늘려도 국내 곡이 하나도 안 걸리는 경우가 실제로 있었음(예: "신나는" 태그 첫 30곡이 전부 해외 팝) — 국내 그룹이 6곡 미만이면 "k-pop" 태그로 별도 조회해서 보충함. 정확히 같은 무드는 아닐 수 있지만, "국내 섹션이 아예 비어있는 것"보다는 낫다는 판단.
- 국내/해외 각각 [[학습노트 27번]]의 가사 필터(`_filter_tracks_with_lyrics`)를 독립적으로 적용해서 최종 10곡씩으로 자름.

**알아둘 한계**: 한글명+알려진 목록 기준이라 목록에 없는 로마자 K팝/K힙합 아티스트는 해외로 잘못 분류될 수 있음(놓치는 아티스트를 발견하면 목록에 추가하는 방식으로 점진 보완 필요). "k-pop" 보충은 정확히 그 무드에 맞는 곡이 아닐 수 있음. 국적 판별이 100% 정확하진 않다는 점에서 편의 기능에 가깝고, 엄밀한 데이터 라벨링은 아님.

**면접에서 말할 포인트**:
- "국적처럼 API가 안 주는 정보를 어떻게 유추했나" -> 정식 메타데이터가 없을 때, 도메인 지식(한국 아티스트는 한글명으로 등록되는 경우가 많다는 관찰)에서 나온 휴리스틱과, 커버리지가 좁은 부분(로마자 K팝)은 수동으로 큐레이션한 목록으로 보완하는 하이브리드 접근을 설계한 사례 — 완벽하지 않다는 한계까지 같이 설명 가능.
- "데이터 소스의 편향을 어떻게 다뤘나" -> Last.fm 크라우드 태그가 서구 팝 위주로 편향돼 있다는 걸 실사용 중 발견하고, 후보 수를 늘리는 것만으로는 편향이 해결 안 될 때(그 카테고리 자체에 국내 데이터가 없을 때) 대체 소스(k-pop 태그)로 보충하는 폴백을 추가한 것 — [[학습노트 23번]]("다음 트랙" 큐가 부족할 때 아티스트 기반으로 보충)에서 이미 썼던 "부족하면 상위 카테고리로 보충" 패턴을 국적 편향 문제에도 재적용.
- "비슷한 요구가 반복될 때 설계를 어떻게 일반화했나" -> 홈 차트에 "국내 인기 차트" 행을 추가했던 것과 본질적으로 같은 요구(국내/해외 구분)가 무드 탐색에서도 나왔다는 걸 인식하고, 각 기능의 데이터 소스 특성에 맞게(차트는 geo 파라미터, 무드는 아티스트명 휴리스틱) 각각 적합한 방법으로 같은 사용자 니즈를 풀어낸 것.

**버그 수정: 응답 스키마를 바꿨는데 옛 캐시가 남아있어서 500 에러가 난 문제**

이 기능을 라이브로 처음 확인하는 자리에서 무드 칩을 누르자 "무드 곡을 불러오지 못했어요"라는 에러가 뜸. 원인은 캐시 키 재사용 — 이 기능을 추가하기 전, 같은 캐시 키(`discover-by-mood:{tag}`)에 옛 응답 형태(`{"tracks": [...]}`)가 이미 Redis에 캐싱(TTL 1시간)돼 있었는데, 코드는 새 형태(`{"domesticTracks": [...], "internationalTracks": [...]}`)를 기대하도록 바뀌었음. 캐시를 히트하면 `cached["domesticTracks"]`를 읽으려 하다가 그 키가 없어서 `KeyError`가 나고, 이게 처리 안 된 예외라 FastAPI가 500을 반환 — 프론트는 이걸 그냥 "실패"로만 인식해서 사용자에게는 이유를 알 수 없는 에러로만 보였음. 캐시 키를 `discover-by-mood:v2:{tag}`로 바꿔서(새 키라 옛 캐시와 충돌 안 함) 해결.

**면접에서 말할 포인트(추가)**: "캐시가 있는 상태에서 응답 스키마를 바꿀 때 뭘 조심해야 하나" -> 캐시는 코드와 별개로 자기 생명주기(TTL)를 갖고 있어서, 코드를 배포해도 캐시된 옛 데이터가 새 코드와 만나면 타입/스키마 불일치로 깨질 수 있다는 걸 실제로 겪은 사례 — 응답 구조가 바뀔 때는 캐시 키에 버전을 넣어 옛 데이터와 새 데이터가 아예 다른 자리에 저장되게 하는 것이 안전하다는 교훈. TTL이 짧으면(1시간) 우연히 안 걸릴 수도 있지만, 그건 운이지 설계가 아니라는 것도 같이 설명 가능.

**성능 개선: "뜨긴 하는데 느리다"는 피드백**

500 에러를 고친 뒤에는 정상 동작했지만 체감 속도가 눈에 띄게 느리다는 피드백을 받음. 원인은 `domestic_tracks = _filter_tracks_with_lyrics(domestic, 10)`과 `international_tracks = _filter_tracks_with_lyrics(international, 10)`을 순서대로 호출하고 있었던 것 — 각 호출은 내부적으로 스레드풀(`ThreadPoolExecutor`)로 병렬 조회하지만, 두 호출 자체는 첫 번째가 끝나야 두 번째가 시작되는 순차 구조라 사실상 병렬화 이점이 절반만 발휘되고 있었음. `_filter_tracks_with_lyrics`에서 가사 유무 조회 부분만 `_lyrics_availability_map()`으로 분리하고, 국내/해외 후보를 합쳐서 이 함수를 딱 한 번만 호출한 뒤 결과만 국내/해외로 다시 나누는 식으로 바꿔서, 최대 동시 조회 수(8→12로도 같이 상향)를 국내/해외 후보 전체에 걸쳐 온전히 활용하게 함.

**면접에서 말할 포인트(추가)**: "병렬화를 적용했는데도 여전히 느렸던 이유" -> 개별 작업 단위(트랙 하나의 가사 조회)는 병렬화했지만, 그 작업 단위를 묶는 "그룹"(국내/해외) 자체를 순차로 처리하고 있어서 병렬화가 그룹 경계에서 끊겨버렸던 것 — 병렬 처리를 적용할 때는 "무엇을 하나의 배치로 묶어서 동시에 돌릴지"의 경계를 어디에 두는지가 실제 성능에 큰 영향을 준다는 걸 확인한 사례.

**UI 개선: "너무 다 카드트랙일 필요는 없다" — 줄형 컴포넌트(CompactTrackRow/CompactTrackGrid) 도입**

유튜브 뮤직 홈 화면 스크린샷(빠른 선곡, 다시 듣기)을 보여주며 "지금 우리 화면은 죄다 큰 정사각 카드라 답답하고, 가로 스크롤도 딱히 티가 안 나서 더 있는지도 모르겠다, 저 사진처럼 작은 썸네일+텍스트 줄 스타일도 섞어 쓰면 좋겠다"는 피드백을 받음. 새 컴포넌트 두 개를 추가함 — `CompactTrackRow`(작은 정사각 썸네일 + 제목/아티스트 2줄 텍스트로 된 가로 한 줄, 즐겨찾기/재생목록 추가 버튼 포함)와, 이를 3열로 배치하고 9개 단위로 좌우 화살표(‹ ›)로 넘기는 `CompactTrackGrid`. 무드 탐색의 국내/해외 인기곡 두 섹션에 적용했고, 글로벌 차트/유튜브 인기 급상승/인기 아티스트 같은 나머지 섹션은 기존 큰 카드(`TrackList`) 그대로 남김 — "모든 곡 목록을 하나의 스타일로 통일해야 한다"는 원칙 대신, 각 섹션의 성격(발견 위주로 이미지가 중요한 카드 vs 이미 익숙한 곡을 빠르게 훑는 목록)에 맞게 스타일을 고를 수 있게 함.

사이드바 "다시 듣기"(`FavoriteTrackRow`)도 직접 언급된 대상이라 같이 손댐 — 내부적으로 `CompactTrackRow`를 그대로 재사용하도록 리팩터링해서, 카드 스타일이 아니었던 기존 텍스트 전용 한 줄을 썸네일 자리가 있는 줄 스타일로 통일함. 다만 즐겨찾기 트랙(`FavoriteTrack`) 도메인엔 애초에 이미지 URL을 저장하는 컬럼이 없어서, 여기 썸네일은 항상 플레이스홀더 아이콘으로만 뜸 — 실제 앨범아트를 보여주려면 Kotlin 쪽 `FavoriteTrack` 엔티티에 `imageUrl` 컬럼을 추가하는 별도 작업이 필요함(이번 범위에서는 안 함).

**면접에서 말할 포인트**:
- "레퍼런스 UI를 그대로 베끼지 않고 우리 상황에 맞게 가져온 지점" -> 유튜브 뮤직은 다양한 섹션마다 각기 다른 레이아웃(카드/줄/캐러셀)을 섞어 쓰는데, 그 이유(콘텐츠 성격에 따라 정보 밀도와 훑어보는 방식이 다름)를 이해하고 우리 서비스에도 "발견용 카드"와 "훑어보기용 줄" 두 스타일을 상황별로 나눠 적용한 것 — 무조건 하나의 컴포넌트로 통일하는 게 항상 좋은 게 아니라는 판단.
- "재사용 가능한 컴포넌트를 어떻게 설계했나" -> `CompactTrackRow`를 페이지네이션 없는 사이드바 세로 목록과, 3열+화살표 페이지네이션이 있는 메인 콘텐츠 그리드 양쪽에서 공유하도록 만들어서, 시각적 통일성은 유지하면서도 배치 로직(그리드/페이지네이션)은 각 사용처에 맞게 분리한 설계.
- "데이터에 없는 필드를 UI에서 어떻게 다뤘나" -> 즐겨찾기 트랙에 이미지 URL이 없다는 걸 알고도 일단 플레이스홀더로 우아하게 저하(graceful degradation)시켜서 기능 자체는 막히지 않게 하고, 실제 이미지가 필요하면 별도 백엔드 작업(컬럼 추가)이 필요하다는 걸 명확히 한계로 남겨둔 것 — 없는 데이터를 억지로 채우거나 기능을 아예 안 만드는 대신, 있는 정보 안에서 최선의 표현을 택한 판단.

---

## 30. 유튜브 인기 급상승 API 연동 — "연동만 해두고 안 쓰는" 문제 해결

**뭘 했나**: "Last.fm이 주는 정보가 별로 없는 것 같은데, 유튜브 API도 가져와놓고 왜 그걸 쓰는 느낌이 없냐"는 지적을 받음. 실제로 그동안 YouTube Data API는 재생 버튼용 `/youtube-search`(트랙 하나당 영상 1개 찾기) 딱 하나에만 쓰이고 있었음. `videos.list(chart=mostPopular)`로 지역별 실제 트렌딩 음악 영상을 가져오는 `GET /youtube-chart?region=KR|US`를 새로 추가하고, 이걸로 홈 화면의 "국내 인기 차트"(원래 Last.fm 기반) 데이터 소스를 교체하고, 짝을 이루는 "해외 인기 급상승"(US 리전) 섹션도 새로 추가함.

**왜 이렇게 했나**: 지적이 정확했음 — YouTube Data API는 이미 API 키까지 발급받아 연동해뒀는데 실제로는 검색 매칭이라는 좁은 용도로만 쓰고 있었던 것. 알고 보니 그동안 써온 `search.list`(호출당 100유닛, 하루 100회 한도)와 달리 `videos.list`는 훨씬 저렴함(요청한 `part` 개수에 비례, 대략 5유닛 수준) — 지금까지 "쿼터 아껴야 하니 검색에만 쓴다"는 암묵적 전제가 있었는데, 사실 트렌딩 조회처럼 저렴한 엔드포인트는 쿼터 걱정 없이 새 기능에 쓸 수 있었던 것. 또 [[학습노트 29번]]에서 국내/해외를 아티스트명 한글 여부로 "추측"해야 했던 것과 달리, YouTube 트렌딩은 `regionCode` 파라미터로 그 나라에서 실제로 지금 보고 있는 영상을 정확하게 가져오므로, 적어도 "국내 인기 차트" 자리에는 추측보다 훨씬 신뢰도 높은 데이터를 쓸 수 있다고 판단.

**어떻게 동작하나**:
- `_youtube_trending(region_code, limit)`: `videos.list`에 `chart=mostPopular`, `regionCode`, `videoCategoryId=10`(음악 카테고리)을 넘겨 그 지역의 인기 급상승 음악 영상을 가져옴.
- Last.fm과 다르게 영상 제목은 "아이유 'Love Wins All' Official MV"처럼 아티스트/곡명이 한 문자열에 섞여 있어서, `_parse_youtube_music_title()`로 최대한 분리함 — 괄호/대괄호 안내 문구를 먼저 지우고(`_strip_title_annotations` 재사용), "아티스트 - 곡명" 구분자가 있으면 그대로 나누고, 없으면 채널명을 아티스트로 대신 씀(공식 음악 채널은 아티스트명 그대로인 경우가 많다는 전제).
- 이 트랙들은 이미 `videoId`를 알고 있어서(트렌딩 조회 자체가 영상 목록이므로), `Track` 타입에 `videoId?: string | null`을 추가하고 `handlePlay`에서 이 값이 있으면 `/youtube-search`를 다시 안 부르고 바로 재생하도록 최적화 — 같은 곡을 두 번 찾는 낭비를 없앰.
- `/youtube-chart`는 3시간 캐싱 — 트렌딩은 자주 바뀌지만, 저렴한 엔드포인트라도 매 요청마다 API를 부르진 않도록 최소한의 캐싱은 유지.

**알아둘 한계**: 제목 파싱이 완벽하지 않음 — "아티스트 - 곡명" 패턴이 아닌 제목(예: 챌린지 영상, 라이브 클립)은 아티스트/곡명이 부정확하게 나올 수 있음. 또한 파싱된 아티스트명이 Last.fm의 공식 표기와 다를 수 있어서, 이 트랙으로 "다음 트랙" 큐(`/similar-tracks`)를 채우려 하면 Last.fm이 그 아티스트를 못 찾아 큐가 비는 경우가 있을 수 있음(이미 빈 큐를 자연스럽게 처리하는 기존 로직 덕분에 에러로 이어지진 않음). `videoCategoryId=10` 트렌딩이 항상 순수 음악만은 아닐 수 있음(뮤직비디오 외 관련 영상이 섞일 가능성).

**면접에서 말할 포인트**:
- "이미 붙여둔 외부 API를 제대로 활용하고 있는지 점검한 경험" -> 연동 자체는 돼 있지만 실제로는 한 가지 좁은 용도로만 쓰이고 있었다는 걸 사용자 피드백으로 알아채고, 그 API가 제공하는 다른 엔드포인트(트렌딩) 중 지금 쓰던 것과 비용 구조가 다른(훨씬 저렴한) 것을 찾아 새 기능에 활용한 사례.
- "같은 API라도 엔드포인트별 비용을 어떻게 고려했나" -> `search.list`(100유닛)와 `videos.list`(약 5유닛)의 쿼터 비용 차이를 인지하고, 비싼 엔드포인트는 캐싱/사용처를 최소화하고 싼 엔드포인트는 새 기능에 자유롭게 쓰는 식으로 예산을 나눠 쓴 판단.
- "이미 알고 있는 정보를 재활용해서 낭비를 없앤 지점" -> 트렌딩 API가 이미 videoId를 주는데, 기존 재생 흐름이 항상 아티스트+곡명으로 다시 검색하도록 짜여 있었던 걸 발견하고, 이미 아는 값이 있으면 검색을 생략하도록 손댄 것 — API 호출을 줄이는 것도 최적화지만, "이미 가진 정보를 안 쓰고 다시 구하는" 낭비를 찾아내는 것도 같은 종류의 최적화라는 점.

**버그 수정: "국내/해외 인기 급상승" 섹션이 통째로 안 뜨던 문제**

라이브로 처음 확인했을 때 두 섹션이 다 비어있었음(에러 표시도 없이 조용히 안 뜸). 원인으로 의심되는 지점은 `chart=mostPopular` + `videoCategoryId=10`(음악 카테고리) 조합이 지역/시점에 따라 결과를 아예 안 주는 경우가 있다는 것 — 프론트는 이미 실패를 "조용히 빈 배열로 처리"하도록 짜여 있어서(다른 곳도 다 이 원칙을 씀) 화면엔 에러 없이 그냥 섹션이 안 보였음. `_youtube_trending()`을 카테고리 필터로 먼저 시도하고, 결과가 비어있으면 카테고리 필터 없이 한 번 더 시도하는 폴백으로 수정 — 카테고리 없이 받은 결과엔 음악이 아닌 영상이 섞일 수 있다는 한계는 있지만, 섹션이 계속 비어있는 것보다는 낫다는 판단. 원인을 100% 확정하진 못해서(유튜브 API 응답을 로그로 직접 봐야 확실함), 여전히 비어있다면 백엔드 터미널의 `[youtube-chart] 요청 실패 ...` 로그로 실제 원인(쿼터/지원 안 되는 조합/키 문제)을 좁혀야 함.

**면접에서 말할 포인트(추가)**: "에러 없이 조용히 실패하는 설계의 트레이드오프" -> 이 프로젝트 전반에서 "외부 API 실패가 UI 전체를 못 쓰게 만들면 안 된다"는 원칙으로 실패를 조용히 빈 배열/null로 처리해왔는데, 그 대가로 "왜 안 보이지"를 진단하기 어려워진다는 걸 이번에 직접 겪음 — 그래서 실패 지점마다 서버 로그(`print`)는 남겨서, 사용자에게는 안 보이지만 운영자는 터미널에서 원인을 추적할 수 있게 균형을 잡았다는 설명.

---

## 31. 아티스트명 "- Topic" 접미사 제거 + 카드 이미지 품질 개선

**뭘 했나**: 세 가지 이미지/데이터 품질 문제를 한 번에 손봄. (1) "다음 트랙" 큐 등 여러 목록에서 아티스트명이 "LiSA - Topic", "aespa - Topic"처럼 이상하게 뜨는 문제 — `_clean_artist_name()`을 새로 만들어 Last.fm 응답과 유튜브 채널명 양쪽에서 이 접미사를 제거하도록 전체 엔드포인트에 적용. (2) 트랙 카드 이미지가 "그냥 가수 프로필 사진처럼 보인다"는 피드백 — `resolve_track_image()`가 Last.fm 이미지를 먼저 쓰던 순서를 뒤집어서 iTunes 검색 결과를 먼저 쓰도록 바꿈. (3) "인기 아티스트" 카드가 이미지 없이 회색 원으로만 뜨는 문제 — Last.fm에 이미지가 없으면 iTunes 앨범 커버로 보완하는 로직을 새로 추가.

**왜 이렇게 했나**:
- "- Topic" 접미사는 유튜브가 공식 채널이 없는 아티스트에게 자동으로 만들어주는 채널 이름(예: "LiSA - Topic")인데, 이게 두 경로로 우리 데이터에 섞여 들어왔음 — 하나는 우리가 만든 `_parse_youtube_music_title()`이 "아티스트 - 곡명" 패턴이 안 맞을 때 채널명을 그대로 아티스트로 쓰던 것(원인 파악), 다른 하나는 Last.fm 자체 데이터에도 케이팝 트랙 상당수가 이미 "- Topic"이 붙은 채로 등록돼 있다는 걸 실사용 중 발견한 것(추가로 발견한 문제 — Last.fm이 크라우드소싱/스크래핑 데이터를 받다 보니 유튜브발 오염된 이름이 그대로 들어온 것으로 추정). 둘 다 증상(이상한 아티스트명 노출, 그 이름으로 이미지 검색이 실패해서 빈 음표 아이콘이 뜸)이 같아서 한 군데(`_clean_artist_name`)에서 같이 해결.
- 이미지 우선순위를 바꾼 이유: Last.fm의 트랙 이미지 배열은 "그 곡의 커버"가 아니라 크라우드소싱 시절에 붙은 범용 아티스트 사진인 경우가 많아서(이미 [[학습노트 11번]]에서 Last.fm 이미지 데이터가 부실하다는 걸 확인한 적 있음), 있으면 우선 쓰던 게 오히려 "곡마다 다른 커버가 아니라 맨날 그 가수 얼굴만 보이는" 느낌을 만든 원인이었음. iTunes Search API(`entity=song`)는 검색어에 맞는 실제 발매 앨범/싱글 커버를 주는 경우가 훨씬 많아서 순서를 바꿈.
- 인기 아티스트 카드는 iTunes에 "아티스트 인물 사진" 자체를 주는 API가 없어서(entity=musicArtist엔 이미지 필드가 없음), 대신 그 아티스트의 대표 앨범 커버(entity=album)를 아바타처럼 쓰는 절충안을 택함 — 완벽한 해법은 아니지만 빈 회색 원보다는 정보량이 많다고 판단.

**어떻게 동작하나**:
- `_clean_artist_name(name)`: 정규식(`\s*[-–]\s*Topic\s*$`, 대소문자 무시)으로 끝에 붙은 "- Topic"만 제거. Last.fm에서 아티스트명을 꺼내는 모든 지점(`/recommend`, `/similar-tracks`, `/chart`, `/discover-by-mood`, `/search`)과 `_parse_youtube_music_title()`의 채널명 폴백에 전부 적용.
- `track_image()`는 이미지 검색 전에 먼저 `_clean_artist_name()`으로 정리한 이름을 씀 — "aespa - Topic"으로 iTunes를 검색하면 매칭이 잘 안 되던 문제도 같이 해결됨(깨끗한 이름일수록 검색 매칭률이 올라감).
- `/chart/artists`: 아티스트 10명의 이미지를 `ThreadPoolExecutor`로 동시에 조회 — 순차로 하나씩 iTunes를 호출하면 응답이 느려지는 걸 피함(다른 곳에서 이미 여러 번 쓴 병렬화 패턴 재사용).

**알아둘 한계**: "- Topic" 정규식은 정확히 그 패턴만 잡아내므로, 다른 형태의 오염된 이름(예: 다른 언어의 접미사, 오타)은 못 걸러냄 — 발견되면 그때그때 패턴을 추가해야 함. 인기 아티스트 이미지가 실제 얼굴 사진이 아니라 대표작 커버라는 점은 근본적인 한계(iTunes/Last.fm 둘 다 무료로 쓸 수 있는 아티스트 인물 사진 API가 마땅치 않음) — Spotify Web API는 아티스트 사진을 제공하지만 이 프로젝트는 이미 승인 이슈로 Last.fm으로 전환한 이력이 있어(`.env.example`에 남겨둔 주석 참고) 재검토 대상은 아님.

**면접에서 말할 포인트**:
- "같은 증상을 만드는 여러 원인을 어떻게 하나로 묶어 해결했나" -> 겉보기엔 하나의 버그("- Topic"이 이상하게 뜬다)였지만 실제로는 두 군데(우리 파싱 로직, Last.fm 원본 데이터)에서 독립적으로 발생하고 있었다는 걸 파악하고, 증상이 아니라 "아티스트명을 쓰는 모든 지점"이라는 공통 접점에 정규화 함수 하나를 심어서 두 원인을 한 번에 방어한 설계.
- "이미지 소스 우선순위를 바꾼 근거" -> 두 이미지 소스(Last.fm/iTunes)가 "있음/없음"이 아니라 "있어도 품질이 다르다"는 걸 실사용 중 인지하고, 단순히 폴백 순서가 아니라 어느 소스가 더 신뢰할 만한 데이터를 주는지를 기준으로 우선순위를 재설계한 것.
- "완벽하지 않은 대안을 택할 때의 기준" -> 아티스트 인물 사진을 못 구하는 상황에서 "아예 안 보여준다"와 "정확하진 않지만 관련 있는 이미지(대표작 커버)를 보여준다" 중 후자를 택한 판단 — 사용자 경험상 무엇이 더 나은지를 근거로 한 트레이드오프 설명.

---

## 32. 검색어에 장르·연대 키워드 지원 ("2010년대 kpop" 검색)

**뭘 했나**: "2010년대 kpop"처럼 아티스트명이나 곡 제목이 아니라 장르+연대 키워드로 검색했을 때, 기존엔 `artist.search`/`track.search`가 그 문자열과 이름이 겹치는 게 없어서 결과가 아예 안 나왔음. `/search`에 Last.fm 태그 검색을 보완해서, 검색어에서 장르 키워드(`_extract_genre_tag`)와 연대 키워드(`_extract_decade_tag`)를 뽑아내 `tag.getTopTracks`로 관련 곡을 추가로 찾아 `tagTracks`라는 별도 필드로 내려줌. 프론트는 이걸 "검색된 곡"과 구분되는 "관련 곡 (장르/연대)" 섹션으로 따로 보여줌(무드 탐색과 같은 `CompactTrackGrid` 줄 스타일 재사용).

**왜 이렇게 했나**: 장르/연대 조합 검색을 얼마나 정확하게 처리할지 먼저 확인함 — Last.fm의 `tag.getTopTracks`는 태그 하나만 받는 API라 "장르 AND 연대"처럼 엄밀한 교집합은 못 만드는데(둘 다 만족하는지 확인하려면 후보마다 태그를 또 조회해야 해서 비용이 커짐), "덜 정확해도 되니 장르와 연대 둘 다 시도해달라"는 방향을 확인하고 정확도보다 커버리지를 택함 — 두 태그 각각의 결과를 합쳐서 보여주는 느슨한(OR에 가까운) 매칭으로 구현. 이 결과는 "정확히 이 곡을 찾겠다"는 검색의 원래 목적과는 성격이 달라서(사용자가 특정 곡 제목을 검색한 게 아니라 "이런 분위기의 곡을 찾고 싶다"는 탐색에 가까움), 기존 검색 결과(`searchTracks`, 가사 필터 미적용)와는 다르게 무드 탐색과 같은 "둘러보기" 목록으로 취급해서 가사 있는 곡만 걸러 보여줌 — [[학습노트 27번]]에서 정한 "검색은 예외, 둘러보기는 필터링" 원칙을 그대로 적용한 것.

**어떻게 동작하나**:
- `GENRE_KEYWORDS`: 무드 칩(`MOODS`)과 같은 아이디어로, 한국어/영어 장르 키워드(케이팝/kpop, 힙합/hip hop, 발라드 등)를 Last.fm 태그로 매핑한 딕셔너리. 검색어에 여러 키워드가 겹칠 수 있어서(예: "k-pop"에 "pop"도 부분 매치) 긴 키워드부터 먼저 검사.
- `DECADE_PATTERN`: `\d{2,4}\s*(년대|s)` 정규식으로 "2010년대"/"2010s"/"90년대" 같은 표현에서 숫자를 뽑고, 10년 단위 태그(예: "2010s")로 정규화. 두 자리 연도는 50 이상이면 19XX, 미만이면 20XX로 보는 관례를 씀(예: "90년대" → 1990s, "10년대" → 2010s).
- 연대 태그를 장르 태그보다 먼저 조회함(더 구체적인 조건이라 우선순위를 높임), 각각 30곡씩 후보로 받아 중복 제거 후 합치고, [[학습노트 27번]]의 `_filter_tracks_with_lyrics()`로 가사 있는 곡만 최종 15곡으로 자름.

**알아둘 한계**: "장르 AND 연대"가 아니라 "장르 OR 연대"에 가까운 느슨한 매칭이라, 결과에 두 조건을 동시에 만족 안 하는 곡이 섞여 있을 수 있음(예: "2010년대 kpop" 검색에 2010년대가 아닌 kpop 곡이나, kpop이 아닌 2010년대 곡이 섞일 수 있음). 장르 키워드 사전이 고정 목록이라 사전에 없는 장르(예: "시티팝", "국내 락")는 그냥 무시되고 기존 아티스트/곡 검색만 동작함. Last.fm의 연대 태그 자체도 크라우드소싱이라 커버리지가 일정하지 않음(최신 곡일수록 아직 "2020s" 태그가 안 붙어 있는 경우도 있음).

**면접에서 말할 포인트**:
- "정확도와 커버리지 중 뭘 우선했나" -> 태그 하나만 받는 API 제약 안에서 엄밀한 AND 매칭을 구현하려면 후보마다 추가 조회가 필요해 비용이 커지는데, 이 기능의 목적(정확한 검색이 아니라 느슨한 탐색)을 고려하면 그 비용을 들일 만큼 가치가 크지 않다고 판단해서 OR에 가까운 매칭으로 스코프를 의도적으로 좁힌 것.
- "기존에 만든 다른 기능의 설계를 어떻게 재사용했나" -> 무드 칩의 한국어→태그 매핑 아이디어, [[학습노트 27번]]의 검색/둘러보기 구분 원칙, 무드 탐색에서 쓴 `CompactTrackGrid` UI를 그대로 가져와서, 새 기능이지만 새로운 패턴을 또 만들지 않고 이미 검증된 패턴들을 조합해서 구현한 사례.

**(추가 수정) §53 AI 폴백이 이 기능을 가려버리던 버그**: "2010년대 kpop"으로 검색하면 "여러 곡 모음"을 기대했는데 실제론 "검색된 곡"에 AI가 추측한 곡 하나("싸이 - 강남스타일")만 뜨고, 정작 이 기능이 만드는 `tagTracks`("관련 곡 (장르/연대)")는 그 밑에 있어서 눈에 안 띈다는 지적을 받음. 원인: `genre_tag`/`decade_tag` 추출이 함수 맨 아래(이 태그 검색 블록)에서만 이뤄져서, 그보다 먼저 실행되는 §53 AI 폴백은 이 검색어가 "장르/연대 탐색"이라는 걸 모른 채 무조건 "특정 곡 하나"를 추측해버림 — 장르/연대 검색은 태생적으로 "한 곡을 정확히 찾겠다"가 아니라 "여러 곡을 둘러보겠다"는 의도인데, 여러 곡 중 아무거나 하나로 좁혀버리는 AI 단일곡 추측은 애초에 잘못된 형태의 응답이었음. `genre_tag`/`decade_tag` 추출을 함수 앞부분(신뢰도 판정 직후)으로 옮기고, `is_genre_or_decade_query`가 참이면 §53 AI 폴백과 §59 유튜브 단일곡 폴백을 둘 다 건너뛰도록 조건 추가 — 이러면 "검색된 곡"이 그냥 비고 "관련 곡 (장르/연대)"만 채워져서 원래 의도한 "모음" 형태가 그대로 드러남. 부수 효과로 이런 검색어에서 불필요한 Claude Haiku 호출도 없어져서 비용도 아낌. 캐시 키 v18 버전업, `[search-tag-tracks]` 디버그 로그 추가 — ast.parse만 확인, 라이브 확인 대기.

**면접에서 말할 포인트 (추가)**:
- "여러 폴백 메커니즘이 서로 간섭할 수 있다는 걸 놓치지 않는 설계 감각" -> §53(AI 단일곡 추측)과 §32(태그 기반 모음)는 각각 독립적으로는 잘 만들었지만, "장르/연대 검색은 원래 여러 곡을 원하는 요청"이라는 의미를 AI 폴백 쪽이 몰랐던 게 문제였음 — 기능 하나하나의 정확성뿐 아니라 여러 폴백이 같은 요청에 대해 서로 다른 가정을 세우고 충돌하지 않는지까지 봐야 한다는 교훈.

**(추가 수정) "OR 매칭"이 사실상 "먼저 채운 태그의 독식"이었던 버그**: 위 수정으로 "관련 곡 (장르/연대)" 섹션이 정상적으로 보이게 됐는데, 막상 "2010년대 kpop"으로 확인해보니 6곡 전부 서구 팝(Harry Styles, Billie Eilish, Doja Cat 등)이고 케이팝이 한 곡도 없다는 지적을 받음. 원인: `decade_tag`("2010s") 후보를 먼저 30개 다 모으고 그다음에야 `genre_tag`("k-pop") 후보를 이어붙이는 순서였는데, Last.fm 이용자층이 서구권에 훨씬 많아서 "2010s" 태그 하나만으로도 가사가 확인되는 곡 15개가 이미 다 채워져버려 `genre_tag` 후보는 최종 15개 안에 들어갈 기회 자체가 없었음 — "장르 OR 연대"라는 설계 의도와 달리 실제로는 "무엇이든 먼저 채운 태그가 결과를 독식"하는 구조였던 것. 두 태그의 후보를 각각 따로 모은 뒤 하나씩 번갈아 섞는(라운드로빈) 방식으로 변경 — 두 태그가 다 있으면 최종 결과에 양쪽이 골고루 섞이도록 함. 캐시 키 v18 → v19.

**알아둘 한계 (추가)**: 라운드로빈으로 "둘 다 있으면 섞인다"는 보장은 생겼지만, 여전히 "장르 AND 연대"(2010년대"이면서" 케이팝)를 엄밀히 보장하진 않음(각 태그 후보 자체가 다른 조건은 무시하고 뽑힌 것이라, 섞여 나온 케이팝 곡이 2010년대가 아닐 수도, 2010년대 곡이 케이팝이 아닐 수도 있음) — 여전히 "장르 OR 연대에 가까운 느슨한 매칭"이라는 §32의 원래 한계 자체는 그대로 남아있고, 이번 수정은 그 안에서 "한쪽이 완전히 안 보이는" 극단적인 편향만 완화한 것.

---

## 33. 가사 하이라이트/클릭 이동이 일부 곡에서 안 되던 버그 수정

**뭘 했나**: [[학습노트 26번]]에서 만든 "재생 위치에 맞춰 가사 줄 하이라이트 + 클릭해서 그 시점으로 이동" 기능이 아이유 "라일락"에서는 아예 동작 안 하고 일반 텍스트 가사만 뜨는 걸 발견 — `_first_lyrics_match()`가 LRCLIB 검색 결과 후보들 중 동기화 가사(`syncedLyrics`) 유무를 안 따지고 "인스트루멘탈이 아니고 가사가 뭐든 있으면 그냥 첫 번째 후보"를 쓰던 로직을, 동기화 가사가 있는 후보를 최우선으로 고르도록 수정.

**왜 이렇게 했나**: 프론트(`MiniPlayer.tsx`)의 하이라이트/클릭 이동은 `lyrics.syncedLyrics`가 있을 때만 렌더링되고, 없으면 `plainLyrics`만 보여주는 폴백으로 빠지는 구조([[학습노트 26번]] 그대로, 이번에 변경 안 함). 그런데 LRCLIB은 같은 곡이라도 여러 버전(다른 앨범 발매/제출자별 등록본)이 검색될 수 있고, 그중 어떤 버전은 동기화 가사가 있고 어떤 버전은 없을 수 있음. 기존 `_first_lyrics_match()`는 "리스트 순서상 먼저 나온" 후보를 그대로 썼기 때문에, 우연히 일반 가사만 있는 버전이 먼저 걸리면 뒤에 동기화 가사가 있는 버전이 있어도 절대 안 씀 — 즉 "이 곡은 동기화 가사가 아예 없다"가 아니라 "있는데도 못 찾고 있었을 가능성"이 있는 선택 로직 버그였음.

**어떻게 동작하나**:
```python
def _first_lyrics_match(candidates):
    valid = [c for c in candidates if not c.get("instrumental") and (c.get("syncedLyrics") or c.get("plainLyrics"))]
    synced_match = next((c for c in valid if c.get("syncedLyrics")), None)
    return synced_match or (valid[0] if valid else None)
```
인스트루멘탈이 아니고 가사가 있는 후보들(`valid`)을 먼저 추리고, 그중 `syncedLyrics`가 있는 첫 후보를 우선 반환. 없으면 기존처럼 `valid`의 첫 번째(일반 가사만 있는 후보)로 폴백 — "가사가 아예 없는 것보다는 일반 텍스트라도 보여주는 게 낫다"는 [[학습노트 26번]]의 원래 실용주의는 그대로 유지.

**알아둘 한계**: `_lookup_lyrics()`는 가사를 30일 캐싱하므로([[학습노트 26번]]), 이미 "라일락"처럼 예전에 조회돼서 캐시에 박제된 곡은 이 수정 이후에도 캐시가 만료되기 전까진 예전(일반 가사만 있는) 결과가 그대로 나감 — 백엔드 재시작만으로는 캐시가 안 지워짐(Redis는 별도 프로세스). 즉시 반영하려면 Redis에서 `lyrics:아이유:라일락` 같은 해당 곡 캐시 키를 직접 삭제해야 함(`redis-cli DEL "lyrics:iu:라일락"` 형태, 캐시 키는 `lyrics:{artist.lower()}:{track.lower()}`). 이 수정으로도 LRCLIB 자체에 동기화 가사가 진짜로 하나도 없는 곡은 여전히 일반 텍스트 폴백만 뜸 — "선택 로직 버그"와 "데이터 자체가 없음"은 다른 문제라 캐시를 지우고 다시 확인해봐야 어느 쪽인지 확정됨. 샌드박스에서 LRCLIB 실제 응답을 직접 확인할 방법이 없어서(외부 도메인 네트워크 차단) 코드 리뷰만으로 판단한 수정이라, 라이브 재확인이 특히 중요함.

**면접에서 말할 포인트**:
- "여러 후보 중 하나를 고르는 로직에서 어떤 실수를 했었나" -> "가사가 있냐 없냐"만 boolean으로 보고 첫 매치를 쓰는 건 흔한 함정인데, 실제로는 "가사"라는 게 동기화/비동기화라는 품질 차이가 있는 데이터라서, 단순 존재 여부가 아니라 품질까지 고려한 우선순위 비교가 필요했던 사례.
- "재현이 안 되는 버그를 어떻게 진단했나" -> 프론트 코드(렌더링 조건)부터 거꾸로 추적해서 "동기화 가사가 비어있다"는 사실까지는 확정했지만, 그게 "데이터가 없어서"인지 "선택을 잘못해서"인지는 실제 LRCLIB 응답을 못 보는 상황에서 코드 구조(정렬 없이 순서대로 첫 매치)만 보고 개연성 있는 가설로 수정한 것 — 확정적 진단이 불가능할 때 코드 리뷰 기반으로 합리적 수정을 하고, 라이브 검증으로 확인/반증하는 접근.

---

## 34. 재생목록 상세 화면(멜론 스타일 체크박스 + 하단 일괄 액션바)

**뭘 했나**: 멜론 앱 재생목록 화면 스크린샷(전체 선택 체크박스 + 곡마다 체크박스 + 하단 선택반복/믹스업/다운/담기/삭제 액션바)을 레퍼런스로 보여주며 "재생목록을 이런 식으로 따로 만들고, 여러 곡을 한 번에 골라서 처리하고 싶다"는 요청. 사이드바 안에서 재생목록 이름을 눌러 좁은 폭으로 펼쳐 보던 아코디언 방식을 걷어내고, 이름을 누르면 [[학습노트 25번]]의 확장 재생 화면과 같은 패턴(`createPortal` 전체화면 오버레이)으로 새 컴포넌트 `PlaylistDetailOverlay`가 뜨도록 바꿈. 체크박스로 곡을 고르면 하단 액션바에서 이어재생/반복재생/담기/삭제 네 가지를 일괄 처리할 수 있음.

**왜 이렇게 했나**: 멜론의 다섯 액션(선택반복/믹스업/다운/담기/삭제)을 그대로 다 베끼지 않고 사용자에게 먼저 물어봄 — "다운"은 Unbeat가 자체 오디오 파일을 갖고 있지 않고 유튜브로 스트리밍만 하는 구조라 애초에 다운로드할 대상이 없고, "믹스업"은 멜론의 AI 기반 자동 믹스 서비스인데 우리에겐 그런 서비스가 없어서 그대로 구현하면 가짜 버튼이 됨. "새 기능은 항상 실제 필요와 맞아야 한다"는 이 프로젝트의 원칙대로, 겉모습만 베끼는 대신 실제로 우리 서비스에서 의미가 있는 나머지 기능(이어재생/반복재생/담기/삭제)만 골라 구현하기로 사용자가 직접 결정. 전체화면 오버레이로 만든 이유도 같은 논리 연장선 — 사이드바 폭 안에 체크박스+액션바까지 우겨넣으면 곡이 조금만 많아져도 답답해질 게 뻔해서, 이미 검증된 확장 재생 화면 패턴을 재사용함(새로운 레이아웃 패턴을 또 만들지 않음).

**어떻게 동작하나**:
- `PlaylistDetailOverlay.tsx`: `useState<Set<number>>`로 체크된 트랙 id를 관리([[학습노트 24번]]의 `StaleTracksModal`과 같은 체크박스 패턴), 전체 선택/해제 토글, 곡 이름을 직접 누르면(체크박스 영역과 별개로) 그 한 곡만 바로 재생.
- **이어재생/반복재생**: 새 백엔드 엔드포인트 없이 프론트 상태만으로 구현. 평소 "다음 트랙" 큐(`queue` state)는 곡이 바뀔 때마다 `/similar-tracks`로 다시 채워지는 라디오 방식인데([[학습노트 23번]]), 재생목록에서 고른 고정된 목록을 그 대신 순서대로 틀어야 해서 `isManualQueueRef`(지금 수동 큐를 소비 중인지)와 `repeatListRef`(반복재생이면 원본 목록을 들고 있다가 다 소진되면 처음으로)라는 두 개의 ref를 추가. `handlePlay`에 `manualNext?: Track[]` 옵션을 추가해서, 값이 있으면 `/similar-tracks` 재조회 없이 그 목록을 그대로 큐에 세팅함. `handleTrackEnded`가 이 두 ref를 보고: 수동 큐가 남아있으면 다음 곡 재생 → 수동 큐가 비었는데 반복재생이면 원본 목록 처음으로 → 반복재생도 아니면 평소 라디오 방식으로 자연스럽게 복귀, 세 갈래로 분기함. `MiniPlayer.tsx`는 전혀 손대지 않음 — "다음 트랙" 패널이 원래 `queue` state를 그대로 보여주는 구조라, 큐 내용만 바꿔치기하면 기존 UI가 그대로 재사용됨.
- **담기**: 기존 트랙 카드 "＋" 버튼이 쓰던 `AddToPlaylistModal`을 재사용하되, 단일 곡(`track: PendingTrack`)만 받던 props를 항상 배열(`tracks: PendingTrack[]`)로 받도록 일반화. 기존 호출부(트랙 카드, 즐겨찾기 목록)는 그냥 `[track]`으로 감싸서 넘기면 되니 변경이 거의 없었고, 재생목록 상세 화면만 체크된 여러 곡을 그대로 배열로 넘김. 모달 안에서 재생목록을 고르면 `Promise.all`로 여러 곡을 한 번에 담음. 이 모달을 확장 재생 화면(z-40) 위에서도 띄워야 해서 z-index를 40 → 50으로 올림(안 그러면 DOM 순서에 따라 뒤에 가려 보일 수 있었음).
- **삭제**: 기존에 재생목록 안 곡 하나씩 지우던 `handleRemoveTrackFromPlaylist(playlistId, trackId)`를 여러 id 배열을 받는 `handleRemoveTracksFromPlaylist(playlistId, trackIds)`로 합침 — 이제 개별 삭제(행 끝 ✕ 버튼)도 액션바 일괄 삭제도 같은 함수를 쓰고, 내부적으로 `Promise.all`로 기존 `DELETE /playlists/{id}/tracks/{trackId}` 엔드포인트를 여러 번 호출함.
- 이어재생/반복재생/담기/삭제 네 가지 모두 **Kotlin(`playlist-service`) 쪽 변경이 전혀 없음** — 기존 REST 엔드포인트(추가/삭제/조회)를 프론트에서 여러 번 호출하는 방식으로 다 처리했기 때문. "Kotlin 코드를 고치면 IntelliJ를 수동으로 재시작해야 한다"는 이 프로젝트의 익숙한 마찰을, 새 벌크 API를 안 만들고 기존 API를 반복 호출하는 선택으로 그냥 피해간 셈.

**알아둘 한계**: 곡을 몇 십 개씩 한 번에 선택해서 삭제/담기를 누르면 `Promise.all`로 그만큼의 HTTP 요청이 한꺼번에 나감 — 재생목록 하나에 그 정도로 곡이 쌓일 상황은 당장 없어서 성능 문제로 이어지진 않겠지만, 진짜 벌크 API(`DELETE /playlists/{id}/tracks?ids=1,2,3`처럼)가 아니라 여러 번 호출을 흉내 낸 것이라는 점은 트레이드오프로 남겨둠. "이어재생" 중 사용자가 "다음 트랙" 패널에서 큐에 있는 특정 곡을 직접 클릭하면(`onSelectQueueTrack`), 그 순간 `manualNext` 없이 `handlePlay`가 호출되게 그대로 둬서 수동 큐 모드가 해제되고 평소 라디오 방식으로 넘어가도록 했음 — "재생목록 재생 중에 큐에서 딴 곡을 골랐다"는 건 사실상 새로 재생을 시작한 것과 같다고 보고 단순하게 처리한 것.

**면접에서 말할 포인트**:
- "레퍼런스 UI를 그대로 베끼지 않고 어떤 기준으로 취사선택했나" -> 우리 서비스가 실제로 가진 능력(REST API로 재생목록 곡 관리, 유튜브 기반 재생/큐)과 안 가진 능력(오디오 파일 다운로드, AI 믹스 생성)을 먼저 구분하고, 후자에 해당하는 버튼은 겉모습만 만들어봤자 클릭해도 아무 일도 안 일어나는 가짜 기능이 되니 아예 빼자고 사용자에게 먼저 확인받은 사례 — "그대로 흉내 내기"와 "우리 서비스에 맞게 재해석하기"를 구분한 것.
- "새 기능을 위해 새 백엔드 API를 안 만든 이유" -> 이어재생/반복재생/담기/삭제 네 기능 모두 "여러 개를 한 번에" 처리해야 하지만, 각각을 개별 호출로 반복해도 사용자 체감상 차이가 없을 만큼 작은 스케일(재생목록 하나에 수십 곡 수준)이라, 벌크 전용 엔드포인트를 새로 설계·구현·테스트하는 비용 대신 프론트에서 `Promise.all`로 기존 엔드포인트를 여러 번 호출하는 실용적 선택을 한 것 — Kotlin 서비스 변경 없이 배포/재시작 마찰도 피함.

**추가 개선(발견 즉시 수정)**: 라이브 확인 도중 사용자가 재생목록 이름("팝 2")을 눌러도 되는 건지 못 알아보고 두 번 연속 헷갈리는 걸 발견 — 사이드바의 즐겨찾기 아티스트 목록(★ 태그)은 테두리+배경이 있는 알약 버튼이라 한눈에 "누르는 것"으로 보이는데, 재생목록 이름은 그냥 평범한 텍스트 스타일이라 클릭 가능한 요소라는 시각적 힌트가 전혀 없었던 게 원인. 버튼에 호버 시 배경(`hover:bg-white/5`)과 오른쪽 끝 "›" 화살표를 추가해서 "여기 눌리는 곳"이라는 걸 명확히 함 — 기능은 처음부터 멀쩡히 동작했고, 순전히 발견 가능성(discoverability) 문제였던 사례.

**추가 개선 2(삭제 확인 단계)**: 하단 액션바 "삭제"를 누르면 확인 없이 곧바로 지워지는 게 불안하다는 피드백 — "삭제할까요?"처럼 한 번 물어봤으면 좋겠다는 요청. 특히 "전체 선택" 상태에서 실수로 "삭제"를 누르면 재생목록에 담긴 곡 전부가 확인 없이 한 번에 사라질 수 있어서, 되돌릴 수 없는 파괴적 동작치고는 확인 단계가 없는 게 실제로 위험하다고 판단. `isConfirmingDelete` 상태를 추가해서 "삭제" 버튼을 누르면 바로 지우지 않고 "선택한 N곡을 삭제할까요?"라는 작은 확인 모달(`AddToPlaylistModal`과 같은 z-50 팝업 스타일)을 먼저 띄우고, 그 안의 "삭제" 버튼을 한 번 더 눌러야 실제 `onDeleteSelected`가 호출되도록 함. 개별 곡 옆 ✕ 버튼(한 곡만 삭제)은 그대로 즉시 삭제 유지 — 실수해도 되돌리기 쉬운(다시 담기 한 번이면 복구되는) 작은 단위 동작이라, 여러 곡을 한 번에 잃을 수 있는 액션바 "삭제"만큼 위험하지 않다고 구분해서 판단.

**추가 개선 3(버그 수정: 곡을 눌러도 재생이 안 되는 것처럼 보임)**: 재생목록 상세 화면에서 곡 이름을 눌러도 아무 반응이 없다는 피드백 — 코드상으로는 `onPlayTrack`이 `handlePlay`를 정상 호출해서 실제로는 재생이 시작되고 있었지만, 이 상세 화면 자체가 `fixed inset-0 z-40`짜리 불투명한 전체화면이라 하단 미니 플레이어(`z-30`)를 통째로 가리고 있어서 사용자 눈에는 아무 변화도 안 보였던 것 — "재생이 안 된다"가 아니라 "재생됐다는 걸 보여줄 화면이 없다"는 문제였음. `handlePlaySelectedPlaylistTracks`(이어재생/반복재생)에서는 이미 재생 시작과 동시에 `setOpenPlaylistId(null)`로 화면을 닫아 미니 플레이어를 드러내고 있었는데, 곡 이름 직접 클릭 경로(`onPlayTrack`)에는 그 처리가 빠져 있었던 게 원인 — 같은 화면 안에서 "재생을 시작하는" 모든 경로(직접 클릭/이어재생/반복재생)가 동일하게 화면을 닫도록 통일해서 해결.

---

## 35. "관련 항목" 탭 제거 + "다음 트랙" 전체를 재생목록에 한 번에 저장

**뭘 했나**: [[학습노트 25번]]에서 확장 재생 화면에 추가했던 "다음 트랙"/"관련 항목"/"가사" 3개 탭 중, "관련 항목"(지금 아티스트의 유사 아티스트 인기곡, `/recommend` 재사용)을 완전히 제거하고 탭을 "다음 트랙"/"가사" 2개로 줄임. 대신 유튜브 뮤직 확장 플레이어를 다시 참고해서, "다음 트랙" 탭 목록 위에 "＋ 재생목록에 저장" 버튼을 추가 — 지금 큐에 떠 있는 곡 전체를 한 번에 재생목록으로 옮길 수 있음.

**왜 이렇게 했나**: "관련 항목"(아티스트 단위 유사곡)과 "다음 트랙"(트랙 단위 유사곡, [[학습노트 23번]])이 실사용해보니 결국 둘 다 "지금 곡과 비슷한 곡 목록"이라는 같은 역할을 하는 걸 사용자가 직접 느끼고 "너무 비슷하다"는 피드백을 줌. 처음 설계할 때는 "아티스트 단위 추천"과 "트랙 단위 추천"이 다른 정보라고 생각해서 탭을 분리했는데, 실제 화면에서 두 탭을 나란히 써보니 사용자 입장에선 "둘 다 그냥 다음에 들을 만한 곡 리스트"로 보여서 탭 하나가 군더더기였던 셈 — 기능을 미리 다 설계하기보다 실사용 피드백으로 중복을 발견하고 쳐내는 것도 이 프로젝트에서 반복되는 패턴([[학습노트 20번]]의 무드별 그룹핑 되돌리기와 같은 종류). 빈 탭이 하나 사라지는 대신, 유튜브 뮤직이 "다음 트랙" 옆에 실제로 두는 "저장" 버튼을 그 자리에 채워 넣어서 화면이 허전해지지 않게 함.

**어떻게 동작하나**:
- `MiniPlayer.tsx`: `ExpandedTab` 타입에서 `"related"`를 빼고 `"queue" | "lyrics"`로 축소, `relatedTracks`/`isLoadingRelated`/`lastRelatedArtistRef` 상태와 그걸 채우던 `recommendByArtist` 호출 `useEffect`를 통째로 삭제.
- 새 prop `onSaveQueueToPlaylist?: (tracks: Track[]) => void`를 추가하고, "다음 트랙" 탭 콘텐츠(큐 목록) 맨 위에 버튼을 하나 둠 — 누르면 지금 `queue` state를 그대로 이 콜백에 넘김.
- `page.tsx`에서는 새 API나 새 모달을 만들지 않고, [[학습노트 34번]]에서 이미 "여러 곡을 배열로 받도록" 일반화해둔 `AddToPlaylistModal`을 그대로 재사용함. 재생목록 상세 화면의 "담기"(`PlaylistTrack[]`)와 다음 트랙의 "저장"(`Track[]`)은 원본 타입이 서로 다른데, `handleOpenBulkAddToPlaylist`를 `{ name, artist, image }` 공통 형태를 받는 함수로 다듬어서 두 호출부가 각자 자기 타입을 이 형태로 변환만 해서 넘기면 되게 함 — "같은 일(여러 곡을 재생목록에 담기)은 입구가 여러 개여도 하나의 공통 함수로 모은다"는 설계.

**알아둘 한계**: 저장 버튼은 큐 목록과 같은 분기(`queue.length === 0 ? 안내문구 : (버튼 + 목록)`) 안에 있어서 큐가 비어있을 땐 버튼도 같이 안 뜨므로 "0곡인데 저장이 눌리는" 문제는 없음. 다만 "다음 트랙" 큐는 곡이 바뀔 때마다 계속 갱신되는 라디오 목록이라([[학습노트 23번]]), 저장한 뒤 곡이 넘어가면 그 목록도 다시 바뀌어서 "방금 저장한 목록"을 나중에 다시 보거나 이어서 관리할 방법은 없음 — 그 순간의 스냅샷을 재생목록에 복사해두는 일회성 액션으로만 동작함.

**면접에서 말할 포인트**:
- "미리 설계했던 기능을 나중에 없앤 경험" -> 아티스트 단위/트랙 단위 추천을 서로 다른 정보라고 가정하고 탭을 분리했지만, 실제 사용자 피드백은 "둘 다 똑같아 보인다"였음 — 데이터 소스가 다르다고 사용자 경험까지 다르게 느껴지는 건 아니라는 걸 확인한 사례. 기능을 추가하는 것만큼 "겹치는 걸 발견하면 빼는" 판단도 중요하다는 근거로 쓸 수 있음.
- "여러 곳에서 같은 모달/로직을 재사용하려는데 입력 타입이 다를 때 어떻게 처리했나" -> 각 호출부가 자기 도메인 타입(`PlaylistTrack`, `Track`)을 그대로 넘기게 하지 않고, 공통 함수의 입력 형태를 가장 작은 공통분모(`{name, artist, image}`)로 좁혀서 변환 책임을 호출부 쪽에 두는 방식 — 공통 함수 내부가 여러 타입을 분기 처리하지 않아도 되게 함.

---

## 36. 버그 수정: 이미지 우선순위를 바꿨는데(§31) 캐시가 안 바뀌어서 카드가 전부 같은 사진으로 뜨던 문제

**뭘 했나**: 홈 화면 "지금 인기 있는 곡 (글로벌)" 섹션에서 서로 다른 곡 4개(Ariana Grande의 Petal/hate that i made.../Kiss Me/Stay)의 카드 이미지가 전부 똑같은 사진(아티스트 프로필 사진)으로 뜨는 걸 발견 — `/chart`뿐 아니라 같은 이미지 로직을 쓰는 `/chart/artists`·`/search`·`/recommend`·`/similar-tracks`·`/discover-by-mood` 캐시 키에 전부 버전 접미사를 붙여서 해결.

**왜 이렇게 했나**: 원인을 추적해보니 이미지를 만드는 함수(`resolve_track_image`) 자체는 [[학습노트 31번]]에서 이미 "iTunes 우선, Last.fm 폴백"으로 고쳐져 있었음 — 코드는 멀쩡했다는 뜻. 문제는 `/chart`(그리고 같은 함수를 쓰는 다른 엔드포인트들)가 응답을 통째로 Redis에 캐싱하는데, 이 이미지 로직을 바꾼 시점에 그 캐시 키를 그대로 뒀다는 것. 즉 §31 수정 전에 이미 저장돼 있던 캐시(트랙 이미지가 전부 옛날 방식 — Last.fm 우선 — 으로 계산된 상태)가 기본 TTL(1시간) 동안 그대로 응답으로 나가고 있었던 것. 이건 [[학습노트 29번]]에서 이미 한 번 겪었던 "응답 스키마가 바뀌면 캐시 키도 버전을 올려야 한다"는 교훈과 정확히 같은 종류의 실수인데, 그때는 "스키마(필드 구조)가 바뀌는 경우"에만 적용된다고 좁게 생각해서, "같은 스키마인데 필드 값을 계산하는 로직만 바뀌는 경우"(이번 케이스)엔 그 원칙을 적용하는 걸 깜빡함 — 캐시 무효화가 필요한 조건을 "구조 변경"이 아니라 "저장된 값이 새 코드로 다시 계산하면 달라지는 모든 경우"로 넓게 잡았어야 했다는 걸 이번에 다시 확인함.

**어떻게 동작하나**: `track_image()`/`resolve_track_image()`의 결과를 캐싱하는 6개 엔드포인트(`/chart`, `/chart/artists`, `/search`, `/recommend`, `/similar-tracks`, `/discover-by-mood`) 캐시 키에 전부 버전을 올림 — `chart:top-tracks` → `chart:top-tracks:v2`, `chart:top-artists` → `chart:top-artists:v2`, `search:{q}` → `search:v2:{q}`, `recommend:{artist}` → `recommend:v2:{artist}`, `similar-tracks:{artist}:{track}` → `similar-tracks:v2:{artist}:{track}`, `discover-by-mood:v2:{tag}`(스키마 변경용 버전)는 이미지 변경분까지 반영해 `discover-by-mood:v3:{tag}`로 한 번 더 올림. 버전을 올리면 새 키에는 캐시가 없으니 자연히 새 코드로 다시 계산되고, 옛 키의 캐시는 그냥 버려짐(TTL 지나면 Redis가 알아서 정리하므로 수동 삭제 불필요).

**알아둘 한계**: 캐시 버전 올리기는 "이 요청은 이제부터 새로 계산된다"만 보장하고, 이미 나가 있던 옛 응답(예: 사용자가 그 순간 보고 있던 화면)까지 소급해서 고치진 못함 — 새로고침을 해야 새 캐시로 다시 받아옴. 또한 이번에 6개를 한 번에 올렸지만, 앞으로 `track_image()`/`resolve_track_image()`/`fetch_itunes_artwork()` 등 이미지 관련 공용 함수를 또 고치면 똑같은 문제가 재발할 수 있음 — 근본적으로는 "이 함수를 쓰는 캐시 키 목록"을 한 곳에 정리해두거나, 이미지 로직 버전을 별도 상수(`IMAGE_LOGIC_VERSION`)로 빼서 관련 캐시 키에 공통으로 꽂는 방식이 더 안전했을 것(이번엔 시간 관계상 개별 수정으로 처리).

**면접에서 말할 포인트**:
- "캐시 무효화를 언제 해야 하는지 어떻게 판단하나" -> "API 응답의 필드 구조가 바뀔 때"만이 아니라 "같은 필드에 들어가는 값을 계산하는 로직이 바뀔 때"도 캐시 무효화 대상이라는 걸, 좁게 적용했다가 직접 버그를 내고 나서 넓혀 잡은 경험 — 캐시 무효화 조건을 "이 코드를 다시 실행하면 다른 결과가 나오는가"로 일반화해서 판단해야 한다는 원칙을 얻음.
- "같은 종류의 버그가 여러 번 반복됐는데 왜 근본적으로 안 고쳤나" -> 시간 대비 효과를 고려해서, 지금 당장은 영향받는 엔드포인트를 찾아 개별적으로 버전을 올리는 실용적 수정을 택했고, 재발 방지를 위한 구조적 해법(이미지 로직 버전 상수화)은 "알아둘 한계"로 명시적으로 남겨서 다음에 또 이미지 로직을 바꿀 때 놓치지 않게 기록해둠 — 모든 걸 한 번에 완벽하게 고치기보다, 지금 문제를 해결하면서 왜 구조적 해법을 미뤘는지도 같이 남기는 습관.

---

## 37. 홈 화면 "다시 듣기"/"빠른 선곡" 섹션 추가 — "AI 추천을 넣을 순 없나?"에 대한 절충안

**뭘 했나**: "지금 인기 있는 곡(글로벌)"이 새로고침해도 계속 똑같다는 피드백 → "그럼 유튜브 뮤직처럼 새로고침할 때마다 좀 달라졌으면 좋겠다" → "추천 AI를 넣을 순 없냐"로 이어진 대화 끝에, 차트는 실제 순위이므로 그대로 두고, 그 아래에 "다시 듣기"(즐겨찾기한 곡을 홈 화면 메인에도 노출)와 "빠른 선곡"(즐겨찾기 태그 기반 후보를 새로고침마다 무작위로 섞어서 보여줌) 두 섹션을 새로 추가.

**왜 이렇게 했나**: "추천 AI"는 이 프로젝트 로드맵 맨 위 목표에 "`AI Agent/LLM 자체 개발(RAG, MCP 등)은 이번 로드맵에서 제외, 추후 확장 옵션으로만 남겨둠`"이라고 프로젝트 시작 시점부터 명시적으로 범위 밖에 뒀던 항목(`ROADMAP.md` 최상단 "목표" 섹션 참고). 그렇다고 "그럼 안 됨"으로 끝내지 않고, 이미 [[학습노트 9번]](트랙 즐겨찾기 + Last.fm 태그 자동 수집)에서 쌓아온 "취향 신호"(즐겨찾기한 곡의 Last.fm 태그)와, ROADMAP에 이미 계획돼 있던 "Spring Batch로 즐겨찾기 태그를 취향 프로필로 집계해서 추천에 쓰기" 항목을 활용하면, 진짜 ML/LLM 없이도 "그래도 내 취향에 뿌리를 둔" 추천 비슷한 걸 만들 수 있다고 판단. 다만 그 배치 작업은 아직 데이터가 며칠 쌓인 뒤 착수할 계획이라 지금 당장 손댈 단계가 아니어서, 배치 대신 "즐겨찾기 태그 중 가장 많이 나온 태그 하나로 무드 탐색을 돌린다"는 훨씬 가벼운 온디맨드 버전으로 축소해서 지금 바로 쓸 수 있게 함 — 나중에 배치 기반 정교한 버전이 생기면 이 자리를 교체하면 됨.

**어떻게 동작하나**:
- `favoriteTracks`(이미 홈 화면에서 불러오고 있던 즐겨찾기 목록, `FavoriteTrack.tags` 보유)에서 태그별 등장 횟수를 세어 가장 많이 나온 태그 하나를 고름.
- 그 태그로 기존 `discoverByMood()`(무드 탐색과 완전히 같은 엔드포인트, [[학습노트 10번]])를 호출해서 국내/해외 후보를 받고, 두 목록을 합친 뒤 Fisher-Yates로 무작위로 섞어 9곡만 "빠른 선곡"에 표시.
- 즐겨찾기가 아직 없는 신규 사용자는 태그 자체가 없으니, 글로벌 차트(`chartTracks`)를 대신 섞어서 보여줌 — 완전히 빈 섹션보다는 낫지만, 이 경우엔 "취향 기반"이 아니라 "그냥 무작위 노출"이라는 점을 명확히 인지하고 있음(사용자에게 그렇게 티내진 않지만, 다음에 이 로직을 볼 사람은 알아야 함).
- "다시 듣기"는 완전히 새 로직 없이, 사이드바에서 쓰던 `favoriteTracks`를 `CompactTrackGrid`가 요구하는 `Track` 형태로 변환만 해서 메인 화면에도 그대로 노출 — 사이드바 버전(접이식, 전체 목록 관리용)과 메인 화면 버전(훑어보기용)의 쓰임새가 다르다고 보고 중복 노출을 의도적으로 허용.
- 새 백엔드 엔드포인트는 하나도 안 만듦 — 기존 `discoverByMood`/`getTopChart`/`getFavoriteTracks`를 프론트에서 조합만 함.

**추가 개선(버그 수정: "인기 있는 곡"과 "빠른 선곡"이 똑같은 아티스트로 도배됨)**: 실제로 라이브에서 확인해보니, 태그 기반으로 뽑은 "빠른 선곡"도 결국 [[학습노트 36번]]에서 발견한 "지금 인기 있는 곡"의 문제와 똑같이 한 아티스트(Ariana Grande) 곡들로 가득 찼음. 원인은 즐겨찾기 태그 중 제일 많이 나온 태그가 "pop"처럼 아주 넓은 장르였고, Last.fm의 `tag.getTopTracks("pop")`도 결국 그 순간 전 세계에서 제일 많이 스크로블되는(=글로벌 차트를 뒤덮은) 아티스트를 그대로 반영하기 때문 — 태그를 장르 단위로 좁혀도, 그 태그 자체가 넓으면 여전히 같은 "지금 뜨는 아티스트 쏠림" 문제를 못 피함. 태그(장르) 기반 대신 "즐겨찾기한 아티스트 중 하나를 무작위로 골라 그 아티스트와 비슷한 아티스트들의 인기곡"(`/recommend`, `artist.getSimilar` 기반)으로 바꿔서 해결 — 한 아티스트가 아니라 그와 비슷한 여러 다른 아티스트에 후보가 걸쳐 있어서, 특정 아티스트 하나가 전체를 도배할 가능성이 훨씬 낮아짐.

**알아둘 한계**: 이건 "진짜 추천 시스템"이 아니라 "차트처럼 고정돼 보이지 않게 하는" 눈속임에 가까운 절충안임 — 태그 하나만 보고 후보를 뽑기 때문에 즐겨찾기가 여러 장르에 걸쳐 있으면 그중 제일 많은 장르 하나에만 쏠리고, 즐겨찾기가 하나도 없으면 사실상 완전 무작위(글로벌 차트 셔플)라 "빠른 선곡"이라는 이름값을 못함. 매 페이지 로드마다 클라이언트에서 새로 섞기 때문에, 같은 세션에서 뒤로가기/다시 방문해도 계속 다른 순서로 보임(의도한 동작이지만, "아까 그 곡 다시 보고 싶은데 안 보인다"는 불만이 나올 수 있는 트레이드오프). 진짜 협업 필터링/임베딩 기반 추천이나 LLM 기반 대화형 추천은 여전히 ROADMAP의 "보류(추후 확장 옵션)"에 남아있는 별개 항목.

**면접에서 말할 포인트**:
- "AI를 넣어달라는 요청을 어떻게 스코프 조정했나" -> 프로젝트 시작 시점에 이미 "AI/LLM 자체 개발은 범위 밖"이라고 정해둔 원칙을 지키면서도, 사용자가 원하는 건 정확히는 "AI"가 아니라 "고정되지 않고 내 취향에 맞는 느낌"이었다는 걸 구분해서, 이미 쌓아둔 데이터(태그)와 기존 엔드포인트 재사용만으로 그 체감을 만족시키는 훨씬 저비용의 절충안을 제시한 사례 — 요청의 표면적 문구("AI 넣어줘")가 아니라 그 요청이 해결하려는 진짜 문제("매번 똑같아서 지루하다")에 집중한 것.
- "규칙 기반 추천과 ML 기반 추천의 경계를 어떻게 설명하나" -> 이번 구현은 "태그 하나 골라서 그 태그로 조회 + 무작위 셔플" 수준으로, 사용자 행동 데이터를 실제로 반영한다는 점에서 순수 랜덤보다는 낫지만, 여러 신호를 가중치로 조합하거나 유사도를 학습하는 게 아니라서 엄밀히는 "개인화"라기보다 "태그 필터 + 셔플"에 가깝다는 걸 명확히 구분해서 설명할 수 있어야 함 — 과장 없이 정확한 수준을 말하는 것도 면접에서 중요한 역량.

---

## 38. "빠른 선곡"에 실제 LLM(Claude Haiku 4.5) 추천 도입 — 이 프로젝트 첫 유료 API

**뭘 했나**: [[학습노트 37번]]에서 "AI 추천을 넣을 순 없나?"라는 질문에 규칙 기반(태그/아티스트 셔플) 절충안으로 답했었는데, 사용자가 "그래도 요즘 유튜브가 쓰는 방식을 실제로 해보고 싶다"고 다시 요청함. 백엔드에 `POST /quick-picks/ai` 엔드포인트를 새로 추가해서 즐겨찾기 목록 전체를 Claude Haiku 4.5(Anthropic API)에게 보여주고 새로운 곡 추천을 받고, 프론트 "빠른 선곡" 섹션이 이 엔드포인트를 우선 호출하도록(실패 시 §37의 아티스트 기반 방식으로 자동 폴백) 바꿈.

**왜 이렇게 했나**: `ROADMAP.md` 최상단에는 "AI Agent/LLM 자체 개발(RAG, MCP 등)은 이번 로드맵에서 제외"라고 프로젝트 시작 시점부터 명시돼 있었지만, 사용자가 이 결정을 직접 재검토하고 "할 수 있으면 하고 싶다"고 확정했음. 진짜 유튜브 뮤직 수준의 대규모 학습 기반 추천(수억 사용자의 시청 로그로 학습한 자체 모델)은 혼자 만드는 포트폴리오 프로젝트 범위를 벗어나는 게 명백해서, 그 대신 요즘 실무에서도 널리 쓰이는 "LLM API를 활용한 추천/재랭킹"을 현실적인 등가물로 제안하고 진행함. 모델 후보 중 Haiku 4.5를 고른 이유는 추천 하나에 필요한 프롬프트(즐겨찾기 목록 + 간단한 지시문)가 길지 않고 구조화된 JSON 출력만 받으면 되는 가벼운 작업이라, 더 비싼 모델의 추론 능력이 굳이 필요하지 않았기 때문(입력 $1/1M · 출력 $5/1M 토큰, 호출당 대략 $0.002~0.003 수준으로 사전에 비용을 계산하고 사용자에게 먼저 공유한 뒤 진행).

**어떻게 동작하나**:
- 프론트(`page.tsx`)가 `favoriteTracks`를 `{artist, name}` 배열로 변환해서 `POST /quick-picks/ai`에 보냄.
- 백엔드는 세션ID+오늘 날짜로 캐시 키(`quick-picks-ai:v1:{session}:{date}`)를 만들어 하루 한 번만 실제로 Claude를 호출하도록 함 — 새로고침마다 비용이 나가지 않게 하기 위한 장치(TTL도 24시간으로 맞춤). `x_session_id`가 없으면(세션 쿠키 발급 전 등) `anon` 키로 묶여서 캐싱은 되지만 사용자별로 분리되진 않음.
- 캐시가 없으면 즐겨찾기 목록(최대 20개, 프롬프트 길이 제한용)을 텍스트로 만들어 Claude에게 "이미 있는 곡은 빼고, 설명 없이 JSON 배열로만" 새 곡을 추천해달라고 요청.
- **모델이 실존하지 않는 곡을 지어낼 위험(hallucination)을 막기 위해, 추천받은 "아티스트 - 곡" 이름표를 그대로 신뢰하지 않고 각각 Last.fm `track.search`로 재검증함** — 검색 결과가 없으면(=실존하지 않는 곡일 가능성) 그냥 건너뛰고, 검색된 진짜 메타데이터(정식 아티스트명, 청취자 수, 이미지)로 다시 채워서 응답. 즉 LLM은 "이름표만 생성"하고, 실제 트랙 데이터는 항상 우리 쪽 검증된 소스에서 가져오는 구조 — 모델이 완전히 새로운 곡 정보(가짜 청취자 수, 가짜 이미지 URL 등)를 만들어내게 하지 않음.
- 프론트는 이 엔드포인트가 503(키 없음)/502(호출·파싱 실패)를 내거나 빈 배열을 주면 조용히 §37의 기존 아티스트 기반(`recommendByArtist`) 방식으로 폴백 — AI 기능이 이 프로젝트의 유일한 유료 의존성이라, 키가 없는 환경(예: 다른 사람이 이 레포를 클론해서 볼 때)에서도 앱이 죽지 않고 항상 뭔가는 뜨게 만드는 걸 우선함.
- `requirements.txt`에 `anthropic` 패키지 추가, `.env.example`/`.env`에 `ANTHROPIC_API_KEY` 플레이스홀더 추가(값은 비워둠 — [[학습노트 28번]]의 `ADMIN_SECRET`과 같은 패턴).

**알아둘 한계**: (1) 재검증 단계에서 `track.search`가 아티스트명 철자가 살짝 다르거나(예: 밴드명 표기 차이) 마이너한 곡이면 실존하는 곡인데도 못 찾아서 걸러질 수 있음 — 재현율보다 "가짜 곡을 절대 내보내지 않는다"는 정확도를 우선한 설계. (2) 하루 캐싱이라 즐겨찾기를 새로 추가해도 그날은 추천에 바로 반영 안 됨(다음 날 또는 캐시가 만료돼야 반영) — 실시간성보다 비용 통제를 우선한 트레이드오프. (3) 세션ID가 없는 사용자는 캐시가 전부 `anon` 키로 뭉쳐서, 사실상 그날 첫 요청의 결과를 서로 다른 익명 사용자끼리 공유하게 됨(세션ID는 프론트에서 항상 발급하므로 실사용에선 거의 발생하지 않을 케이스). (4) `anthropic` 패키지를 새로 추가했으니 로컬에서 `pip install -r requirements.txt` 재실행 필요.

**면접에서 말할 포인트**:
- "LLM을 제품 기능에 붙일 때 가장 신경 쓴 부분은?" -> hallucination 대응. LLM 출력을 최종 데이터로 바로 쓰지 않고 "제안"으로만 취급해서, 실제로 서비스에 노출되는 트랙 데이터(이름, 아티스트, 이미지, 청취자 수)는 항상 기존에 신뢰하던 소스(Last.fm)로 재검증·재조회한 뒤에만 내보내는 구조를 설계함 — LLM은 "탐색 범위를 넓혀주는 역할", 실제 데이터 정합성은 기존 파이프라인이 책임지는 역할 분리.
- "유료 API를 도입할 때 비용을 어떻게 통제했나" -> 호출 자체를 사용자 행동(새로고침)에 직접 연결하지 않고, 세션+날짜 단위로 캐싱해서 하루 최대 호출 횟수를 실질적으로 세션 수만큼으로 제한함. 기능을 만들기 전에 먼저 호출당 예상 비용을 계산해서 사용자에게 공유하고 승인을 받은 뒤 구현에 들어간 것도, 실무에서 유료 외부 의존성을 도입할 때 필요한 절차라고 설명할 수 있음.
- "이전에 스코프에서 뺐던 결정을 어떻게 뒤집었나" -> [[학습노트 37번]]에서는 "AI 자체 개발은 제외"라는 원칙을 지키면서 규칙 기반으로 절충했는데, 사용자가 직접 이 원칙을 재검토하고 명시적으로 승인한 뒤에야 유료 API 의존성을 새로 들였음 — 프로젝트 초기 결정이라도 고정불변은 아니고, 근거(비용/실현 가능성)를 투명하게 공유해서 의사결정권자(이 경우 프로젝트 오너인 사용자 본인)가 판단할 수 있게 하는 게 우선이라는 태도.

**라이브 확인 완료 (2026-08-12)**: `ANTHROPIC_API_KEY` 발급·등록, `pip install -r requirements.txt`, 백엔드 재시작까지 마친 뒤 Safari 웹 인스펙터 네트워크 탭에서 직접 확인 — `POST http://127.0.0.1:8000/quick-picks/ai` 요청이 `200 OK`로 응답했고, "빠른 선곡" 섹션에 기존 즐겨찾기 아티스트(아이유, 허각&정은지)에는 없던 새 아티스트(잔나비, 아이브, JENNIE 등)가 다양하게 섞여 나오는 것도 확인함 — 아티스트 기반 폴백(§37)이었다면 나오기 어려운 다양성이라 실제로 LLM 추천 경로가 탄 것으로 판단. 일부 트랙(Paradise, thank u 등)은 카드 이미지가 음표 플레이스홀더로 뜨는 게 관찰됐는데, 이건 `resolve_track_image()`가 개별 곡마다 iTunes에서 못 찾는 경우가 실제로 있다는 기존에 이미 문서화된 한계([[학습노트 36번]])와 같은 종류라 새 버그로 보진 않음 — 다만 다른 섹션 대비 플레이스홀더 비율이 유독 높아 보이면 다음에 이 부분만 따로 다시 살펴볼 것. 이 관찰이 바로 아래 §39로 이어짐.

---

## 39. "빠른 선곡" 이미지 보강 — iTunes/Last.fm 둘 다 실패한 곡에 유튜브 영상 썸네일 폴백 추가

**뭘 했나**: [[학습노트 38번]] 라이브 확인 중 "빠른 선곡" 일부 트랙(Paradise, thank u 등)이 음표 플레이스홀더로 뜨는 걸 보고, 사용자가 유튜브 뮤직에서 "쇼미더머니" 참가곡처럼 정식 음원 유통이 안 된 곡도 이미지(그 회차 포스터)가 잘 뜨는 걸 예로 들며 "우리도 좀 다양하게 못 쓰나"라고 요청. `resolve_track_image()`(iTunes → Last.fm)가 둘 다 실패했을 때, 마지막 수단으로 그 곡의 유튜브 영상 썸네일을 이미지로 쓰는 `_youtube_thumbnail_fallback()`을 추가해서 "빠른 선곡"(`POST /quick-picks/ai`)에만 적용함.

**왜 이렇게 했나**: 유튜브 뮤직이 비공식 발매곡도 이미지가 뜨는 원리를 먼저 짚었음 — 그건 "앨범 커버"가 아니라 그 곡이 올라간 유튜브 영상 자체의 썸네일(업로더가 지정한 회차 포스터 등)을 쓰는 것이고, 유튜브 자체 서비스라 이 썸네일에 항상 접근 가능함. 우리도 재생 버튼용으로 이미 유튜브 검색(`/youtube-search`)을 하고 있으니 기술적으로는 같은 방법을 쓸 수 있는데, 문제는 유튜브 검색이 하루 쿼터(10,000유닛, 검색 1회=100유닛=하루 약 100회) 제한이 있는 유료성 리소스라는 것 — "다시 듣기"처럼 곡 수가 많은 목록까지 적용하면 홈 화면 한 번 로드에 쿼터 대부분을 써버릴 위험이 있음. 그래서 어디까지 적용할지를 사용자에게 직접 물어봤고("빠른 선곡만 적용" 선택), 하루 9곡(`AI_QUICK_PICKS_COUNT`)에 세션+날짜 캐싱까지 되는 "빠른 선곡"에만, 그것도 iTunes/Last.fm이 이미 실패한 곡에만 최후 수단으로 적용하도록 범위를 최대한 좁힘.

**어떻게 동작하나**:
- `_youtube_thumbnail_fallback(artist, track)`은 `/youtube-search`가 쓰는 것과 **정확히 같은 캐시 키**(`youtube-search:{artist}:{track}`)를 그대로 재사용함 — 이미 그 곡을 재생해서 캐싱된 videoId가 있으면 API를 다시 안 부르고 캐시에서 바로 꺼내 쓰고, 반대로 여기서 새로 검색해서 캐싱해두면 나중에 그 곡을 재생할 때 `/youtube-search`도 이 캐시를 그대로 씀 — 두 기능이 캐시(와 그 밑에 깔린 유튜브 쿼터)를 공유하는 구조.
- 썸네일 자체는 `https://i.ytimg.com/vi/{videoId}/hqdefault.jpg` 형태의 공개 정적 이미지 URL이라, videoId만 한 번 알면(검색 1회, 100유닛) 그 이후 이미지를 몇 번을 보여주든 추가 쿼터가 안 듦.
- `/quick-picks/ai`에서 `resolve_track_image()`가 `None`을 반환할 때만 이 폴백을 호출 — iTunes/Last.fm 둘 다 있는 곡은 기존 경로 그대로, 아예 없는 곡만 추가 비용을 씀.
- 이미지 계산 로직이 바뀌었으니 [[학습노트 36번]]에서 배운 대로 `/quick-picks/ai`의 캐시 키를 `v1` → `v2`로 올려서, 오늘 이미 저장된 옛 캐시(새 폴백 없이 계산된 결과)가 그대로 나가지 않게 함.

**알아둘 한계**: (1) 유튜브 영상 썸네일은 정식 앨범 커버가 아니라 "그 영상에 업로더가 붙인 이미지"라서, 곡과 무관한 섬네일(예: 라이브 클립의 무대 사진, 팬이 편집한 영상의 임의 이미지)이 뜰 수도 있음 — iTunes/Last.fm보다 정확도가 떨어지는 최후 수단이라는 걸 인지하고 마지막 우선순위로 둠. (2) 여전히 "다시 듣기" 섹션(즐겨찾기 목록)엔 이 폴백을 적용하지 않음 — 거기는 애초에 이미지 조회 자체를 안 하고 항상 `null`로 두는 상태([[학습노트 29번]])라, 그쪽까지 고치려면 훨씬 큰 범위의 결정(즐겨찾기 목록 전체에 대해 iTunes+유튜브 조회를 도입할지, 그 경우 쿼터를 어떻게 지킬지)이 필요해서 이번 범위에서는 의도적으로 제외함. (3) `YOUTUBE_API_KEY`가 없으면 이 폴백도 조용히 `None`을 반환 — 앱이 죽지 않고 그냥 플레이스홀더가 계속 뜨는 것으로 자연스럽게 낮아짐.

**면접에서 말할 포인트**:
- "제한된 외부 API 쿼터를 여러 기능이 나눠 써야 할 때 어떻게 설계했나" -> 새 기능(이미지 보강)이 기존 기능(재생)과 정확히 같은 캐시 키·스키마를 공유하게 만들어서, 같은 곡에 대한 쿼터 소비를 기능 간에 중복 없이 재사용되게 설계함 — 두 기능을 독립적으로 만들면 같은 곡을 두 번 검색하게 되는데, 캐시 레이어를 "리소스(이 곡의 유튜브 정보)" 단위로 공유하도록 설계하면 이런 중복을 막을 수 있다는 걸 보여주는 사례.
- "기능 적용 범위를 어떻게 정했나" -> 기술적으로는 모든 섹션에 적용 가능했지만, 하루 쿼터라는 명확한 제약이 있어서 "이 정도 사용량이면 안전한가"를 먼저 따져보고, 애매하면 임의로 결정하지 않고 사용자에게 선택지를 준 사례(빠른 선곡만 / 다시 듣기까지 확대 / 보류). 기능을 만들 수 있다는 것과 지금 그 범위로 만들어도 되는지는 별개 판단이라는 태도.

---

## 40. "다시 듣기"/"빠른 선곡" 가로 드래그 스크롤 전환 + "빠른 선곡" 9곡 → 15곡 증량

**뭘 했나**: "다시 듣기"/"빠른 선곡" 두 섹션에서 "양을 늘리고 싶고, 스크롤을 오른쪽으로 당길 수 있게 해달라"는 요청. `CompactTrackGrid`에 `scrollable` prop을 추가해서, 켜면 기존 페이지네이션 버튼(‹ 1/2 ›) 대신 3행 고정 + 가로 드래그 스크롤로 표시되게 함 — "다시 듣기"/"빠른 선곡" 두 곳에만 적용. "빠른 선곡" 곡 수도 9개 → 15개로 늘림(백엔드 `AI_QUICK_PICKS_COUNT`, 프론트 폴백 경로의 `slice` 상한 전부 함께 조정).

**왜 이렇게 했나**: `CompactTrackGrid`는 무드 탐색 국내/해외, 검색의 장르/연대 결과([[학습노트 29번]], [[학습노트 32번]])까지 총 5곳에서 재사용 중인 공용 컴포넌트라, 스크롤 방식으로 무작정 바꾸면 그 5곳 전부에 영향이 감. 요청은 "다시 듣기"/"빠른 선곡" 두 곳에 한정된 거라, 무드 탐색·검색 결과까지 같이 바꿀지 사용자에게 직접 확인했고 "두 곳만"으로 범위를 좁힘 — 그래서 컴포넌트를 통째로 바꾸는 대신 옵션(prop)으로 갈라서 기존 5곳 중 나머지 3곳은 회귀 없이 그대로 두게 함. "빠른 선곡" 개수 증량은 AI에게 더 많이 추천받고 Last.fm 재검증도 그만큼 더 하는 것뿐이라 비용 증가가 사실상 없어서(§38에서 이미 세션+일 단위로 캐싱해 호출 자체가 하루 1번인 구조), 몇 곡으로 늘릴지도 사용자에게 직접 확인 후 15곡으로 정함.

**어떻게 동작하나**:
- `CompactTrackGrid`에 `scrollable?: boolean`(기본 `false`) 추가. `true`면 `grid-flow-col grid-rows-3 auto-cols-[minmax(220px,1fr)] overflow-x-auto`로 렌더링 — 3행짜리 세로 묶음이 옆으로 여러 열 늘어서고, 넘치는 만큼 가로로 드래그/스크롤해서 봄. 페이지 state(`page`)와 ‹ › 버튼은 이 모드에선 아예 렌더링 안 함. `false`(기본값)일 땐 기존 3열 + 9개씩 페이지네이션 버튼 방식 그대로 유지 — 무드 탐색/검색 결과 2곳은 코드 변경 없이 회귀 없음.
- 아이템 렌더링 로직(`CompactTrackRow` 매핑)을 `renderRow` 함수로 뽑아서 두 모드가 공유 — 스크롤이든 페이지네이션이든 각 트랙 카드 자체는 완전히 동일하게 그림.
- 백엔드 `AI_QUICK_PICKS_COUNT`를 15로 올리고(Claude에게 요청하는 후보 수도 `COUNT * 2`라 자동으로 30개로 늘어남), 캐시 키를 `v2` → `v3`로 버전업([[학습노트 36번]]과 같은 이유 — 오늘 이미 저장된 9곡짜리 캐시가 그대로 나가는 걸 막기 위함). 프론트 `quickPickTracks` 계산 로직의 `slice(0, 9)` 4곳(AI 성공/아티스트 폴백/차트 폴백 경로 전부)을 `slice(0, 15)`로 맞춤 — "빠른 선곡" 폴백 경로가 쓰는 `/recommend`는 이미 최대 15곡까지 주므로 프론트 쪽 슬라이스만 맞추면 됐음.
- "다시 듣기"는 원래도 즐겨찾기 전체 개수만큼 데이터를 다 갖고 있었고 페이지네이션(9개씩)만 걸려 있던 거라, 별도 개수 증량 없이 `scrollable` 옵션만 켜면 전체 목록이 스크롤로 다 보임.

**알아둘 한계**: 가로 스크롤은 마우스 휠 세로 스크롤로는 안 움직이고(트랙패드 가로 스와이프나 시프트+휠, 또는 직접 드래그해야 함) 스크롤바가 항상 얇게 보여서 "스크롤이 되는 자리"라는 힌트가 약할 수 있음 — 필요하면 나중에 좌우 화살표 버튼을 스크롤과 병행 표시하는 것도 고려 가능(지금은 요청받은 범위 밖이라 미룸).

**면접에서 말할 포인트**:
- "공용 컴포넌트를 재사용하는 여러 화면 중 일부에만 다른 동작을 넣어야 할 때 어떻게 하나" -> 컴포넌트를 복제하거나 분기 로직을 화면마다 따로 만드는 대신, 동작 차이를 boolean prop 하나로 표현해서 호출부가 원하는 모드를 선언적으로 고르게 함 — 내부 렌더링 로직(트랙 카드 자체)은 함수로 뽑아 두 모드가 공유해서 중복 없이 유지.
- "UI 변경 범위를 어떻게 판단했나" -> 공용 컴포넌트라 요청받지 않은 다른 화면까지 영향받을 수 있다는 걸 먼저 인지하고, 임의로 전체를 바꾸지 않고 사용자에게 범위(어디까지 바꿀지)를 확인한 뒤 옵션화로 구현 — 요청보다 넓게 바꾸는 게 항상 "더 나은 개선"은 아니라는 판단.

---

## 41. (기술 검토) AWS Personalize 같은 "진짜 추천 엔진 API"를 사더라도 콜드 스타트 문제는 못 피함

**뭘 검토했나**: §38(Claude Haiku 4.5 LLM 추천) 작업 이후 나온 개인적인 질문 — "유튜브 뮤직의 진짜 강점은 결국 추천 알고리즘인데, 이것도 Anthropic API처럼 돈 주고 살 수 있는 거 아닌가?"에 대한 조사. AWS Personalize(아마존이 자사 쇼핑몰 추천 기술을 API로 파는 서비스)를 예로 실제 최소 데이터 요구 조건을 찾아봄.

**왜 이게 중요한가**: 협업 필터링(collaborative filtering, "이 곡을 좋아한 다른 사용자들은 이런 곡도 좋아했다" 방식)은 알고리즘을 API로 사도 그 안에 넣을 "여러 사용자의 실제 행동 데이터"가 있어야 동작함 — 알고리즘과 데이터는 별개 자원이고, 유튜브 뮤직의 진짜 경쟁력은 알고리즘 자체보다 그걸 학습시킬 수 있는 수억 사용자 규모의 데이터라는 결론. AWS Personalize 공식 문서 기준 실제 하한선을 확인함:
- **절대 최소(이 밑이면 훈련 자체가 에러로 거부됨)**: 서로 다른 사용자 25명 이상, 각자 상호작용 2건 이상, 전체 상호작용 1,000건 이상
- **품질 기준 권장치**: 사용자 1,000명 이상, 상호작용 50,000건 이상

Unbeat는 실사용자가 사실상 본인 한 명이라 이 최소 기준(25명)에도 크게 못 미침 — 돈이 있어도(무료 티어 포함) 데이터가 없으면 애초에 학습 자체가 안 돌아가는 구조.

**콜드 스타트 문제를 우회하는 방법 (합성 데이터 부트스트랩)**: 이건 업계에서도 실제로 쓰는 정식 기법이라, "적은 사용자를 많아 보이게 하는 꼼수 없나"는 질문에 대한 답으로 정리함 — 이미 갖고 있는 Last.fm 집계 데이터(태그별 인기곡 차트)를 활용해서, 각기 다른 취향(태그 조합)을 무작위로 부여받은 "가상 사용자" 수백 명을 프로그램으로 생성하고, 각 가상 사용자가 그 취향에 맞는 태그 차트에서 곡을 재생했다는 상호작용 로그를 대량으로 만들어 AWS Personalize의 최소 데이터 기준을 채우는 방식. 그 위에 실제 사용자(학술제 발표 시 정연 본인 + 참여자 몇 명)의 진짜 상호작용을 얹으면 최소 기준도 넘기고, 발표 중 "실시간으로 곡을 눌러보면 추천이 바뀐다"는 데모도 가능해짐.

**알아둘 한계 (중요, 발표 때 정직하게 설명해야 할 부분)**: 이 방식으로 만든 추천은 "진짜 개인화"가 아니라 "그럴듯하게 보이는 데모"에 가까움 — 합성 사용자의 취향은 결국 Last.fm 집계 데이터를 재구성한 것이라, 정말로 그 개인에게 최적화된 추천이라고 보기는 어려움. 발표에서는 "완성된 추천 시스템"이 아니라 "콜드 스타트 문제를 이해하고, 실무에서 이걸 어떻게 완화하는지 직접 시도해본 경험"으로 프레이밍하는 게 훨씬 신뢰도 있는 설명이 됨.

**다음 계획**: 합성 데이터 생성 로직 자체는 AWS를 안 건드리고 지금 갖고 있는 Last.fm 연동만으로 미리 만들어둘 수 있음(무료 티어 2개월 타이머는 AWS Personalize 리소스를 실제로 만드는 순간부터 도니까). 학술제 발표가 11월 말이라 2개월 무료 기간 안에 들어오도록 10월경에 실제 AWS Personalize 리소스를 생성해서 시도할 예정 — 아직 착수 전, 이 노트는 착수 전 조사·결정 사항 기록.

**면접에서 말할 포인트**:
- "추천 시스템을 만들 때 알고리즘과 데이터 중 뭐가 더 중요한가" -> 알고리즘은 API로 사도 데이터(특히 충분한 규모의 실제 사용자 상호작용)가 없으면 무용지물이라는 걸 AWS Personalize의 실제 최소 데이터 요구 조건(25명/1,000건)을 직접 찾아보고 확인한 경험 — "권한/자원이 있어도 근본 제약(데이터 규모)은 못 피한다"를 구체적 수치로 설명할 수 있음.
- "제약 조건을 어떻게 우회했나" -> 합성 데이터로 최소 기준을 채우는 부트스트랩 기법을 검토했고, 이걸 "진짜 개인화"라고 과장하지 않고 한계를 명확히 인지한 채로 데모 목적에 맞게 정직하게 프레이밍하기로 함 — 기술적 한계를 숨기지 않고 정확히 설명하는 것도 엔지니어링 커뮤니케이션의 일부라는 태도.

---

## 42. 가사 요청 관리자 페이지 — "가사 올려주세요"가 요청만 쌓이고 처리할 화면이 없던 문제

**뭘 했나**: [[학습노트 28번]]에서 "가사 올려주세요" 버튼(`POST /lyrics/request`)과 수동 등록 엔드포인트(`POST /lyrics/override`)를 만들어뒀는데, 정작 "쌓인 요청을 어디서 보고 처리하는지"는 안 만들어져 있었음 — 라이브 확인 중 "가사 올려주세요를 눌렀는데 계속 안 올라가면 안 되지 않냐, 이걸 어디서 관리해야 하지"라는 지적으로 발견. `GET /admin/lyrics-requests`(미해결 요청 목록)를 새로 추가하고, `/admin/lyrics`에 비밀번호로 들어가는 관리자 페이지를 만들어서 목록 확인 + 가사 등록까지 한 화면에서 되게 함.

**왜 이렇게 했나**: §28을 만들 때는 "요청 남기기 → 운영자가 구해서 등록"이라는 파이프라인의 앞뒤(버튼, 저장 엔드포인트)만 만들고 가운데(그 요청들을 실제로 확인하는 화면)를 빠뜨렸음 — 기능을 "요청이 어딘가에 기록되긴 한다"까지만 만들고 "그래서 그걸 누가 어떻게 보고 처리하나"까지 끝까지 생각하지 않은 전형적인 미완성 파이프라인 사례. `lyrics_request` 이벤트가 기존 `events` 테이블(AARRR 분석에도 쓰는 원본 로그)에 그대로 쌓이고 있었으니, 새 테이블 없이 이 데이터를 그대로 재사용해서 목록 화면을 완성함.

**어떻게 동작하나**:
- `db.py`에 `get_lyrics_requests()` 추가 — `events` 테이블에서 `event_type='lyrics_request'`인 것만 `artist_name`(로깅 시점에 "아티스트 - 곡명" 형태로 합쳐 저장됨) 기준으로 묶어서, 몇 번 요청됐는지(`request_count`)와 마지막 요청 시각까지 같이 집계.
- `GET /admin/lyrics-requests`(`ADMIN_SECRET` + `X-Admin-Secret` 헤더로 접근 제한, §28과 동일한 패턴)가 이 목록을 받아 "아티스트 - 곡명" 문자열을 다시 분리하고, 각각에 대해 이미 `lyrics-override` 캐시가 있는지(=이미 처리됐는지) 확인해서 있으면 목록에서 제외 — 매번 "아직 안 끝난 것"만 보이게 함.
- `POST /lyrics/override`를 쿼리 파라미터 방식에서 **JSON 바디**로 바꿈 — 가사 본문이 보통 수백~수천 글자라 URL 쿼리스트링에 넣으면 브라우저/서버 URL 길이 제한에 걸릴 수 있다는 걸 실제 화면을 만들면서 뒤늦게 깨달음(지금까지는 이 엔드포인트를 UI 없이 curl로만 테스트해서 안 드러났던 문제). 텍스트가 길어질 수 있는 필드는 처음부터 바디로 설계해야 한다는 교훈.
- 프론트 `/admin/lyrics`는 메인 앱과 링크로 연결되지 않은 별도 라우트 — 비밀번호 입력(로컬 state에만 보관, 매 요청마다 `X-Admin-Secret` 헤더로 전송) → 통과하면 미해결 요청 목록과 각 항목의 "가사 등록" 버튼 → 누르면 인라인 폼(가사 필수, 동기화 가사 선택)이 펼쳐지고 등록하면 그 항목이 목록에서 바로 사라짐(성공 시 로컬 state에서 filter).

**알아둘 한계**: "아티스트 - 곡명" 분리는 첫 " - "를 기준으로 자르는 방식이라, 아티스트명 자체에 " - "가 들어간 극소수 케이스(예: 있을 수 있는 콜라보 표기)는 잘못 분리될 수 있음 — 다른 유사 파싱(`_parse_youtube_music_title` 등)과 같은 종류의 한계. 관리자 페이지는 인증이 `ADMIN_SECRET` 평문 비교뿐이라 실서비스 수준의 보안은 아니고, 본인만 로컬에서 접근할 용도로 스코프를 의도적으로 좁힘(§28과 동일한 판단).

**추가 개선(라이브 확인 중 발견 1 — 실제로 " - " 분리 버그가 재현됨)**: 라이브 확인 중 "JENNIE · Topic - Less than a Lover"처럼 잘못 분리된 요청이 실제로 목록에 뜨는 걸 확인함 — 원인은 이 요청이 로깅될 당시 아티스트명이 "JENNIE - Topic"(유튜브 자동생성 채널 접미사가 안 지워진 상태, [[학습노트 31번]]에서 다룬 문제)이었고, 이게 곡명과 합쳐져 "JENNIE - Topic - Less than a Lover"로 저장된 뒤 내 분리 로직이 첫 " - "만 자르면서 "Topic"이 곡명 쪽에 남아버린 것. 근본 원인은 `/lyrics/request`를 호출하는 시점(예: 재생 중인 곡 정보)에 `_clean_artist_name()`이 항상 적용되진 않는다는 것 — 아직 수정 안 함, 다음에 손볼 후보로 남겨둠.

**추가 개선(라이브 확인 중 발견 2 — 브라우저 새로고침 시 로그인 풀림)**: 페이지 안의 "새로고침" 버튼(목록만 다시 불러옴)과 브라우저 자체 새로고침(Cmd+R, 페이지 전체를 다시 불러와 React state가 초기화됨)을 혼동해서, 브라우저 새로고침을 할 때마다 비밀번호를 다시 쳐야 하는 게 불편하다는 피드백. `sessionStorage`에 로그인 성공 시 비밀번호를 저장해뒀다가, 페이지가 새로 뜰 때(`useEffect`) 저장된 값이 있으면 자동으로 재로그인 시도하도록 수정 — 브라우저를 완전히 새로고침해도 로그인이 유지됨. `localStorage`(브라우저를 껐다 켜도 계속 남음) 대신 `sessionStorage`(탭을 닫으면 사라짐)를 쓴 이유는 관리자 비밀번호를 브라우저에 무기한 남겨둘 필요는 없어서 — 편의성과 최소 보안 사이의 절충. 저장된 값이 틀렸거나 만료됐어도 그냥 로그인 화면으로 돌아가고 세션스토리지 값도 지워지니, 실패해도 안전하게 처리됨.

**면접에서 말할 포인트**:
- "기능을 만들 때 놓치기 쉬운 부분이 뭐라고 생각하나" -> "요청을 남긴다"와 "그 요청을 실제로 처리할 수 있다"는 서로 다른 완성도라는 걸, 파이프라인의 입구(버튼)와 저장소(DB 이벤트)만 만들고 그 사이를 잇는 화면을 빠뜨렸던 경험으로 설명할 수 있음 — 기능은 "데이터가 어딘가에 쌓인다"가 아니라 "그 데이터로 실제 행동(가사 등록)까지 이어진다"를 기준으로 완성 여부를 판단해야 한다는 것.
- "API 파라미터를 쿼리로 할지 바디로 할지 어떻게 판단하나" -> 값의 길이가 예측 가능하게 짧은지(ID, 이름 등)와 길어질 수 있는지(자유 텍스트, 이번 경우 가사 전문)를 기준으로 판단 — 처음엔 테스트 편의상 쿼리로 만들었다가 실제 UI를 붙이면서 길이 제한 문제를 발견하고 바디로 바꾼 경험을 실제 설계 판단 사례로 들 수 있음.

---

## 43. 가사 자동 폴백에 lyrics.ovh 추가 — "가사/추천이 이 사이트 핵심인데 빈 곳이 있으면 안 된다"

**뭘 했나**: 가사 관리자 페이지([[학습노트 42번]])를 써보다가 "가사를 수기로 다 채워야 하냐"는 질문이 나왔고, "없는 건 있을 수 없다, 가사랑 추천(알고리즘)이 이 사이트 메인이다"라는 확고한 답을 받음. LRCLIB 4단계 폴백([[학습노트 26번]])을 전부 실패했을 때만 시도하는 **5번째 자동 폴백**으로 `lyrics.ovh`(무료, 키 불필요, 일반 가사 텍스트 제공)를 추가해서, 관리자 수동 등록으로 넘어가기 전에 자동으로 채워지는 곡을 최대한 늘림.

**왜 이렇게 했나**: LRCLIB이 못 찾으면 그냥 "요청 남기기 → 수동 등록"으로 넘어가던 게 기존 흐름이었는데, 자동으로 더 채울 수 있는 여지가 있는지부터 확인해봄 — Genius(라이선스상 가사 원문 자체를 API로 안 줌, [[학습노트 26번]])와 Musixmatch(무료 티어가 가사 전체가 아니라 일부 미리보기만 줌, 같은 노트에서 이미 검토·기각)는 여전히 막혀있는데, `lyrics.ovh`는 무료로 가사 전문을 그대로 줌. 다만 이 서비스가 어디서 가사를 모으는지 공식적으로 안 밝히고 있어서, LRCLIB처럼 "크라우드소싱 데이터를 자유롭게 재배포하겠다"고 명시된 서비스와는 다르게 저작권 측면에서 명확하다고 말하긴 어려운 회색지대라는 걸 먼저 짚고, 사용자에게 이 트레이드오프를 그대로 전달해서 직접 판단하게 함 — "개인 포트폴리오 프로젝트 수준이면 감수 가능한 리스크"라는 답을 받고 진행(상업 서비스였다면 안 썼을 선택이라는 걸 명확히 인지하고 있음).

**어떻게 동작하나**: `_lookup_lyrics()`의 LRCLIB 4단계(정확 매칭 → 자유 검색 → 표기 제거 후 정확 매칭 → 표기 제거 후 자유 검색) 시도가 전부 실패했을 때만 `_search_lyrics_ovh(artist, track)`를 호출 — `GET https://api.lyrics.ovh/v1/{artist}/{track}`, 404면 그냥 `None` 반환해서 여느 실패와 동일하게 조용히 "못 찾음"으로 처리. 동기화(LRC) 타임스탬프는 안 주는 서비스라 `syncedLyrics: None, plainLyrics: <가사 텍스트>`로만 채워짐 — 이 경로로 찾은 곡은 하이라이트/자동스크롤 없이 일반 텍스트만 보임(LRCLIB에서 찾은 곡과 표시 방식이 자연스럽게 갈림). 캐시 키(`lyrics:{artist}:{track}`)를 `lyrics:v2:...`로 버전업 — 이전에 LRCLIB만으로 "못 찾음"으로 캐싱된 곡들이 새 폴백을 시도도 안 해보고 그대로 나가는 걸 막기 위함([[학습노트 36번]]과 같은 이유).

**알아둘 한계**: (1) 저작권 출처가 불명확한 서비스라는 점은 여전함 — 실제 운영 서비스로 확장할 경우 반드시 재검토가 필요하고, 지금은 "개인 프로젝트 수준의 리스크"로 사용자가 직접 승인한 선택임을 코드/문서에 명시해둠. (2) 동기화 가사가 없어서, 같은 곡이라도 LRCLIB에서 찾았느냐 lyrics.ovh에서 찾았느냐에 따라 가사 탭 경험(하이라이트 유무)이 달라짐 — 사용자에게 왜 다른지 설명할 근거가 필요할 수 있음. (3) 이것도 결국 "완전 자동화"는 아님 — 두 소스를 다 실패한 곡은 여전히 관리자 수동 등록 경로로 떨어짐. 다만 실패하는 곡의 비율 자체는 줄어들 것으로 기대.

**면접에서 말할 포인트**:
- "무료 API를 쓸 때 라이선스가 불분명하면 어떻게 판단하나" -> 서비스마다 저작권 명확성이 다르다는 걸 구분해서 설명할 수 있어야 함(LRCLIB은 명시적 재배포 라이선스, lyrics.ovh는 출처 불명 무료 API, Genius/Musixmatch는 아예 막힘) — 리스크를 숨기지 않고 의사결정권자(이 경우 프로젝트 오너)에게 트레이드오프를 그대로 전달해서 판단하게 한 것도, 실무에서 애매한 라이선스의 외부 의존성을 들일 때 필요한 절차라고 설명할 수 있음.
- "완전히 못 채우는 데이터 갭을 어떻게 다뤘나" -> 자동 소스를 최대한 늘려서 갭을 줄이되(LRCLIB 4단계 + lyrics.ovh), 그래도 못 채우는 나머지는 수동 관리자 도구([[학습노트 42번]])로 흡수하는 이중 구조 — "0%를 목표로 완전 자동화"가 아니라 "자동화 비율을 최대화하고 나머지는 사람이 개입할 수 있는 도구를 갖추는" 현실적인 접근.

---

## 44. 가사 출처 고지 + 저작권 삭제 요청(takedown) 대응 체계

**뭘 했나**: lyrics.ovh([[학습노트 43번]])처럼 출처가 불명확한 회색지대 소스를 계속 쓰는 것에 대한 불안("포트폴리오로 쓸 건데 위법이면 어떡하나")이 나와서, (1) 가사 탭에 "가사 출처: 커뮤니티 가사 데이터베이스 · 저작권자 요청 시 즉시 삭제됩니다" 고지 문구를 추가하고, (2) 실제로 저작권자(또는 대리인)가 삭제를 요청하면 관리자가 즉시 내리고 다시는 자동으로 안 뜨게 만드는 `POST /admin/lyrics/takedown` 엔드포인트 + 관리자 페이지 삭제 폼을 추가함.

**왜 이렇게 했나**: 대화 중 "유튜브에 라이선스가 있으니 그 안의 가사 영상에서 텍스트를 뽑아오면 안 되냐"는 질문이 나왔는데, 확인해보니 (1) 유튜브 공식 API의 자막 다운로드(`captions.download`)는 영상 소유자만 OAuth로 꺼낼 수 있어서 제3자 영상에선 기술적으로 막혀있고, (2) 설령 우회해도 "영상을 그대로 보여주기"(임베드, 안전)에서 "텍스트를 추출해 재배포하기"(스크래핑과 동일, 회색지대)로 성격이 바뀌어버려서 근본적으로 도움이 안 됨 — 이 과정에서 이 프로젝트가 오디오(유튜브 IFrame Player API 임베드)/메타데이터(Last.fm API 약관)는 "이미 라이선스된 파이프를 빌려 쓰는" 구조라 안전하고, 가사만 그런 파이프가 없어서 계속 회색지대로 남는다는 게 명확해짐. 진짜 라이선스(LyricFind, Musixmatch 상업 티어)는 존재하지만 개인 프로젝트가 접근 가능한 가격/방식이 아님(AWS Personalize와 같은 구조, [[학습노트 41번]]).
회색지대 소스 자체를 거두는 대신, "실제 문제가 제기되면 즉시 대응할 수 있는 체계"를 만드는 쪽으로 리스크를 관리하기로 함 — 저작권 단속은 트래픽/수익이 있는 서비스에 집중되지 사용자 없는 개인 포트폴리오까지 찾아오는 경우는 현실적으로 드물다는 판단과, 그럼에도 "감수 가능한 리스크"라고 말하려면 최소한의 notice-and-takedown 대응 수단은 있어야 한다는 판단을 같이 반영함.

**어떻게 동작하나**:
- `_lyrics_blocklist_key(artist, track)` = `lyrics-blocklist:{artist}:{track}` (TTL 없음, 영구). `_lookup_lyrics()`에서 override 확인보다도 먼저 이 키를 체크해서, 있으면 override/LRCLIB/lyrics.ovh 뭐가 나오든 상관없이 항상 `{"found": False, "takenDown": True}`를 반환.
- `POST /admin/lyrics/takedown` (ADMIN_SECRET 헤더 필요, [[학습노트 28번]]과 같은 최소 접근 제한 패턴): `lyrics-override:` 캐시와 `lyrics:v2:` 캐시를 둘 다 지우고(`cache.py`에 새로 추가한 `delete_cached()`), 블록리스트에 영구 등록. 캐시만 지우면 다음 조회 때 lyrics.ovh가 또 같은 가사를 찾아올 수 있어서 캐시 삭제만으론 부족함 — 블록리스트가 핵심.
- `GET /admin/lyrics-requests`(대기 목록)도 블록리스트에 있는 곡은 걸러서, "삭제된 곡"이 "처리 대기 중인 곡"으로 다시 나타나지 않게 함.
- 프론트(`MiniPlayer.tsx`): 가사가 정상적으로 보이는 두 경우(동기화/일반 텍스트) 모두 하단에 출처 고지 문구 추가. `lyrics.takenDown`이 true면 "이 곡의 가사를 찾지 못했어요"(아직 못 찾은 것)와 구분해서 "저작권자 요청으로 이 곡의 가사가 내려갔어요"로 안내하고, 다시 요청해봐야 어차피 차단되므로 "가사 올려주세요" 버튼은 숨김.
- `/admin/lyrics` 페이지: 요청 목록과 별개로 아티스트/곡명을 직접 입력해서 삭제하는 폼 추가(삭제 대상은 이미 정상적으로 가사가 떠 있는 곡일 수도 있어서, 요청 목록 항목과 무관하게 동작해야 함).

**알아둘 한계**: (1) 이건 저작권 문제를 "해결"하는 게 아니라 "대응 체계를 갖추는" 것 — lyrics.ovh 자체의 회색지대 성격은 그대로 남아있음. (2) 삭제는 관리자가 수동으로 실행해야 함(자동 감지 아님) — 실제로 문제가 제기됐을 때 사람이 직접 처리하는 걸 전제로 한 최소 구현. (3) 블록리스트 해제(삭제 취소) 기능은 아직 없음 — 실수로 삭제했을 경우 Redis에서 직접 키를 지워야 함, 필요해지면 추가.

**면접에서 말할 포인트**:
- "회색지대 의존성을 완전히 없앨 수 없을 때 어떻게 하나" -> 리스크를 0으로 만들 수 없다면, 실제 문제가 제기됐을 때 즉시 대응할 수 있는 수단(notice-and-takedown)을 미리 갖춰두는 게 현실적인 리스크 관리라는 걸 설명할 수 있음 — 실무에서도 서드파티 데이터 소스를 쓸 때 흔히 쓰는 패턴.
- "캐시 삭제와 완전 차단의 차이" -> 캐시만 지우면 자동 폴백이 같은 데이터를 다시 채워넣을 수 있어서, TTL 없는 별도 블록리스트로 "재조회 자체를 막는" 게 필요하다는 걸 실제 버그 시나리오로 설명할 수 있음.

## 45. 검색 버그 — 띄어쓰기 없는 한글 검색어에서 엉뚱한 "아티스트"가 뜨는 문제

**뭘 했나**: "꽃이피고지듯이"(조승우/한즈의 "꽃이 피고 지듯이" OST 곡, 원제는 띄어쓰기 있음)로 검색했더니 실제 곡은 하나도 안 뜨고, "As Flowers Bloom and Fall / 꽃이피고지듯이 English Lyrics"라는 청취자 2명짜리 엉뚱한 "아티스트" 카드만 뜨는 버그를 발견. Last.fm API를 직접 호출해서 원인을 확인하고, 이런 쓰레기 아티스트 매치를 걸러내는 필터(`_is_junk_artist_match`)를 `/search`에 추가.

**왜 이렇게 됐나**: Last.fm `track.search`에 직접 요청해서 확인해보니, 띄어쓰기 없는 "꽃이피고지듯이"는 `totalResults: 0`(진짜로 0건)이고, 띄어쓰기를 넣은 "꽃이 피고 지듯이"로 검색하면 37건이 나오고 정확히 조승우의 원곡이 1등으로 나옴 — 즉 Last.fm의 한글 검색은 띄어쓰기에 매우 민감하고, 우리 쪽에서 그걸 보정해주는 로직이 없었음. 같은 검색어로 `artist.search`를 호출하면(0건이 아니라) 청취자 2명짜리 "아티스트"가 매칭되는데, 이건 실제 아티스트가 아니라 누군가 유튜브 스크로블러로 "As Flowers Bloom and Fall / 꽃이피고지듯이 English Lyrics"라는 영상을 재생했을 때 영상 제목이 통째로 "아티스트명" 필드에 잘못 들어간 오염 데이터로 보임(Last.fm은 사용자가 스크로블한 원본 메타데이터를 그대로 신뢰하기 때문에 이런 쓰레기가 실제로 검색 결과에 섞여 나올 수 있음).

**어떻게 동작하나**: `_is_junk_artist_match(item)` — Last.fm이 돌려준 아티스트 청취자 수가 5명 이하이고, 이름에 "lyrics"/"가사"/"cover"/"mv"/"official audio"/"official video" 같은 유튜브 영상 제목에 흔한 표기가 섞여 있으면 진짜 아티스트가 아니라 오염 데이터로 간주하고 `/search`의 `artists` 목록에서 제외. 캐시 키를 `search:v2` → `search:v3`로 버전업(예전에 이 쓰레기 결과로 캐싱된 검색어들이 새 필터를 안 거치고 그대로 나가는 걸 막기 위함, [[학습노트 36번]]과 같은 이유).

**알아둘 한계**: 필터 기준(청취자 5명 이하 + 특정 키워드)이 정교하지 않아서, 이름에 우연히 "cover"/"mv" 같은 단어가 들어간 진짜 소규모 아티스트를 놓칠 수 있음 — 실사용 중 오탐 발견하면 조건 조정 필요.

**면접에서 말할 포인트**:
- "외부 API 결과를 그대로 믿으면 안 되는 이유" -> Last.fm 같은 크라우드소싱 데이터 소스는 사용자가 잘못 남긴 메타데이터(스크로블러 파싱 오류)를 그대로 갖고 있을 수 있어서, API가 200을 주고 결과를 줬다고 그대로 신뢰하면 안 되고 응답 품질을 검증하는 로직이 필요하다는 걸 실제 사례로 설명할 수 있음.
- "버그를 어떻게 진단했나" -> 프론트 화면만 보고 추측하지 않고 백엔드가 호출하는 외부 API를 직접 호출해서(`track.search` 띄어쓰기 유/무 비교) 원인을 정확히 특정한 뒤 고쳤다는 걸 설명할 수 있음.

(설치 버그) 로컬(macOS)에서 `pip install -r requirements.txt`를 돌렸는데 `numpy`조차 안 깔려서 `ModuleNotFoundError`가 난 걸 발견 — PyKoSpacing의 `setup.py`를 직접 열어보니 `platform.system() == 'Darwin'`이면 의존성 목록을 아예 빈 리스트로 반환하도록 하드코딩돼 있었음(Apple Silicon에서 TensorFlow 설치가 까다로운 걸 우회하려는 의도로 추정). 그래서 macOS에서는 pip이 pykospacing 패키지만 설치하고 numpy/tensorflow는 하나도 안 받아옴 — 리눅스 환경에서 먼저 테스트했을 땐 멀쩡히 다 설치돼서 못 잡아낸 플랫폼별 차이. `requirements.txt`에 `numpy`/`tensorflow`를 명시적으로 따로 선언해서 해결. "서드파티 패키지의 설치 스크립트가 실행 중인 OS에 따라 다르게 동작할 수 있다"는 걸 실제로 겪은 사례로 남겨둠.

(기능 추가) "엉뚱한 결과만 막는" 걸로는 부족하고 "띄어쓰기 없이도 원곡을 찾아줘야 한다"는 요청으로, 한국어 띄어쓰기 교정 라이브러리를 붙임. 후보로 PyKoSpacing(사전 학습된 딥러닝 모델, TensorFlow 의존)과 soyspacing(가벼운 휴리스틱, 하지만 사전 학습 모델을 안 주고 직접 코퍼스로 학습해야 함)을 비교 — soyspacing은 즉시 쓸 수 있는 모델이 없어서 이번 스코프에 안 맞았고, PyKoSpacing은 설치 용량이 크지만(TensorFlow 포함 약 300MB) 바로 쓸 수 있는 사전 학습 모델을 제공해서 선택. 실제로 설치해서 "꽃이피고지듯이" → "꽃이 피고 지듯이"로 정확히 교정되는 것 확인(모델 로딩 4.2초, 교정 자체는 문장당 30ms 이하 — 로딩은 앱 시작 시 한 번만 하면 되니 요청마다 느려지진 않음). `/search`에서 아티스트/곡 둘 다 하나도 못 찾았고 띄어쓰기가 없는 한글 검색어일 때만(정확한 조건 우선, 없을 때만 넓히는 기존 폴백 원칙) 교정된 문자열로 한 번 더 재시도하도록 추가. 이미 정상 검색되는 짧은 단어(예: "아이유", "뉴진스")는 모델이 그대로 안 건드리는 것도 확인함. 모델 로드 실패 시(패키지 미설치 등) 서비스 전체가 죽지 않고 교정 없이 원래대로 동작하도록 예외 처리. 캐시 키 `search:v3` → `search:v4` 버전업. PyPI에 없는 패키지라 `requirements.txt`에 git URL로 등록 — 설치 용량이 커서 `pip install -r requirements.txt` 시간이 평소보다 오래 걸릴 수 있음(정상).

---

## 46. "다음 트랙" 큐가 완전히 무관한 장르로 채워지는 문제 — 동명이인 아티스트 오매칭

**뭘 했나**: 검색 띄어쓰기 교정(§45)으로 찾은 "꽃이 피고 지듯이"(Hanz 버전, 싱어게인 커버)를 재생했더니, "다음 트랙" 큐가 엑소/지드래곤/비스트처럼 발라드/사극 OST 느낌과 전혀 안 맞는 댄스/아이돌 곡으로 채워지는 걸 발견. `/similar-tracks`가 유사 아티스트를 조회할 때, 지금 재생 중인 아티스트 이름 대신 "같은 제목의 다른 버전 중 가장 많이 들은 아티스트"를 찾아서 그 아티스트 기준으로 조회하도록 수정.

**왜 이렇게 됐나**: Last.fm에 직접 물어봐서 확인함 — `artist.getSimilar(artist="Hanz")`가 실제로는 노르웨이 출신 칠합(chillhop) 프로듀서 "Hanz"(청취자 12만 6천명, Nasty Jazz 크루)의 유사 아티스트를 돌려주고 있었음. 싱어게인에서 이 곡을 리메이크한 Hanz(이 트랙 자체 청취자 69명)와는 이름만 같은 완전히 다른 사람인데, Last.fm이 "Hanz"라는 이름 자체를 그 노르웨이 아티스트 프로필로 정규화해버려서 벌어진 일. 처음엔 "청취자 수 격차로 거른다"/"장르 태그로 대조한다" 두 방법을 고려했는데 둘 다 실측해보니 안 통했음 — (1) 원래 인기 아티스트도 "아티스트 전체 청취자"가 "곡 하나 청취자"보다 수백~수천 배 많은 게 정상이라 그 격차 자체가 오매칭 신호가 안 됨(이 기준으로 걸렀으면 정상적인 경우도 대량으로 오탐났을 것), (2) 이 트랙 자체엔 `track.getTopTags`로 확인한 결과 태그 데이터가 아예 없어서(청취자가 너무 적어 아무도 안 남김) 장르 대조 근거 자체가 없었음. 대신 "이 노래의 원곡/더 알려진 버전 아티스트로 조회해보면 어떠냐"는 아이디어로 `artist.getSimilar("조승우")`를 직접 확인해보니 오만석/박은태/최정원/홍광호 같은 실제 뮤지컬 배우들이 정확하게 나옴(조승우가 뮤지컬 배우 출신이라 장르적으로 딱 맞음) — 커버 버전 아티스트 대신 원곡/대표 버전 아티스트의 데이터를 빌려 쓰는 게 훨씬 신뢰할 만하다는 게 확인됨.

**어떻게 동작하나**: 트랙 기반 유사곡(`track.getSimilar`)이 부족해서 아티스트 단위로 보충하기 직전에, `track.search(track=track)`(아티스트 제한 없이 같은 제목 전체 검색)로 이 곡의 다른 버전들을 모으고, 그중 청취자 수가 가장 많은 버전의 아티스트를 찾음. 그 아티스트가 현재 재생 중인 아티스트와 다르면(=더 잘 알려진 다른 버전이 있으면) `artist.getSimilar`를 그 아티스트 이름으로 호출 — 없으면 기존대로 원래 아티스트 그대로 진행. 대표 버전 조회 자체가 실패해도(네트워크 오류 등) 예외를 삼키고 원래 아티스트로 진행해서 기능 전체가 죽지 않게 함. 캐시 키 `similar-tracks:v2` → `v3` 버전업.

**알아둘 한계**: (1) 완벽한 해법은 아님 — 같은 제목인데 완전히 다른 곡(흔한 제목, 예: "사랑", "봄")인 경우 엉뚱한 아티스트로 바뀔 위험이 이론적으로 있음. 다만 "청취자 수가 가장 많은 버전"을 고르는 거라, 실제로 그 정도로 흔한 제목이 우연히 이 필터를 통과할 확률은 낮다고 판단. (2) 원곡조차 Last.fm 데이터가 빈약한 경우(둘 다 무명)엔 여전히 도움이 안 됨 — 이건 Last.fm 자체의 데이터 한계라 API를 바꿔도 해결 안 됨(유튜브 공식 API의 "관련 영상" 기능은 2023년에 폐지됐고, 비공식으로 우회하는 건 스크래핑이라 배제).

**면접에서 말할 포인트**:
- "겉보기 그럴듯한 해법을 실측으로 기각한 경험" -> 청취자 수 격차/장르 태그 두 가지 필터링 아이디어가 이론상 그럴듯해 보였지만, 실제 API 응답으로 검증해보니 둘 다 정상 케이스와 오매칭 케이스를 구분 못 한다는 걸 확인하고 폐기, 다른 접근(대표 버전 치환)으로 전환한 과정을 설명할 수 있음 — "될 것 같다"가 아니라 "실제로 되는지 데이터로 확인하고 결정한다"는 원칙.
- "외부 데이터의 엔티티 중복/오매칭 문제를 어떻게 다루나" -> 동명이인 문제는 크라우드소싱 음악 메타데이터에서 흔한 데이터 품질 이슈이고, 이름 문자열만으로 조회하는 API는 이런 리스크가 항상 있다는 것, 그리고 "더 신뢰할 만한 참조점(원곡)으로 우회하는" 실용적 해법을 설명할 수 있음.

---

## 47. "다음 트랙" 큐가 곡이 바뀔 때마다 통째로 교체되던 문제 — 전량 교체 대신 이어붙이기로 전환

**뭘 했나**: 큐 안에 있는 곡을 눌러서 재생했더니, 아직 안 들은 나머지 대기곡들이 전부 사라지고 방금 재생한 곡 기준의 새 추천 목록으로 큐 전체가 바뀌는 걸 발견. `frontend/app/page.tsx`의 `refreshQueue()`가 매번 `setQueue(data.tracks)`로 큐를 통째로 덮어쓰던 걸, 기존 대기곡을 유지한 채 새 추천을 뒤에 이어붙이는 방식으로 바꿈.

**왜 이렇게 했나**: 라이브 확인 중 큐 안의 곡을 클릭해서 재생했더니 화면이 완전히 무관한 장르로 바뀌는 걸 보고 "이건 노래를 아예 바꾸는것보단 노래수를 늘리고 안바뀌는게 나을거 같은데"라는 피드백을 받음 — 지금까지 듣지도 않은, 나중에 들으려고 큐에 남아있던 곡들이 "다음 곡이 재생 시작됐다"는 이유만으로 통째로 날아가는 게 불필요한 손실이라는 지적. 큐 새로고침의 원래 목적(방금 재생된 곡과 비슷한 곡을 계속 채워 넣는 "라디오" 방식)은 그대로 두되, 이미 쌓여있는 후보를 버리지 않고 누적시키는 쪽으로 방향을 바꿈.

**어떻게 동작하나**:
- `refreshQueue(artist, name)` 내부를 `setQueue(prev => ...)` 형태의 함수형 업데이트로 바꿈 (이전 상태를 인자로 받아 새 상태를 계산하는 방식이라, 비동기 호출 여러 개가 겹쳐도 최신 `queue` 상태 기준으로 안전하게 계산됨).
- 아티스트+곡명을 소문자로 바꾸고 공백을 정리한 문자열을 dedup 키로 씀. 이 키로 (1) 방금 재생을 시작한 곡을 이전 큐에서 제외(더 이상 "다음 곡"이 아니므로), (2) 백엔드가 새로 받아온 유사곡 중 이미 큐에 있는 곡은 건너뜀 — 같은 곡이 중복으로 쌓이지 않게 함.
- 남은 이전 대기곡 뒤에 새로 받아온 추천을 이어붙이고, 새로 정한 `MAX_QUEUE_SIZE = 40`을 넘으면 뒤쪽(가장 최근에 추가된, 아직 검증 안 된 항목)부터 잘라냄 — 앞쪽(곧 재생될 순서)은 그대로 보존하면서 무한정 쌓이는 것만 막음.
- API 호출이 실패했을 때도 기존엔 `setQueue([])`로 큐를 통째로 비웠는데, 이제는 `.catch(() => {})`로 그냥 아무것도 안 함 — 한 번의 유사곡 조회 실패가 이미 화면에 떠 있던 대기곡 전체를 지워버리면 안 된다는 원칙을 에러 경로에도 동일하게 적용.
- 이 로직은 곡이 끝까지 재생되어 자동으로 다음 곡으로 넘어가는 경로(`handleTrackEnded` → `handlePlay`)와 사용자가 큐 안의 곡을 직접 클릭하는 경로(`onSelectQueueTrack`) 둘 다에서 적용됨. 재생목록 이어재생/반복재생에 쓰는 "수동 큐" 모드(`manualNext` 옵션)는 애초에 `refreshQueue`를 안 타는 별개 경로라 이번 변경의 영향을 받지 않음.

**(추가 수정) 이어붙이기를 무조건 적용하면 장르가 섞이는 문제 발견**: 위 수정을 배포하고 사용자가 라이브 확인하던 중, 빅뱅/GD&TOP 계열 곡을 듣다가 완전히 다른 곡("꽃이 피고 지듯이", Hanz)을 다시 재생했는데 "다음 트랙" 큐에 여전히 빅뱅/지드래곤/SHINee/4minute 같은 무관한 후보가 40곡까지 쌓여있는 걸 발견 — "이어붙이기가 그 전에 들었던 노래랑 현재 다른 노래의 다음 트랙을 붙여놓는 거면 장르에 상관없이 붙는 거 아니냐"는 정확한 지적을 받음. 원인은 `refreshQueue`가 "방금 재생 시작한 곡"만 이전 큐에서 제외할 뿐, 나머지 항목이 지금 재생 중인 곡과 얼마나 관련 있는지는 전혀 안 따지고 무조건 유지했기 때문 — 검색으로 완전히 다른 곡을 틀어도 예전 곡 기준으로 쌓인 후보가 계속 눌러붙어 있었음.

이걸 고치려고 "이어붙이기"를 적용하는 조건을 좁힘: `handlePlay`에 `fromQueue` 플래그를 추가해서, 지금 큐 안에 이미 떠 있던 곡이 재생으로 이어지는 경우(자동 이어재생 `handleTrackEnded`, 큐 목록 직접 클릭 `onSelectQueueTrack`)에만 `refreshQueue(artist, name, { append: true })`로 이어붙이고, 그 외(검색 결과, 차트, 즐겨찾기, 재생목록 상세 화면에서 곡을 고르는 것 등 큐 바깥에서 새 곡을 선택하는 모든 경로)는 `append: false`로 예전처럼 큐를 통째로 새로 채움. 실패 시 동작도 append 여부에 따라 나뉨 — append 모드 실패는 기존 큐 유지(no-op), 비append 모드 실패는 `setQueue([])`로 비움(새 곡 맥락인데 추천을 못 받았으면 옛 맥락 후보를 보여주는 게 오히려 혼란스러우므로).

핵심 판단 기준: "같은 라디오 세션이 자연스럽게 이어지는 중인가(큐 안에서 다음 곡으로 넘어가는 것)"와 "완전히 새로운 재생 맥락이 시작됐는가(검색/차트 등에서 다른 곡을 고르는 것)"를 구분해야, "후보를 잃고 싶지 않다"는 원래 요청과 "장르가 섞이면 안 된다"는 요구를 동시에 만족시킬 수 있음.

**알아둘 한계**: (1) 아직 브라우저 라이브 확인 전 — tsc 통과만 확인된 상태. (2) 사용자가 스크린샷으로 같이 지적했던 "새로 추가된 곡의 커버 이미지가 안 뜨는" 현상은 처음엔 무명 아티스트의 이미지 데이터 부실 문제로 추정했었는데, 나중에(§48) 실제 원인이 iTunes API 호출량 문제였던 것으로 재확인·수정됨 — 자세한 내용은 §48 참고. (3) `MAX_QUEUE_SIZE = 40`은 임의로 정한 값이라, 실사용 중 너무 크거나 작다고 느껴지면 조정 필요. (4) "큐 안에서 이어지는 중"이라고 해서 장르가 100% 일관되는 건 아님 — 큐 자체가 처음부터 백엔드의 아티스트 유사도 보충 로직([[학습노트 46번]])으로 채워진 것이라, 그쪽에서 장르가 어긋나면 이어붙이기도 그대로 어긋난 채 누적됨. 이번 수정은 "관련 없는 옛 맥락이 안 섞이게" 막는 것이지, 큐 자체의 장르 정확도를 보장하진 않음.

**면접에서 말할 포인트**:
- "상태를 갱신할 때 통째로 교체할지 병합할지 어떻게 판단하나" -> 처음엔 "사용자가 이미 인지하고 있는, 아직 소비 안 한 데이터를 잃지 않아야 한다"는 이유로 무조건 병합했다가, 실사용 피드백으로 "병합이 무관한 맥락까지 섞어버릴 수 있다"는 반례를 발견하고 조건을 추가한 경험 — 병합/교체는 "같은 세션의 연속"인지 "새 세션의 시작"인지에 따라 갈려야 한다는 걸, 한 번의 잘못된 일반화를 거쳐 구체적으로 배움.
- "실패 시 상태를 어떻게 처리하나" -> 기존엔 실패하면 빈 배열로 초기화했는데, 이게 "0건"과 "아직 이 요청 결과를 못 받음"을 구분 못 해서 화면에 있던 유효한 데이터까지 지워버리는 부작용이 있었다는 걸 짚고, 이제는 같은 실패라도 "이어붙이기 중이었는지"에 따라 안전한 기본값이 달라진다는(맥락이 이어지면 유지, 맥락이 바뀌면 비움) 더 세밀한 기준을 설명할 수 있음.

---

## 48. "다음 트랙" 큐 이미지가 뒤로 갈수록 안 뜨는 문제 + 큐 초기 개수 늘림 (+ "다시 듣기" 정렬 기준 확인)

**뭘 했나**: 세 가지를 다룸 — (1) `backend/main.py`의 `resolve_track_image()`에 Redis 캐싱을 추가하고, `/similar-tracks`에서 이미지 조회(iTunes 호출) 시점을 "후보를 모으는 즉시"에서 "가사 필터를 통과해 실제로 큐에 남을 곡만 확정된 뒤"로 미룸. (2) `SIMILAR_TRACKS_FINAL_COUNT`를 15 → 25로 올려서 큐 초기 개수를 늘림. (3) "다시 듣기" 섹션이 방금 재생한 곡을 안 보여주는 게 버그인지 확인 — 코드를 읽어보니 의도된 동작이었음(아래 설명).

**왜 이렇게 했나**: 사용자가 "다음 트랙" 큐를 스크롤하며 스크린샷을 찍어 보여줬는데, 앞쪽 곡들(1~11번대)은 앨범 커버가 뜨는데 뒤쪽 곡들(15번대 이후)은 전부 음표 플레이스홀더로 비어있는 패턴을 발견 — "이미지가 사라지는 게 노래가 많아서 그런 거냐"는 질문을 받음. 코드를 다시 보니 `/similar-tracks`가 최종적으로 25곡(구 15곡)만 반환하면서도, 그 뒤에 가사 필터로 걸러질 최대 40개 원시 후보 전부에 대해 미리 이미지를 조회하고 있었음 — 즉 실제로 쓰이지도 않을 나머지 15곡어치 iTunes 호출이 낭비되고 있었고, 한 번의 요청 안에서 순차적으로 최대 40번 iTunes Search API를 부르다 보니(공식 문서에 없는 비공식 API라 요청 빈도 제한 여부가 불명확하지만) 뒤쪽 호출부터 응답이 비거나 실패하는 패턴이 사용자가 관찰한 것과 정확히 일치함. 게다가 같은 인기곡이 검색/차트/추천/무드 탐색/다음 트랙 큐 등 여러 엔드포인트에서 반복 조회되는데도 매번 새로 iTunes를 불렀던 것도 낭비 — 캐싱이 없다는 것 자체가 근본 원인 중 하나.

큐 초기 개수를 늘린 건 별개 피드백 — "처음 곡 들어가면 다음 트랙 목록이 짧은데(15개), 큐 안의 곡을 하나 클릭하면 §47 이어붙이기 때문에 갑자기 29개, 40개로 확 늘어나는 게 부자연스럽다, 처음부터 많이 나오게 하면 안 되냐"는 지적. 다만 개수를 무작정 40까지 올리면 위 이미지 리졸브 비용 문제가 그대로 재현되므로, 이번 지연 리졸브 최적화로 확보한 여유분(40 → 25회 호출)을 여기 쓰는 방식으로 절충함.

**어떻게 동작하나**:
- `resolve_track_image(artist_name, track_name, images)` 진입 시 `track-image:{artist}:{track}` 캐시 키로 Redis를 먼저 확인하고, 있으면(이미지가 없었던 경우 `{"url": None}`으로도 캐싱되어 있음) iTunes를 아예 안 부르고 캐시값을 반환. 없으면 기존처럼 iTunes → Last.fm 순으로 조회하고, 그 결과를 30일 TTL로 캐싱. 이 함수는 검색/차트/추천/무드/다음 트랙 큐 등 이미지가 필요한 모든 엔드포인트가 공유하는 단일 지점이라, 한 번 여기 캐싱을 넣은 게 전체 엔드포인트에 동시에 적용됨.
- `/similar-tracks`의 `add_track()`은 이제 이미지를 바로 조회하지 않고, Last.fm이 준 원본 이미지 배열만 `_lastfm_images`라는 임시 키에 담아둠(`"image": None`). 가사 필터(`_filter_tracks_with_lyrics`)로 최대 40개 후보를 25개로 추린 "다음" 시점에, 그 25개에 대해서만 `resolve_track_image()`를 호출해 `image`를 채움. 임시 키(`_lastfm_images`)는 다 쓰고 나서 `pop`으로 제거해 응답에 안 섞이게 함.
- 위 두 변경이 합쳐지면, 한 번의 `/similar-tracks` 요청에서 iTunes를 부르는 최대 횟수가 40회(캐싱 전) → 25회(지연 리졸브만) → 사실상 0회에 가까움(캐싱까지 더해지면, 인기곡일수록 이미 캐시에 있을 가능성이 높음)로 줄어듦.
- `SIMILAR_TRACKS_FINAL_COUNT`를 15 → 25로 올림 — `SIMILAR_TRACKS_RAW_CAP`(원시 후보 상한, 40)은 그대로 둬서 가사 필터링용 버퍼는 유지.
- **"다시 듣기" 섹션 관련**: 코드를 확인한 결과 버그가 아니라 의도된 정렬 기준 차이였음. `playlist-service`의 `FavoriteTrackService.list()`가 `findBySessionIdOrderByCreatedAtDesc`를 쓰기 때문에, "다시 듣기" 섹션은 "최근에 재생한 곡" 순이 아니라 "최근에 즐겨찾기(별표)한 곡" 순으로 정렬됨. 방금 재생한 곡이라도 즐겨찾기를 새로 누른 게 아니라 예전에 이미 즐겨찾기해둔 곡이면, 그 즐겨찾기 시점 기준으로 순서가 정해지기 때문에 맨 앞에 나타나지 않음(다른 곡을 더 최근에 즐겨찾기했다면 그게 앞에 옴). 새로 즐겨찾기를 누르면 프론트에서 `setFavoriteTracks((prev) => [saved, ...prev])`로 즉시 맨 앞에 반영되는 것도 확인함(재생과는 무관하게, "즐겨찾기 행위" 자체가 트리거).

**알아둘 한계**: (1) 아직 브라우저 라이브 확인 전 — ast.parse만 확인된 상태. (2) 캐싱의 30일 TTL 동안은 iTunes에 실제로 그 곡의 아트워크가 새로 등록되거나 바뀌어도 반영이 안 됨 — 앨범 커버는 거의 안 바뀐다는 전제로 감수한 트레이드오프. (3) 이미지가 여전히 안 뜨는 곡이 남아있을 수 있음 — 그건 iTunes/Last.fm 양쪽 다 그 곡 자체의 데이터가 없는 순수한 데이터 부재 케이스(레퍼토리 밖 인디/커버 음원 등)로, 이번 수정 범위 밖. (4) 캐싱을 추가한 지점(`resolve_track_image`)이 검색(`_build_track_results`)에서도 쓰이므로 검색 결과 이미지도 같이 빨라질 것으로 예상되지만 직접 확인은 안 함.

**(추가 수정) 캐시 키 버전을 안 올려서 위 수정이 반영 안 됐던 실수**: 사용자가 백엔드를 재시작하고 다시 확인했는데도 여전히 큐가 15개로 시작하고, 큐 안 곡을 클릭하면 29개(15+15-1)로 늘어나며, 15번째부터 이미지가 안 뜨는 걸 발견 — 정확히 이 수정 이전(구 `SIMILAR_TRACKS_FINAL_COUNT=15`, 이미지 즉시 조회)의 동작 그대로였음. 원인은 `/similar-tracks`의 캐시 키(`similar-tracks:v3:...`)를 이번 변경(최종 개수·이미지 리졸브 시점 변경)에서 그대로 뒀던 것 — 코드는 새 로직으로 바뀌었어도, 이미 v3 키로 캐싱돼 있던 옛 응답(15곡, 이미지 즉시 조회분)이 캐시가 만료(기본 TTL 1시간)되기 전까지 계속 그대로 나가고 있었음. 이 프로젝트에서 "캐시가 참조하는 로직이 바뀌면 캐시 키 버전도 같이 올려야 한다"는 원칙을 §36, §43, §44, §45, §46에서도 반복해서 확인했는데, 이번에도 똑같은 실수를 또 함 — 캐시 키를 `v3` → `v4`로 올려서 수정. 앞으로는 `/similar-tracks`, `/recommend`처럼 응답을 캐싱하는 엔드포인트의 로직을 바꿀 때 "캐시 키 버전을 올렸는지"를 코드 수정의 마지막 체크리스트 항목으로 명시적으로 확인할 것.

**면접에서 말할 포인트**:
- "성능 문제를 어떻게 진단했나" -> 사용자가 준 스크린샷에서 "이미지가 있는 항목과 없는 항목의 위치가 큐 안에서 뒤섞이지 않고 명확히 앞/뒤로 갈린다"는 패턴을 먼저 확인하고, 이게 "특정 아티스트의 데이터 부재"라면 설명이 안 되는 규칙성(MIYEON처럼 유명 아티스트도 뒤쪽이면 이미지가 없었음)이라는 걸 근거로 "아티스트 데이터 문제"가 아니라 "요청 순서/횟수에 의존하는 문제"라는 가설을 세우고, 코드에서 실제로 순서대로 최대 40번 동기 호출하는 지점을 찾아 확인한 과정을 설명할 수 있음.
- "외부 API를 감싸는 함수에 캐싱을 넣을 때 어디에 넣을지 어떻게 정하나" -> 여러 엔드포인트가 각자 이미지 조회 로직을 따로 구현했다면 캐싱도 각각 넣어야 했겠지만, 이미 `resolve_track_image()`라는 공용 함수로 정리돼 있었기 때문에 그 안에 한 번만 캐싱을 넣는 것으로 전체 엔드포인트에 동시 적용시킬 수 있었던 경험 — 공용 함수로 묶어두는 것 자체가 나중에 이런 횡단 관심사(캐싱, 로깅 등)를 붙이기 쉽게 만든다는 걸 실감함.
- "무조건 다 캐싱/미리 계산하지 않고 필요한 만큼만 지연 계산하는 판단 기준" -> "가사 필터를 통과 못 할 수도 있는 후보"에 미리 비용을 들이는 게 낭비라는 걸 알면서도 처음엔 구현 순서상 먼저 넣어뒀던 걸, 실사용 피드백으로 뒤늦게 알아차린 사례 — 필터링이 있는 파이프라인에서는 "필터 이후에만 필요한 비용"을 필터 이전에 계산해두고 있지 않은지 점검하는 습관을 짚어 설명할 수 있음.

---

## 49. "다시 듣기" 홈 섹션이 사이드바 "즐겨찾기"와 데이터가 겹치던 문제 — 진짜 최근 재생 기록으로 교체

**뭘 했나**: 홈 화면 메인의 "다시 듣기" 섹션이 즐겨찾기(favoriteTracks)를 그대로 보여주던 걸, 실제로 재생을 시작한 곡을 기록하는 새 데이터(recentPlays)로 교체함. 백엔드에 `POST/GET /recent-plays`(Redis 세션별 목록, `/recent-searches`와 같은 패턴) 추가, 프론트는 유튜브 플레이어가 실제로 재생을 시작한 시점(`handleActuallyPlaying`)에 기록을 남기고, 홈 화면 "다시 듣기" 섹션은 이제 이 기록을 보여줌.

**왜 이렇게 했나**: 사용자가 사이드바 "즐겨찾기" 목록 스크린샷을 보여주면서 "다시 듣기는 최근에 들었던 노래가 나오는 게 맞지 않냐, 즐겨찾기는 오른쪽에 따로 나오게 했는데 다시 듣기도 즐겨찾기한 애들이 나오면 중복되지 않냐"고 지적함. 코드를 보니 실제로 "다시 듣기" 섹션([[학습노트 26번]] 추가 당시)이 "유튜브 뮤직 홈 화면처럼 즐겨찾기를 메인에도 노출하자"는 의도로 즐겨찾기 데이터를 그대로 재사용하고 있었음 — 당시엔 사이드바에만 즐겨찾기가 있었으니 "메인에도 노출"이 새로운 정보였지만, 이후 사이드바에 "즐겨찾기"라는 이름의 전용 섹션이 명확히 자리 잡으면서 두 섹션이 완전히 같은 데이터를 다른 이름으로 두 번 보여주는 상태가 되어 있었음. "다시 듣기"라는 이름 자체가 "최근 재생"을 뜻하므로, 이름에 맞는 실제 데이터로 바꾸는 게 맞다고 판단.

**어떻게 동작하나**:
- `cache.py`에 `push_recent_play(session_id, artist, name, url, image)` / `get_recent_plays(session_id)` 추가 — 최근 검색(`push_search_history`/`get_search_history`)과 동일한 패턴: Redis 세션별 리스트, 같은 곡 재생 시 기존 항목을 지우고 맨 앞에 다시 넣어 "최신순 + 중복 없음" 유지, 최대 20개, TTL 90일(즐겨찾기보다 짧게 — 오래 방문 안 하면 자연 소멸). 분석용 이벤트 로그(MariaDB `play_track` 이벤트)를 그대로 조회하지 않고 별도 Redis 목록을 둔 이유는 최근 검색과 동일 — 실시간 UI 조회용 데이터와 배치/분석 원본 로그를 같은 저장소에 안 둔다는 이 프로젝트의 일관된 원칙.
- 백엔드에 `POST /recent-plays`(기록, body: artist/track/url/image)와 `GET /recent-plays`(조회) 추가. 세션ID가 없으면 기록/조회 둘 다 빈 값으로 조용히 처리(로그인 없는 프로젝트라 세션ID 없이는 개인화 불가능한 게 당연한 제약).
- 프론트 `handleActuallyPlaying`(클릭 시점이 아니라 유튜브 플레이어가 실제로 재생을 시작한 시점에 호출되는 기존 훅 — 즐겨찾기 `markOpened`도 이미 이 시점을 씀)에 `recordRecentPlay` 호출을 추가. 화면 반응성을 위해 `setRecentPlays`로 로컬 상태도 즉시 갱신(맨 앞에 추가, 기존 중복 제거)하고, 서버 기록은 실패해도 화면에 영향 없게 `.catch(() => {})`로 흘려보냄(다른 낙관적 업데이트들과 같은 패턴).
- 홈 화면 "다시 듣기" 섹션의 데이터 소스를 `favoriteTracksAsTrack`(삭제됨)에서 `recentPlaysAsTrack`(recentPlays를 Track 형태로 변환한 것)으로 교체. 사이드바 "즐겨찾기" 목록은 원래부터 `favoriteTracks`를 직접 썼지 `favoriteTracksAsTrack`을 쓴 적이 없어서 영향 없음.

**알아둘 한계**: (1) 아직 브라우저 라이브 확인 전 — ast.parse/tsc만 확인된 상태. (2) 새로 배포된 시점부터의 재생만 기록되므로, 처음엔 "다시 듣기" 섹션이 비어있을 것 — 즐겨찾기처럼 예전 데이터를 소급 반영할 방법이 없음(애초에 재생 기록 자체를 안 남기고 있었으니 당연함). (3) 세션ID 기반이라 브라우저를 바꾸거나 쿠키/로컬스토리지를 지우면 기록이 끊김 — 로그인이 없는 이 프로젝트의 공통 제약([[학습노트 21번]] 등과 동일).

**면접에서 말할 포인트**:
- "기존 기능이 나중에 다른 기능과 이름/의미가 겹치게 되는 걸 어떻게 발견하고 정리하나" -> "다시 듣기"가 처음 추가될 땐 "즐겨찾기를 메인에도 노출"이 유일한 목적이라 즐겨찾기 데이터를 재사용하는 게 합리적인 선택이었지만, 이후 사이드바에 "즐겨찾기"라는 이름의 명확한 전용 섹션이 생기면서 두 섹션의 역할이 겹치게 된 걸 실사용 피드백으로 알아챈 경험 — 기능을 추가할 당시엔 맞았던 설계가 다른 기능이 늘어나면서 재검토가 필요해지는 사례로 설명 가능.
- "실시간 UI용 목록과 분석용 이벤트 로그를 왜 분리해서 관리하나" -> 이미 있던 최근 검색 기능과 똑같은 패턴을 재사용한 것 — 분석 파이프라인(dbt/AARRR)은 원본 이벤트가 안 지워지고 쌓여야 하는데, UI 편의 기능(개별 삭제, 순서 유지)은 사용자 조작으로 자유롭게 바뀌어야 해서 같은 저장소에 두면 요구사항이 충돌한다는 걸 설명할 수 있음.

---

## 50. 쓰레기 아티스트 필터가 키워드 없는 항목을 못 거르던 문제 — 청취자 수만으로 판단하도록 단순화

**뭘 했나**: `_is_junk_artist_match()`(§45에서 추가한 "검색된 아티스트" 쓰레기 필터)가 "청취자 5명 이하 AND 이름에 lyrics/가사/cover/mv 같은 키워드 포함"일 때만 걸러내던 걸, 이름 키워드 조건을 없애고 "청취자 `JUNK_ARTIST_LISTENER_THRESHOLD`(5)명 이하면 무조건 제외"로 단순화. `/search` 캐시 키 `v4` → `v5`로 동반 조정.

**왜 이렇게 했나**: "꽃이 피고 지듯이" 검색 결과의 "검색된 아티스트" 섹션에 "국보급 허스키 보이스로 몰입시키는😨 8호 가수의 〈꽃이 피고 지듯이〉♪"(청취자 2명)처럼 방송/영상 설명문이 통째로 아티스트 이름으로 등록된 항목이 그대로 노출되는 걸 사용자가 발견 — §45 필터가 요구하는 키워드(lyrics/cover/mv 등)가 이 이름엔 하나도 없어서 안 걸러진 것. 사용자가 "이 섹션이 애초에 필요한가, 이미 검색된 곡에 다 나오는데"라고 근본적인 질문을 던짐 — 실제로 확인해보니 "검색된 아티스트" 카드를 클릭하면 그 아티스트 기준 추천(`/recommend`)이 뜨는, 트랙 검색 결과와는 다른 기능이라 완전히 무의미하진 않지만(동명이인 아티스트가 여러 명일 때 고르는 용도, 실제 아티스트명으로 검색했을 때), "꽃이피고지듯이"처럼 문장/제목형 검색어에서는 애초에 진짜 아티스트가 나올 수가 없는 상황이라 이 섹션이 쓰레기만 보여주는 게 사실상 필연적이었음. 완전히 없애기보다는, 실사용 중 관찰한 패턴("Last.fm에 진짜 등록된 아티스트가 청취자 5명 이하인 경우는 사실상 없었고, 청취자 극소수 항목은 거의 다 스크로블 오매칭이었다")을 근거로 필터 자체를 더 단순하고 강하게 만드는 쪽을 선택.

**어떻게 동작하나**: `_is_junk_artist_match(item)`이 이제 `listeners <= 5` 하나만 검사함(이름 키워드 검사 제거). `_build_artist_results()`에서 이 함수로 걸러진 뒤 결과에 포함됨. `/search`의 캐시 키를 `v5`로 올려서, 예전 필터 기준으로 캐싱된 응답(쓰레기 포함)이 캐시 만료 전까지 그대로 나가는 걸 방지.

**알아둘 한계**: (1) 아직 브라우저 라이브 확인 전 — ast.parse만 확인된 상태. (2) 매우 드물게, 방금 Last.fm에 등록된 진짜 신인 아티스트가 청취자 5명 이하인 시점에 검색되면 같이 걸러질 수 있음 — 실사용 중 이런 오탐이 실제로 관찰되면 임계값을 조정하거나 다시 이름 기반 신호를 보조적으로 추가할 것. (3) "검색된 아티스트" 섹션 자체를 없애거나, 트랙 결과가 이미 있을 때 숨기는 등 더 근본적인 UX 변경은 이번엔 하지 않음 — 필터 정확도를 높이는 선에서 대응.

**면접에서 말할 포인트**:
- "필터 조건을 좁힐지 넓힐지 어떻게 판단하나" -> 처음엔 "정상 아티스트를 잘못 거르면 안 된다"는 우려로 조건을 여러 개 AND로 좁게 걸었는데(청취자 수 + 키워드), 실사용 데이터로 "그 부가 조건이 실제로 막아주는 오탐 사례는 못 봤고, 오히려 조건을 하나 더 요구해서 진짜 쓰레기를 통과시키는 부작용만 냈다"는 걸 확인하고 조건을 줄인 경험 — 안전하게 보이는 조건 추가가 항상 더 정확한 필터를 만드는 게 아니라는 걸 실사용으로 검증.
- "기능 자체가 필요한지 의심될 때 어떻게 판단하나" -> 사용자가 "이 섹션이 왜 필요하냐"고 물었을 때, 코드를 직접 추적해서 "클릭 시 실제로 다른 데이터(아티스트 추천)를 보여준다"는 걸 확인한 뒤 "완전히 무의미하진 않지만, 이 검색어 유형에서는 구조적으로 항상 쓰레기만 보여준다"는 걸 구분해서 설명한 것 — 기능의 존재 이유와 특정 상황에서의 무용성은 다른 문제라는 걸 짚어 설명 가능.

---

## 51. 이미지 조회 실패를 30일씩 캐싱해버려서 메이저 아티스트 곡들도 이미지가 안 뜨던 버그

**뭘 했나**: `resolve_track_image()`의 Redis 캐싱(§48에서 추가)이 iTunes 조회에 실패한 경우(`image=None`)까지 성공한 경우와 똑같이 30일 TTL로 캐싱하고 있던 걸, 실패한 경우엔 30분만 캐싱하도록 분리. 캐시 키를 `track-image:v2`로 올려서 기존에 이미 잘못 캐싱된("이미지 없음"으로 굳어버린) 항목들도 자동으로 초기화되게 함.

**왜 이렇게 했나**: 사용자가 싸이 "That That" 큐에서 BLACKPINK("How You Like That", "Kill This Love", "DDU-DU DDU-DU", "Pink Venom")와 Lisa, JISOO 솔로곡들이 전부 이미지 없이 음표 아이콘으로만 뜨는 걸 발견 — 마이너 아티스트 데이터 부재로는 설명이 안 되는 경우였음(BLACKPINK는 전세계에서 가장 많이 스트리밍되는 그룹 중 하나라 iTunes에 커버가 없을 리 없음). 코드를 다시 보니 §48에서 "이미지를 한 번 찾으면 30일 캐싱"을 넣으면서, `resolve_track_image()`가 성공(`image`가 실제 URL)이든 실패(`image=None`)든 구분 없이 똑같은 30일 TTL로 캐싱하고 있었다는 걸 발견함. iTunes Search API가 짧은 시간에 몰린 요청 중 일부를 일시적으로 실패시키는 건 §48에서 이미 확인한 사실인데, 그 "일시적 실패"의 결과(`None`)까지 30일 동안 "이 곡은 이미지가 없다"고 영구히(사실상) 확정 지어버리는 게 진짜 원인이었음 — 즉 §48이 "iTunes를 덜 부르게" 만드는 데는 성공했지만, 그 과정에서 실패까지 똑같이 오래 캐싱하는 새로운 버그를 만들어낸 것.

**어떻게 동작하나**: `TRACK_IMAGE_NEGATIVE_CACHE_TTL_SECONDS = 60 * 30`(30분)을 추가하고, `resolve_track_image()` 끝에서 `ttl = TRACK_IMAGE_CACHE_TTL_SECONDS if image else TRACK_IMAGE_NEGATIVE_CACHE_TTL_SECONDS`로 분기해서 `set_cached`에 넘김. 성공한 이미지는 여전히 30일 캐싱(앨범 커버는 안 바뀌니까)되지만, 실패는 30분 뒤 자동으로 다시 시도됨 — 그사이 iTunes 쪽 일시적 부하가 풀렸을 가능성이 높음. 캐시 키도 `track-image:{artist}:{track}` → `track-image:v2:{artist}:{track}`로 올려서, 이미 §48 배포 이후 잘못 캐싱된 "실패 30일" 항목들이 새 키 아래에서는 아예 없는 것으로 취급되어 바로 재조회됨(수동으로 `redis-cli`를 안 돌려도 됨).

**알아둘 한계**: (1) 아직 브라우저 라이브 확인 전 — ast.parse만 확인된 상태. (2) 30분이라는 값도 임의로 정한 것이라, 실사용 중 여전히 자주 실패한다면 더 줄이거나(재시도를 더 자주) iTunes 호출 자체에 재시도/백오프 로직을 추가하는 걸 고려할 수 있음(이번엔 그렇게까지는 안 함). (3) 이번 버그는 "캐싱을 추가하면서 성공/실패를 구분 안 한" 전형적인 실수라, 앞으로 실패할 수 있는 외부 API 호출에 캐싱을 넣을 때마다 "실패도 똑같이 오래 캐싱되고 있진 않은지"를 확인할 것.

**면접에서 말할 포인트**:
- "캐싱을 추가할 때 흔히 하는 실수가 뭔가" -> 이번처럼 "성공/실패를 구분하지 않고 똑같은 TTL로 캐싱"하는 게 대표적 — 실패까지 길게 캐싱하면 일시적 장애가 영구 장애처럼 보이는 결과를 낳는다는 걸 실제로 겪고 고친 경험. 캐싱 전략을 설계할 때 "이 값이 왜 이 TTL을 가져야 하는가"를 성공/실패 케이스별로 따로 따져봐야 한다는 원칙을 이 사례로 설명할 수 있음.
- "성능 최적화가 새로운 버그를 만들 수 있다는 걸 어떻게 겪었나" -> §48에서 "iTunes 호출을 줄이자"는 목적으로 캐싱을 넣었는데, 그 구현이 미묘하게 실패 케이스를 잘못 다뤄서 오히려 "한 번 실패하면 한 달 동안 계속 실패로 보이는" 더 나쁜 상황을 만들 뻔한 경험 — 최적화를 넣은 뒤에는 그 최적화 자체가 만들 수 있는 새로운 실패 모드를 한 번 더 점검해야 한다는 교훈으로 설명 가능.

---

## 52. 띄어쓰기 없는 영문 검색어("thatthat")도 §45와 같은 문제 — wordninja로 대칭 교정

**뭘 했나**: §45에서 한글 전용으로 만든 "띄어쓰기 없는 검색어 교정" 폴백을 영문에도 대칭으로 추가. `wordninja`(영어 단어 빈도 기반으로 이어붙은 문자열을 가장 그럴듯하게 쪼개는 순수 파이썬 라이브러리) 도입, `_correct_english_spacing()` 추가, `/search`의 재시도 조건을 "한글이면 PyKoSpacing, 아니면 wordninja"로 분기. `requirements.txt`에 `wordninja` 추가, 캐시 키 `v5` → `v6`.

**왜 이렇게 했나**: 라이브 검증 중 "That That"(싸이 & 슈가, 굉장히 유명한 곡)을 이미 재생해둔 상태에서 같은 곡을 다시 찾으려고 띄어쓰기 없이 "thatthat"으로 검색했는데, 실제 곡은 전혀 안 뜨고 "THOTTWAT"(청취자 83,122명이나 되는 완전히 다른 아티스트), "Gurnazar Chattha", "thaithai" 같이 우연히 글자가 비슷한 무관한 결과만 뜨는 걸 발견. §45에서 "꽃이피고지듯이"로 확인했던 것과 정확히 같은 실패 패턴(Last.fm의 검색이 붙어 쓴 문자열은 부분 문자열/편집 거리 기준으로 엉뚱하게 매칭한다는 것)이 한글에만 국한된 문제가 아니라 영문에도 똑같이 나타난다는 걸 실제 사례로 확인함.

**어떻게 동작하나**:
- `wordninja.split(text)`로 이어붙은 문자열을 단어 리스트로 쪼갠 뒤 공백으로 합침. `PyKoSpacing`과 달리 사전 학습된 딥러닝 모델이 아니라 영어 단어 빈도표 하나만 내장한 동적계획법 알고리즘이라 TensorFlow 같은 무거운 의존성이 없고 설치도 훨씬 가벼움 — `pip install wordninja`로 바로 됨(PyKoSpacing처럼 git URL이나 macOS 특수 처리 불필요).
- 결과 검증: `len(words) >= len(text)`면(예: 숫자/기호가 섞여서 한 글자씩 쪼개지는 등 비정상 분리) 버리고, 원래 문자열과 같으면(이미 온전한 한 단어라 쪼갤 필요 없었던 경우, 예: "dynamite") 그대로 버림 — `_correct_korean_spacing`과 동일한 가드.
- `/search`의 재시도 블록이 이제 `HANGUL_PATTERN.search(q)`로 한글/영문을 나눠서 각각 다른 교정 함수를 호출하지만, 재시도 로직(아티스트/트랙 재검색, 결과 조립) 자체는 공유함.
- 로컬에서 직접 테스트해 확인: `wordninja.split("thatthat")` → `['that', 'that']`, `"flowerdance"` → `['flower', 'dance']`, `"dynamite"`(이미 온전한 단어) → `['dynamite']`(가드에 걸려 교정 안 함, 의도대로).

**알아둘 한계**: (1) 아직 브라우저 라이브 확인 전 — ast.parse만 확인된 상태(로컬 파이썬 인터프리터로 `wordninja.split()` 동작 자체는 확인함). (2) `wordninja`는 영어 단어 빈도 기반이라 한국 아이돌 그룹명처럼 사전에 없는 고유명사가 섞이면(예: "bts", "iu") 예상 밖으로 쪼갤 수 있음 — 다만 이 폴백 자체가 "이미 아무 결과도 없을 때만" 시도되는 마지막 수단이라 큰 리스크는 아님. (3) §45와 마찬가지로 이 폴백에 걸렸다는 걸 프론트에 별도로 표시하지 않음(오타 교정과 달리 "띄어쓰기 교정됨" UI 문구 없음) — 필요하면 나중에 추가 가능.

**면접에서 말할 포인트**:
- "한 언어에서 발견한 버그를 다른 언어로 일반화하는 판단 기준" -> §45에서 한글 전용으로 좁게 고쳤던 문제가 실제로는 "검색 API가 붙어 쓴 문자열을 잘 못 다룬다"는 언어 무관한 근본 원인이었다는 걸, 영문에서 같은 증상을 실제로 재현해서 확인한 뒤에야 일반화한 경험 — 처음 발견 시점엔 스코프를 좁게 잡는 게 안전하지만, 같은 패턴이 다른 곳에서 재현되면 그때 공통 해법으로 통합하는 순서가 합리적이라는 걸 설명할 수 있음.
- "비슷한 문제에 왜 서로 다른 라이브러리를 썼나" -> 한국어 띄어쓰기 교정은 형태소 단위라 규칙 기반으로 풀기 어려워 딥러닝 모델(PyKoSpacing)이 필요했지만, 영어는 이미 공백으로 단어가 구분되는 언어라 "단어 사전 빈도 + 동적계획법"이라는 훨씬 가벼운 고전적 알고리즘으로 충분히 풀리는 문제라는 걸 구분해서 설명 가능 — 문제의 언어적 특성에 따라 딥러닝이 필요한지 고전적 알고리즘으로 충분한지 판단하는 근거.

---

## 53. 검색 마지막 폴백으로 Claude Haiku 도입 — "댓댓" 같은 한글 발음 전사 검색어 대응

**뭘 했나**: §45(한글 띄어쓰기 교정)·§52(영문 띄어쓰기 교정)까지 다 실패했을 때 쓰는 세 번째 폴백 추가. `_ai_guess_query()`가 Claude Haiku 4.5에게 "사용자가 실제로 찾으려던 곡"을 추측시키고(예: "댓댓" → {"artist": "PSY & Suga", "track": "That That"}), 그 추측을 Last.fm에 실제로 검색해서 진짜 존재하는지 재검증한 뒤에만 결과로 채택. 검색어 자체를 키로 90일 캐싱(전역 공유). `/search` 응답에 `aiGuessedQuery` 필드 추가해서 프론트에 "왜 이 결과가 나왔는지" 힌트 표시. 캐시 키 `v6` → `v7`.

**왜 이렇게 했나**: 사용자가 이미 자기 곡("That That")을 재생해둔 상태에서, 곡명의 한글 발음 표기인 "댓댓"으로 검색했더니 완전히 무관한 팬캠/리액션 영상 관련 결과만 뜨는 걸 직접 발견. 유튜브 뮤직은 같은 입력에 정상적으로 자동완성이 뜨는 걸 비교해서 보여주며 "얼마나 거지같이 써도 검색이 되게 하고 싶다"고 요청함. §45/§52는 규칙 기반 교정(띄어쓰기)이라 "다른 표기 체계로 옮겨 적은 경우"(발음 전사, 줄임말, 오타)는 애초에 대응 범위 밖 — 규칙으로는 한계가 명확해서, 이 케이스만큼은 LLM의 추론 능력이 실질적으로 필요하다고 판단. AI 사용 확장(로드맵에서 원래 제외했던 범위)이라 먼저 승인을 구했고, 사용자가 "AI 쓸 수만 있으면 좋다"며 승인하되 "비용 측은 계속 말해달라"고 명시적으로 요청함.

**어떻게 동작하나**:
- `/quick-picks/ai`(§38)에서 이미 검증된 패턴을 그대로 재사용: `anthropic.Anthropic` 클라이언트로 Haiku 호출 → 마크다운 코드펜스 제거 → `json.loads()` 파싱(실패해도 빈 dict로 방어).
- **anti-hallucination 원칙(§38과 동일)**: 모델이 준 아티스트/곡명을 그대로 내보내지 않음. `guessed_track`이 있으면 `track.search`로, 없으면 `guessed_artist`로 `artist.search`를 실제로 호출해서 Last.fm에 진짜 존재하는 결과가 나올 때만 `tracks`/`artists`를 채움 — 모델이 지어낸 곡을 그대로 노출하는 사고를 막기 위함. 재검증까지 실패하면(모델이 헛소리를 했거나 애초에 `ANTHROPIC_API_KEY`가 없으면) 조용히 빈 결과 유지 — 검색 자체가 AI 키 유무에 좌우되면 안 되기 때문에 `/quick-picks/ai`처럼 503을 던지지 않고 그냥 스킵.
- **캐싱 설계가 `/quick-picks/ai`와 다른 지점**: quick-picks는 사용자마다 즐겨찾기가 달라 개인화된 추천이라 세션+날짜 키로 캐싱했지만, 이건 "같은 검색어면 누가 쳐도 같은 추측"이 맞는 문제라 검색어(`q`) 자체를 키로 써서 세션/사용자 구분 없이 전역 공유 캐싱(`search-ai-guess:{q}`, 90일). 즉 한 사용자가 "댓댓"을 검색해서 한 번 호출이 나가면, 그 뒤로 그 누가 "댓댓"을 검색해도(같은 세션이든 다른 세션이든) 실제 API 호출은 다시 안 나감.
- `/search`의 기존 폴백 체인(오타 교정 → 띄어쓰기 교정)과 같은 자리, 같은 조건(`if not artists and not tracks`)에 이어붙임 — 앞 단계에서 이미 뭔가 찾았으면 이 단계까지 안 감(가장 비싸고 느린 수단이라 최후순위).

**비용 (사용자 요청으로 계속 추적)**: Claude Haiku 4.5 기준 입력 $1/M 토큰, 출력 $5/M 토큰(2026-08 기준, WebSearch로 확인). 프롬프트가 짧아서(검색어 하나 + 지시문, 대략 100~150 입력 토큰) 출력도 JSON 한 줄(약 30~50 토큰) 정도면, 호출 1회당 대략 $0.0004~0.0005 수준으로 추정. **핵심은 이 비용이 "새로운 실패 검색어 하나당 딱 한 번만" 발생**한다는 것 — 90일 전역 캐싱 덕분에 같은 검색어가 아무리 많이 재검색돼도(다른 사용자 포함) 추가 비용이 없음. quick-picks(세션+날짜별, §38에서 추정한 호출당 $0.002~0.003)보다 호출 자체가 훨씬 드물 것으로 예상 — quick-picks는 매 세션·매일 새로 발생하지만, 이건 "이미 규칙 기반 교정 두 단계가 다 실패한 검색어"만 해당돼서 전체 검색 트래픽 중 극히 일부만 도달함.

**알아둘 한계**: (1) 아직 라이브 확인 전 — ast.parse/tsc만 통과. (2) 모델이 아예 틀린 추측을 하면(예: 존재하지 않는 아티스트명을 만들어냄) Last.fm 재검증에서 자연히 걸러지지만, "그럴듯하게 틀린" 추측(실제 존재하는 다른 곡으로 잘못 매칭)은 재검증을 통과해버릴 수 있음 — 이 경우 사용자가 원치 않는 곡이 검색 결과로 뜰 수 있는데, `aiGuessedQuery` 힌트로 "이 검색어로 찾은 결과"라는 걸 보여줘서 최소한 왜 이런 결과가 나왔는지는 알 수 있게 함. (3) 캐시가 "실패한 추측"(빈 dict)도 90일 캐싱하기 때문에, 만약 모델이 첫 시도에 잘못 추측했다면 90일 동안 같은 검색어에 대해 계속 같은(틀린) 결과가 나감 — negative-cache TTL 원칙(§51)을 여기 그대로 적용하면 더 안전하지만, 실패 자체가 API 장애성이 아니라 "모델 추론 결과"라 §51과 성격이 달라서 일단 보류함. 재검증까지 실패해서 완전히 빈 dict가 캐싱된 경우도 마찬가지로 90일 락인됨 — 추후 문제 되면 negative TTL을 짧게 분리하는 걸 고려.

**면접에서 말할 포인트**:
- "규칙 기반 해법의 한계를 어떻게 판단하고 언제 AI로 넘어갔나" -> §45/§52는 정확히 정의된 문제(띄어쓰기 복원)라 규칙/고전 알고리즘으로 충분했지만, "발음 전사"는 입력과 출력 사이의 매핑 규칙 자체가 사실상 무한하고 문맥 의존적이라 규칙 기반으로 감당이 안 된다고 판단한 근거를 설명할 수 있음 — "AI를 왜 여기서만 쓰는가"에 대한 명확한 기준.
- "LLM 출력을 프로덕션에 그대로 노출하지 않는 설계" -> quick-picks(§38)에서 세운 "모델 출력은 반드시 authoritative source(Last.fm)로 재검증 후에만 사용" 원칙을 검색 폴백에도 동일하게 재사용한 것 — 같은 원칙을 다른 기능에 일관되게 적용한 사례로 설명 가능.
- "AI 기능의 비용을 어떻게 통제했나" -> 세션/사용자 단위가 아니라 "검색어 문자열" 단위로 캐시 키를 잡아서, 같은 실패 검색어에 대한 비용이 전체 사용자 기준으로 최대 1회만 발생하도록 설계 — 개인화가 필요 없는 문제라는 걸 파악하고 그에 맞게 캐시 범위를 좁힌(전역 공유) 판단 근거를 설명할 수 있음.

---

## 54. §53 라이브 확인 중 발견 — 트랙 검색엔 쓰레기 필터가 없어서 AI 폴백이 아예 발동을 안 함

**뭘 했나**: "댓댓" 라이브 확인 스크린샷을 보니 §53(AI 검색 폴백)이 전혀 작동하지 않고 여전히 팬캠/방송클립 쓰레기 트랙("[FANCAM] 22071...", "윤기 깜짝게스트 댓...", 각각 청취자 43명/23명/11명/7명)만 뜨는 걸 확인. `_is_junk_track_match()`를 새로 추가해 `_build_track_results()`에 적용(청취자 50명 이하는 걸러냄), 캐시 키 `v7` → `v8`.

**왜 이렇게 했나**: §45/§50에서 아티스트 검색용 쓰레기 필터(`_is_junk_artist_match`)를 만들었는데, 정작 트랙 검색(`track.search`)엔 같은 필터를 안 만들어놨다는 걸 놓치고 있었음. Last.fm `track.search`가 "댓댓"이라는 문자열이 제목에 들어간 팬캠/방송 클립 스크로블(진짜 존재하는 Last.fm 데이터지만 사용자가 찾는 원곡이 아님)을 그대로 매치해서 `tracks`가 채워져버리니, §53에서 만든 `if not artists and not tracks:` 게이트를 통과 못 하고 AI 폴백까지 아예 도달을 안 하는 게 근본 원인이었음.

**어떻게 동작하나**: 아티스트 필터(§50)와 같은 원칙(청취자 수 기준)을 쓰되 임계값은 다르게 잡음 — 아티스트 청취자 수는 그 아티스트의 모든 곡을 합친 값이라 무명이어도 보통 수십 명은 넘지만, 트랙 청취자 수는 곡 하나만의 값이라 자연히 더 낮게 나옴. 실사용 관찰(팬캠류는 거의 다 50명 미만)을 근거로 `JUNK_TRACK_LISTENER_THRESHOLD = 50`으로 설정. 사용자에게 "필터만 추가" vs "필터+AI 병행" vs "필터 없이 AI 우선순위 조정" 세 가지 옵션을 먼저 제시했고, 가장 단순한 "필터 추가"를 선택받아 진행 — 트랙 검색 파이프라인의 유일한 공용 변환 함수(`_build_track_results`)에 적용해서 원본 검색/띄어쓰기 교정 재시도/§53 AI 재검증까지 모든 경로에 자동으로 적용됨.

**알아둘 한계**: (1) 임계값 50은 관찰 근거(4개 샘플)치고는 표본이 적음 — 실사용 중 진짜 인디/신인 정식 발매곡이 부당하게 걸러지는 사례가 나오면 조정 필요(§50과 같은 트레이드오프 구조). (2) 아직 라이브 재확인 전 — ast.parse만 통과.

**면접에서 말할 포인트**:
- "라이브 테스트로 설계 결함을 어떻게 발견했나" -> 코드 리뷰만으론 안 보였던 문제(아티스트엔 있고 트랙엔 없는 비대칭 필터)를, 실제 사용자가 스크린샷으로 보여준 "고친 게 반영이 안 된다"는 신호를 통해 역추적한 사례 — 새 기능이 안 켜진 것처럼 보이는 버그 중 상당수가 "새 기능 자체의 버그"가 아니라 "그 기능에 도달하기 전 단계의 게이트 조건이 막고 있는 것"일 수 있다는 디버깅 관점을 설명할 수 있음.

---

## 55. §54 트랙 쓰레기 필터가 실제 정식곡까지 걸러내는 걸 발견 — 청취자 수 임계값을 패턴 기반으로 재설계

**뭘 했나**: "동암역 2번 출구"(도민, 2017년 발매 니치 발라드) 검색 진단을 위해 백엔드에 디버그 로그(`[search-ai-fallback]`, `[search-junk-track-filter]`)를 추가하고 사용자가 라이브로 재현. 로그를 보니 §54의 청취자 수 임계값(50명 이하) 필터가 장범준(버스커버스커)의 정식 발매곡 "신풍역 2번 출구 블루스"(청취자 6~46명)까지 쓰레기로 걸러내고 있는 걸 확인. `_is_junk_track_match()`를 청취자 수 기준에서 패턴 기반(대괄호로 시작하는 제목, 아티스트 필드의 6자리 날짜, "OO CH/TV" 채널명 패턴, 팬캠/직캠 등 명시적 키워드)으로 재설계. 동시에 §53 AI 폴백의 발동 조건을 "트랙이 아예 없을 때"에서 "있어도 전부 청취자 100명 미만으로 저신뢰일 때"로 넓히고, AI가 더 확실한 트랙을 찾으면 기존 저신뢰 트랙 위에 얹는(완전히 지우지 않는) 방식으로 변경. 캐시 키 `v9` → `v10`.

**왜 이렇게 했나**: §54에서 "청취자 수로 아티스트처럼 판별하면 되지 않을까"라고 가정했는데, 실사용 로그로 그 가정이 틀렸다는 게 실제로 확인됨. 아티스트 청취자 수는 그 아티스트의 모든 곡을 합산한 값이라 무명이어도 보통 두 자릿수 이상은 나오는데, 트랙 청취자 수는 곡 하나만의 값이라 Last.fm의 얕은 한국 트랙 스크로블 데이터 특성상 유명 가수의 정식곡도 자릿수가 똑같이 낮게 나옴 — "신풍역 2번 출구 블루스"(6~46명)와 이전에 확인한 진짜 쓰레기(팬캠 등, 7~46명)가 숫자 범위에서 거의 완전히 겹침. 즉 청취자 수는 트랙 단위에서 "진짜/가짜"를 구분하는 신호로 근본적으로 부적합했음 — 이걸 코드 리뷰만으로는 못 잡았고, 사용자가 실제로 다른 검색어("동암역 2번 출구")를 테스트하고 백엔드 로그를 같이 확인해줘서 발견함.

**어떻게 동작하나**:
- `_is_junk_track_match()`: 청취자 수 조건을 완전히 없애고, 관찰된 쓰레기 사례들의 공통된 "형식적" 특징만 봄 — 제목이 `[`로 시작(예: "[FANCAM] ..."), 아티스트 필드에 `\d{6}` 패턴(방송/이벤트명에 흔한 YYMMDD 날짜, 예: "220716 싸이 흠뻑쇼"), 아티스트 이름이 "...CH"/"...TV"로 끝남(유튜브 채널명 패턴), 팬캠/직캠/리액션 등 키워드 포함. 이 패턴들에 안 걸리는 애매한 쓰레기는 여전히 통과할 수 있다는 한계를 인정하고, 그 보완책으로 아래 AI 폴백 조건 확장을 같이 넣음.
- `tracks_low_confidence` 판정: 트랙이 비어있거나, 있어도 전부 청취자 100명 미만이면 "저신뢰"로 보고 AI 폴백(§53)을 추가로 시도. AI가 검증된 더 확실한 트랙을 찾으면 그 결과를 리스트 맨 앞에 두고, 기존 저신뢰 트랙 중 중복만 제거해서 뒤에 이어붙임 — 완전히 덮어쓰지 않는 이유는, AI도 실패하면(예: Last.fm에 그 곡 자체가 없는 경우) 저신뢰 트랙이라도 남아있는 게 아무 결과도 없는 것보단 나아서.
- AI 폴백 내부 로직도 조정: 트랙 추측이 이미 검증에 성공했으면 아티스트 추측 검증은 스킵(`track_guess_verified` 플래그로 상호배타적으로 처리) — 안 그러면 트랙을 이미 잘 찾았는데 아티스트 추측 검증이 뒤늦게 `ai_guessed_query`를 아티스트 이름만으로 덮어써버리는 사이드이펙트가 생김.

**알아둘 한계**: (1) "동암역 2번 출구" 자체가 Last.fm 카탈로그에 있는지는 여전히 미확인 — 있다면 이번 수정으로 뜰 것이고, 없다면(도민이라는 아티스트가 Last.fm에 아예 인덱싱 안 됐을 가능성, 외부 검색으로 정황상 그럴 가능성이 있어 보임) AI가 아무리 정확히 추측해도 재검증 단계에서 막혀 안 뜨는 게 맞는 동작 — 이건 버그가 아니라 Last.fm 자체의 데이터 커버리지 한계. (2) 새 패턴 기반 필터도 완벽하지 않음 — "PSY 싸이 - That T..." (아티스트명 "료화", 정황상 커버 영상으로 추정)처럼 패턴에 안 걸리는 애매한 항목은 여전히 통과할 수 있음. (3) 청취자 100명 기준으로 AI 폴백을 추가로 트리거하는 게 예전보다 호출 빈도를 늘림 — 다만 §53에서 이미 검색어 단위 90일 전역 캐싱을 해둬서 같은 검색어의 반복 비용은 여전히 없음. (4) 아직 라이브 재확인 전 — ast.parse만 통과.

**면접에서 말할 포인트**:
- "가정을 실사용 데이터로 반증하고 재설계한 경험" -> §54에서 "아티스트처럼 청취자 수로 트랙도 판별될 것"이라 가정했는데, 실제 로그 데이터(장범준 정식곡이 쓰레기 임계값에 걸리는 것)로 그 가정이 틀렸다는 걸 확인하고, 숫자 기반에서 패턴 기반으로 판별 전략 자체를 바꾼 경험 — 코드 리뷰나 직관만으로는 못 잡고 실제 도메인 데이터의 분포(한국 트랙의 Last.fm 스크로블이 원래 얕다는 것)를 확인하고서야 알 수 있었던 문제라는 걸 설명 가능.

**(추가 수정) 새 패턴의 쓰레기 발견 — "단체 무대" 방송 커버 영상**: "위대한 쇼맨 ost" 검색 결과에서 "호야팀 K-WAVE 단체 무대 ♬ 위대한 쇼맨 OST"(댄싱하이, 8~15명) 같은 항목이 §54/§55 필터를 통과해서 나오는 걸 사용자가 직접 스크린샷으로 짚어줌 — 댄스 서바이벌 방송("댄싱하이" 등)에서 참가팀이 그 노래에 맞춰 춘 커버 무대 영상이지 실제 곡이 아님. 이 필터가 (1) 아예 무관한 쓰레기와 (2) 진짜 원곡인데 업로더가 아티스트 필드에 부가 설명("위대한 쇼맨 OST")을 덧붙여 지저분해 보이는 경우를 구분하지 못하고 있었다는 걸 함께 확인함 — 같은 검색 결과 안의 "Hugh Jackman 위대한 쇼맨 OST - From Now On"(극중 실제 부른 곡)과 "호야팀 K-WAVE 단체 무대... - 댄싱하이"(커버 무대 영상)는 겉보기엔 비슷하게 지저분해 보이지만 전자는 진짜, 후자는 쓰레기라는 걸 사용자가 직접 구분해줌. `JUNK_TRACK_KEYWORD_MARKERS`에 "단체 무대" 키워드 추가, 캐시 키 v16 → v17 — ast.parse만 확인, 라이브 재확인 대기.
- "완벽한 필터 대신 다층 방어" -> 패턴 필터가 모든 쓰레기를 못 잡는다는 걸 인정하고, 대신 "신뢰도가 낮으면 AI로 한 번 더 검증"이라는 두 번째 방어선을 추가한 설계 — 한 가지 규칙으로 완벽을 노리기보다, 불완전한 규칙 여러 겹을 쌓아서 전체 신뢰도를 높이는 접근 방식을 설명할 수 있음.

---

## 56. §55의 "신뢰도" 판단이 청취자 수만 보고 검색어 관련성을 안 봐서 생긴 버그

**뭘 했나**: "동암역 2번 출구" 재확인 중 AI 폴백이 여전히 발동을 안 해서 로그(`[search-confidence]` 신규 추가)로 추적. 원인은 §55에서 추가한 `tracks_low_confidence` 판정이 청취자 수만 봐서, "신풍역 2번 출구 블루스"(장범준, 청취자 318명 — 완전히 다른 역 이름의 다른 곡)가 100명을 넘긴다는 이유만으로 전체 결과를 "신뢰함"으로 오판하고 AI 폴백을 스킵한 것. `_track_matches_query_well()`을 추가해 청취자 수 조건에 rapidfuzz `partial_ratio` 기반 검색어-트랙명 유사도 조건(90점 이상)을 AND로 더함. 캐시 키 `v10` → `v11`.

**왜 이렇게 했나**: Last.fm의 `track.search`가 "동암역 2번 출구"에 대해 "역 2번 출구"라는 부분 문자열이 겹치는 완전히 다른 역 이름의 곡들("신풍역 2번 출구 블루스", "상수역 2번 출구")을 반환하는데, 이 중 하나가 인기(청취자 318명)가 있다는 이유만으로 전체가 신뢰할 만하다고 판단한 게 문제였음. 청취자 수(인기도)와 검색어 관련성(진짜 찾던 곡이 맞는지)은 서로 다른 축인데 §55에서 이걸 하나로 뭉뚱그려 판단한 게 원인 — "아무리 인기 있어도 전혀 다른 곡이면 신뢰할 이유가 없다"는 당연한 조건을 놓쳤음.

**어떻게 동작하나**:
- 처음엔 §45/§50에서 이미 쓰던 `rapidfuzz.fuzz.token_sort_ratio`를 그대로 재사용하려 했는데, 로컬에서 직접 여러 케이스를 비교해보니 이 함수가 이 상황엔 안 맞았음 — "동암역 2번 출구" vs 진짜 정답 "동암역 2번 출구 (feat. 김무성 & 성지영)"는 뒤에 부가정보가 붙어 토큰 수가 늘어나서 50점밖에 안 나오는데, "동암역 2번 출구" vs 가짜 "상수역 2번 출구"(역 이름만 다름, 글자 수는 비슷)는 오히려 77.8점이 나와서 가짜가 진짜보다 점수가 높은 역전 현상이 있었음.
- 대신 `fuzz.partial_ratio`(부분 문자열 정렬 기반 유사도)로 바꿈 — 검색어가 트랙명에 그대로 포함되면(뒤에 "(feat...)"가 붙어도) 94~100점을 주고, 글자 수는 비슷해도 핵심 단어(역 이름)가 다르면 77.8~87.5점을 줘서 두 케이스가 훨씬 잘 갈림. 대소문자 차이도 영향을 줘서("blueming" vs "Blueming"이 100점이 아니라 93.3점) 비교 전에 양쪽 다 소문자로 정규화. 임계값은 90점으로 잡아서 진짜 정답(94~100)과 가짜(77.8~87.5) 사이 확실한 경계에 둠.
- `[search-confidence]` 로그를 추가해서 매 검색마다 `tracks_low_confidence` 판정 결과와 트랙 목록(아티스트/곡명/청취자 수)을 터미널에서 바로 볼 수 있게 함 — 앞으로 비슷한 오판이 생기면 코드를 다시 안 뜯어봐도 로그만으로 원인을 좁힐 수 있음.

**알아둘 한계**: (1) 이번에도 로컬에서 몇 개 케이스로만 검증한 임계값(90점)이라, 실사용 중 다른 패턴의 오탐/누락이 또 나올 수 있음 — 같은 패턴(§54→§55→§56)으로 세 번째 조정이 될 수도 있다는 걸 인지하고 있어야 함. (2) `partial_ratio`는 "검색어가 부분 문자열로 포함되는가"에 최적화된 지표라, 어순이 크게 바뀌거나 발음이 다르게 표기된 경우(§45/§52가 다루는 케이스)는 여전히 낮은 점수가 나옴 — 이건 의도된 동작(그런 경우엔 애초에 AI 폴백을 타야 함). (3) 아직 라이브 재확인 전 — ast.parse만 통과.

**면접에서 말할 포인트**:
- "지표를 고를 때 직접 검증하는 습관" -> `token_sort_ratio`가 이 문제엔 안 맞다는 걸 문서나 직관이 아니라 실제 후보 문자열들을 놓고 로컬에서 점수를 찍어보고서야 알았던 경험 — 같은 라이브러리 안에서도 함수마다 특성이 다르고(순서 무관 토큰 비교 vs 부분 문자열 정렬), 문제 상황에 맞는 지표를 고르려면 후보들을 직접 돌려봐야 한다는 걸 설명할 수 있음.
- "하나의 값으로 여러 판단을 뭉뚱그리면 생기는 버그" -> "신뢰도"라는 하나의 개념 안에 "인기도"와 "관련성"이라는 서로 독립적인 두 축이 숨어있었는데 이걸 구분 안 하고 청취자 수 하나로만 판단하다 생긴 버그 — 판단 기준을 설계할 때 그 기준이 실제로 하나의 축인지, 서로 다른 여러 축이 섞여있는 건 아닌지 점검하는 습관의 중요성을 설명 가능.

---

## 57. AI 폴백이 "아티스트만 맞고 곡은 무관한" 결과를 검증 통과시킨 버그 + 캐싱을 검증 결과 기준으로 재설계

**뭘 했나**: "동암역 2번 출구" 재확인 중 AI가 "싸이(PSY) - 동암역 2번 출구"로 완전히 틀린 추측을 했는데, 그 추측이 Last.fm 재검증까지 통과해서 화면에 "AI 추측: 싸이 동암역 2번 출구로 검색됨"이라며 싸이의 무관한 다른 곡들("아름다운 이별 2", "Sleepless" 등)이 뜨는 걸 실제로 발견. 트랙 검증 단계에 `fuzz.partial_ratio(guessed_track, 결과곡명) >= 80` 조건을 추가해서, 아티스트만 맞고 곡명이 무관한 결과는 걸러내도록 수정. 동시에 §53에서 `_ai_guess_query()` 내부에 있던 캐싱을 호출부(`search()`)로 옮기고, 검증 성공 여부에 따라 TTL을 다르게 줌(성공 90일 / 실패 1일).

**왜 이렇게 했나**: 두 가지 문제가 겹쳐 있었음. (1) Last.fm `track.search`의 `artist` 파라미터가 엄격한 AND 필터가 아니라 느슨한 힌트라서, "이 아티스트의 이 곡"을 찾으려 했는데 "이 아티스트의 아무 곡"이 검증을 통과해버림 — "아티스트가 맞다"와 "그 아티스트의 그 곡이 맞다"를 같은 걸로 취급한 게 원인. (2) 사용자가 화면에서 AI가 명백히 틀린 결과를 봤을 때 "매번 검색할 때마다 Claude API 비용이 나가는 거 아니냐"는 정당한 우려를 제기함 — 실제로는 검색어 단위 캐싱(§53)이 있어서 같은 검색어 재테스트는 추가 비용이 안 드는 게 맞지만, 그 캐싱이 "검증 성공/실패 여부와 무관하게 항상 90일"로 걸려 있어서, 한 번 잘못된 추측이 나오면 90일 동안 계속 그 틀린 결과가 나가는 문제가 있었음 — §51에서 이미 겪은 "네거티브 캐시는 짧게" 원칙을 이 캐시 레이어엔 적용을 안 해뒀던 것.

**어떻게 동작하나**:
- 트랙 검증: Last.fm이 돌려준 각 후보의 `name`을 AI가 추측한 `guessed_track`과 `fuzz.partial_ratio`로 비교해서 80점 이상인 것만 남김 — 실제 데이터로 확인한 값(무관한 곡들은 22~36점, §56에서 확인한 "동암역"/"신풍역" 같은 진짜 헷갈리는 케이스는 77.8~100점)을 기준으로 안전하게 갈리는 지점에 임계값을 잡음.
- 캐싱 재설계: `_ai_guess_query()`는 이제 캐싱을 전혀 안 하고 순수하게 "Claude 호출"만 담당. `search()`가 `search-ai-fallback:v1:{검색어}` 키로 "Claude 추측 + Last.fm 검증까지 끝난 최종 결과"를 캐싱함 — 검증까지 성공했으면 90일(같은 검색어 재검색 시 Claude/Last.fm 호출 전부 스킵, 완전 무료 재사용), 실패했으면(모델이 헛짚었거나 카탈로그에 정말 없거나) 1일로 짧게 둬서 다음 날엔 다시 시도할 기회를 줌.
- `[search-ai-fallback]` 로그에 캐시 히트/미스와 TTL 선택 근거까지 찍히도록 해서, 앞으로 비슷한 "왜 캐싱됐는데 결과가 이상하지" 류의 혼란이 생기면 로그만으로 바로 판단 가능하게 함.

**알아둘 한계**: (1) `partial_ratio >= 80`도 로컬 소수 케이스로만 검증한 임계값이라, 다른 패턴의 오탐/누락이 또 나올 수 있음(§54→§55→§56과 같은 반복 패턴). (2) 실패 캐시 TTL을 1일로 잡은 근거는 "적당히 짧으면서도 같은 날 반복 테스트로 비용이 계속 나가진 않게"라는 감으로 정한 것 — 실사용 트래픽이 쌓이면 더 데이터 기반으로 조정할 수 있음. (3) 여전히 "동암역 2번 출구"(도민) 자체가 Last.fm 카탈로그에 있는지는 미확인 — 이번 수정 후에도 AI가 못 찾으면 카탈로그 자체에 없다고 봐도 될 근거가 더 쌓이는 것. (4) 아직 라이브 재확인 전 — ast.parse만 통과.

**면접에서 말할 포인트**:
- "제3자 API의 파라미터를 문서만 보고 신뢰하지 않는 습관" -> Last.fm의 `track.search` `artist` 파라미터가 이름과 달리 엄격한 필터가 아니라는 걸 실제 응답으로 확인하고서야 알았던 경험 — 외부 API 파라미터의 실제 동작은 이름이나 문서 설명만으로 단정하지 말고 실제 응답을 보고 확인해야 한다는 원칙.
- "사용자 신뢰를 지키는 캐싱 설계" -> 캐싱 자체는 비용 절감에 좋지만, "검증되지 않은 결과"까지 "검증된 결과"와 같은 TTL로 캐싱하면 한 번의 실수가 오래 지속되는 부작용이 있다는 걸 실제 사고로 겪고, 캐시 정책을 결과의 신뢰도에 따라 분리하는 설계로 대응한 경험 — 캐싱은 "얼마나 오래 저장할까"뿐 아니라 "이 값을 얼마나 신뢰할 수 있는가"까지 같이 고려해야 한다는 설명 가능.

---

## 58. AI 폴백이 "실존하는 아티스트"라는 이유만으로 완전 무관한 결과를 검증 통과시킨 버그

**뭘 했나**: §57 수정 이후에도 "동암역 2번 출구" 검색이 여전히 무관한 결과를 보여줌 — 로그를 보니 트랙 검증은 정상적으로 실패("'싸이 - 강남역 1번 출구'와 이름이 닮은 매치를 못 찾음")했는데 바로 다음 줄에 "결과 캐싱함 (검증됨=True, TTL=90일)"이 찍히는 모순을 발견. 원인은 트랙 추측이 실패하면 "아티스트만이라도" 검증해서 보여주던 폴백 분기(`if guessed_artist and not track_guess_verified`) — "싸이"라는 아티스트 자체는 워낙 유명해서 Last.fm에 당연히 존재하니 이 아티스트 단독 검증은 항상 성공해버림. 조건을 `if guessed_artist and not guessed_track`(AI가 애초에 트랙을 추측 안 했을 때만)으로 좁혀서, 트랙 추측이 있었는데 실패한 경우엔 아티스트 폴백도 같이 실패 처리하도록 수정.

**왜 이렇게 했나**: "아티스트가 실존한다"와 "그 아티스트가 이 검색어와 관련 있다"는 완전히 다른 질문인데, 유명한 아티스트는 거의 항상 "실존한다"가 참이라서 이 구분이 없으면 아티스트 단독 검증 자체가 사실상 무의미해짐 — §57에서 트랙에 겪은 것과 정확히 같은 종류의 문제가 아티스트 폴백 분기에도 남아있었던 것. 사용자가 로그와 화면을 같이 보고 "트랙은 실패라는데 왜 성공으로 캐싱됐냐"는 모순을 직접 짚어줘서 발견함 — 로그 메시지 사이의 논리적 불일치를 사용자가 먼저 알아챈 사례.

**어떻게 동작하나**: 원래 의도(트랙 추측이 실패했을 때 최소한 아티스트라도 보여주자)는 사실 위험한 설계였음 — "사용자가 원래 아티스트만 찾고 있었다"는 것과 "AI가 트랙까지 구체적으로 추측했는데 그게 틀렸다"는 것은 전혀 다른 상황인데 같은 분기로 처리했음. 이제는 `guessed_track`이 애초에 없었을 때(=AI가 "이건 아티스트 검색이다"라고 판단한 경우)만 아티스트 단독 검증을 시도하고, 트랙까지 구체적으로 추측했다가 실패하면 그 시도 전체를 실패로 간주함(부분 성공을 인정 안 함).

**알아둘 한계**: 아티스트 단독 추측(`guessed_track`이 처음부터 없는 경우) 자체에는 여전히 관련성 검증이 없음 — 그 경우에도 이론적으로 "실존하지만 무관한 아티스트"를 보여줄 위험은 남아있음(다만 실제 트래픽에서 이 경로는 드물 것으로 예상). 아직 라이브 재확인 전.

**면접에서 말할 포인트**: "로그 메시지 사이의 논리적 일관성 점검" -> 코드가 "성공"이라고 찍은 로그를 그 자체로 믿지 않고, 바로 위 "실패" 로그와 모순된다는 걸 사용자와 함께 짚어내면서 실제 조건 분기의 허점을 찾은 경험 — 로그가 있다고 안심하지 말고, 로그들 사이의 narrative가 실제로 말이 되는지까지 점검해야 한다는 원칙.

**(추가 수정) 중첩 캐시 버전 누락**: §59(유튜브 폴백) 작업까지 마친 뒤 처음으로 "동암역 2번 출구"를 라이브 테스트했는데도 여전히 §58 수정 전(잘못된 "싸이" 결과)이 그대로 나옴. 로그를 보니 `/search` 본체 캐시(`search:v14`)는 정상적으로 새로 계산됐는데(`[search-confidence]` 로그가 찍힘), 바로 다음 줄이 `[search-ai-fallback] '동암역 2번 출구' -> 캐시된 결과 재사용(호출 없음), 검증됨=True`였음 — AI 폴백 결과 자체가 `/search` 캐시와는 **별도의 독립된 캐시**(`search-ai-fallback:v1:...`, §57에서 캐싱 책임을 호출부로 옮기며 신설)로 저장되고 있었는데, 이 안쪽 캐시 키는 §58에서 로직을 고칠 때 버전을 안 올려서, §58 수정 전에 저장된 "검증됨=True"(잘못된) 값을 그대로 재사용하고 있었던 것. 안쪽 캐시를 `v2`로 올려서 해결했는데, 그 사이 이미 `/search` 바깥쪽 캐시(`v14`)에도 이 잘못된 결과가 통째로 저장돼버려서, 바깥쪽도 `v15`로 같이 올려야 했음 — **캐시 안에 또 다른 캐시가 중첩된 구조에서는, 안쪽 캐시의 로직을 고칠 때 안쪽 키뿐 아니라 그 값을 담고 있는 바깥쪽 캐시 키도 같이 올려야 한다**는 걸 실제로 겪음. 이 프로젝트에서 캐시 키 버전 관리 실수가 반복적으로 나오는데(§29, §36, §43~46, §51 등), 이번엔 "중첩 캐시"라는 새로운 변종이었음 — 앞으로 여러 겹의 캐시를 만들 때는 안쪽/바깥쪽 캐시 키를 항상 같이 검토할 것.

---

## 59. Last.fm+AI 폴백까지 다 실패하면 유튜브 자체 검색으로 마지막 시도

**뭘 했나**: "동암역 2번 출구"가 Last.fm 카탈로그 자체에 없을 가능성이 높다는 결론 이후, "정말 아무 방법도 없냐"는 질문에 옵션을 제시하고(유튜브 폴백 추가 / 안내 문구만 개선 / 보류) 유튜브 폴백을 선택받아 구현. `_youtube_raw_query_search()` 신규 추가 — 이미 연동된 YouTube Data API로 검색어 원문 그대로 영상을 찾아서, Last.fm 메타데이터 없이 영상 하나로 재생만 가능한 "비검증" 트랙 결과를 만듦. `/search` 응답에 `youtubeFallbackUsed` 플래그 추가, 프론트는 이 결과를 "Last.fm에 없어서 유튜브에서 찾았어요 · 청취자 수 등 정보 없음" 문구로 구분 표시.

**왜 이렇게 했나**: Last.fm은 사용자가 실제로 재생/스크로블한 기록으로 채워지는 크라우드소싱 카탈로그라, 정식 발매됐지만 인지도가 낮은 곡은 아예 등록이 안 돼 있을 수 있다는 게 이번 세션에서 실제로 확인된 한계(§55/§56/§58). 반면 유튜브는 업로드된 모든 영상이 검색 대상이라 훨씬 넓은 인덱스 — 사용자가 유튜브 뮤직에서 "댓댓"에 자동완성이 바로 뜨는 걸 직접 확인했던 것도 이 차이 때문. Last.fm이 못 찾아도 유튜브엔 있을 가능성이 있으니, 정식 메타데이터 없이라도 재생만은 가능하게 하자는 판단.

**어떻게 동작하나**:
- `/search`의 마지막 단계(§45/§52 교정 + §53/§57/§58 AI 폴백까지 전부 실패한 뒤)에서만 시도 — 가장 비용이 크고 부정확할 수 있는 수단이라 최후순위 유지.
- 기존 `/youtube-search`(재생 버튼용, 아티스트+곡명 기반)와 별개로, 검색어 원문(`q`) 그대로 YouTube `search.list`를 호출 — 사용자가 "얼마나 거지같이 써도" 검색이 되길 원했던 것과 같은 이유로, 이미 교정을 다 거친 뒤에도 실패한 경우엔 오히려 원문 그대로가 유튜브 자체의 관련도 랭킹에 더 잘 맞을 수 있음.
- 결과 영상의 제목/채널명은 §28에서 만든 `_parse_youtube_music_title()`(트렌딩 차트에도 쓰는 공용 파싱 함수)로 아티스트/곡명을 최대한 나눔 — 완벽하진 않지만 기존에 검증된 절충안 재사용.
- 캐싱은 검색어 단위(성공 14일 — `/youtube-search`와 동일 기준, 실패 1일 — §51/§57과 동일한 네거티브 캐시 원칙)로 해서, 같은 검색어 재검색 시 쿼터를 다시 안 씀. 이 폴백 자체가 "모든 폴백이 실패했을 때만" 도달하는 마지막 수단이라, 호출 빈도 자체가 이미 낮게 통제돼 있음.
- 프론트: `Track` 타입에 이미 있던 `videoId` 필드(§28에서 유튜브 급상승용으로 추가된 필드)를 그대로 재사용 — 재생 시 `/youtube-search`를 다시 안 태우고 바로 재생됨(쿼터 이중 사용 방지).

**알아둘 한계**: (1) 유튜브 검색 결과는 공식 음원/팬 업로드/커버/라이브 영상이 섞여 있어서 품질 관리가 Last.fm보다 약함 — 관련 없는 영상이 1순위로 뜰 수 있음(향후 실사용 중 패턴이 보이면 필터 추가 고려, §54와 같은 패턴이 재발할 수 있음). (2) `listeners`, `url`(Last.fm 페이지 링크) 같은 필드가 없어서 일부 UI에서 다른 트랙과 시각적으로 다르게 보일 수 있음 — 안내 문구로 우선 대응. (3) YouTube Data API 일일 쿼터(하루 100회, §28 기준) 안에서 `/youtube-search`(재생 버튼)와 이 폴백이 쿼터를 공유함 — 트래픽이 늘면 쿼터 소진 속도를 모니터링할 필요.

**(추가 확인) 라이브 검증 완료**: 중첩 캐시(§58 추가 수정)까지 고친 뒤 "동암역 2번 출구"로 실제 확인 — 첫 번째 결과로 "동암역 2번출구"(Dom) 트랙이 뜨고, "검색된 곡" 헤더에 "(Last.fm에 없어서 유튜브에서 찾았어요 · 첫 곡은 청취자 수 등 정보 없음)" 안내 문구도 정상 노출됨. 이 곡을 마지막까지 못 찾은 건 결국 Last.fm 카탈로그 자체의 커버리지 한계였다는 게 최종 확인됨.

**면접에서 말할 포인트**:
- "서로 다른 데이터 소스의 강점을 조합하는 설계" -> Last.fm(정확한 메타데이터, 좁은 커버리지)과 YouTube(메타데이터 없음, 훨씬 넓은 커버리지)를 계층적 폴백으로 조합해서, 정확도를 우선하되 그래도 안 되면 커버리지를 우선하는 순서로 설계한 근거를 설명할 수 있음.
- "비용이 큰 최후 수단을 어떻게 안전하게 넣나" -> 가장 느슨하고 품질이 낮을 수 있는 수단일수록 호출 조건을 가장 좁게(모든 이전 단계 실패 시에만) 잡고, 캐싱 TTL도 기존 관례(§28의 14일)를 그대로 재사용해서 새로운 정책을 만들지 않고 기존 검증된 패턴에 맞춘 설계 판단을 설명 가능.

---

## 60. 유튜브 폴백으로 재생 중인 곡은 "다음 트랙"이 텅 비는 문제 — 같은 아티스트의 다른 영상으로 보충

**뭘 했나**: §59 유튜브 검색 폴백을 라이브로 확인하던 중, "동암역 2번출구"(Dom)를 실제로 재생까지는 잘 됐는데 "다음 트랙" 패널이 "비슷한 곡을 찾는 중이거나 없어요"로 텅 빈 걸 발견. `/similar-tracks`가 Last.fm `track.getSimilar`/`artist.getSimilar` 확장까지 다 돌려도 후보가 하나도 없으면, 유튜브에서 같은 아티스트명으로 검색해서 그 아티스트의 다른 영상들로 큐를 채우는 폴백 추가. 캐시 키 v4 → v5.

**왜 이렇게 했나**: "Dom"이 Last.fm 카탈로그 자체에 없는 아티스트라(§59에서 확인한 바로 그 이유) 비슷한 곡을 찾을 Last.fm 데이터 자체가 없는 게 당연한 결과였음. 사용자가 "왜 그냥 빈 상태로 두는 게 낫냐, 얘도 유튜브에서 찾아서 비슷한 곡으로 못 하냐"고 되물어서, 기술적 제약(유튜브의 "관련 영상" API는 2016년에 폐기돼서 Last.fm 같은 진짜 유사곡 추천은 불가능)을 설명한 뒤, 대안으로 "같은 아티스트의 다른 곡"을 채우는 절충안을 제시하고 구현.

**어떻게 동작하나**:
- Last.fm 기반 후보를 가사 필터(§44)까지 다 거친 뒤에도 `tracks`가 비어있을 때만 시도 — 유튜브 검색 쿼터를 쓰는 폴백이라 최후순위 유지.
- YouTube `search.list`를 아티스트명으로 호출(`maxResults=8`)해서 나온 영상들을 §28의 `_parse_youtube_music_title()`로 파싱, 이미 큐에 있는 곡과 중복만 제거하고 추가.
- 이 폴백 결과는 가사 필터를 일부러 안 탐 — 그 필터는 "둘러보기 목적" 목록에만 적용한다는 기존 원칙(§44)인데, 애초에 Last.fm/LRCLIB에 데이터가 없는 아티스트라 필터를 걸면 무조건 다 걸러져서 필터 자체가 무의미함. 대신 `image`/`videoId`를 이 단계에서 바로 채워서 내려주므로, §48에서 만든 "필터 통과한 곡만 나중에 이미지 리졸브" 로직(iTunes 호출)은 이 경로에서 건너뜀.
- 프론트는 추가 수정 없이 그대로 동작 — `Track` 타입에 이미 있던 `videoId` 필드(§28)를 그대로 채워 보내므로, 재생 시 `/youtube-search`를 다시 안 태우고 바로 재생됨.

**알아둘 한계**: (1) "비슷한 곡"이 아니라 "같은 아티스트의 다른 곡"이라 무드/장르 다양성은 없음 — 사용자에게 이 차이를 UI로 명시하진 않음(추후 필요하면 "이 아티스트의 다른 곡" 같은 라벨 추가 고려). (2) 유튜브 검색 결과 채널에 실제로 음악이 아닌 영상(브이로그, 반응 영상 등)이 섞여 있으면 그대로 큐에 들어감 — §54와 같은 패턴의 오염 가능성이 있지만 이 폴백 자체가 워낙 드물게 발동해서 일단 보류. (3) YouTube 일일 쿼터를 §28/§59와 공유함. (4) 아직 라이브 확인 전.

**면접에서 말할 포인트**:
- "완벽한 대안이 없을 때 가장 가까운 차선을 찾는 판단" -> "비슷한 곡 추천"이라는 원래 기능을 유튜브로 완전히 대체할 수는 없다는 기술적 한계(관련 영상 API 폐기)를 사용자에게 정직하게 설명한 뒤, "같은 아티스트의 다른 곡"이라는 더 좁지만 구현 가능한 대안으로 좁혀서 제시한 경험 — 요청을 그대로 다 들어줄 수 없을 때 무엇이 기술적으로 불가능하고 무엇이 대안이 될 수 있는지 구분해서 설명하는 능력.

**(추가 수정) 폴백 코드에 도달조차 못 하던 버그**: 라이브 확인 중 정작 이 폴백을 위해 만든 케이스("Dom" - "동암역 2번출구")에서 `/similar-tracks`가 200 대신 400 Bad Request를 냄. 원인: 공통 헬퍼 `lastfm_get()`이 Last.fm의 "Track not found"(error code 6) 같은 정상적인 실패 응답도 무조건 `HTTPException`으로 던지도록 돼 있는데, `track.getSimilar`/`artist.getSimilar` 두 호출 다 이 예외를 안 잡고 있어서 함수 맨 앞부분에서 그대로 죽어버림 — 아래에 이미 짜둔 유튜브 폴백 코드까지 실행이 아예 안 갔던 것. 두 호출을 각각 `try/except HTTPException`으로 감싸서 "이 곡/아티스트는 Last.fm에 없음"을 빈 목록으로 취급하고 계속 진행하도록 수정. 이미 성공적으로 캐싱된 기존 v5 캐시는 이 변경과 무관(원래도 실패했던 케이스만 새로 정상화됨)이라 캐시 버전은 그대로 둠 — ast.parse만 확인, 라이브 재확인 대기.

---

## 61. 검색 중엔 홈 화면 "지금 인기 있는 곡"/"인기 아티스트" 대신 검색한 아티스트의 다른 곡으로 채우기

**뭘 했나**: "검색해도 검색된 곡 빼고는 다 똑같다, 유튜브 뮤직처럼 검색한 것과 관련된 곡이 홈에도 떴으면 좋겠다"는 요청으로, 검색 중엔 검색과 무관한 "지금 인기 있는 곡 (글로벌)"/"인기 아티스트" 두 섹션을 숨기고, 그 자리를 검색된 아티스트(또는 검색된 곡의 아티스트) 본인의 다른 인기곡 섹션으로 대체함. 백엔드에 `GET /artist-top-tracks` 신규 엔드포인트 추가.

**왜 이렇게 했나**: AskUserQuestion으로 세 가지 대안(예: 관련 태그/장르로 채우기, 완전히 숨기기, 검색한 아티스트의 다른 곡으로 채우기)을 제시했고, 사용자가 "검색한 아티스트의 다른 곡들로 채우기 (권장)"를 선택함 — 검색 의도와 가장 직접적으로 관련 있고, 이미 있는 `artist.getTopTracks` API로 구현이 단순해서 권장으로 제시했었음. 기존 `/recommend`(§?, `handleSelectArtist`가 씀)를 재활용하지 않고 새 엔드포인트를 만든 이유: `/recommend`는 `artist.getSimilar`로 "비슷한 아티스트들"의 곡을 모으는 용도(그 아티스트 데이터가 없을 때만 본인 곡으로 폴백)라 의미가 다름 — 검색 맥락에서 사용자가 원하는 건 "이 아티스트와 비슷한 다른 아티스트"가 아니라 "이 아티스트의 다른 곡"이라는 좁고 명확한 의도라서 분리함.

**어떻게 동작하나**:
- 백엔드 `/artist-top-tracks?artist_name=...`: `artist.getTopTracks`를 그대로 호출해서(limit=30, 가사 필터로 걸러질 걸 감안해 넉넉히), §44의 `_filter_tracks_with_lyrics()`로 가사 있는 곡만 15개로 추려서 반환. 캐시 키 `artist-top-tracks:v2:{artist_name}`(v1: `lastfm_get()`이 "Artist not found" 같은 정상 실패도 예외로 던지는데 여기서 못 잡아서 섹션이 통째로 안 뜨던 버그를 §60과 같은 패턴으로 수정하며 버전업, `[artist-top-tracks]` 디버그 로그도 같이 추가).
- 프론트 `handleSearch()`: 검색 결과에서 "맥락 아티스트"를 정함 — 아티스트 검색 결과(`artists[0]`)가 있으면 그 이름, 없으면(곡만 검색된 경우) 첫 곡의 아티스트(`tracks[0].artist`). 이 아티스트로 `getArtistTopTracks()`를 호출해서 `searchArtistTracks`에 저장.
- 렌더: `searchContextArtist`가 있으면 `"{아티스트}"의 다른 곡` 섹션을 보여주고, `chartTracks`("지금 인기 있는 곡")와 `topArtists`("인기 아티스트") 두 섹션은 `!searchContextArtist` 조건으로 숨김. "다시 듣기"/"빠른 선곡"/"국내·해외 인기 급상승" 세 섹션은 사용자가 명시적으로 언급한 범위가 아니라서 그대로 유지.
- `handleSelectMood()`에도 이 새 상태(`searchContextArtist`, `searchArtistTracks`)를 초기화하는 코드를 추가 — 검색 후 무드 칩을 누르면 검색 잔존 상태가 남는 걸 막는 기존 패턴(§10 버그 수정)과 동일한 이유.

**알아둘 한계**: (1) "관련 곡"이 아니라 "이 아티스트 본인의 곡"이라, 여러 아티스트가 섞인 애매한 검색어(장르/연대 키워드 검색, §32)에서는 첫 곡의 아티스트로만 좁혀지는 게 사용자 기대와 다를 수 있음. (2) 아티스트 자체가 Last.fm에 없는 경우(§59의 유튜브 폴백처럼 곡만 겨우 찾은 경우) `artist.getTopTracks`도 당연히 빈 결과라 이 섹션 자체가 안 뜸 — 별도 안내 문구는 없음. (3) 검색된 트랙 목록에 이미 나온 곡이 이 "다른 곡" 섹션에도 중복으로 뜰 수 있음(중복 제거 로직 없음) — 사용성에 문제되면 추후 추가 고려.

**면접에서 말할 포인트**:
- "비슷해 보이지만 의미가 다른 두 요구사항을 구분해서 별도 엔드포인트로 분리한 판단" -> 기존 `/recommend`(비슷한 아티스트들의 곡)를 그대로 재사용할 수도 있었지만, 사용자 요청의 정확한 의도("이 아티스트의 다른 곡")와 어긋난다고 판단해서 API 하나를 더 만든 트레이드오프(코드 중복 vs 의미적 정확성)를 설명할 수 있음.
- "정적인 홈 화면을 검색 맥락에 맞춰 동적으로 바꾸는 UX 설계" -> 유튜브 뮤직 같은 참고 서비스와 비교해서 어떤 섹션은 대체하고 어떤 섹션은 유지할지(사용자가 명시한 범위만 우선 반영, 나머지는 과도한 변경 자제) 판단한 근거를 설명 가능.

**(추가 수정) `lastfm_get()` 예외 미처리로 섹션이 통째로 안 뜨던 버그**: 라이브 확인 중 "댓댓 싸이" 검색 시 이 섹션 자체가 안 뜸 — `track.search`의 `artist` 파라미터는 느슨한 힌트라 매치된 트랙의 artist 필드가 "싸이"처럼 저장돼 있어도, `artist.getTopTracks`는 정확한 아티스트명이 필요해서 Last.fm이 못 찾으면 `lastfm_get()`이 `HTTPException`을 던지고 그대로 죽어버림(§60 `/similar-tracks`에서 겪은 것과 같은 패턴). `try/except`로 감싸 실패해도 빈 결과로 처리하고 원인 로그(`[artist-top-tracks]`)를 남기도록 수정, 캐시 키 v2 버전업.

**(추가 확인) 라이브 검증 완료 + 별개의 원인**: 위 수정 후 재시작해서 다시 확인해보니, 실제로는 백엔드가 이미 정상적으로 15곡을 반환하고 있었음(`[artist-top-tracks] '싸이' -> 가사 필터 통과 15곡` 로그, `200 OK`) — 그런데도 화면에 안 떴던 진짜 원인은 이 버그가 아니라, 맥북이 재부팅되면서 프론트 개발 서버는 새로 켰지만 브라우저 탭은 하드 리프레시를 안 해서 예전 JS 번들을 그대로 물고 있었던 것. **Cmd+Shift+R로 하드 리프레시 후 재검색하니 "싸이"의 다른 곡" 섹션이 정상 노출되는 것 확인됨.** 다만 `lastfm_get()` 예외 미처리 자체는 실제로 존재하던 잠재 버그라(다른 아티스트명에서는 언제든 재발 가능) 수정은 그대로 유지함 — "증상 재현 안 됨"과 "버그가 없음"은 다르다는 걸 실제로 겪은 사례.

**(추가 수정) 아티스트 바꿔 검색하면 잠깐 직전 아티스트의 곡이 보이던 버그**: "싸이"로 검색한 뒤 "아이유"로 다시 검색했더니, 헤더는 바로 `"아이유"의 다른 곡`으로 바뀌는데 그 밑 목록엔 잠깐 직전 검색(싸이)의 곡(강남스타일 등)이 그대로 남아있는 게 실제로 보임. 원인은 `setSearchContextArtist(contextArtist)`는 검색 결과가 오자마자 바로 바뀌는데, `searchArtistTracks`는 뒤이어 나가는 `getArtistTopTracks()` fetch가 끝나야 바뀌는 구조라, 그 사이 "새 아티스트 이름 + 옛 아티스트의 곡"이라는 잘못된 조합이 한 프레임 동안 화면에 뜨는 것. 새 fetch를 시작하기 전에 `searchArtistTracks`를 먼저 비우도록 수정 — 로딩 중엔 섹션이 잠깐 안 보이는 쪽을 택함(틀린 아티스트의 곡을 보여주는 것보단 나음) — tsc만 확인, 라이브 재확인 대기.

---

## 62. 한글 아티스트 오타 교정이 무관한 검색어를 엉뚱한 아티스트로 오교정하던 버그

**뭘 했나**: "위대한 쇼맨 ost"(영화 OST를 찾으려던 검색, 실제로 원하던 곡은 "Rewrite the Stars")로 검색했더니 "위너"(WINNER, K팝 그룹)로 엉뚱하게 교정돼서 전혀 무관한 결과가 뜨는 버그 발견. §25/§45의 한글 아티스트 후보 목록 기반 오타 교정(`fuzzy_process.extractOne(q, KNOWN_KOREAN_ARTIST_NAMES, score_cutoff=60)`)의 `score_cutoff`가 너무 낮아서(60), "위대한"과 "위너"처럼 글자 하나 정도만 겹쳐도 매치로 인정되던 게 원인. `score_cutoff`를 65로 상향. 캐시 키 v15 → v16.

**왜 이렇게 했나**: 로컬 sandbox에서 rapidfuzz로 여러 검색어를 직접 실측 비교함 — 진짜 정상적으로 교정돼야 하는 케이스("아이위"→"아이유", "블핑"→"블랙핑크", "뉴진스니"→"뉴진스" 등)는 전부 스코어 66.67점 이상인 반면, 이번에 발견한 오탐("위대한 쇼맨 ost"→"위너")과 테스트 중 추가로 찾아낸 오탐("지드래곤"이라는 정확한 철자의 실존 유명 아티스트가 "있지"로 잘못 교정됨, "너의 이름은 ost"→"위너")은 전부 정확히 60.0점에 몰려있는 걸 확인 — 그 사이(65)로 컷오프를 올리면 기존 정상 케이스는 하나도 안 깨지면서 이 오탐들만 걸러낼 수 있다는 걸 실측으로 검증한 뒤 반영. `HTTPException`/캐싱 같은 이번 세션의 다른 버그들과 달리 이건 사용자가 직접 겪은 게 아니라 원인 진단 과정에서 로컬 실험으로 먼저 찾아낸 케이스라, 실제 리포트("위대한 쇼맨 ost")와 함께 같은 근본 원인으로 걸리는 "지드래곤" 사례도 같이 고쳤음.

**어떻게 동작하나**: 로직 자체는 안 바꾸고 `score_cutoff` 숫자만 60→65로 조정. 이 교정이 잘못 매치되면 `artists`가 채워져서, 이후 §53 AI 폴백의 발동 조건(`if not artists and tracks_low_confidence`)이 스킵돼버리는 부작용도 있었음 — 즉 이 버그는 "틀린 결과가 뜨는" 문제일 뿐 아니라 "맞는 결과를 찾아줄 수 있는 AI 폴백 자체를 막아버리는" 상위 문제이기도 했음. 컷오프를 올려 오매치가 안 걸리면 `artists`/`tracks`가 그대로 비고, 자연스럽게 AI 폴백(§53)이 "위대한 쇼맨 ost" → "Rewrite the Stars"를 추측하는 경로로 넘어갈 수 있게 됨.

**알아둘 한계**: (1) 실측 중 이 방식으로는 못 막는 오탐 패턴도 확인함 — 후보 이름이 아주 짧고(2~3글자) 검색어 안에 그대로 부분 문자열로 포함되는 경우(예: "제니퍼 로렌스"→"제니", "선미촌"→"선미")는 90점까지 나와서 컷오프 조정만으로는 걸러지지 않음. 다만 이건 실제 리포트가 아니라 내가 강건성 확인을 위해 임의로 만든 테스트 케이스라, 실사용 중 실제로 이런 검색어가 문제되는 걸 확인하면 그때 길이 비율 등 추가 규칙을 고려하기로 함(§29에서도 쓴 "실사용 중 발견하면 추가" 원칙과 동일). (2) `score_cutoff` 값 자체가 "우연히 60/65 근처에 진짜/가짜 케이스가 갈라져 있었다"는 경험적 관찰에 기반한 튜닝이라, `KNOWN_KOREAN_ARTIST_NAMES` 후보 목록이 늘어나면 이 경계도 다시 흔들릴 수 있음.

**면접에서 말할 포인트**:
- "실측 없이 임계값을 감으로 정하지 않는 습관" -> 버그를 코드만 보고 "cutoff를 올리면 되겠지"로 추측하는 대신, 실제 문제 케이스와 기존에 잘 동작하던 케이스들의 점수를 로컬에서 직접 뽑아 비교해서 "어느 값이 안전한지"를 데이터로 확인한 뒤에 수정한 과정을 설명할 수 있음.
- "임계값 튜닝의 한계를 스스로 알고 문서화하는 태도" -> 이번 수정으로 다 못 막는 사례(짧은 후보명의 부분 문자열 오탐)를 실제로 찾아냈지만, 확인되지 않은 가상 시나리오에 과도하게 대응하기보다 "실사용 중 재발하면 추가 규칙을 고려한다"는 명시적 판단 기준을 세운 것.

---

## 63. ngrok으로 폰 테스트 중 홈 화면이 통째로 비어 보이던 버그 — 경고 페이지가 API 응답에 끼어듦

**뭘 했나**: ngrok 무료 터널로 폰(카카오톡 인앱 브라우저)에서 접속했을 때, 헤더/검색창/무드 칩은 뜨는데 그 아래 "빠른 선곡"/"지금 인기 있는 곡" 같은 곡 목록이 전부 비어 보이는 문제 발견. 처음엔 "빠른 선곡" 섹션이 스크롤을 내려야 나오는 자리(무드 칩 밑이 아니라 "지금 인기 있는 곡"/"다시 듣기" 아래)라 안 보이는 줄 알고 그 섹션을 무드 칩 바로 아래로 끌어올렸는데, 사용자가 "빠른선곡이 없다 이거보단 그냥 노래 자체가 안 떠서 문제"라고 정정 — 순서 문제가 아니라 데이터 자체가 안 오고 있었음.

진짜 원인은 ngrok 무료 티어의 "방문 확인" 경고 페이지. 브라우저로 사이트에 처음 들어갈 때뿐 아니라, 그 페이지 안에서 실행되는 `fetch()` 요청(`/proxy/py/chart`, `/proxy/py/quick-picks/ai` 등)에도 똑같이 이 경고 페이지(HTML)가 끼어들어서 응답으로 내려옴. `fetchJson`/`playlistFetch`는 이 HTML을 JSON으로 파싱하려다 실패해서 catch로 빠지고, 조용히 빈 배열로 폴백하도록 짜여 있어서(백엔드가 꺼져 있어도 검색은 동작해야 한다는 §의도로 만든 방어 로직) 에러 화면 없이 그냥 아무 곡도 안 뜨는 것처럼 보였음.

**왜 이렇게 했나**: ngrok 공식 문서에서 안내하는 방법대로, 모든 fetch 요청 헤더에 `ngrok-skip-browser-warning: true`를 추가해서 해결(`api.ts`의 `fetchJson`, `playlistApi.ts`의 `playlistFetch` 둘 다). 이 헤더는 ngrok 터널을 거칠 때만 의미가 있고 로컬(`localhost`)이나 나중에 진짜 배포했을 때는 그냥 무시되는 헤더라 부작용이 없음 — 그래서 "ngrok으로 접속했을 때만" 조건부로 넣지 않고 항상 보내도록 함.

**어떻게 동작하나**: 로직 변경 없이 헤더 하나만 추가. 그래서 데스크톱(`localhost`)에서 "빠른 선곡"이 정상 동작하는 걸 확인한 것과, 실제로 이 헤더가 폰 테스트(ngrok 경유)에서 문제를 해결하는지는 서로 다른 확인임 — 데스크톱 확인만으로 이 버그가 고쳐졌다고 볼 수 없음.

**알아둘 한계**: ngrok 무료 티어로 외부인에게 테스트시키는 건 임시 방편이라, 정식 배포 전까지는 이런 종류의 "터널 특유 버그"가 더 나올 수 있음. 친구 테스트 때 곡 목록 자체가 아예 안 뜨는 증상이 다시 보이면, 이 헤더 문제보다 먼저 ngrok 웹 인스펙터(`127.0.0.1:4040`)에서 실제 응답 본문이 JSON인지 HTML인지부터 확인하는 게 빠름.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 64. 모바일 레이아웃 1차 정리 — 재생바 3줄로 쌓이던 문제 + 검색창 화면 밖으로 밀리던 문제

**뭘 했나**: 폰(카카오톡 인앱 브라우저)으로 실제 테스트하다가 발견한 두 가지.

(1) 하단 미니 플레이어(`MiniPlayer.tsx`)가 데스크톱 기준 3분할 그리드(좌: 앨범아트+곡정보+즐겨찾기 / 가운데: 재생+진행바 / 우: 다음 트랙+볼륨+닫기)를 모바일에서 `grid-cols-1`로 그대로 세로로 쌓고 있었음 — 화면 좁은 기기에서 바 높이가 3배가 되고, 아래쪽 줄(볼륨/닫기)이 화면 밖으로 밀려 잘려 보이는 문제였음("재생바가 규격이 안 맞는 느낌"). 데스크톱 레이아웃은 `sm:` 이상에서만 보이게 그대로 두고, 모바일 전용으로 얇은 진행바 + 한 줄짜리 축약 바(썸네일·곡정보·재생/일시정지·닫기)를 새로 추가함. 큐/볼륨 조작은 모바일에서 바를 눌러 여는 확장 재생 화면("다음 트랙" 탭)으로 넘김.

(2) 검색창(`SearchBar.tsx`)의 입력창을 감싸는 `flex-1` div에 `min-w-0`이 없어서, flex 아이템 기본 동작(내용 기준 최소 너비를 지키려 함) 때문에 좁은 화면에서 검색창+"검색" 버튼이 화면 밖으로 밀려나 잘려 보였음(스크린샷에서 "검" 한 글자만 겨우 보이던 증상). `min-w-0` 한 줄 추가로 수정.

(3) (추가 발견) 미니 플레이어를 눌러서 여는 확장 재생 화면도 모바일에서 앨범아트/상단 닫기 버튼이 안 보이고 곡 정보(제목) 없이 아티스트명과 재생 버튼부터 바로 보이는 증상 발견. 원인으로 보이는 것 두 가지를 같이 고침: ① 오버레이가 정적 `100vh` 기준이라 주소창이 접혔다 펴졌다 하는 모바일 브라우저에서 실제 보이는 영역과 어긋날 수 있어 `h-[100dvh]`(동적 뷰포트 높이)로 교체. ② 데스크톱 2열 그리드(`lg:grid-cols-2`)가 모바일에서 1열로 접힐 때, 왼쪽(앨범아트+정보+컨트롤)/오른쪽(탭+다음 트랙 목록) 블록이 각각 자체 `overflow-hidden`/`overflow-y-auto`로 높이가 갇혀 있어서, 내용이 한 화면에 다 안 들어가면 스크롤 없이 그냥 잘렸던 것 — 그 내부 스크롤 제약들을 `lg:` 전용으로 바꾸고, 모바일에서는 오버레이 전체(바깥 컨테이너)가 통째로 스크롤되게 바꿈. 다만 ①/②가 실제 원인이 맞는지는 코드 리딩으로 추정한 것이라, 라이브로 확인해봐야 확실함.

**왜 이렇게 했나**: "이런 게 더 있을 것 같다"는 지적대로, 데스크톱 우선으로 짜인 그리드/플렉스 레이아웃이 좁은 화면에서 시험을 안 거친 채로 쌓여있었을 가능성이 높음. 다만 전체를 한 번에 다 훑기보다, 지금 실제로 눈에 띈 두 곳부터 고치고 나머지는 계속 써보면서 걸리는 대로 고치는 쪽을 택함 — 데스크톱 화면이 멀쩡한 상태에서 모바일 대응 코드를 검증 없이 대량으로 바꾸면 오히려 데스크톱을 깨뜨릴 위험이 더 크다고 판단.

**어떻게 동작하나**: Tailwind 반응형 접두사(`sm:`)로 완전히 분리된 두 벌의 마크업(모바일용 `sm:hidden`, 데스크톱용 `hidden sm:grid`)을 쓰는 방식이라, 데스크톱 쪽 코드/스타일은 전혀 안 건드림 — 회귀 위험이 거의 없음.

**알아둘 한계**: 사이드바("내 즐겨찾기"/"내 재생목록")는 `lg:` 미만에서 그리드가 1열로 접히면서 메인 콘텐츠 위/아래로 순서대로 쌓이게 되는데, 이게 모바일에서 보기 좋은 위치인지는 아직 확인 안 함(우선순위상 지금은 안 건드림). 이번 두 가지 외에 다른 화면(재생목록 상세, 관리자 페이지 등)도 모바일에서 검증된 적 없어서, 써보다가 또 나올 수 있음.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 65. 확장 재생 화면(앨범아트 큰 화면)이 모바일에서 스크롤 없이 잘려 보이던 문제

**뭘 했나**: 확장 재생 화면(§25)이 데스크톱 2단 레이아웃(왼쪽 앨범아트+컨트롤 / 오른쪽 다음트랙·가사 탭)을 만들 때 양쪽 블록에 각각 `overflow-y-auto`를 줘서 "한쪽이 길어져도 서로 독립적으로 스크롤되게" 했는데(§25 설계 의도), 모바일에서 세로로 쌓이는 순간 이 방식이 오히려 문제가 됐음 — 전체 화면(`fixed inset-0`) 자체엔 스크롤이 없고 안쪽 두 블록만 각자 스크롤이라, 두 블록의 콘텐츠 높이 합이 화면보다 크면 전체 레이아웃이 화면 밖으로 밀려도 바깥에서 스크롤할 방법이 없어 아래쪽이 그냥 잘려 보였음.

**어떻게 고쳤나**: 바깥 컨테이너(`fixed inset-0 ...`)에 `h-[100dvh] overflow-y-auto`를 주고, 안쪽 두 블록의 `overflow-y-auto`/`min-h-0`는 `lg:` 접두사를 붙여 데스크톱(2단 레이아웃일 때)에서만 독립 스크롤을 쓰게 함. 모바일에선 전체가 하나의 스크롤 영역이 되고, 데스크톱은 기존 동작(양쪽 독립 스크롤) 그대로 유지.

**알아둘 한계**: 코드만 보고 판단해서 고친 거라 실제 기기에서 재생해보면서 확인 필요.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 66. Media Session API 연동 + Capacitor 안드로이드 래퍼 초기 세팅 — 배경 재생 문제의 근본 해결 착수

**뭘 했나**: 폰(카카오톡 인앱 브라우저, 실제 사파리 둘 다)에서 화면을 끄거나 다른 앱으로 나가면 재생이 끊기는 문제(§63/§64 논의에서 이어짐)의 원인은 유튜브 iframe이 크로스 오리진 콘텐츠라 모바일 브라우저가 백그라운드에서 그 안의 미디어를 강제로 멈추기 때문 — 이건 코드 몇 줄로 못 고치는 플랫폼 차원 제약이라는 걸 확인함. 유튜브 iframe 재생 방식 자체는 유지하면서(오디오 직접 추출/재호스팅은 유튜브 ToS 위반이라 배제) 실제로 해결하는 방법은, 이 웹앱을 네이티브 앱 껍데기로 감싸서 OS에게 "이 앱은 백그라운드에서도 오디오 재생할 앱"이라고 선언하는 것 — Capacitor를 선택함.

오늘 한 것 두 가지:
1. `MiniPlayer.tsx`에 Media Session API 연동 추가 — 곡이 바뀌면 `navigator.mediaSession.metadata`(제목/아티스트/앨범아트)를 채우고, 재생/일시정지 상태를 `playbackState`에 반영하고, 잠금화면·알림의 재생/일시정지/탐색 버튼이 실제 플레이어를 조작하도록 `setActionHandler`로 연결함. 이건 그 자체로 배경 재생을 보장하진 않지만, 나중에 네이티브 껍데기가 "지금 진짜 미디어를 재생 중"이라고 인식하기 위한 전제 조건이고, 지금 당장도 안드로이드 크롬 등에서 잠금화면에 곡 정보가 뜨는 부수 효과가 있음.
2. `frontend/`에 Capacitor(`@capacitor/core`, `@capacitor/cli`, `@capacitor/android`) 설치 + 안드로이드 플랫폼 추가(`frontend/android/` 생성됨). `capacitor.config.ts`는 정적 빌드를 번들로 넣는 대신 `server.url`로 지금 켜져 있는 ngrok 주소(`https://frenzy-chaste-snowsuit.ngrok-free.dev`)를 그대로 WebView로 열게 설정 — 코드/배포 구조를 새로 안 만들고 "OS 배경 재생 허용"이라는 이 시점에 필요한 한 가지만 얻는 최소 변경.

**왜 이렇게 했나**: 처음엔 "친구가 실제로 불편해하는지 보고 나서 결정하자"고 제안했는데, 본인이 이미 써봐서 아는 문제고("공부하면서 폰 꺼두고 듣는다") 요즘 음악 앱은 다 당연히 되는 기본 기능이라 검증이 필요한 니즈가 아니라는 지적을 받고 방향을 바꿈. iOS는 TestFlight 배포에 애플 개발자 계정($99/년)이 필요해서 막혀 있는 상태라(학과/지인 계정 대여 알아보는 중, [[학습노트-project-safelink-backend-security-fix]]처럼 별도 메모는 아니고 이 세션 내 논의), 계정 없이도 완전 무료로 되는 안드로이드부터 먼저 검증하기로 함 — 친구에게 빌려준 공기계 안드로이드 폰을 돌려받기로 함.

**어떻게 동작하나**: `npx cap add android`가 `frontend/android/`에 표준 Gradle 프로젝트를 만들어줌 — Android Studio로 이 폴더를 열어서 빌드하면, 실제로는 브라우저 앱이 아니라 하나의 네이티브 앱으로 설치되고, 그 안의 WebView가 ngrok 주소를 로드함(기존 웹 버전과 화면/기능은 완전히 동일, 껍데기만 다름). 아직 배경 재생을 위한 추가 네이티브 설정(포그라운드 서비스 등)은 안 건드림 — 안드로이드는 iOS보다 백그라운드 미디어 제약이 느슨한 편이라, 이 최소 구성만으로 이미 꽤 개선될 수도 있다고 보고 일단 실기기로 먼저 확인한 뒤 필요하면 추가하기로 함.

**알아둘 한계**: (1) 이 세션(샌드박스)에서 실제 안드로이드 빌드/설치까지는 못 해봄 — Android Studio/에뮬레이터가 없는 환경이라 `frontend/android/` 프로젝트 구조를 만들고 파일을 실제 저장소에 복사해두기만 함, 맥에서 직접 열어서 빌드해야 함. `npm install`을 다시 한 번 돌려서 `node_modules`를 `package-lock.json`과 맞춰야 함(코드는 복사했지만 `node_modules`는 안 옮김 — 개발 서버가 그 폴더를 계속 쓰고 있어서 건드리지 않음). (2) ngrok 무료 터널 주소가 바뀌면(도메인이 재할당되면) `capacitor.config.ts`의 `server.url`도 같이 바꿔야 함. (3) 배경 재생이 이 최소 구성만으로 충분한지 아직 모름 — 안 되면 포그라운드 서비스(알림 상시 표시) 방식의 네이티브 플러그인을 추가로 붙여야 할 수 있음. (4) iOS는 아직 시작 안 함 — 애플 개발자 계정 문제가 풀려야 진행 가능, 그때는 여기에 더해 `Info.plist`의 `UIBackgroundModes: audio`와 오디오 세션 카테고리 설정이 추가로 필요할 걸로 예상.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 67. 무드 탐색 국내 결과가 무드와 무관하게 거의 똑같이 뜨던 버그 — k-pop 고정 차트 폴백을 큐레이션 목록으로 교체

**뭘 했나**: "집중"이랑 "신나는" 무드를 눌렀을 때 국내(domesticTracks) 목록이 순서까지 완전히 똑같이 뜨는 걸 실사용 중 발견함. 원인은 `discover_by_mood`에서 Last.fm의 무드 태그(`study`, `party` 등)로 국내 곡을 찾다가 6곡 미만이면 무드와 무관한 "k-pop" 태그의 고정 인기 차트로 채우던 로직(§10) — 한국 아티스트는 Last.fm에 무드 태깅이 거의 안 돼 있어서 사실상 대부분의 무드에서 이 조건에 걸리고, "k-pop" 차트 자체는 거의 안 바뀌니 결과가 똑같이 보였던 것. 해외(internationalTracks)는 Last.fm 영어 무드 태그 데이터가 충분해서 다르게 나왔던 것과 대비됨.

"이런 기본 기능이 안 되면 왜 쓰냐"는 지적을 받고("확인이 필요한 니즈"가 아니라 바로 고쳐야 하는 문제로 판단), `CURATED_DOMESTIC_MOOD_TRACKS`라는 딕셔너리를 새로 추가함 — 무드 태그(workout/chill/study/party/mellow/sad/love/driving) 8개 각각에 실제로 그 상황에 어울리는 국내 곡을 직접 골라 (아티스트, 곡명) 쌍으로 등록해둠. `_resolve_curated_tracks()`가 이 목록을 Last.fm `track.search`로 실제 메타데이터(청취자 수/이미지/url)까지 채워서 반환하고(§38 AI 추천 검증과 같은 패턴), 국내 후보가 부족할 때 이 큐레이션 목록을 k-pop 고정 차트보다 우선 사용하도록 바꿈. 큐레이션 목록에 없는 태그이거나 검색이 다 실패하는 극단적 케이스에 한해서만 예전 k-pop 차트 폴백이 마지막 안전망으로 남아있음.

**왜 이렇게 했나**: Last.fm 태그 데이터 자체의 한계(한국 무드 태깅 부족)는 코드로 못 고치는 부분이라, "Last.fm 데이터에 기대는 걸 그만두고 직접 큐레이션한다"는 방향으로 바꿈. 캐시(`curated-track:v1:...`)는 1주일로 태그 캐시보다 길게 잡았는데, 이 목록은 코드를 고치기 전엔 안 바뀌는 고정 데이터라 Last.fm/iTunes 호출을 아낄 수 있어서.

**어떻게 동작하나**: `discover-by-mood` 캐시 키를 v3→v4로 올려서 옛 k-pop 폴백 결과가 캐시 만료 전까지 계속 나가는 일이 없게 함.

**알아둘 한계**: (1) 큐레이션 목록의 곡 선정은 내가 일반적인 무드 기준으로 고른 거라, 실제 취향에 안 맞을 수 있음 — `CURATED_DOMESTIC_MOOD_TRACKS` 딕셔너리만 고치면 바로 반영되니, 써보면서 마음에 안 드는 곡은 직접 바꿔도 됨. (2) 목록에 넣은 (아티스트, 곡명)이 Last.fm에 정확히 그 이름으로 안 걸리면 조용히 후보에서 빠짐 — 국내 목록이 여전히 6곡 미만이면 어떤 곡이 검색 실패했는지 로그로 확인 필요(현재 별도 로그는 안 남기고 있음). (3) 해외(internationalTracks)는 이번에 안 건드림 — 원래도 무드별로 다르게 나오고 있었음.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 68. §67 라이브 확인 중 발견 — 무드 조회가 "잔잔한"/"슬픔"에서 유독 느리거나 실패하던 문제(iTunes 순차 호출)

**뭘 했나**: §67 확인 중 "신나는"은 잘 되는데 "잔잔한"/"슬픔"은 에러("무드 곡을 불러오지 못했어요")가 뜨거나 응답이 한참 안 오는 걸 발견. 백엔드 로그를 직접 보고 원인 확인 — 무드 하나를 조회할 때마다 Last.fm 태그 후보 50곡 각각의 앨범 커버를 iTunes Search API로 조회하는데(`_tag_top_tracks` → `track_image` → `resolve_track_image`, §31/§48), 이 50번의 iTunes 호출이 순차(한 번에 하나씩, 앞 호출이 끝나야 다음 호출 시작)로 처리되고 있었음. 마침 이날 여러 무드를 짧은 시간에 연달아 테스트하면서 iTunes의 비공식 호출 한도에 걸려 429/403 응답이 무더기로 뜨기 시작했는데, 순차 처리라 50개를 하나하나(각각 최대 3초 타임아웃) 다 거쳐야 응답이 나가서 "잔잔한"/"슬픔"처럼 하필 캐시가 비어있던(=50곡을 전부 새로 조회해야 했던) 무드에서 체감상 멈추거나 타임아웃처럼 보였음. "신나는"이 멀쩡했던 건 그 곡들 이미지가 앞서 테스트 때 이미 캐싱돼 있어서 iTunes를 거의 새로 안 불렀기 때문.

**어떻게 고쳤나**: `_tag_top_tracks`의 이미지 조회를 `top_artists()`/`_lyrics_availability_map()`과 같은 패턴(`ThreadPoolExecutor(max_workers=10)`)으로 병렬화함 — 50곡을 순서대로 하나씩이 아니라 최대 10개씩 동시에 처리해서 최악의 경우에도 대기 시간이 대략 5분의 1 수준으로 줄어듦.

**왜 이렇게 했나**: iTunes 쪽 호출 한도 자체(비공식 rate limit)는 우리가 통제할 수 없는 외부 제약이라(§48에서 이미 한 번 겪은 문제) 없앨 순 없음 — 대신 "그 제약에 걸렸을 때 사용자가 체감하는 대기 시간"을 줄이는 쪽으로 대응함.

**알아둘 한계**: (1) 이건 완화지 완전한 해결이 아님 — iTunes 호출이 몰리는 시점(여러 무드/기능을 짧은 시간에 연달아 쓸 때)엔 여전히 이미지 없는 카드가 늘어날 수 있음(§51에서 이미 "실패는 30분만 캐싱해서 곧 재시도"로 다뤄지고 있는 부분과 같은 종류의 한계). (2) 이번에 화면에 떴던 "무드 곡을 불러오지 못했어요"(빨간 에러)가 정확히 이 지연 때문이었는지, 아니면 그 시점에 우연히 다른 요인(예: 재시작 타이밍)까지 겹쳤던 건지는 완전히 특정하지 못함 — 이번 병렬화 이후에도 같은 에러가 재현되면 그건 다른 원인일 가능성이 있음.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 69. 검색창 자동완성(타이핑 중 후보 노출) — 유튜브 뮤직 검색창 참고

**뭘 했나**: 검색창에 "numb"까지만 쳐도 "Numb Little Bug"처럼 뒷부분을 몰라도 곡을 찾을 수 있게, 유튜브 뮤직 검색창처럼 타이핑 중간에 후보 목록이 뜨는 자동완성을 추가함. 두 글자 이상 입력하면 250ms 디바운스 후 새 엔드포인트(`GET /search-suggest`)를 호출해서 트랙/아티스트 후보를 드롭다운에 보여주고, 클릭하면 바로 그 검색어로 `/search`(기존 통합 검색)를 실행함.

**왜 별도 엔드포인트로 뺐나**: 기존 `/search`는 오타 교정(한글 후처리, wordninja), Claude Haiku AI 추측 폴백, 유튜브 검색 폴백, iTunes 이미지 리졸브까지 다 포함된 "정확한 최종 결과"용이라(§45~§59) 무겁고 느림 — 한 글자 칠 때마다 이 전체 파이프라인을 태우면 안 됨. 그래서 이미지 리졸브 없이 이름만 빠르게 주는 `/search-suggest`를 분리하고, 기존 쓰레기 필터(`_is_junk_track_match`, `_is_junk_artist_match`, §54)는 재사용해서 팬캠/방송클립 같은 쓰레기가 후보에 섞이는 것만 최소한으로 막음.

**어떻게 고쳤나**:
- 백엔드(`backend/main.py`): `/search-suggest?q=` 신규 — `track.search`(limit 8) + `artist.search`(limit 5) 호출 후 쓰레기 필터만 거쳐 이름만 반환. 후보 목록은 최신성보다 속도가 중요해서 캐시 TTL을 6시간으로 길게 줌(`search-suggest:v1:...`).
- 프론트(`frontend/lib/api.ts`): `searchSuggest()` 추가.
- 프론트(`frontend/components/SearchBar.tsx`): `value`가 바뀔 때마다 디바운스 타이머로 `searchSuggest` 호출, 결과를 `suggestions` 상태에 저장. 입력값이 2글자 미만이면 기존 '최근 검색' 드롭다운을, 2글자 이상이면 이 자동완성 후보 드롭다운을 보여주도록 분기(`isSuggestingMode`).

**알아둘 한계**: (1) 캐시가 검색어 접두어별이 아니라 완성된 문자열별이라("num", "numb", "numb "처럼 한 글자씩) 같은 단어를 타이핑할 때마다 사실상 다른 캐시 키를 타서, 첫 방문자는 매번 Last.fm을 실제로 호출함 — 트래픽이 커지면 접두어 기반 캐싱(trie 등)으로 개선 여지 있음. (2) 로그인/세션 기록(`log_event`)을 안 남기므로 이 엔드포인트 자체는 분석 대시보드에 안 잡힘(의도한 것 — 타이핑 중간값이라 실제 검색 의도가 아님).

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 70. 무드 탐색 결과도 화살표 페이지네이션 대신 가로 스크롤로 전환

**뭘 했나**: "출퇴근길" 같은 무드 탐색 결과(국내/해외 인기곡)가 좌우 화살표(1/2, ‹ ›)로 페이지를 넘기는 방식이었는데, "다시 듣기"/"빠른 선곡"처럼 가로 드래그 스크롤로 바꿔달라는 요청. `CompactTrackGrid`에 이미 있던 `scrollable` prop(§40에서 다시 듣기/빠른 선곡용으로 추가됨)을 무드 결과 두 그리드(`frontend/app/page.tsx`의 국내/해외 인기곡 `CompactTrackGrid`)에도 켜기만 하면 됐음 — 새 코드 작성 없이 기존 옵션 재사용.

**왜 이렇게 했나**: `scrollable` prop이 이미 "3행 고정 + 가로 드래그 스크롤" 동작을 구현해뒀어서, 새 컴포넌트를 만들 필요 없이 prop 하나만 켜면 끝나는 문제였음. 이제 이 그리드가 페이지네이션 방식으로 남은 곳은 검색 결과 하나뿐.

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 71. 개발 서버 관리 스크립트(dev-up.sh/dev-down.sh) — 터미널 여러 개 켜야 하는 번거로움 해소

**뭘 했나**: 로컬 개발할 때마다 레디스(켜져있는지도 헷갈림) + uvicorn 백엔드 + ngrok까지 터미널을 여러 개 띄워야 했던 걸, `backend/dev-up.sh` 한 번 실행으로 (1) 레디스가 꺼져있으면 `brew services start redis`로 켜고 이미 켜져있으면 그냥 넘어가고, (2) uvicorn을 백그라운드로 띄우고, (3) ngrok도 백그라운드로 띄운 뒤 ngrok 로컬 관리 API(4040 포트)에서 방금 발급된 공개 주소를 자동으로 읽어와 터미널에 보여주게 함. 켜져있는 프로세스는 PID 파일(`backend/.pids/`)로 추적해서 중복 실행을 막고, `backend/dev-down.sh`로 uvicorn/ngrok만 깔끔히 종료할 수 있게 함(레디스는 상시 서비스로 두고 안 끔).

**왜 프론트엔드는 안 넣었나**: 프론트는 이미 VS Code에서 별 불편함 없이 잘 켜고 있다고 해서, 실제로 불편했던 백엔드 쪽(레디스 상태 확인 + uvicorn + ngrok)만 스크립트로 묶음 — 안 불편한 부분까지 손대면 오히려 기존 워크플로우에 혼란만 더할 수 있다고 판단.

**알아둘 한계**: (1) `redis`/`ngrok` 커맨드가 PATH에 잡혀있어야 함(둘 다 이미 터미널에서 직접 실행해본 적 있으니 될 것으로 예상하지만 처음 실행 시 "command not found"가 뜨면 각각 `brew install redis`/`brew install ngrok`로 설치 필요할 수 있음). (2) `backend/venv/bin/python`을 직접 지정해서 uvicorn을 켜므로, 가상환경 경로가 바뀌면 스크립트도 같이 수정해야 함. (3) ngrok 무료 플랜은 터널 하나만 동시에 열 수 있어서, 다른 곳에서 이미 ngrok을 켜둔 상태면 이 스크립트의 ngrok 실행이 실패할 수 있음(로그: `backend/logs/ngrok.log`에서 확인).

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 72. 안드로이드 백그라운드 재생 — Capacitor만으론 안 됨을 실기(에뮬레이터)로 확인 + 포그라운드 서비스 플러그인 추가

**뭘 했나**: §66에서 세팅한 Capacitor 안드로이드 앱을 에뮬레이터에 처음 설치해서 실제로 테스트해봄. 앱 자체는 정상 로드되고(빠른 선곡/차트/무드/검색 다 실제 데이터로 뜸) 곡 재생도 되는데, 홈 버튼 눌러 앱을 백그라운드로 보내는 순간 소리가 완전히 멈추는 걸 확인함 — 예상했던 대로 iOS의 `UIBackgroundModes: audio`에 대응하는 게 안드로이드엔 없고, 대신 "포그라운드 서비스"(알림바에 항상 알림이 떠있는 방식, 유튜브 뮤직/스포티파이가 재생 중 알림에 곡 정보 보여주는 것과 같은 원리)를 직접 붙여야 웹뷰(오디오 포함)가 안 죽는다는 걸 실기로 재확인.

**어떻게 고쳤나**:
- `cordova-plugin-background-mode`(+의존 플러그인 `cordova-plugin-device`)를 `frontend/package.json`에 추가. Capacitor는 코르도바 플러그인도 그대로 쓸 수 있어서 이 오래됐지만 검증된 플러그인을 선택함(포그라운드 서비스를 웹/JS 쪽에서 `enable()`/`disable()` 호출만으로 켜고 끌 수 있게 해줌).
- `frontend/components/MiniPlayer.tsx`에 `isPlaying`이 바뀔 때마다 `window.cordova?.plugins?.backgroundMode`를 켜고 끄는 이펙트 추가(재생 중일 때만 켜서, 안 쓸 때까지 알림이 계속 떠 있는 UX 저하를 피함). 이 객체는 일반 브라우저엔 아예 없어서 옵셔널 체이닝으로 안전하게 무시됨 — 웹/안드로이드 앱 양쪽 같은 코드로 동작.
- **중요한 함정을 미리 막음**: 이 프로젝트의 `targetSdkVersion`이 36(API 34+ 규칙 적용 대상)인데, `cordova-plugin-background-mode`는 몇 년째 업데이트가 없어서 자기 모듈 매니페스트에 서비스를 `foregroundServiceType` 없이 선언함. API 34부터는 타입 없는 포그라운드 서비스를 시작하면 `MissingForegroundServiceTypeException`으로 즉시 죽는 게 플랫폼 규칙이라, 이 플러그인을 아무 손질 없이 그대로 쓰면 백그라운드 모드를 켜자마자 앱이 크래시할 게 거의 확실했음. 플러그인 소스는 직접 못 고치니, `frontend/android/app/src/main/AndroidManifest.xml`에 같은 `android:name`(`de.appplant.cordova.plugin.background.ForegroundService`)으로 서비스를 다시 선언하면서 `android:foregroundServiceType="mediaPlayback"`을 추가하고, `FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_MEDIA_PLAYBACK` 권한도 같이 추가함 — 안드로이드 매니페스트 병합 규칙상 같은 컴포넌트를 앱 매니페스트에서 다시 선언하면 속성이 합쳐지는 걸 이용한, 서드파티 라이브러리 매니페스트를 못 고칠 때 쓰는 표준적인 우회법.
- 작업 방식: `npx cap sync android`를 실행하면 이 프로젝트가 마운트된 샌드박스 경로에서 `Unknown system error -35`가 나서(§ 이전 세션에서 `cap add android` 때 겪은 것과 같은 FUSE 마운트 이슈로 추정), 임시 스크래치 복사본에서 `npm install` + `npx cap sync android`를 실행해 필요한 네이티브 프로젝트 변경분(매니페스트, 코르도바 브릿지 JS, 플러그인 자바 소스)만 진짜 프로젝트로 다시 복사하는 §66과 같은 우회 방식을 또 씀.

**알아둘 한계**: (1) `capacitor.config.ts`가 로컬 정적 빌드가 아니라 `server.url`(ngrok 실주소)을 그대로 로드하는 방식이라, Capacitor의 코르도바 브릿지(`window.cordova`)가 원격 페이지에도 정상 주입되는지는 이론상/문서상 그렇다고 알려져 있지만 이번에 직접 라이브 확인은 아직 못 함 — 안 뜨면(`window.cordova`가 `undefined`) 백그라운드 모드 자체가 아예 발동을 안 하는 것부터 다시 진단해야 함. (2) 포그라운드 서비스가 켜지면 상단 알림바에 "Unbeat / 음악을 재생하고 있어요" 알림이 재생 중 내내 떠있게 됨 — 이건 안드로이드가 요구하는 방식이라 없앨 수 없고, 유튜브 뮤직/스포티파이도 똑같이 이럼. (3) `frontend/android` 네이티브 파일을 스크래치 복사본에서 손으로 다시 복사해온 방식이라, 나중에 사용자가 직접 `npx cap sync android`를 다시 돌리면(맥 터미널에서는 이 FUSE 문제 없이 정상 실행될 것으로 예상) `AndroidManifest.xml`의 `foregroundServiceType` 수동 패치가 덮어써질 수 있음 — 그런 경우 이 노트를 보고 서비스 선언을 다시 추가해야 함.

**§72 라이브 디버깅 추가 기록 (실기로 두 단계 더 파고든 과정)**:
- Android Studio Logcat(`package:mine` 필터)으로 실제 크래시를 잡음: `FATAL EXCEPTION ... Unable to create service de.appplant.cordova.plugin.background.ForegroundService: IllegalArgumentException: Targeting S+ (version 31 and above) requires that one of FLAG_IMMUTABLE or FLAG_MUTABLE be specified when creating a PendingIntent`. `cordova-plugin-background-mode`는 프로젝트 안에 소스(.java)가 그대로 들어있어서(프리컴파일 jar가 아니라 빌드 시 같이 컴파일됨) 직접 고칠 수 있었음 — `android/capacitor-cordova-android-plugins/src/main/java/de/appplant/cordova/plugin/background/ForegroundService.java`의 `PendingIntent.getActivity(...)` 호출에 `PendingIntent.FLAG_IMMUTABLE` 추가.
- 크래시는 없어졌는데 재생 중 알림바에 알림이 여전히 안 뜨는 문제가 남음 — Logcat에 크래시 나기 전 남아있던 경고(`Attempted to start a foreground service ... with a broken notification (no icon: ...)`)를 다시 보고 원인 확인: 이 플러그인의 기본 알림 아이콘 이름이 `"icon"`인데, Capacitor가 기본 생성한 안드로이드 프로젝트엔 그 이름의 리소스가 없어서(`ic_launcher`/`ic_launcher_round`/`ic_launcher_foreground`만 있음) 아이콘 리졸브가 실패 → `setSmallIcon(0)` → 알림 자체가 렌더링 안 됨(크래시는 안 나고 조용히 안 보이기만 함). `frontend/components/MiniPlayer.tsx`의 `backgroundMode.setDefaults(...)` 호출에 `icon: "ic_launcher_foreground"`를 명시해서 해결(어댑티브 아이콘 XML인 `ic_launcher` 자체는 알림 아이콘으로 못 쓰는 리소스 타입이라 피함).
- 아이콘 수정 후에도 알림바에 알림이 여전히 하나도 안 뜸(설정 화면 "Configure qwerty2" 알림만 보임) — `enable()` 호출 자체엔 에러도 크래시도 없었는데(Logcat 상 `configure`→`enable` 정상 기록), 알림만 조용히 안 나타남. 원인: Android 13(API 33)부터는 알림을 화면에 "보여주는" 것 자체에 런타임 권한(`POST_NOTIFICATIONS`)이 필요한데, `cordova-plugin-background-mode`는 Android 13이 나오기 한참 전 플러그인이라 이 권한을 요청하는 코드가 아예 없음(플러그인 자바 소스 전체를 검색해도 `POST_NOTIFICATIONS` 관련 코드 없음 확인). 이 권한이 없으면 포그라운드 서비스 자체는 떠도 알림만 시스템이 조용히 숨김 — 크래시도 경고 로그도 안 남아서 가장 찾기 어려웠던 원인.
- 고친 방법: `frontend/android/app/src/main/AndroidManifest.xml`에 `<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />` 추가하고, `frontend/android/app/src/main/java/com/unbeat/app/MainActivity.java`의 `onCreate()`에서 API 33+일 때 `ActivityCompat.requestPermissions(...)`로 앱 시작 시 직접 요청하도록 네이티브 코드 추가(플러그인이 안 해주니 앱 쪽에서 대신 요청). 이건 네이티브 코드 변경이라 §69~71과 달리 Fast Refresh로는 안 되고, Android Studio에서 Gradle 재동기화 + Run(재설치)까지 해야 반영됨.
- 이 세 문제 다 "오래된 서드파티 플러그인 vs 최신 안드로이드(API 34+, targetSdk 36) 규칙" 간극에서 나온 것으로, 이 플러그인을 Capacitor+최신 안드로이드에 쓸 때 흔히 겪는 함정으로 보임 — 나중에 비슷한 코르도바 플러그인을 추가할 때도 PendingIntent 플래그, 포그라운드 서비스 타입, 알림 아이콘 리소스 이름, 그리고 (Android 13+ 타깃이면) `POST_NOTIFICATIONS` 런타임 권한까지 네 가지를 의심해볼 것.

**§72 최종 결론 (실기로 근본 원인 확정)**: 알림/권한/크래시를 위 세 가지 다 고친 뒤에도 홈 버튼을 누르면 소리가 여전히 끊김을 확인함. Logcat 타임스탬프를 정밀 대조해서 원인을 특정함 — `App paused`(Bridge.onPause) 로그 → 약 1초 뒤 `App stopped`(Bridge.onStop) 로그 → 그 직후 딱 70ms 만에 `AAudioStream_requestStop(...)`가 찍힘. 즉 오디오가 끊기는 시점은 액티비티가 `Stopped` 상태로 넘어가는 순간과 거의 정확히 일치함. 원인은 자바스크립트의 `document.hidden`/`visibilitychange`가 아니었음(직접 로그를 남겨서 검증함 — 홈 버튼을 눌러도 이 이벤트 자체가 한 번도 안 찍힘). 진짜 원인은 액티비티가 `onStop()`으로 넘어가는 순간 그 액티비티에 물린 웹뷰의 렌더링 서피스를 안드로이드 시스템이 정리하면서, 거기 물려있던 오디오 스트림까지 같이 끊기는 것 — 크로미움 웹뷰가 액티비티 생명주기에 묶여 있는 근본적인 동작이라, `cordova-plugin-background-mode`(또는 어떤 알림/권한 설정)로도 못 고침. `KeepRunning`(Capacitor 기본값 true) 덕분에 JS 타이머 자체는 안 멈추지만, 그것과 이 서피스 정리는 별개임. 결론: **화면에 보이는 액티비티의 웹뷰 안에서 유튜브 iframe을 재생하는 구조로는 안드로이드 백그라운드 재생이 원천적으로 불가능함.**

**아직 라이브 확인 안 됨** — 다음에 이어서 정리할 것에 추가.

---

## 73. 안드로이드 백그라운드 재생 재설계 — 헤드리스 웹뷰를 소유하는 포그라운드 서비스

**뭘 했나**: §72에서 확정한 근본 원인(액티비티 웹뷰는 onStop() 시 서피스가 정리되며 오디오까지 끊김)을 우회하기 위해, 실제 유튜브 iframe 재생을 액티비티가 아니라 **포그라운드 서비스가 소유하는, 화면에 안 보이는 별도 웹뷰**로 완전히 옮김. 화면에 보이는 Next.js 앱(MiniPlayer.tsx)은 이제 오디오 소스가 아니라 재생 상태를 보여주고 컨트롤 명령만 보내는 "리모컨" 역할만 함.

**어떻게 고쳤나**:
- `frontend/android/app/src/main/assets/player.html` (신규): 정적 로컬 파일. 유튜브 IFrame API를 로드해서 `YT.Player`를 만들고, `window.nativeLoadAndPlay/nativePlay/nativePause/nativeSeekTo/nativeSetVolume/nativeMute/nativeUnmute` 같은 전역 함수로 조종할 수 있게 노출함. 상태 변화(`ready`/`stateChange`/`timeUpdate`)는 `AndroidBridge.onEvent(json)`으로 네이티브에 전달. `assets/public/`(Capacitor가 `cap sync`마다 덮어쓰는 폴더) 바깥에 둬서 나중에 `cap sync`를 다시 돌려도 안 지워짐.
- `frontend/android/app/src/main/java/com/unbeat/app/PlaybackService.java` (신규): 포그라운드 서비스. `onCreate()`에서 헤드리스 `WebView`를 만들어(화면에 절대 안 붙임) `file:///android_asset/player.html`을 로드하고, `addJavascriptInterface`로 `AndroidBridge`를 노출해서 위 페이지의 이벤트를 받음. 유튜브 상태가 PLAYING(1)이 될 때만 `startForeground()`로 알림을 띄우고, PAUSED/ENDED가 되면 내림(§72 때 정한 "재생 중일 때만 알림" 원칙 그대로 유지). 알림 아이콘/채널/PendingIntent 플래그는 §72에서 이미 검증된 값(`ic_launcher_foreground`, `IMPORTANCE_LOW`, `FLAG_IMMUTABLE`)을 그대로 재사용.
- `frontend/android/app/src/main/java/com/unbeat/app/PlaybackPlugin.java` (신규): 커스텀 Capacitor 플러그인(`@CapacitorPlugin(name = "PlaybackBridge")`). `load()`에서 `PlaybackService`에 `bindService(..., BIND_AUTO_CREATE)`로 붙고(이것만으로 서비스가 시작됨), `loadAndPlay/play/pause/seekTo/setVolume/mute/unmute` 같은 `@PluginMethod`를 JS에 노출해서 서비스의 같은 이름 메서드로 그대로 전달함. 서비스에서 온 이벤트는 `notifyListeners(...)`로 JS에 되돌려줌.
- `frontend/android/app/src/main/java/com/unbeat/app/MainActivity.java`: `super.onCreate()` 호출 전에 `registerPlugin(PlaybackPlugin.class)` 추가 — npm 패키지가 아니라 앱 모듈 안에 직접 넣은 로컬 플러그인이라 자동으로 스캔이 안 돼서 수동 등록 필요.
- `frontend/android/app/src/main/AndroidManifest.xml`: `PlaybackService`를 `foregroundServiceType="mediaPlayback"`으로 선언 추가(§72와 같은 이유 — API 34+ 규칙).
- `frontend/lib/nativePlayer.ts` (신규): `PlaybackBridge` 플러그인을 감싸서, 기존 `YT.Player` 객체와 거의 같은 모양(`playVideo`/`pauseVideo`/`seekTo`/`setVolume`/`mute`/`unMute`/`getCurrentTime`/`getDuration`/`loadVideoById`/`destroy`)의 프록시 객체로 노출하는 어댑터. `isNativeAndroid()`로 안드로이드 네이티브 앱인지 판별.
- `frontend/components/MiniPlayer.tsx`: 플레이어 생성 이펙트에서 `isNativeAndroid()`면 `createNativePlayer(...)`(위 어댑터)를, 아니면 기존처럼 `new YT.Player(...)`를 만들도록 분기. `onReady`/`onStateChange` 콜백 로직 자체는 두 경로가 공유(중복 코드 최소화). `window.YT.PlayerState`를 안 쓰고(네이티브 경로에선 이 화면에 유튜브 API 자체를 안 띄우므로 존재 안 함) 유튜브의 안정적인 공개 상수값을 `YT_STATE`로 하드코딩해서 양쪽에서 재사용. 예전에 쓰던 `cordova-plugin-background-mode` 켜고 끄는 이펙트는 이제 `PlaybackService`가 자체적으로 알림을 관리하므로 통째로 제거함.

**왜 이렇게 했나**: Capacitor 문서/커뮤니티를 보면 "웹뷰 기반 오디오는 백그라운드에서 못 돌린다"가 사실상 알려진 한계이고, 스포티파이/유튜브뮤직 같은 실서비스도 웹뷰가 아니라 네이티브 미디어 파이프라인을 씀. 유튜브 오디오 스트림을 직접 추출하는 방식(예: yt-dlp류)은 유튜브 이용약관 위반 소지가 명확해서 배제하고, 대신 유튜브 공식 IFrame API를 그대로 쓰되 그걸 담는 그릇(웹뷰)만 액티비티 생명주기에서 서비스 생명주기로 옮기는 절충안을 택함 — 공식 API를 정상적인 방법으로 부르는 것이라 스트림 추출보다는 훨씬 안전하지만, 유튜브 API 이용약관에 "플레이어를 숨기지 말라"는 취지의 조항이 있어 완전히 안전하다고 단정할 수는 없음(사용자와 상의 후, 개인 프로젝트 단계에서는 감수하기로 결정함).

**구현 후 실기로 잡은 추가 버그 3개** (위 "어떻게 고쳤나"에 이미 반영된 최종 코드 기준):
1. **리액트 StrictMode 이중 마운트로 재생이 영원히 시작 안 됨**: `player.html`은 서비스 생명주기 동안 `YT.Player` 인스턴스가 딱 하나뿐인 싱글턴인데, 개발 모드 StrictMode가 컴포넌트를 일부러 두 번 마운트하는 습성 때문에 `loadAndPlay`가 같은 videoId로 수십ms 간격 두 번씩 불려서 플레이어가 로딩 도중 계속 리셋되는(BUFFERING↔UNSTARTED 무한 반복) 현상을 실기로 확인함. `window.nativeLoadAndPlay`에 "같은 videoId면 1500ms 안엔 무시" 디바운스를 추가해 해결.
2. **`window.nativeLoadAndPlay is not a function` — 페이지 로딩 완료 전에 명령이 도착**: `PlaybackService`가 `evaluateJavascript`로 `player.html`의 함수를 부르는 시점에 그 페이지가 아직 로딩(파싱) 안 끝난 상태일 수 있음(특히 앱 켜자마자 바로 곡을 누르면). `WebViewClient.onPageFinished`가 될 때까지 실행할 스크립트를 큐(`pendingScripts`)에 모아뒀다가 로드 완료 시 한번에 흘려보내는 방식으로 해결.
3. **`ready`/`stateChange`가 단 한 번도 안 찍힘, `onPageFinished`는 찍히는데 실제로는 크롬 에러 페이지가 뜬 상태였음**: 처음엔 `file:///android_asset/player.html`로 로드했더니 유튜브 IFrame API가 origin을 null 취급해서 `onReady`가 영원히 안 불림. 이를 우회하려고 `androidx.webkit.WebViewAssetLoader`로 `https://appassets.androidx.local/assets/player.html`이라는 가짜 보안 오리진을 통해 서빙하도록 바꿨는데, 이 헤드리스 서비스 웹뷰에서는 `shouldInterceptRequest` 가로채기가 실제로 안 먹혀서 진짜 네트워크로 존재하지 않는 그 도메인에 접속을 시도하다가 `net::ERR_CONNECTION_TIMED_OUT`(에러 코드 -8)으로 실패함 — `onPageFinished`는 실패해도 원래 요청 URL을 그대로 보고하는 WebView 특성 때문에 성공한 것처럼 보였던 것(`onReceivedError` 로그를 추가해서 실제 원인을 잡음). 최종 해결: `WebViewAssetLoader`를 아예 걷어내고, `player.html` 내용을 자바에서 `getAssets().open(...)`로 직접 읽어서 `webView.loadDataWithBaseURL("https://appassets.androidx.local/", htmlContent, ...)`로 즉시 주입하는 방식으로 변경 — 네트워크 왕복 자체가 없어 타임아웃/가로채기 문제가 원천적으로 없어지면서도, origin은 여전히 정상적인 https로 인식되게 함. 이 수정 직후 `stateChange state:1`(PLAYING)과 실시간으로 흐르는 `timeUpdate`를 실기로 확인함.

**§73 최종 라이브 확인 완료 (2026-08-31 21:2x경)**: 곡 재생 중 홈 버튼을 눌러 다른 앱(유튜브 앱)으로 전환해도 소리가 끊기지 않고 계속 재생됨을 사용자가 직접 확인함 — 이번 안드로이드 배경 재생 재설계 작업 전체의 핵심 목표 달성. (참고: 테스트 도중 소리가 한 번 안 들렸던 적이 있었는데, 이는 재생 로직과 무관한 에뮬레이터 자체의 AAudio 백엔드 버그로, 콜드 부트로 해결됨 — §72에서도 같은 증상을 겪은 적 있음, 실기기에서는 안 겪을 것으로 예상.)

**알아둘 한계**:
1. 이건 네이티브 코드(Java) + 새 asset 파일 변경이라 Fast Refresh로는 절대 안 되고, Android Studio에서 Gradle 재동기화 + Run(재설치)까지 필요함.
2. `PlaybackPlugin.load()`에서 `startForegroundService()`가 아니라 `bindService(BIND_AUTO_CREATE)`만 쓴 이유: 안드로이드가 "`startForegroundService()` 호출 후 몇 초 안에 `startForeground()`를 안 부르면 크래시(`ForegroundServiceDidNotStartInTimeException`, API 31+)" 규칙을 강제하는데, 우리는 실제 재생이 시작될 때까지(유튜브 iframe 로딩 시간 포함, 몇 초 넘을 수 있음) 알림을 안 띄우기로 했어서 이 규칙에 걸릴 위험이 있었음. `bindService`만으로 서비스를 띄우고 나중에 원하는 시점에 서비스 스스로 `startForeground()`를 부르는 방식으로 피함.
3. Capacitor 플러그인의 `@PluginMethod`는 메인(UI) 스레드가 아니라 전용 백그라운드 스레드(`CapacitorPlugins` HandlerThread)에서 실행됨 — `WebView.evaluateJavascript()`/`startForeground()`는 UI 스레드에서만 안전해서, `PlaybackService`의 모든 컨트롤 메서드가 내부적으로 `Handler(Looper.getMainLooper())`로 다시 포스팅함. 이걸 빠뜨리면 `CalledFromWrongThreadException`류 크래시나 조용한 무반응이 날 수 있음.
4. 헤드리스 웹뷰가 로드하는 `player.html`은 정적 로컬 asset이라 ngrok/개발 서버 상태와 무관하게 항상 로드됨 — 다만 유튜브 IFrame API 자체(`https://www.youtube.com/iframe_api`)와 실제 영상 스트리밍은 인터넷 연결이 필요함(당연하지만).
5. 화면(액티비티) 쪽 `containerRef`(1x1 숨김 div)는 네이티브 경로에서는 더 이상 안 쓰임 — 웹(브라우저)에서 그대로 쓰이는 코드 경로 보존을 위해 지우지 않고 남겨둠.

**홈 버튼/앱 전환 배경 재생 생존, 화면 꺼짐 생존 둘 다 라이브 확인 완료.** (화면 꺼짐 테스트 중 한 번 프로세스가 이유 없이 죽은 적 있었으나, 콜드 부트 직후 시스템이 여러 앱을 freeze/unfreeze하며 정리하던 타이밍과 겹친 것으로 보이고 재현 안 됨 — 에뮬레이터 한정 노이즈로 결론.)

**추가 기능: 알림 재생/일시정지 토글 버튼** — `PlaybackService`의 알림에 재생/일시정지 버튼을 추가함(`ACTION_TOGGLE_PLAY_PAUSE`를 액션으로 담아 서비스 자신을 다시 시작시키는 `PendingIntent.getService`, `onStartCommand`에서 처리). 아이콘은 별도 리소스 없이 안드로이드 표준 `android.R.drawable.ic_media_play`/`ic_media_pause` 사용. 이걸 위해 알림 표시 정책도 바꿈 — 예전엔(§72) 일시정지하면 알림을 아예 내렸는데, 이제 알림에서 바로 재생 재개가 가능하니 일시정지 중에도 알림은 유지하고 버튼만 "재생" 아이콘으로 바뀌게 함. 완전히 곡이 끝났을 때(state 0)만 알림을 내림. **다음/이전 곡 버튼은 범위에서 제외** — 그건 큐 로직이 화면 쪽(액티비티 웹뷰) 리액트 코드에 있어서 네이티브 서비스가 알아서 처리 못 하고, 백그라운드에서 그 웹뷰가 얼마나 잘 응답하는지도 불확실해서 재생/일시정지보다 훨씬 복잡함(사용자와 상의 후 지금 범위에선 제외하기로 결정).

**아직 라이브 확인 안 됨**: 알림 재생/일시정지 버튼 자체(방금 구현, 빌드 후 테스트 필요), 곡 전환, 앱 종료 시 정리. 곡이 끝났을 때 백그라운드+화면꺼짐 상태에서도 다음 곡으로 자동 전환되는지는 에뮬레이터에서 프로세스 재시작과 뒤섞여 테스트가 애매해짐(재현 실패) — 만약 안 된다면 원인은 다음 곡 자동전환 로직이 액티비티 웹뷰의 리액트 코드에 있어서 백그라운드 시 그 자바스크립트가 스로틀링될 가능성. 알려진 한계로 남겨두고 실기기에서 재확인 필요.

**실기기(갤럭시 S21, SM-G991N, 안드로이드 15) 테스트 중 발견 + 수정한 버그 (2026-09-05)**: 알림의 재생/일시정지 버튼은 실기기에서 정상 동작 확인(라이브 확인 완료). 그런데 곡을 두 번째 이상 바꿔서 틀면(수동 선택이든 자동 다음곡 전환이든) **알림에 뜨는 제목/아티스트가 세션 첫 곡으로 계속 고정**되는 버그를 발견함 — 실제 재생되는 오디오/영상은 정상적으로 바뀌는데 알림 텍스트만 안 바뀜. 원인: `frontend/lib/nativePlayer.ts`의 `createNativePlayer(videoId, title, artist, ...)`가 반환하는 `loadVideoById(id)`가 새 `videoId`는 인자로 받아 반영하면서도 `title`/`artist`는 함수 생성 시점에 클로저로 캡처된 값을 그대로 재사용하고 있었음(`b.loadAndPlay({ videoId: id, title, artist })`). `MiniPlayer.tsx`가 곡 전환 시 `loadVideoById(track.videoId)`만 부르고 새 제목/아티스트를 안 넘겨준 것도 원인의 일부. 수정: `loadVideoById`가 `title`/`artist`를 선택 인자로 받아 클로저 내부 상태(`currentTitle`/`currentArtist`)를 갱신하도록 바꾸고, `MiniPlayer.tsx`에서 `loadVideoById(track.videoId, track.name, track.artist)`로 호출부를 고침. 이 앱은 `capacitor.config.ts`에서 정적 빌드가 아니라 켜져 있는 Next.js 개발 서버(ngrok)를 그대로 로드하는 구조라, 이 수정은 네이티브 재빌드 없이 프론트 서버만 재시작(또는 Fast Refresh)하면 반영됨. **아직 라이브 재확인 안 됨** — 다음에 곡을 3번 이상 연달아 바꿔가며 알림 제목/아티스트가 매번 그 곡으로 바뀌는지 확인 필요.

**곡 자동전환 관련, 조사 중인 별개 이슈**: 위 알림 버그와는 별도로, 백그라운드(화면 꺼짐/앱 전환) 상태에서 곡이 끝났을 때 실제로 다음 곡 오디오까지 안 넘어가는 경우가 있는지는 아직 확실히 구분 안 됨 — 알림 텍스트가 고정돼 있어서 "안 넘어갔다"는 관찰이 실제 오디오 문제였는지 알림 버그 때문에 그렇게 보인 것뿐이었는지 헷갈렸음. 알림 버그 수정 후 (1) 화면 켜진 채로 곡이 끝날 때, (2) 화면 꺼진 채로 곡이 끝날 때 각각 실제로 오디오가 넘어가는지 다시 나눠서 확인 필요.

**전원 버튼(화면 꺼짐) 상태에서 다음 곡 자동전환이 실제로 안 되는 것으로 실기 확인됨 (2026-09-05)** — 위 항목 이어서. 사용자가 "취업 포트폴리오용이니 학생 수준으로 타협하지 말고, 스포티파이/유튜브 뮤직처럼 화면 꺼도(자면서/헬스장/지하철에서) 다음 곡까지 자연스럽게 넘어가야 한다"고 명확히 요구 — 알려진 한계로 남겨두지 않고 바로 수정 작업 진행함.

*원인*: "다음 곡으로 넘어갈지" 판단 로직(`page.tsx`의 `handleTrackEnded` — 큐/재생목록/반복 모드 소비)이 화면에 보이는 액티비티 웹뷰(React) 안에서만 돌고 있었음. 안드로이드는 화면이 완전히 꺼지면(전원 버튼) 그 액티비티 웹뷰의 JS 실행을 강하게 억제하는 반면, 실제 오디오를 내는 헤드리스 웹뷰(`player.html`, `PlaybackService` 소유)는 화면 상태와 무관하게 계속 잘 도는 게 이미 §73에서 확인돼 있었음 — 그래서 "소리는 안 끊기는데 다음 곡 판단만 멈추는" 정확히 이 증상이 나온 것.

*고친 방법 (헤드리스 웹뷰에 "다음 곡 1곡 미리보기"를 미리 넘겨두는 방식)*:
- `frontend/android/app/src/main/assets/player.html`: `queue` 배열 + `window.nativeSetQueue(jsonArray)` 추가. `onPlayerStateChange`에서 `state===0`(ENDED)일 때 큐에 곡이 있으면 액티비티를 거치지 않고 스스로 `ytPlayer.loadVideoById(next.videoId)`로 넘어가고 `autoAdvance` 이벤트(videoId/title/artist 포함)를 보냄. 이때 원래의 raw `stateChange(state:0)` 이벤트는 안 보냄(PlaybackService가 "완전히 끝남"으로 오해해서 알림을 내려버리는 것을 막기 위함).
- `PlaybackService.java`: `setQueue(String)` 추가(→ `window.nativeSetQueue` 호출). `JsBridge.onEvent`에 `"autoAdvance"` 타입 처리 추가 — 새 title/artist로 `currentTitle`/`currentArtist` 갱신하고 `startForeground()`를 다시 불러 알림을 유지(안 내림).
- `PlaybackPlugin.java`: `setQueue` `@PluginMethod` 추가. 기존 `onEvent`가 `state`/`duration`/`currentTime` 필드만 JS로 넘기고 있던 걸 `videoId`/`title`/`artist`도 넘기도록 화이트리스트 확장(안 하면 `autoAdvance` 이벤트가 JS 쪽에 빈 데이터로 도착함).
- `frontend/lib/nativePlayer.ts`: `PlaybackBridgePlugin`/`NativePlayerHandle`에 `setQueue` 추가, `NativePlayerCallbacks`에 `onAutoAdvance` 콜백 추가, `"autoAdvance"` 이벤트 리스너 등록.
- `frontend/components/MiniPlayer.tsx`: (1) `queue` prop(이미 "다음 트랙" 패널용으로 받고 있던 것)의 맨 앞 곡 videoId를 `searchYoutubeVideo`로 미리 resolve해서 네이티브에 `setQueue`로 넘기는 effect 추가(같은 곡으로 중복 호출 안 하도록 key로 캐싱). (2) 헤드리스 웹뷰가 스스로 넘어간 직후(`onAutoAdvance`) `lastAutoAdvancedVideoIdRef`를 세팅해두고, 곧이어 `nowPlaying`이 바뀌면서 도는 "트랙 전환" effect가 이미 재생 중인 영상을 또 `loadVideoById`로 처음부터 재로드(=끊기고 되감김)하지 않도록 그 ref로 가드.
- `frontend/app/page.tsx`: `handleTrackEnded`가 선택적으로 `nativeOverride`(네이티브가 실제로 넘어간 곡의 videoId/title/artist)를 받아서, 기존 큐/재생목록/반복 소비 로직은 그대로 두되 재생할 곡의 식별자만 그 값으로 덮어쓰도록 함. `<MiniPlayer onNativeAutoAdvance={...}>`로 연결.

*설계상 알려진 한계 (최초 구현, 이후 확대됨)*: 다음 곡 후보는 Last.fm 기반이라 videoId가 재생 직전에야 유튜브 검색으로 resolve됨(쿼터 절약을 위해 전체 큐를 미리 다 resolve하지 않음, §참고: "유튜브 인기 급상승 트랙만 videoId를 미리 앎"). 최초 구현은 딱 1곡 앞만 미리 resolve해서 네이티브에 넘겼는데, 사용자가 "포트폴리오용이니 스포티파이/유튜브 뮤직 수준으로 화면 꺼놔도(자면서/헬스장/지하철) 여러 곡이 자연스럽게 이어져야 한다"고 명확히 요구해서, **`MiniPlayer.tsx`의 `NATIVE_LOOKAHEAD` 상수를 5로 늘려 앞쪽 5곡을 한 번에 resolve해 네이티브에 통째로 넘기도록 확대함**(큐 자체는 이미 최대 40곡을 들고 있어서 숫자만 늘리면 되는 문제였음; `player.html`의 `advanceQueue()`는 애초에 임의 길이 배열을 `shift()`로 소비하는 구조라 네이티브 쪽 추가 수정은 불필요, 순수 프론트엔드(JS/TS)만 변경 — 네이티브 재빌드 불필요). 개별 곡의 유튜브 검색이 실패해도 그 자리만 건너뛰고 나머지 순서는 유지함.

**쿼터 비용 재계산 + 캐싱 추가**: `NATIVE_LOOKAHEAD`를 늘리면 곡 전환마다 그만큼 `/youtube-search`(호출당 100유닛, 하루 쿼터 10,000) 호출이 늘어나는데, 캐싱 없이 그대로면 1시간 재생(대략 17곡 기준) 기준 곡당 최대 400유닛(미리보기 3~5곡 검색 + 실제 재생 시작 시 같은 곡 중복 검색)까지 나가서 하루 쿼터를 세션 하나로 거의 다 쓸 위험이 있었음. 그래서 `lib/api.ts`의 `searchYoutubeVideo`에 세션 메모리 캐시(`youtubeVideoIdCache`, 키: `artist::track` 소문자)를 추가함 — 미리보기 때 resolve해둔 곡을 실제 재생 시작 시 재검색하지 않고 캐시로 재사용. 이 캐싱 덕분에 정상 상태(steady state)에서는 곡 전환마다 "새로 미리보기 창에 들어온 딱 1곡"만 실제로 새로 검색하면 돼서, `NATIVE_LOOKAHEAD`를 3이든 5든 늘려도 쿼터 비용은 이 기능을 추가하기 전(곡당 약 100유닛, 1시간에 약 1,700유닛)과 거의 같게 유지됨.

남은 한계: 화면을 꺼둔 채 `NATIVE_LOOKAHEAD`(5)곡을 초과해서 계속 재생되는 경우, 6번째 곡부터는 그사이 액티비티 웹뷰가 한 번이라도 깨어나 큐를 다시 채워주는지에 달려 있음(실기 미확인) — 사용자가 "학술제에서 직접 선보일 기능은 아니다"라고 판단해서 지금은 이 정도 깊이로 마무리하고 더 깊게 파고들진 않기로 함.

**진행바 드래그 중 재생 위치가 계속 0초로 돌아가는 별개의 버그 발견+수정** — 위 자동전환 기능을 실기로 테스트하려고 곡 끝부분으로 진행바를 직접 드래그해서 옮기려다가 발견함. 원인: `<input type="range">`는 드래그하는 동안 픽셀 단위로 `onChange`가 수십~수백 번 연달아 발생하는데, 예전 `handleSeek`는 그때마다 매번 실제 `seekTo`(안드로이드 네이티브 경로에서는 헤드리스 웹뷰의 실제 유튜브 iframe까지 왕복)를 호출했음 — 너무 잦은 연속 seek 요청이 서로를 끊고 들어가면서 유튜브 플레이어가 버퍼링을 안정시킬 틈이 없어 재생 위치가 순간적으로(또는 계속) 0으로 보고되는 현상이 있었던 것으로 보임. `MiniPlayer.tsx`에서 `handleSeek`를 `handleSeekPreview`(onChange — 화면 슬라이더 위치만 갱신)와 `handleSeekCommit`(onMouseUp/onTouchEnd — 손을 뗀 순간 딱 한 번만 실제 seekTo 호출)로 분리해서 수정함. 순수 프론트엔드 변경이라 네이티브 재빌드 불필요. **아직 라이브 확인 안 됨** — 진행바를 드래그해서 곡 끝부분 등 임의 위치로 옮겼을 때 그 위치에서 정상적으로 이어 재생되는지, 웹(브라우저) 경로에서도 회귀 없이 그대로 동작하는지 확인 필요.

**추가로 발견된 별개 버그: 폰에서는 진행바가 손가락 드래그로 아예 안 움직임** — 사용자가 위 자동전환 테스트를 하려고 진행바를 곡 끝부분으로 드래그하려다 안 돼서, 대신 "가사" 탭에서 뒷부분 가사를 눌러 그 시점으로 점프하는 방식으로 우회하고 있던 것을 보고 알게 됨. 원인: `<input type="range">` 자체의 실제 높이를 `h-1`(4px)로 얇게 잡고 그 배경색으로 트랙을 그렸는데, 그러면 실제 터치 가능한 히트 영역도 똑같이 4px로 줄어들어서 손가락으로 그 얇은 줄 위를 정확히 짚어야만 드래그가 시작됨 — 데스크톱은 마우스 커서가 정확해서 문제가 안 드러났던 것. `MiniPlayer.tsx`의 `SLIDER_CLASS`를 수정해서, `<input>` 자체는 `h-6`(24px)의 투명한 터치 영역으로 키우고 실제로 보이는 얇은 막대는 `::-webkit-slider-runnable-track`/`::-moz-range-track` 가상 요소에 `h-1`로 따로 그리도록 분리함 — 눈에 보이는 굵기는 그대로인데 터치 가능한 세로 범위는 훨씬 넓어짐. 손잡이가 트랙보다 두꺼워서 웹킷 기준 세로 중앙 정렬용 음수 `margin-top`(-4px)도 같이 줌. 진행바/볼륨바가 공유하는 스타일이라 볼륨바(데스크톱 전용, `sm:block`)에도 같이 적용되지만 데스크톱 마우스 조작엔 영향 없음. 순수 CSS/프론트엔드 변경이라 네이티브 재빌드 불필요. **아직 라이브 확인 안 됨** — 폰에서 진행바를 손가락으로 드래그했을 때 이제 정상적으로 움직이는지 확인 필요.

**아직 라이브 확인 안 됨 (네이티브 코드 변경 포함이라 Android Studio에서 Gradle 재동기화 + Run 재설치 필요)**: (1) 화면을 꺼둔 채 곡 하나가 끝났을 때 실제로 다음 곡 소리가 자연스럽게 이어지는지, (2) 그때 알림 제목/아티스트도 새 곡으로 같이 바뀌는지, (3) 폰을 다시 켰을 때 앱 화면의 곡 정보/진행바가 실제 재생 중인 곡과 맞게 나오는지(끊기거나 처음으로 되감기지 않는지), (4) 화면 켜진 채로/홈 버튼으로 나간 상태에서의 기존 자동전환 동작이 이번 변경으로 회귀하지 않았는지.

**실기기 테스트 관련 중요 발견 (확정)**: 사용자가 테스트하려던 물리 기기가 삼성 갤럭시 On7(안드로이드 6.0, API 23)인데, `minSdkVersion`을 24→23으로 낮춰서 지원 시도해봤으나 **cordova-android 14.0.1 라이브러리가 강제로 minSdk 24를 요구**해서 빌드 자체가 실패함(`Manifest merger failed: uses-sdk:minSdkVersion 23 cannot be smaller than version 24 declared in library`). `tools:overrideLibrary`로 강제 우회하는 방법이 있지만 "런타임 실패를 유발할 수 있다"는 경고가 있어 채택 안 하고 24로 되돌림. **결론: 갤럭시 On7에는 이 앱을 설치할 수 없음 — 안드로이드 7.0(API 24) 이상 기기가 실기기 테스트에 반드시 필요.**

**모바일 전용: "다음 트랙" 확장 패널이 둥근 테두리 밖으로 넘쳐 보이는 버그 — 두 번의 잘못된 추측 끝에 sandbox 브라우저로 실제 구조를 재현해서 근본 원인 확정 (2026-09-05)**: 사용자가 실기기(갤럭시 S21) 스크린샷으로 "다음 트랙" 목록에서 곡 7개쯤 지나면 회색 둥근 테두리 박스가 먼저 끝나버리고, 그 아래 곡들(8번째부터)은 테두리 없이 그대로 삐져나와 보인다고 제보함. 데스크톱에서는 재현 안 됨.

*틀렸던 추측 두 가지*: (1) 개별 썸네일 `<img>` 로드 실패 시 브라우저 기본 깨진 이미지 아이콘이 `overflow-hidden` 박스 밖으로 그려지는 것으로 추정해 `onError` 폴백(플레이스홀더로 교체)을 추가했으나 라이브에서 그대로였음. (2) 안드로이드 웹뷰의 `overflow-hidden`+`border-radius` 합성 레이어 클리핑 버그로 추정해 `<img>` 자체에도 `rounded` 이중 적용을 시도했으나 이것도 그대로였음. 사용자가 훨씬 선명한 스크린샷을 다시 보내줘서(회색 둥근 테두리 박스 자체가 7번째 곡 근처에서 끝나는 게 명확히 보임) 그제서야 문제가 개별 썸네일이 아니라 **패널 전체를 감싸는 테두리 박스의 높이 계산**이라는 걸 알아챔.

*진짜 원인 (sandbox 브라우저로 375×812 뷰포트에 실제 마크업 구조를 그대로 재현해서 `getBoundingClientRect()`로 직접 확인)*: 확장 화면 오버레이는 `h-[100dvh]`로 높이가 고정돼 있고, 그 안의 그리드(`<div className="mx-auto grid ... min-h-0 ... flex-1 grid-cols-1 ... lg:grid-cols-2">`)가 `min-h-0`+`flex-1`로 오버레이의 "남는 높이"만큼만 차지하도록 눌려 있었음. `grid-cols-1`(모바일)이라 앨범아트/다음트랙 패널이 각자 다른 행에 쌓이는데도, CSS Grid의 기본값(`align-content: normal`)이 높이가 정해진 그리드 컨테이너에서는 `stretch`처럼 동작해서 그 눌린 높이를 각 행에 강제로 배분함 — 그 결과 "다음 트랙" 패널(`rounded-xl border`)도 실제 목록 내용보다 짧게 눌려서 그려지고, 목록 자체는 `overflow-hidden`이 아니라서 안 잘리고 그 눌린 테두리 밖으로 그대로 삐져나와 보인 것. (패널 안쪽 스크롤 div의 `flex-1`/`min-h-0`도 모바일에서 걸려있던 게 있어서 먼저 `lg:`로 옮겼지만, sandbox에서 재현해보니 그것만으론 안 고쳐지고 한 단계 위 그리드 자체가 진짜 원인이었음.)

*고친 방법*: `frontend/components/MiniPlayer.tsx`의 그리드 className에서 `min-h-0 ... flex-1`을 제거하고 `lg:min-h-0 lg:flex-1`로 데스크톱에서만 걸리게 바꿈 — 모바일에서는 그리드가 그냥 콘텐츠 높이만큼 자연스럽게 늘어나고, §65에서 이미 정한대로 오버레이 바깥 `overflow-y-auto`로 통째로 스크롤됨. sandbox 브라우저에 실제 컴포넌트 마크업을 그대로 주입해서 수정 전(overflow 발생)/수정 후(overflow 없음) 둘 다 `getBoundingClientRect()`로 직접 검증 완료. 순수 프론트엔드(Tailwind 클래스) 변경이라 네이티브 재빌드 불필요.

**교훈**: 이 세션에서 같은 버그를 두 번 잘못 짚은 이유는 사용자가 준 사진 증거(글레어 있는 사진, 확대 안 된 사진)만으로 추측하고 코드를 고쳤기 때문 — 실제로 선명한 스크린샷을 받고 나서야 "개별 썸네일"이 아니라 "패널 전체 테두리"라는 걸 알 수 있었음. 또한 CSS 레이아웃 버그는 짐작보다 **실제 컴포넌트 구조를 그대로(전체 조상 체인 포함) 재현해서 `getBoundingClientRect()`로 직접 측정**하는 게 훨씬 빠르고 정확함 — 격리된 조각만 테스트했을 때는(패널만 따로 뗀 테스트) 버그가 재현조차 안 됐고, 오버레이+그리드+패널 전체 구조를 다 넣고 나서야 재현됨.

**아직 라이브 확인 안 됨** — 실기기에서 "다음 트랙" 목록에 9곡 이상 있을 때 테두리가 목록 전체를 끝까지 잘 감싸는지, 스크롤이 자연스러운지 확인 필요.

---

## 74. 모바일 헤더 개편 — 로고(레코드판 b) 도입 + 보관함 아이콘 + 검색/보관함 아이콘 정렬

**뭘 했나**: 학술제 데모를 앞두고 "디자인에서 가장 중요한 건 메인 색"이라는 사용자 판단에 따라, 기존에 어디에도 안 쓰이던 강조색(코랄레드 `#ff3b5c`)을 처음으로 실제 로고에 적용함. 여러 후보(웨이브바 아이콘, U/b 겹치기, 워드마크 색칠 등)를 `mcp__visualize`로 빠르게 스케치해보며 좁혀나간 끝에, **"b"의 둥근 부분(bowl)을 레코드판(그루브 2겹 + 센터홀)으로 그린 마크**로 최종 확정함 — 음악 서비스라는 게 아이콘만 봐도 읽히면서 글자 b로도 자연스럽게 읽힘. 그리고 이전 세션에서 추가한 "보관함" 아이콘을 포함해 헤더 오른쪽 아이콘들(돋보기+보관함)을 서로 붙여서 정렬함.

**어떻게 고쳤나**:
- `frontend/app/page.tsx` 헤더: `<h1>Unbeat</h1>` 단독 텍스트를 `<svg>`(레코드판-b 로고) + `<h1>Unbeat</h1>`을 담은 flex 그룹으로 교체. 헤더 바깥 컨테이너를 `grid grid-cols-1 ... lg:grid-cols-[...]`(모바일에서 세로로 쌓임)에서 `flex items-center justify-between ... lg:grid lg:grid-cols-[...]`로 바꿔서, 모바일에선 로고 그룹(왼쪽)과 검색+보관함 아이콘 그룹(오른쪽)이 한 줄에 양 끝으로 배치되고, `lg` 이상에서는 기존 3열 그리드(로고 200px / 검색창 중앙 / 오른쪽 여백 200px)로 그대로 복귀함.
- `frontend/components/SearchBar.tsx`: 루트 div가 `w-full max-w-xl`이라 모바일에서 실제로는 작은 돋보기 아이콘 하나만 보이는데도 보이지 않는 넓은 박스가 자리를 다 차지해서, 옆에 붙인 보관함 아이콘이 화면 오른쪽 끝까지 밀려 둘이 멀리 떨어져 보이던 문제 발견. `w-auto sm:w-full sm:max-w-xl`로 바꿔서 모바일에서는 아이콘 크기만큼만 차지하도록 수정 — 이 한 줄 수정으로 돋보기와 보관함 아이콘이 실제로 붙어 보이게 됨.

**로고 디자인 과정에서 배운 것**: 처음엔 추상적인 웨이브바/원 아이콘, 그다음 "Unbeat" 글자 자체에 색만 칠하거나 U/b를 억지로 겹치는 시도를 했는데 전부 반려됨("이건 그냥 u자나") — 사용자가 원한 건 Bfresh(소용돌이가 D 안에 자연스럽게 녹아든 로고), SOO Casting(볼드한 커스텀 워드마크) 같은 **글자의 여백/카운터 공간에 실제 의미 있는 도형이 녹아든 수준의 완성도**였음. "b"의 bowl에 레코드 그루브를 넣는 안이 이 기준을 만족(음악 서비스라는 의미 + 글자로도 읽힘)해서 최종 채택됨.

**아직 라이브 확인 안 됨**: 이번 로고/헤더 변경은 Claude Browser 툴로 모바일(375px)/데스크톱(1280px) 스크린샷까지는 확인했지만, 사용자가 실제 폰이나 본인 브라우저에서 직접 본 적은 없음 — 다음에 로컬 사이트를 열어서 (1) 로고가 헤더 왼쪽에 Unbeat 옆에 잘 뜨는지, (2) 모바일 폭에서 돋보기+보관함 아이콘이 서로 붙어서 오른쪽 끝에 나란히 있는지, (3) 데스크톱 폭에서 기존 레이아웃(중앙 검색창, 오른쪽 사이드바)이 그대로 유지되는지 직접 확인 필요.

---

## 다음에 이어서 정리할 것

- §74 모바일 헤더 개편(레코드판-b 로고 + 아이콘 정렬) 라이브 확인 필요([[학습노트 74번]]) — 로컬 사이트를 열어서 (1) 로고가 헤더 왼쪽 Unbeat 옆에 정상적으로 뜨는지, (2) 모바일 폭(폰 또는 브라우저 창 좁게)에서 돋보기+보관함 아이콘이 서로 붙어서 오른쪽에 나란히 보이는지, 멀리 떨어져 보이지 않는지, (3) 데스크톱 폭에서 기존 레이아웃(중앙 검색창, 로고 200px 칸, 오른쪽 사이드바)이 그대로 유지되는지(회귀 없는지) 확인
- §73 헤드리스 웹뷰 기반 백그라운드 재생 — **핵심 목표(홈 버튼/앱 전환 시 재생 유지)는 라이브 확인 완료**([[학습노트 73번]], §72를 대체하는 최종 구조). 남은 체크리스트: (1) 상단 알림바에 "Unbeat" 알림이 제목/아티스트 텍스트 포함해서 정확히 뜨는지, (2) 알림에서 재생/일시정지 컨트롤이 동작하는지(현재는 알림에 컨트롤 버튼 자체를 아직 안 넣음 — 필요하면 추가 구현 필요), (3) 화면을 꺼도(전원 버튼) 재생이 유지되는지, (4) 곡을 바꿔도(다른 트랙 선택) 헤드리스 웹뷰 쪽 영상이 정상적으로 교체되는지, (5) 앱을 완전히 종료하면 알림/서비스도 같이 정리되는지. 안 되면 Logcat에서 `PlaybackService`/`Capacitor/Plugin` 태그로 필터링해서 어느 단계에서 끊기는지 확인
- §71 dev-up.sh/dev-down.sh 라이브 확인 필요([[학습노트 71번]]) — 터미널에서 `chmod +x backend/dev-up.sh backend/dev-down.sh` 먼저 실행(권한이 실제로 적용 안 됐을 수 있음) 후 `cd backend && ./dev-up.sh` 실행해서 (1) 레디스/uvicorn/ngrok이 다 정상적으로 뜨는지, (2) ngrok 공개 주소가 화면에 자동으로 뜨는지, (3) 그 주소로 폰 접속이 실제로 되는지 확인. 그 다음 `./dev-down.sh`로 정리했을 때 uvicorn/ngrok 프로세스가 실제로 꺼지는지(`ps aux | grep uvicorn`)도 확인
- §70 무드 탐색 결과 가로 스크롤 전환 라이브 확인 필요([[학습노트 70번]]) — 프론트 새로고침 후 무드 칩(예: "출퇴근길")을 눌러서 국내/해외 인기곡 섹션이 이제 화살표 없이 가로로 드래그 스크롤되는지, 곡이 3행으로 고정돼 보이는지 확인. 검색 결과 그리드는 기존처럼 화살표 페이지네이션 그대로인지(회귀 없는지)도 확인
- §69 검색창 자동완성 라이브 확인 필요([[학습노트 69번]]) — 프론트/백엔드 재시작 후 검색창에 "numb"를 천천히 쳐서 (1) 두 글자부터 드롭다운에 후보(트랙명+아티스트, 아티스트명)가 뜨는지, (2) "Numb Little Bug" 같은 뒷부분 모르는 곡도 후보로 뜨는지, (3) 후보를 클릭하면 그 검색어로 실제 검색이 실행되는지, (4) 한 글자만 쳤을 땐 후보 없이 기존 '최근 검색'이 그대로 뜨는지, (5) 입력값을 지우면 다시 '최근 검색'으로 돌아오는지 확인. 터미널에서 `search-suggest` 관련 Last.fm 호출이 찍히는지도 확인

- §68 무드 조회 이미지 병렬화 라이브 확인 필요([[학습노트 68번]]) — 백엔드 재시작 후, 캐시가 비어있을 만한 무드(안 눌러본 무드)를 하나 새로 눌러서 (1) §67 적용 전보다 체감상 빨리 뜨는지, (2) 에러("무드 곡을 불러오지 못했어요") 없이 정상적으로 곡 목록이 채워지는지 확인. 그래도 느리거나 에러 나면 백엔드 터미널에 `[itunes]` 로그가 여전히 몰리는지, `GET /discover-by-mood?tag=...`의 실제 응답 시간이 얼마나 걸리는지 확인 필요
- §67 무드별 국내 큐레이션 목록 라이브 확인 필요([[학습노트 67번]]) — 백엔드 재시작 후 "집중"과 "신나는"을 번갈아 눌러서 국내 목록이 이제 서로 다르게 뜨는지, 뜬 곡들이 실제로 그 아티스트/곡명이 맞는지(오탐 없는지) 확인. 다른 6개 무드(운동/휴식/잔잔한/슬픔/로맨스/출퇴근길)도 한 번씩 눌러서 각각 6곡 이상 채워지는지, 곡이 무드에 얼추 맞는 느낌인지 확인. 즐겨찾기 아티스트가 있는 상태와 없는 상태(새 세션) 둘 다에서 확인

- §66 Capacitor 안드로이드 래퍼 실기기 확인 필요([[학습노트 66번]]) — 맥에서 `cd frontend && npm install` 실행(Capacitor 패키지 node_modules에 반영) 후 Android Studio로 `frontend/android` 폴더 열기 → 친구에게 돌려받은 안드로이드 공기계를 USB로 연결(또는 에뮬레이터) → Run으로 설치. 확인할 것: (1) 앱 아이콘이 홈 화면에 생기고 실행하면 브라우저 없이 바로 Unbeat 화면이 뜨는지, (2) 곡 재생 후 홈 버튼 눌러서 다른 앱으로 나갔다가 몇 분 뒤 돌아와도 재생이 끊기지 않는지(이게 핵심 검증 포인트), (3) 화면을 꺼도(잠금화면) 계속 재생되고 잠금화면에 곡 정보/재생·일시정지 버튼이 뜨는지, (4) 안 되면(백그라운드에서 끊기면) 포그라운드 서비스 방식 플러그인 추가가 다음 단계
- §65 확장 재생 화면 모바일 스크롤 수정 라이브 확인 필요([[학습노트 65번]]) — 폰에서 곡 재생 중 앨범아트/곡정보를 눌러 확장 화면을 연 뒤, 화면 전체가 하나로 스크롤되면서 "다음 트랙"/"가사" 탭 콘텐츠까지 다 보이는지 확인. 데스크톱에서도 기존처럼 왼쪽(앨범아트)과 오른쪽(탭) 블록이 각자 독립적으로 스크롤되는지(회귀 없는지) 같이 확인
- §64 모바일 재생바 축약 바 + 검색창 overflow 수정 라이브 확인 필요([[학습노트 64번]]) — 프론트 새로고침 후 폰으로 재접속해서 (1) 곡 재생 시 하단 바가 한 줄(얇은 진행바 + 썸네일/곡정보/재생/닫기)로 뜨는지, 화면 밖으로 안 잘리는지, 눌렀을 때 확장 화면이 정상적으로 열리는지, (2) 검색창+검색 버튼이 이제 화면 안에 다 들어오는지, 검색이 정상 동작하는지 확인. 데스크톱(`localhost`)에서도 재생바가 기존 3분할 레이아웃 그대로인지(회귀 없는지) 같이 확인
- §63 ngrok 경고 페이지로 인한 홈 화면 빈 곡 목록 버그 수정 라이브 확인 필요([[학습노트 63번]]) — 프론트 서버 재시작(HMR로 이미 반영됐을 수도 있지만 확실히 하려면 재시작) 후 ngrok 터널을 다시 켜고 폰(가능하면 카카오톡 인앱 브라우저 포함)으로 접속해서 "빠른 선곡"/"지금 인기 있는 곡"/"다시 듣기" 등 곡 목록이 이제 실제로 채워지는지 확인. 안 되면 `127.0.0.1:4040`(ngrok 웹 인스펙터)에서 `/proxy/py/chart` 요청의 실제 응답이 JSON인지 다시 확인 필요
- §32 장르+연대 라운드로빈 믹싱 라이브 확인 필요([[학습노트 32번]] 추가 수정) — 백엔드 재시작(캐시 키 v19 전환) 후 "2010년대 kpop"으로 재검색해서 "관련 곡 (장르/연대)"에 케이팝과 서구 팝이 섞여 나오는지(더 이상 서구 팝 일색이 아닌지) 확인. "발라드"처럼 장르만 있고 연대가 없는 검색어, 순수 아티스트/곡 제목 검색("아이유")은 회귀 없이 그대로 동작하는지도 확인
- §32 장르/연대 검색이 AI 단일곡 폴백에 가려지던 버그 수정 라이브 확인 필요([[학습노트 32번]] 추가 수정) — 백엔드 재시작(캐시 키 v18 전환) 후 "2010년대 kpop"으로 재검색해서 "검색된 곡"엔 AI가 추측한 곡 하나가 안 뜨고(또는 아예 안 뜨고), "관련 곡 (장르/연대)" 섹션에 여러 곡이 채워지는지 확인. 터미널에 `[search-tag-tracks]` 로그로 실제 후보/최종 개수도 확인 가능. "발라드"처럼 장르만 있고 연대가 없는 검색어, 순수 아티스트/곡 제목 검색("아이유")은 회귀 없이 그대로 동작하는지도 확인
- §54/§55 트랙 쓰레기 필터에 "단체 무대" 키워드 추가 라이브 확인 필요([[학습노트 55번]] 추가 수정) — 백엔드 재시작(캐시 키 v17 전환) 후 "위대한 쇼맨 ost"로 재검색해서 "호야팀 K-WAVE 단체 무대..." 항목이 더 이상 안 뜨는지, 반대로 "Hugh Jackman ... From Now On"/"Loren Allred ... Never Enough" 같은 진짜 원곡은 여전히 그대로 뜨는지(부당하게 같이 안 걸러지는지) 확인
- §62 한글 아티스트 오타 교정 컷오프 조정 라이브 확인 필요([[학습노트 62번]]) — 백엔드 재시작(캐시 키 v16 전환) 후 "위대한 쇼맨 ost"로 검색해서 더 이상 "위너"로 안 뜨고, 대신 §53 AI 폴백이 시도돼 "Rewrite the Stars" 계열 결과가 뜨는지 확인(안 뜨면 AI가 이 영화 OST를 못 맞춘 것일 수도 있음 — 그 경우 결과가 없는 것 자체는 정상, "위너"로 안 뜨는 것만 확인해도 이 버그 수정은 성공). "지드래곤"으로도 검색해서 이제 "있지"가 아니라 실제 지드래곤 관련 결과가 뜨는지 확인. 기존 정상 케이스("아이위"→아이유, "블핑"→블랙핑크)가 회귀 없이 여전히 잘 되는지도 확인
- §61 검색 맥락 아티스트 다른 곡 섹션 — 핵심 동작(검색 시 "지금 인기 있는 곡"/"인기 아티스트" 대신 "'싸이'의 다른 곡" 섹션 노출)은 "댓댓 싸이" 검색으로 라이브 확인 완료([[학습노트 61번]]). 이어서 "아이유"로 재검색하며 발견한 "직전 아티스트 곡이 잠깐 보이는" 버그도 수정함(fetch 시작 전 목록 비우기) — 이 수정이 실제로 깜빡임 없이 잘 넘어가는지 재확인 필요. 그 외 남은 것: 이 섹션의 곡들이 실제로 재생/즐겨찾기/재생목록 담기가 되는지, 검색을 지우거나 무드 칩을 눌렀을 때 원래 "지금 인기 있는 곡"/"인기 아티스트"가 다시 뜨는지(원상 복구), §59 유튜브 폴백으로만 찾아진 곡(예: "동암역 2번 출구")처럼 아티스트가 Last.fm에 아예 없는 경우 이 섹션 없이도 에러 없이 넘어가는지 확인
- "댓댓" 재검색 회귀 확인 완료 — v15 이후 최신 스크린샷에서 "That That (feat. SUGA)" 등 정상 결과와 "(AI 추측: PSY That That로 검색됨)" 힌트가 정상 노출되는 것 확인됨([[학습노트 57번]], [[학습노트 58번]], [[학습노트 59번]])
- §60 다음 트랙 유튜브 폴백 라이브 확인 필요([[학습노트 60번]]) — 백엔드 재시작(캐시 키 v5 전환) 후, 유튜브 폴백으로 재생 중인 "동암역 2번출구" 같은 곡에서 "다음 트랙" 패널에 이제 Dom의 다른 영상들이 뜨는지, 그 트랙을 눌렀을 때 실제로 재생되는지, 곡이 끝났을 때 자동으로 그쪽으로 넘어가는지(handleTrackEnded) 확인. 일반 Last.fm 기반 곡(예: 아이유)의 "다음 트랙"은 회귀 없이 그대로 동작하는지도 확인
- §59/§58/§57 라이브 확인 완료 — "동암역 2번 출구" 재검색해서 "동암역 2번출구"(Dom, 유튜브 폴백)가 첫 결과로 뜨고, 무관한 "싸이" 결과가 더 이상 안 뜨고, "신풍역 2번 출구 블루스"/"상수역 2번 출구" 같은 관련 정식곡도 정상 노출되는 것 확인됨([[학습노트 59번]], [[학습노트 58번]], [[학습노트 57번]]). 유튜브 폴백 트랙 재생 자체는 실제 확인됨(0:18/4:59로 정상 재생). 남은 것: 진짜 의미 없는 문자열로도 테스트해서 에러 없이 빈 결과 처리되는지 확인
- "댓댓" 재검색으로 회귀 확인 필요 — v15까지 이어진 여러 차례 수정(§54~§58) 이후로 "댓댓" 자체를 재검증 안 함. That That이 여전히 정상적으로 뜨는지, 이번엔 검증된 결과라 90일 캐싱되는지 확인
- §56 관련성 검증 추가 라이브 재확인 필요([[학습노트 56번]]) — 백엔드 재시작(캐시 키 v11 전환) 후, "동암역 2번 출구"로 다시 검색해서 터미널 `[search-confidence]` 로그로 `tracks_low_confidence=True`가 찍히는지, 그 다음 `[search-ai-fallback]` 로그가 실제로 실행되는지 확인. AI가 뭘 추측했고 Last.fm 재검증이 성공/실패했는지까지 확인해서 이 곡이 Last.fm 카탈로그 자체에 없는 건지 최종 결론. "아이유", "blueming"처럼 이미 잘 되던 검색어들이 이번 유사도 조건 때문에 불필요하게 AI 폴백까지 안 가는지(회귀 없는지)도 확인
- §55 트랙 쓰레기 필터 재설계 + AI 폴백 조건 확장 라이브 재확인 필요([[학습노트 55번]]) — 백엔드 재시작(캐시 키 v10 전환) 후, "동암역 2번 출구"로 다시 검색해서 (1) 이제 정확한 결과(있다면)나 최소한 "신풍역 2번 출구 블루스"/"상수역 2번 출구" 같은 관련 정식곡이 쓰레기로 안 걸러지고 뜨는지, (2) 그래도 정확한 곡이 없으면 AI 폴백이 시도되는지(터미널 로그로 확인), Last.fm에 진짜 없는 곡이면 그 사실을 사용자에게 어떻게 보여줄지도 고민 필요. "댓댓" 재검색도 회귀 없는지(패턴 필터로 바뀌면서 예전에 잡던 쓰레기가 여전히 잘 걸러지는지) 같이 확인
- 트랙 검색 쓰레기 필터 라이브 재확인 필요([[학습노트 54번]]) — 백엔드 재시작(캐시 키 v8 전환) 후, "댓댓"으로 다시 검색해서 (1) 팬캠/방송클립 쓰레기 트랙이 더 이상 안 뜨는지, (2) §53 AI 폴백이 발동해서 실제로 PSY & Suga "That That"(청취자 수 많은 정식 곡)이 뜨는지, (3) "검색된 곡" 헤더에 "(AI 추측: ...)" 힌트가 뜨는지 확인. 그 다음 진짜 마이너하지만 실존하는 곡을 몇 개 검색해서 청취자 50명 이하라는 이유로 부당하게 걸러지지 않는지도 확인
- AI 검색 폴백 라이브 검증 필요([[학습노트 53번]]) — 백엔드 재시작(캐시 키 v7 전환) 후, "댓댓"으로 검색해서 실제로 PSY & Suga "That That"이 뜨는지, "검색된 곡" 헤더에 "(AI 추측: ...)" 힌트가 뜨는지 확인. 이미 규칙 기반 교정으로 찾아지는 검색어(예: "아이유", "thatthat")는 이 폴백까지 안 가고 그전 단계에서 끝나는지(불필요한 API 호출 없는지)도 확인. `ANTHROPIC_API_KEY`를 잠깐 비워보고도 검색 자체가 에러 없이(그냥 빈 결과로) 동작하는지 확인. `redis-cli KEYS "search-ai-guess:*"`로 실제 캐싱 여부, `redis-cli GET "search-ai-guess:댓댓"`로 저장된 추측 내용도 확인 가능
- 영문 띄어쓰기 없는 검색어 교정 라이브 검증 필요([[학습노트 52번]]) — 백엔드 재시작(`pip install -r requirements.txt` 먼저 필요, wordninja 신규 설치) 후, "thatthat"으로 검색해서 실제로 "That That"(싸이 & 슈가)이 뜨는지 확인. "dynamite"처럼 원래 한 단어인 검색어나 "BTS" 같은 축약어가 이상하게 쪼개져서 오히려 결과가 나빠지지 않는지도 확인. 한글 띄어쓰기 교정("꽃이피고지듯이")이 여전히 정상 동작하는지(회귀 없는지)도 같이 확인
- 이미지 실패 캐싱 버그 수정 라이브 검증 필요([[학습노트 51번]]) — 백엔드 재시작 후, 싸이 "That That"처럼 아까 이미지가 안 떴던 메이저 아티스트(BLACKPINK/Lisa/JISOO) 곡들을 다시 큐에 띄워서 이미지가 이제 뜨는지 확인. 바로 안 뜨면(캐시가 아직 없는 곡이라 첫 조회가 실패할 수도 있음) 30분 뒤 다시 확인해서 자동으로 채워지는지도 확인
- 쓰레기 아티스트 필터 단순화 라이브 검증 필요([[학습노트 50번]]) — 백엔드 재시작 후 "꽃이피고지듯이"/"꽃이 피고 지듯이"로 검색해서 "검색된 아티스트"에 청취자 극소수 쓰레기 항목이 더 안 뜨는지 확인. 그 다음 "아이유"처럼 실제 유명 아티스트를 검색해서 정상적으로 잘 뜨는지(회귀 없는지), 진짜 무명이지만 실존하는 작은 아티스트도 몇 개 테스트해서 부당하게 걸러지지 않는지 확인
- "다시 듣기" 최근 재생 기록 라이브 검증 필요([[학습노트 49번]]) — 백엔드 재시작 후, 곡을 하나 재생해서 (1) 홈 화면 "다시 듣기" 섹션 맨 앞에 그 곡이 즉시 뜨는지, (2) 같은 곡을 다시 재생해도 목록에 중복으로 안 쌓이고 맨 앞으로만 이동하는지, (3) 이 목록이 오른쪽 사이드바 "즐겨찾기" 목록과 이제 서로 다른 기준으로(재생 시점 vs 즐겨찾기 시점) 다르게 나오는지, (4) 새로고침해도(서버에 기록된 값이므로) 유지되는지, (5) 즐겨찾기 안 한 곡을 재생해도 "다시 듣기"엔 뜨는지(즐겨찾기 여부와 무관해야 함) 확인. `redis-cli KEYS "recent-plays:*"`로 실제 저장 여부도 확인 가능
- "다음 트랙" 큐 이미지 캐싱 + 지연 리졸브 + 초기 개수(25) 라이브 검증 필요([[학습노트 48번]]) — 백엔드 재시작 후, "꽃이 피고 지듯이"(Hanz) 같은 곡을 재생해서 (1) 큐 첫 로딩부터 25곡 정도로 꽉 차 보이는지, (2) 스크롤해서 끝까지 봤을 때 이전처럼 뒤쪽 항목들이 전부 이미지 없이 뜨는 현상이 줄었는지(완전히 없어지진 않을 수 있음 — 순수 데이터 부재 케이스는 남음), (3) 같은 곡을 검색/차트/무드 탐색 등 다른 화면에서도 봤을 때 이미지가 서로 일치하고 두 번째부터는 로딩이 더 빨라진 느낌이 있는지 확인. Redis에 `track-image:*` 키들이 실제로 쌓이는지도 `redis-cli KEYS "track-image:*"`로 확인 가능
- "다음 트랙" 큐 이어붙이기(전량 교체 → 조건부 병합) 라이브 검증 필요([[학습노트 47번]]) — 아직 브라우저에서 확인 안 함(tsc만 통과). (1) 큐 안의 곡을 클릭해서 재생하거나 곡이 끝까지 재생돼 자동으로 넘어갈 때 — 나머지 대기곡들이 사라지지 않고 남아있는지, 방금 재생 시작한 곡만 빠지는지, 새 추천이 뒤에 이어붙는지(중복 없이), 큐가 40곡을 안 넘는지, 유사곡 조회 실패 시 기존 큐가 유지되는지. (2) 검색/차트/즐겨찾기/재생목록처럼 큐 바깥에서 완전히 다른 곡을 골랐을 때 — 이번엔 반대로 큐가 그 새 곡 기준으로 통째로 새로 채워지는지(예전 곡 기준 후보가 안 섞이는지) 확인 — 이게 (추가 수정) 단락에서 고친 핵심 지점
- "다음 트랙" 대표 버전 치환 라이브 검증 필요([[학습노트 46번]]) — 백엔드 재시작(캐시 키 v3 전환) 후, "꽃이 피고 지듯이"(Hanz 버전) 재생 시 "다음 트랙" 큐가 이제 뮤지컬/발라드 계열로 뜨는지 확인. 기존에 잘 동작하던 인기 아티스트(지드래곤, 아이유 등)는 회귀 없이 그대로 잘 동작하는지도 확인
- 한국어 띄어쓰기 교정 라이브 검증 필요([[학습노트 45번]]) — **먼저 `pip install -r requirements.txt` 재실행 필요**(PyKoSpacing/TensorFlow 신규 설치, 용량 커서 시간 좀 걸림), 그다음 백엔드 재시작(캐시 키 v4 전환). "꽃이피고지듯이"(띄어쓰기 없이)로 검색해서 조승우 원곡이 실제로 뜨는지 확인 — 백엔드 터미널에 `[search] PyKoSpacing 로드 실패` 로그가 안 뜨는지도 확인(뜨면 설치가 덜 된 것). "아이유"처럼 원래 잘 되던 짧은 검색어가 여전히 정상 동작하는지(회귀 없는지)도 확인. 첫 요청이 모델 로딩 때문에 느려지진 않는지(로딩은 서버 시작 시 1회, 4초 정도)도 체감 확인
- 검색 쓰레기 아티스트 필터 라이브 검증 필요([[학습노트 45번]]) — "꽃이피고지듯이"로 검색했을 때 이전의 엉뚱한 아티스트 카드가 더 이상 안 뜨는지 확인. 정상적인 소규모 아티스트(청취자 수 적은 진짜 아티스트)가 실수로 걸러지진 않는지 다른 검색어로도 몇 개 더 확인
- 가사 출처 고지 + takedown 라이브 검증 필요([[학습노트 44번]]) — 백엔드 재시작 후, (1) 가사가 정상적으로 뜨는 곡의 가사 탭 하단에 출처 고지 문구가 보이는지, (2) 관리자 페이지의 "가사 삭제(저작권 요청 대응)" 폼에 아티스트/곡명을 넣고 삭제 실행 → 그 곡의 가사 탭이 "저작권자 요청으로 이 곡의 가사가 내려갔어요"로 바뀌는지, (3) 삭제 후 관리자 페이지 "처리 대기" 목록에 그 곡이 다시 안 뜨는지, (4) `ADMIN_SECRET` 없이/틀리게 호출하면 403이 뜨는지 확인
- lyrics.ovh 폴백 라이브 검증 필요([[학습노트 43번]]) — 백엔드 재시작(캐시 키 v2 전환) 후, LRCLIB에 없던 곡(예: "거울 ; Mirror", "BEAUTIFUL - LIVE 071318")의 가사 탭을 열어서 이제 가사가 뜨는지, 뜬 경우 동기화 없이 일반 텍스트로만 보이는지, 그래도 못 찾는 곡은 여전히 "가사를 찾지 못했어요" + 요청 버튼이 정상 노출되는지 확인. 관리자 페이지의 "대기 건수"도 자동으로 채워진 곡만큼 줄어드는지 확인(새로고침 후)
- 가사 요청 관리자 페이지 나머지 검증 필요([[학습노트 42번]]) — 로그인 + 목록 노출(요청 남긴 "거울 ; Mirror"가 뜨는 것)까지는 라이브로 확인됨. 남은 것: 가사 등록하면 목록에서 사라지고 실제로 그 곡 가사 탭에 반영되는지, 이미 등록된 곡은 다시 안 뜨는지, 브라우저를 진짜로 새로고침(Cmd+R)해도 로그인이 유지되는지(세션스토리지 수정분), 틀린 비밀번호로 시도하면 거절되는지 확인. "- Topic" 분리 버그(추가 개선 1)는 아직 미수정 — 다음에 `_clean_artist_name()`을 `/lyrics/request` 호출 시점에도 적용하도록 고칠 것
- "다시 듣기"/"빠른 선곡" 가로 스크롤 + 15곡 증량 라이브 검증 필요([[학습노트 40번]]) — 백엔드 재시작(캐시 키 v3 전환) 후, 두 섹션이 화살표 버튼 없이 가로로 드래그/스크롤되는지, "빠른 선곡"이 실제로 15곡까지 나오는지, "다시 듣기"도 즐겨찾기 전체가 스크롤로 다 보이는지, 무드 탐색/검색의 장르·연대 결과는 기존 페이지네이션 방식 그대로 회귀 없이 동작하는지 확인
- "빠른 선곡" 유튜브 썸네일 폴백 라이브 검증 필요([[학습노트 39번]]) — 백엔드 재시작 후(코드 변경 반영+ 캐시 키 v2 전환), "빠른 선곡"에서 이전에 플레이스홀더였던 곡들(Paradise, thank u 등)이 이제 이미지가 뜨는지, 뜨는 이미지가 그 곡과 그럴듯하게 관련 있어 보이는지 확인. `YOUTUBE_API_KEY`가 설정 안 된 상태를 가정하면(테스트해보려면 `.env`에서 잠깐 비워보기) 앱이 안 죽고 그냥 플레이스홀더로 자연스럽게 낮아지는지도 확인
- "다시 듣기"/"빠른 선곡" 홈 섹션 라이브 검증 필요([[학습노트 37번]]) — 새로고침 후 "다시 듣기" 섹션에 즐겨찾기한 곡이 메인 화면에도 뜨는지(사이드바와 별개로), "빠른 선곡" 섹션이 새로고침마다 순서/구성이 달라지는지, 즐겨찾기가 없는 상태(시크릿 창 등)에서는 글로벌 차트 기반으로 대체되는지, 두 섹션 모두 카드 클릭 시 재생/즐겨찾기/재생목록 담기가 다른 섹션과 동일하게 동작하는지 확인
- 이미지 우선순위 캐시 무효화 라이브 검증 필요([[학습노트 36번]]) — 재시작 후 홈 화면 "지금 인기 있는 곡 (글로벌)"/"인기 아티스트" 카드 이미지가 서로 다르게(더 이상 전부 같은 사진 아니게) 뜨는지, 검색/추천/다음 트랙 큐/무드 탐색 결과의 카드 이미지도 각각 다르게 나오는지 확인
- 관련 항목 탭 제거 + 다음 트랙 저장 버튼 라이브 검증 필요([[학습노트 35번]]) — 확장 재생 화면에 탭이 "다음 트랙"/"가사" 2개만 뜨는지, "다음 트랙" 탭에서 "＋ 재생목록에 저장" 버튼을 눌렀을 때 지금 큐에 있는 곡 전체가 모달에 뜨는지, 기존 재생목록에 실제로 다 담기는지, 큐가 비어있을 때(유사곡을 아직 못 찾았을 때) 버튼이 어떻게 보이는지 확인
- 재생목록 상세 화면 라이브 검증 필요([[학습노트 34번]]) — 재생목록 이름을 눌러서 전체화면으로 뜨는지, 체크박스 전체선택/개별선택이 되는지, 곡 이름을 직접 눌렀을 때 화면이 닫히며 하단 미니 플레이어가 뜨고 실제로 재생되는지("추가 개선 3" 버그 수정 확인), 이어재생(순서대로 재생 후 평소 라디오로 복귀)/반복재생(다 재생하면 처음으로 되돌아가는지)/담기(다른 재생목록에 실제로 복사되는지)/삭제(확인 모달이 뜨고, 확인해야만 지워지는지)가 각각 의도대로 동작하는지, 트랙 카드 "＋" 버튼으로 여는 기존 단일 추가 모달이 회귀 없이 그대로 동작하는지 확인
- 가사 하이라이트/클릭 이동 버그 수정 라이브 검증 필요([[학습노트 33번]]) — 재시작 후, 캐시된 "라일락" 결과가 예전 값일 수 있으니 먼저 Redis에서 `lyrics:아이유:라일락`(정확한 키는 `lyrics:{artist.lower()}:{track.lower()}`) 캐시를 지우고 다시 가사 탭을 열어서 하이라이트/클릭 이동이 동작하는지 확인. 여전히 일반 텍스트만 뜨면 이 곡은 LRCLIB에 동기화 가사 자체가 없는 것으로 결론(선택 로직 문제 아님). 다른 곡들도 몇 개 더 확인해서 회귀(기존에 잘 되던 곡이 깨졌는지)가 없는지 점검
- 검색어 장르/연대 키워드 라이브 검증 필요([[학습노트 32번]]) — 재시작 후 "2010년대 kpop", "발라드", "90년대" 같은 검색어로 "관련 곡 (장르/연대)" 섹션이 뜨는지, 일반 아티스트/곡 검색("아이유")은 예전처럼 그대로 동작하는지, dbt `stg_events`에 새 이벤트 타입 추가는 필요 없는지(기존 `search` 이벤트 재사용이라 불필요할 것으로 예상) 확인
- 아티스트명 정리 + 이미지 우선순위 변경 라이브 검증 필요([[학습노트 31번]]) — 재시작 후 "다음 트랙"/차트 등에서 "- Topic"이 더 이상 안 뜨는지, 트랙 카드 이미지가 이전보다 실제 커버처럼 보이는지, "인기 아티스트" 카드에 회색 원 대신 이미지가 채워지는지 확인. iTunes를 먼저 조회하는 구조라 트랙/아티스트 카드 로딩이 전반적으로 느려지진 않는지도 체감 확인 필요
- 빠른 선곡 스타일(CompactTrackRow/CompactTrackGrid) 라이브 검증 필요([[학습노트 29번]]) — 재시작 후 무드 탐색 국내/해외 섹션이 3열 줄 스타일로 뜨는지, 9개 넘는 무드에서 화살표로 다음 페이지가 넘어가는지, 사이드바 "다시 듣기"가 썸네일 자리 있는 줄 스타일로 바뀌었는지(이미지는 플레이스홀더인 게 정상) 확인
- 유튜브 인기 급상승 재검증 필요([[학습노트 30번]] 버그 수정) — 카테고리 필터 폴백을 추가했으니, 재시작 후 "국내 인기 급상승"/"해외 인기 급상승" 두 섹션이 실제로 채워지는지 다시 확인. 여전히 비어있으면 백엔드 터미널의 `[youtube-chart] 요청 실패 ...` 로그를 보고 정확한 원인(쿼터 초과/403/그 외) 파악 필요. 카드 클릭 시 바로 재생되는지(재검색 없이), 제목 파싱이 심하게 틀린 곡이 많지는 않은지도 확인. 또한 `/chart?country=south korea`(기존 Last.fm 국내 차트)가 이제 아무 데서도 안 쓰이는 코드로 남았는지도 점검 필요(제거할지 유지할지 결정)
- 무드 탐색 재검증 필요([[학습노트 29번]] 버그 수정) — 캐시 키를 v2로 바꿨으니, 재시작 후 무드 칩을 눌러서 500 에러 없이 국내/해외 두 섹션이 뜨는지 다시 확인
- 무드 탐색 국내/해외 구분 라이브 검증 필요([[학습노트 29번]]) — 재시작 후 "신나는"/"출퇴근길" 등 여러 무드 칩을 눌러서 국내/해외 섹션이 각각 채워지는지, 국내 후보가 적은 태그에서 k-pop 보충이 자연스러운지, 명백히 잘못 분류된 아티스트는 없는지 확인
- 무드 칩 선택 시 검색 결과 초기화 라이브 검증 필요([[학습노트 10번]] 버그 수정) — 새로고침 후 아티스트 검색 → 무드 칩 클릭 시 검색 결과가 사라지고 무드 결과로 완전히 교체되는지 확인
- "관련 항목" 탭 폴백 라이브 검증 필요([[학습노트 25번]] 버그 수정) — 재시작 후 김하온 "Skrr" 등 유사 아티스트 데이터 없는 곡에서 관련 항목 탭에 그 아티스트의 다른 곡이 뜨는지 확인
- 가사 요청/수동 등록 기능 라이브 검증 필요([[학습노트 28번]]) — `.env`에 `ADMIN_SECRET` 값 추가 후 재시작, 가사 없는 곡에서 "가사 올려주세요" 버튼 눌러 요청이 남는지, `POST /lyrics/override`로 수동 등록 후 그 곡 가사 탭에 바로 반영되는지 확인
- 차트/추천/무드/다음 트랙 큐 가사 필터 라이브 검증 필요([[학습노트 27번]]) — 재시작 후 각 목록에 나오는 곡을 재생해서 실제로 가사 탭에 가사가 나오는지, 검색 결과는 필터 없이 그대로 다 나오는지, 첫 로딩 체감 속도가 너무 느려지진 않는지 확인
- 가사 탭 라이브 검증 필요([[학습노트 26번]]) — 확장 재생 화면에서 "가사" 탭을 눌러서 실제 가사가 나오는지, 동기화 가사가 있는 곡에서 재생 위치에 맞춰 줄이 하이라이트/자동 스크롤되는지, 가사가 없는 곡에서 안내 문구가 잘 뜨는지 확인
- "다음 트랙" 자동 이어재생 큐 라이브 검증 필요 — 재시작 후 곡을 끝까지 들어서(또는 시크바로 끝까지 당겨서) 다음 곡이 자동으로 이어지는지, 재생바의 큐 아이콘 눌러서 목록이 뜨고 클릭하면 바로 재생되는지 확인
- 확장 재생 화면 라이브 검증 필요([[학습노트 25번]]) — 미니 플레이어 앨범아트/곡 정보 눌러서 전체화면으로 전환되는지, "다음 트랙"/"관련 항목" 탭에서 목록이 뜨고 클릭하면 그 곡으로 바뀌는지, 접기(⌄)/닫기(✕) 버튼이 각각 의도대로 동작하는지 확인
- **안 듣는 곡 정리 기능 로컬 빌드+라이브 검증 필요**: `FavoriteTrackService.findStale`/`/favorite-tracks/stale` 추가분은 아직 로컬에서 재시작 안 해봄([[학습노트 24번]] 참고). IntelliJ에서 favorite-service(또는 playlist-service, 실제 패키지 위치 확인) 재시작 → "다시 듣기" 헤더의 "정리하기" 버튼 눌러서 6개월 이상 안 들은 곡이 모달에 뜨는지, 체크 해제/선택 삭제가 실제로 반영되는지 확인 필요
- Spring Batch로 정기 배치: (1) 즐겨찾기 태그를 취향 프로필로 집계해서 신곡 추천 — 신호가 충분히 쌓인 뒤 착수. (2) "N개월간 안 들은 곡 자동 분류"는 [[학습노트 24번]]의 온디맨드 정리 기능으로 상당 부분 대체됨 — 배치로 따로 남길 필요가 있는지는 (1)만 남기고 재검토
- 구독/결제 모의 플로우, JUnit 테스트 확장
- `mood_browse` 이벤트를 활용한 AARRR 확장 분석 (검색 진입 vs 무드 탐색 진입 비교)
- dbt `stg_events` 스키마 테스트(`accepted_values`)에 `mood_browse`, `play_track`, `lyrics_request` 이벤트 타입 추가 필요 (아직 안 함 — 지금 dbt 테스트 돌리면 새 이벤트 타입들 때문에 실패할 수 있음)
- "국내 인기 차트" 행이 라이브에서 안 뜨는 문제 진단 중 — `backend/main.py`의 `lastfm_get()`에 에러 로깅(`print`) 추가함, 다음에 백엔드 재시작 후 새로고침해서 터미널에 `[lastfm] API 오류 ...` 로그가 찍히는지 확인 필요 (아직 원인 미확정)
- 재생목록(수동) 기능 로컬 빌드는 확인됨(오타 수정 후 정상 기동) — 실제 생성/곡 담기/삭제 라이브 확인은 아직 남음
- **재생목록 자동 생성(취향 분석) 기능 로컬 빌드+라이브 검증 필요**: `AutoPlaylistService`/`PlaylistController`의 `/playlists/auto-generate` 추가분은 아직 로컬에서 재시작 안 해봄. IntelliJ에서 재시작 → 즐겨찾기 2곡 이상(같은 장르 태그) 상태에서 ✨ 버튼 눌러서 재생목록이 실제로 만들어지는지, 다시 눌렀을 때 중복 안 쌓이는지 확인 필요
- 재생목록 안 곡 순서를 사용자가 드래그로 바꾸는 기능 (지금은 추가한 순서 고정)
- 즐겨찾기 태그를 신곡 추천에 쓰는 Spring Batch 작업은 재생목록과는 별개로 계속 예정대로 진행 (자동 취향 신호 축적용, [[학습노트 21번]] 참고)
- YouTube 미니 플레이어 관련 후속 아이디어: (1) IFrame Player API로 전환한 뒤에도 브라우저 자동재생 정책(특히 Safari) 때문에 `playVideo()` 자동 호출이 항상 성공하진 않을 수 있음 — 안 되면 사용자가 직접 재생 버튼을 눌러야 함. (2) YouTube 검색 결과가 항상 정확한 원곡이 아닐 수 있음(커버곡/라이브 영상이 먼저 뜨는 경우) — 필요하면 결과 후보를 여러 개 받아와 프론트에서 고르게 하는 것도 고려 가능. (3) 볼륨 조절 UI는 아직 없음(필요하면 `player.setVolume()` 연동 추가).
