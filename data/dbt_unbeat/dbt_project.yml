name: 'unbeat_analytics'
version: '1.0.0'
config-version: 2

profile: 'unbeat'

model-paths: ["models"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  unbeat_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
